// print_uint64 of 30 band 42 = 10
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 30;
        b: uint64 <- 42;
        result: atom <- print_uint64(a band b)
    produces
        pure result
    end
}
