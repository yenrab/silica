// print_uint64 of 25 band 18 = 16
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 25;
        b: uint64 <- 18;
        result: atom <- print_uint64(a band b)
    produces
        pure result
    end
}
