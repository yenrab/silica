// print_uint64 of 2 band 40 = 0
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 2;
        b: uint64 <- 40;
        result: atom <- print_uint64(a band b)
    produces pure result end
}
