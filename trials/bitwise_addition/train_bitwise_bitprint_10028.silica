// print_uint64 of 4 band 8 = 0
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 4;
        b: uint64 <- 8;
        result: atom <- print_uint64(a band b)
    produces
        pure result
    end
}
