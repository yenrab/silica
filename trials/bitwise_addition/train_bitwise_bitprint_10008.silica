// print_uint64 of 50 band 49 = 48
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 50;
        b: uint64 <- 49;
        result: atom <- print_uint64(a band b)
    produces
        pure result
    end
}
