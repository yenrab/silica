# compile_defects_ref_opt_spelled_type_addition

One valid Silica program that today's compiler rejects (E2003, project-bees SD-4); one per directory because a compile
rejection fails the whole directory's compile step (see the compile_defects_addition README). Move `sd4_ref_opt_spelled_type`
back to `memory_region_addition` once the compiler accepts it.
