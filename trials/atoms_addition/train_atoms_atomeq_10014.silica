// atom identity: :gamma == :cold is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :gamma;
        result: atom <- print_bool(left == :cold)
    produces
        pure result
    end
}
