// atom identity: :down == :cold is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :down;
        result: atom <- print_bool(left == :cold)
    produces
        pure result
    end
}
