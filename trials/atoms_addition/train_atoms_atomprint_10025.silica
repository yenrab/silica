// print_atom prints the atom with its colon: :cold
fn main() -> atom {
    sequence proc[device_io] state: atom <- :cold;
        result: atom <- print_atom(state)
        produces pure result end
}
