// atom identity: :idle != :done is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :idle;
        result: atom <- print_bool(left != :done)
    produces
        pure result
    end
}
