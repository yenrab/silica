// atom identity: :gamma != :fail is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :gamma;
        result: atom <- print_bool(left != :fail)
    produces pure result end
}
