// atom identity: :done != :up is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :done;
        result: atom <- print_bool(left != :up)
    produces
        pure result
    end
}
