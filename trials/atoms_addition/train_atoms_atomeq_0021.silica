// atom identity: :done == :hot is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :done;
        result: atom <- print_bool(left == :hot)
    produces
        pure result
    end
}
