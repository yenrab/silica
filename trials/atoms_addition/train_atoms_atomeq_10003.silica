// atom identity: :retry == :down is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :retry;
        result: atom <- print_bool(left == :down)
    produces
        pure result
    end
}
