// atom identity: :beta == :green is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :beta;
        result: atom <- print_bool(left == :green)
    produces
        pure result
    end
}
