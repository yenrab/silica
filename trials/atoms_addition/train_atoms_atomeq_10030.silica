// atom identity: :idle != :up is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :idle;
        result: atom <- print_bool(left != :up)
    produces
        pure result
    end
}
