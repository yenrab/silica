// atom identity: :right != :down is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :right;
        result: atom <- print_bool(left != :down)
    produces pure result end
}
