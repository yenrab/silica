// atom identity: :stop == :stop is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :stop;
        result: atom <- print_bool(left == :stop)
    produces
        pure result
    end
}
