// atom identity: :cold != :done is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :cold;
        result: atom <- print_bool(left != :done)
    produces
        pure result
    end
}
