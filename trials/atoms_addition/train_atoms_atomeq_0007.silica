// atom identity: :start == :stop is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result: atom <- print_bool(left == :stop)
    produces
        pure result
    end
}
