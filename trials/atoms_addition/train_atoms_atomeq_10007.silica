// atom identity: :ready != :beta is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :ready;
        result: atom <- print_bool(left != :beta)
    produces pure result end
}
