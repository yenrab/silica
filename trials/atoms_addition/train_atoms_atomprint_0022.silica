// print_atom prints the atom with its colon: :retry
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :retry;
        result: atom <- print_atom(state)
    produces
        pure result
    end
}
