// atom identity: :alpha != :alpha is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :alpha;
        result: atom <- print_bool(left != :alpha)
    produces pure result end
}
