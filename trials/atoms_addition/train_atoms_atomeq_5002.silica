// atom identity: :fail != :retry is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :fail;
        result: atom <- print_bool(left != :retry)
    produces
        pure result
    end
}
