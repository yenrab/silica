// atom identity: :red == :retry is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :red;
        result: atom <- print_bool(left == :retry)
    produces pure result end
}
