// atom identity: :start == :hot is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result: atom <- print_bool(left == :hot)
    produces
        pure result
    end
}
