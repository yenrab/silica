// atom identity: :ready != :pending is true
fn main() -> atom {
    sequence proc[device_io] left: atom <- :ready;
        result: atom <- print_bool(left != :pending)
        produces pure result end
}
