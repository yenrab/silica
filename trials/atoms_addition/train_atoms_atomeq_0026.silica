// atom identity: :blue != :blue is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :blue;
        result: atom <- print_bool(left != :blue)
    produces pure result end
}
