// atom identity: :retry == :retry is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :retry;
        result: atom <- print_bool(left == :retry)
    produces
        pure result
    end
}
