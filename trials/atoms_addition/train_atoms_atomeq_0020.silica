// atom identity: :gamma == :gamma is true
fn main() -> atom {
    sequence proc[device_io] left: atom <- :gamma;
        result: atom <- print_bool(left == :gamma)
        produces pure result end
}
