// print_atom prints the atom with its colon: :stop
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :stop;
        result: atom <- print_atom(state)
    produces
        pure result
    end
}
