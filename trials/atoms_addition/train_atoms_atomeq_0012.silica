// atom identity: :right != :right is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :right;
        result: atom <- print_bool(left != :right)
    produces pure result end
}
