// print_atom prints the atom with its colon: :green
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :green;
        result: atom <- print_atom(state)
    produces pure result end
}
