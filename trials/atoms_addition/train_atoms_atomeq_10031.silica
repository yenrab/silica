// atom identity: :done == :done is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :done;
        result: atom <- print_bool(left == :done)
    produces
        pure result
    end
}
