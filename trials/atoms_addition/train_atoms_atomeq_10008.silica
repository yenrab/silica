// atom identity: :gamma == :green is false
fn main() -> atom {
    sequence proc[device_io] left: atom <- :gamma;
        result: atom <- print_bool(left == :green)
        produces pure result end
}
