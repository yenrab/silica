// atom identity: :gamma != :down is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :gamma;
        result: atom <- print_bool(left != :down)
    produces pure result end
}
