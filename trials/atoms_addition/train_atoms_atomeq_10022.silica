// atom identity: :beta != :down is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :beta;
        result: atom <- print_bool(left != :down)
    produces
        pure result
    end
}
