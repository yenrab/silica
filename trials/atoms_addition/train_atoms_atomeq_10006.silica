// atom identity: :start != :ready is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result: atom <- print_bool(left != :ready)
    produces
        pure result
    end
}
