// print_atom prints the atom with its colon: :beta
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :beta;
        result: atom <- print_atom(state)
    produces
        pure result
    end
}
