// print_atom prints the atom with its colon: :alpha
fn main() -> atom {
    sequence proc[device_io] state: atom <- :alpha;
        result: atom <- print_atom(state)
        produces pure result end
}
