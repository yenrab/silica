// atom identity: :idle != :idle is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :idle;
        result: atom <- print_bool(left != :idle)
    produces
        pure result
    end
}
