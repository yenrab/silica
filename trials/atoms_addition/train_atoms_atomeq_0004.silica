// atom identity: :pending != :pending is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :pending;
        result: atom <- print_bool(left != :pending)
    produces pure result end
}
