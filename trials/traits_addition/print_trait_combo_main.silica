use UsesTraitsCombo;

fn main() -> atom {
    sequence proc[device_io]
        n: int64 <- UsesTraitsCombo@scale_twice(3);
        _: atom <- print_int64(n);
        _: atom <- println("");
        result: atom <- :ok
        produces pure result end
}
