use Shape;

fn main() -> atom {
    sequence proc[device_io]
        w: int32 <- 6;
        h: int32 <- 7;
        rect: { width: int32, length: int32 } <- { width: w, length: h };
        a: int32 <- Shape@area(rect);
        _: atom <- print_int32(a);
        _: atom <- println("");
        result: atom <- :ok
        produces pure result end
}
