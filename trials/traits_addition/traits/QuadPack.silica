export trait QuadPack;
export corner_sum/1;

required {
    fn corner_sum(q: QuadPack) -> int64;
}

impl fn corner_sum(q: (int64, int64, int64, int64)) -> int64 {
    (a: int64, b: int64, c: int64, d: int64) <- q;
    a + b + c + d
}
