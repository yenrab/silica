// defect A7: uint32 y % 27 + (z + x) with (10, 16, 16) is 42
fn g(x: uint32, y: uint32, z: uint32) -> uint32 {
    y % 27 + (z + x)
}

fn main() -> atom {
    sequence proc[device_io]
        r: atom <- print_uint32(g(10, 16, 16))
    produces pure r end
}
