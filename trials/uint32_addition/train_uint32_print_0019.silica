// print_uint32 var; prints 1
fn main() -> atom {
    sequence proc[device_io]
        amount: uint32 <- 49 + 8;
        result: atom <- print_uint32(amount / 45)
    produces
        pure result
    end
}
