// print_uint32 two; prints 52 then 1
fn main() -> atom {
    sequence proc[device_io]
        a: uint32 <- 52;
        b: uint32 <- 32 / 30;
        _: atom <- print_uint32(a);
        println("");
        result: atom <- print_uint32(b)
    produces pure result end
}
