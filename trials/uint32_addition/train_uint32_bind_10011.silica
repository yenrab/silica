// uint32 bindings then expression; expect 5
fn main() -> uint32 {
    step: uint32 <- 44 + 19 * 18;
    total: uint32 <- step % step + 53;
    width: uint32 <- step;
    count: uint32 <- (16 * 16 + step);
    ((count % 7) % 34) + total - total + width - width
}
