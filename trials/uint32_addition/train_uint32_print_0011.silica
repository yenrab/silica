// print_uint32 two; prints 4 then 90
fn main() -> atom {
    sequence proc[device_io]
        a: uint32 <- 36 - 32;
        b: uint32 <- 52 + 38;
        _: atom <- print_uint32(a);
        println("");
        result: atom <- print_uint32(b)
    produces
        pure result
    end
}
