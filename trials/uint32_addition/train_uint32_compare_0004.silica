// uint32 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool((6 + 10) > 12);
        println("");
        result: atom <- print_bool(50 * 11 < 2 % 13)
    produces pure result end
}
