// print_uint16 var; prints 1
fn main() -> atom {
    sequence proc[device_io]
        value: uint16 <- 35 + 13;
        result: atom <- print_uint16((value / value))
    produces
        pure result
    end
}
