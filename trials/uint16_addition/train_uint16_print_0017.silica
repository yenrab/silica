// print_uint16 two; prints 4 then 27
fn main() -> atom {
    sequence proc[device_io]
        a: uint16 <- 4 % 34;
        b: uint16 <- 27 % 33;
        _: atom <- print_uint16(a);
        println("");
        result: atom <- print_uint16(b)
    produces
        pure result
    end
}
