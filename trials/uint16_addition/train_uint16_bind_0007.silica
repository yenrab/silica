// uint16 bindings then expression; expect 55
fn main() -> uint16 {
    height: uint16 <- 3 % 29 / (31 / 29);
    m: uint16 <- height;
    step: uint16 <- 27 + m;
    a: uint16 <- 30 * step;
    (height + 52) + 9 % height + a - a
}
