# compile_defects_float_literal_in_comparison_addition

One valid Silica program that today's compiler rejects (E2003, project-bees SD-13); one per directory because a compile
rejection fails the whole directory's compile step (see the compile_defects_addition README). Move `sd13_float_literal_in_comparison`
back to `float64_addition` once the compiler accepts it.
