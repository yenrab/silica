module dangerous_unguarded_null_deref_abort;

// Compile-only stub: integrate runs the unguarded C crash probe separately.
fn main() -> int64 {
    sequence proc[device_io]
        println("skip")
    produces
        pure 0
    end
}
