// Non-tail recursion: factorial(5) = 120 (no lists; int64 only)
fn factorial(n: int64) -> int64 {
    case n <= 1 of {
        true -> 1;
        false -> n * factorial(n - 1)
    }
}

fn main() -> int64 {
    factorial(5)
}
