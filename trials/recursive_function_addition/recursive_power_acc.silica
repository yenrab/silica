// Tail recursion: base^exp via accumulator; pow_acc(2, 7, 1) = 128
fn pow_acc(base: int64, exp: int64, acc: int64) -> int64 {
    case exp <= 0 of {
        true -> acc;
        false -> pow_acc(base, exp - 1, acc * base)
    }
}

fn main() -> int64 {
    pow_acc(2, 7, 1)
}
