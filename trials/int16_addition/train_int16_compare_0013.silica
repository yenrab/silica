// int16 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool((55 / 34) < 53);
        println("");
        result: atom <- print_bool(7 < 30 / 16)
    produces pure result end
}
