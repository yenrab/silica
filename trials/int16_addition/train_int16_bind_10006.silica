// int16 bindings then expression; expect 4
fn main() -> int16 {
    c: int16 <- 40 * 6;
    step: int16 <- c;
    base: int16 <- step;
    total: int16 <- 18 / c;
    4 + base - base + total - total
}
