// uint8 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool((8 + 60) > (20 + 10));
        println("");
        result: atom <- print_bool(16 / 56 < (59 + 7))
    produces
        pure result
    end
}
