// print_uint8 two; prints 1 then 35
fn main() -> atom {
    sequence proc[device_io]
        a: uint8 <- 56 / 51;
        b: uint8 <- 35;
        _: atom <- print_uint8(a);
        println("");
        result: atom <- print_uint8(b)
    produces
        pure result
    end
}
