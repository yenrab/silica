// print_uint8 two; prints 0 then 92
fn main() -> atom {
    sequence proc[device_io]
        a: uint8 <- 7 / 12;
        b: uint8 <- 59 + 33;
        _: atom <- print_uint8(a);
        println("");
        result: atom <- print_uint8(b)
    produces
        pure result
    end
}
