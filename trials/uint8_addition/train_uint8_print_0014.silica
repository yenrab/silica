// print_uint8 two; prints 0 then 91
fn main() -> atom {
    sequence proc[device_io]
        a: uint8 <- 11 / 28;
        b: uint8 <- 43 + 48;
        _: atom <- print_uint8(a);
        println("");
        result: atom <- print_uint8(b)
    produces pure result end
}
