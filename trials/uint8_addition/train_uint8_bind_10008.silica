// uint8 bindings then expression; expect 16
fn main() -> uint8 {
    c: uint8 <- 8;
    height: uint8 <- 49 + c;
    total: uint8 <- 0 / c + c;
    m: uint8 <- height;
    c + c + c / 45 + total - total + m - m
}
