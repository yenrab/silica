// print_uint8 two; prints 45 then 69
fn main() -> atom {
    sequence proc[device_io]
        a: uint8 <- 45;
        b: uint8 <- 13 + 56;
        _: atom <- print_uint8(a);
        println("");
        result: atom <- print_uint8(b)
    produces pure result end
}
