// case on an empty list; expect 58
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 58;
            [h: int64, _: List[int64, mem(normal)]] -> h + 28;
            _: List[int64, mem(normal)] -> 58
        }
    produces
        pure r
    end
}
