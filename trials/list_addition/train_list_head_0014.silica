// head on [34, 7] gives 34
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [34, 7];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
