// list literal of 3 int32 elements; length is 3
fn main() -> int64 {
    sequence proc[mem(normal_writethrough)]
        xs: List[int32, mem(normal_writethrough)] <- [6, 33, 31];
        n: int64 <- length[int32, mem(normal_writethrough)](xs)
    produces pure n end
}
