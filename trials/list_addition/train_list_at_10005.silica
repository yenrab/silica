// list_at index 1 of [22, 34, 0] is 34
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [22, 34, 0];
        v: int64 <- list_at[int64, mem(normal)](xs, 1)
    produces
        pure v
    end
}
