// list literal of 4 uint8 elements; length is 4
fn main() -> int64 {
    sequence proc[mem(normal_writethrough)]
        xs: List[uint8, mem(normal_writethrough)] <- [2, 15, 38, 36];
        n: int64 <- length[uint8, mem(normal_writethrough)](xs)
    produces
        pure n
    end
}
