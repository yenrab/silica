// head on [18, 23, 12] gives 18
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [18, 23, 12];
        first: int64 <- head[int64, mem(normal)](xs)
    produces pure first end
}
