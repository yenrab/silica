// list literal of 2 int32 elements; length is 2
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int32, mem(normal)] <- [22, 27];
        n: int64 <- length[int32, mem(normal)](xs)
    produces pure n end
}
