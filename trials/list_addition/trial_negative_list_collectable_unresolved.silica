// Phase 0.4 negative — List[Collectable, S] without binding context must fail (E2016).
fn main() -> int64 {
    sequence proc[mem(normal)]
        n: int64 <- length[Collectable, mem(normal)](empty[Collectable, mem(normal)]())
    produces
        pure n
    end
}
