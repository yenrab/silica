// head on [20, 22, 4, 40] gives 20
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [20, 22, 4, 40];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
