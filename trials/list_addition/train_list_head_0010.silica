// head on [25, 8] gives 25
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [25, 8];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
