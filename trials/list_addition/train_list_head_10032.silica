// head on [12, 33, 26, 20, 6] gives 12
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [12, 33, 26, 20, 6];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
