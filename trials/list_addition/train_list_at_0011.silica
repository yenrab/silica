// list_at index 0 of [38, 1] is 38
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [38, 1];
        v: int64 <- list_at[int64, mem(normal)](xs, 0)
    produces pure v end
}
