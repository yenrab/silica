// case on a non-empty list; expect 26
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [16, 6, 9];
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 100;
            [h: int64, _: List[int64, mem(normal)]] -> h + 10;
            _: List[int64, mem(normal)] -> 100
        }
    produces
        pure r
    end
}
