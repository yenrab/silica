// list literal of 2 uint32 elements; length is 2
fn main() -> int64 {
    sequence proc[mem(normal_writethrough)] xs: List[uint32, mem(normal_writethrough)] <- [17, 27];
        n: int64 <- length[uint32, mem(normal_writethrough)](xs)
        produces pure n end
}
