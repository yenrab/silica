// list literal of 2 uint8 elements; length is 2
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[uint8, mem(normal)] <- [19, 20];
        n: int64 <- length[uint8, mem(normal)](xs)
    produces pure n end
}
