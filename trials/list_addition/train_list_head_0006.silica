// head on [21, 8, 9, 39] gives 21
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [21, 8, 9, 39];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
