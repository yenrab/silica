// head on [35, 24, 34, 33, 40] gives 35
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [35, 24, 34, 33, 40];
        first: int64 <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
