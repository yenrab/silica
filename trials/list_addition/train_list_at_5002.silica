// list_at index 1 of [28, 10, 10] is 10
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [28, 10, 10];
        v: int64 <- list_at[int64, mem(normal)](xs, 1)
    produces
        pure v
    end
}
