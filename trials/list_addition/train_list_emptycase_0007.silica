// case on an empty list; expect 12
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 12;
            [h: int64, _: List[int64, mem(normal)]] -> h + 31;
            _: List[int64, mem(normal)] -> 12
        }
    produces
        pure r
    end
}
