// case on an empty list; expect 38
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 38;
            [h: int64, _: List[int64, mem(normal)]] -> h + 54;
            _: List[int64, mem(normal)] -> 38
        }
    produces
        pure r
    end
}
