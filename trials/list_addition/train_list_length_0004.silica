// list literal of 4 uint32 elements; length is 4
fn main() -> int64 {
    sequence proc[mem(normal)] xs: List[uint32, mem(normal)] <- [1, 6, 4, 8];
        n: int64 <- length[uint32, mem(normal)](xs)
        produces pure n end
}
