// list literal of 3 uint8 elements; length is 3
fn main() -> int64 {
    sequence proc[mem(normal_writethrough)]
        xs: List[uint8, mem(normal_writethrough)] <- [39, 2, 40];
        n: int64 <- length[uint8, mem(normal_writethrough)](xs)
    produces
        pure n
    end
}
