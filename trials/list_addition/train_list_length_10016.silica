// list literal of 4 uint32 elements; length is 4
fn main() -> int64 {
    sequence proc[mem(normal_writethrough)]
        xs: List[uint32, mem(normal_writethrough)] <- [31, 5, 35, 15];
        n: int64 <- length[uint32, mem(normal_writethrough)](xs)
    produces
        pure n
    end
}
