// case on an empty list; expect 22
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 22;
            [h: int64, _: List[int64, mem(normal)]] -> h + 82;
            _: List[int64, mem(normal)] -> 22
        }
    produces
        pure r
    end
}
