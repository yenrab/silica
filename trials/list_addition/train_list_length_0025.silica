// list literal of 6 uint8 elements; length is 6
fn main() -> int64 {
    sequence proc[mem(normal)] xs: List[uint8, mem(normal)] <- [23, 20, 3, 19, 1, 16];
        n: int64 <- length[uint8, mem(normal)](xs)
        produces pure n end
}
