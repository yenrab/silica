// case on a non-empty list; expect 65
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [9, 24, 39];
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 61;
            [h: int64, _: List[int64, mem(normal)]] -> h + 56;
            _: List[int64, mem(normal)] -> 61
        }
    produces
        pure r
    end
}
