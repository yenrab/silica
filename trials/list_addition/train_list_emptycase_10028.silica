// case on a non-empty list; expect 118
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [22, 11];
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 46;
            [h: int64, _: List[int64, mem(normal)]] -> h + 96;
            _: List[int64, mem(normal)] -> 46
        }
    produces
        pure r
    end
}
