// count_even over [20, 25, 9] = 1
fn count_even(xs: List[int64, mem(normal)]) -> int64 {
    case xs of {
        []: List[int64, mem(normal)] -> 0;
        [h: int64, t: List[int64, mem(normal)]] -> case h % 2 == 0 of {
            true -> 1 + count_even(t);
            false -> count_even(t)
        };
        _: List[int64, mem(normal)] -> 0
    }
}

fn main() -> int64 {
    sequence proc[mem(normal)] xs: List[int64, mem(normal)] <- [20, 25, 9];
        r: int64 <- count_even(xs)
        produces pure r end
}
