// remove_head on [16, 39, 27, 29, 33] gives 4
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [16, 39, 27, 29, 33];
        rest: List[int64, mem(normal)] <- remove_head[int64, mem(normal)](xs);
        n: int64 <- length[int64, mem(normal)](rest)
    produces
        pure n
    end
}
