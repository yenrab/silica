// functional record update through bump; counter ends at 14
fn bump(s: { counter: int64, label: string }, by: int64) -> { counter: int64, label: string } {
    { counter: s.counter + by, label: s.label }
}

fn main() -> int64 {
    s0: { counter: int64, label: string } <- { counter: 6, label: "count" };
    s1: { counter: int64, label: string } <- bump(s0, 4);
    s2: { counter: int64, label: string } <- bump(s1, 4);
    s2.counter
}
