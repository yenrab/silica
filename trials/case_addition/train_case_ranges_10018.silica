// guards partition int64 into three ranges; n = 16 gives 16
fn main() -> int64 {
    n: int64 <- 16;
    case n of {
        n: int64 if n < 11 -> 0;
        n: int64 if n >= 11 and n < 20 -> n;
        n: int64 if n >= 20 -> n * 2
    }
}
