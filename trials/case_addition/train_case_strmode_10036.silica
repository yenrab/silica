// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "other";
    case mode of {
        "silent" -> {
            sequence proc[device_io]
                print_string("silent");
                println("");
                print_int64(7);
                println("")
            produces
                pure :ok
            end
        }
        "quiet" -> {
            sequence proc[device_io]
                print_int64(7);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
