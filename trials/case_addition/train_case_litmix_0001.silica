// literal patterns, a guard and a typed wildcard on uint64; v = 5 gives 20
fn main() -> uint64 {
    v: uint64 <- 5;
    case v of {
        3 -> 32;
        10 -> 10;
        6 -> 39;
        m: uint64 if m > 6 -> 42;
        _: uint64 -> 20
    }
}
