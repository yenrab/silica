// guards partition int64 into three ranges; n = 100 gives 0
fn main() -> int64 {
    n: int64 <- 100;
    case n of {
        n: int64 if n < 8 -> n / 2;
        n: int64 if n >= 8 and n < 90 -> n / 2;
        n: int64 if n >= 90 -> 0
    }
}
