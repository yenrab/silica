// guards partition int64 into three ranges; n = 25 gives 25
fn main() -> int64 {
    n: int64 <- 25;
    case n of {
        n: int64 if n < 30 -> n;
        n: int64 if n >= 30 and n < 89 -> n * 2;
        n: int64 if n >= 89 -> n - 1
    }
}
