// guards partition int64 into three ranges; n = 85 gives 0
fn main() -> int64 {
    n: int64 <- 85;
    case n of {
        n: int64 if n < 28 -> n + 100;
        n: int64 if n >= 28 and n < 50 -> n - 1;
        n: int64 if n >= 50 -> 0
    }
}
