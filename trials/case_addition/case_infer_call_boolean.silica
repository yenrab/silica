// Case scrutinee is a custom function call returning boolean — type inferred from callee.
fn is_positive(n: int64) -> boolean {
    n > 0
}

fn main() -> int64 {
    case is_positive(7) of {
        true -> 1;
        false -> 0
    }
}
