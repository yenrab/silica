// guards partition int64 into three ranges; n = 109 gives 209
fn main() -> int64 {
    n: int64 <- 109;
    case n of {
        n: int64 if n < 12 -> n / 2;
        n: int64 if n >= 12 and n < 52 -> n + 100;
        n: int64 if n >= 52 -> n + 100
    }
}
