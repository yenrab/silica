// Case scrutinee is a custom function call returning uint64 — type inferred from callee.
fn next_u64(n: uint64) -> uint64 {
    n + 1
}

fn main() -> int64 {
    case next_u64(9) of {
        10 -> 1;
        _: uint64 -> 0
    }
}
