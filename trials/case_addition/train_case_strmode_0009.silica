// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "trace";
    case mode of {
        "silent" -> {
            sequence proc[device_io]
                print_string("silent");
                println("");
                print_int64(8);
                println("")
            produces
                pure :ok
            end
        }
        "trace" -> {
            sequence proc[device_io]
                print_int64(8);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
