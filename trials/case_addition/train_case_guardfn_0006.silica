// guard calls a boolean helper; z = 3 gives 6
fn is_positive(v: int64) -> boolean {
    v > 0
}

fn main() -> int64 {
    z: int64 <- 3;
    case z of {
        z: int64 if is_positive(z) -> z * 2;
        _: int64 -> 0
    }
}
