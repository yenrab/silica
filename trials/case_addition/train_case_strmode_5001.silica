// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "verbose";
    case mode of {
        "debug" -> {
            sequence proc[device_io]
                print_string("debug");
                println("");
                print_int64(6);
                println("")
            produces
                pure :ok
            end
        }
        "verbose" -> {
            sequence proc[device_io]
                print_int64(7);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
