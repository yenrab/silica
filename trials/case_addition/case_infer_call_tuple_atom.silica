// Mixed: case on custom call returning (atom, int64) — type inferred from callee.
fn tagged(n: int64) -> (atom, int64) {
    (:ok, n)
}

fn main() -> int64 {
    case tagged(9) of {
        t: (atom, int64) -> {
            (_tag: atom, c: int64) <- t;
            c
        }
    }
}
