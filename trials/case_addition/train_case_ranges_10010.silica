// guards partition int64 into three ranges; n = 3 gives 6
fn main() -> int64 {
    n: int64 <- 3;
    case n of {
        n: int64 if n < 59 -> n * 2;
        n: int64 if n >= 59 and n < 63 -> n / 2;
        n: int64 if n >= 63 -> n * 2
    }
}
