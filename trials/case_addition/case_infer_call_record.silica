// Case scrutinee is a custom function call returning a record — type inferred from callee.
fn point(xv: int64, yv: int64) -> { x: int64, y: int64 } {
    { x: xv, y: yv }
}

fn main() -> int64 {
    case point(3, 4) of {
        _: { x: int64, y: int64 } -> 7
    }
}
