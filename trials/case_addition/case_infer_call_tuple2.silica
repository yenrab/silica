// Case scrutinee is a custom function call returning a 2-tuple — type inferred from callee.
fn pair(n: int64) -> (int64, int64) {
    (n, n + 1)
}

fn main() -> int64 {
    case pair(4) of {
        p: (int64, int64) -> {
            (a: int64, b: int64) <- p;
            a + b
        }
    }
}
