// guards partition int64 into three ranges; n = 55 gives 55
fn main() -> int64 {
    n: int64 <- 55;
    case n of {
        n: int64 if n < 40 -> n;
        n: int64 if n >= 40 and n < 64 -> n;
        n: int64 if n >= 64 -> n * 2
    }
}
