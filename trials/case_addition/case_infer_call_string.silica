// Case scrutinee is a custom function call returning string — type inferred from callee.
fn tag(n: int64) -> string {
    case n > 0 of {
        true -> "pos";
        false -> "non"
    }
}

fn main() -> int64 {
    case tag(3) of {
        "pos" -> 1;
        "non" -> 2;
        _: string -> 0
    }
}
