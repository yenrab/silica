// guards partition int64 into three ranges; n = 35 gives 35
fn main() -> int64 {
    n: int64 <- 35;
    case n of {
        n: int64 if n < 6 -> n / 2;
        n: int64 if n >= 6 and n < 111 -> n;
        n: int64 if n >= 111 -> n + 100
    }
}
