// guards partition int64 into three ranges; n = 93 gives 92
fn main() -> int64 {
    n: int64 <- 93;
    case n of {
        n: int64 if n < 33 -> n - 1;
        n: int64 if n >= 33 and n < 35 -> n / 2;
        n: int64 if n >= 35 -> n - 1
    }
}
