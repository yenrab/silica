// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "trace";
    case mode of {
        "trace" -> {
            sequence proc[device_io]
                print_string("trace");
                println("");
                print_int64(2);
                println("")
            produces
                pure :ok
            end
        }
        "debug" -> {
            sequence proc[device_io]
                print_int64(1);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
