// Spec §3.3.3 "Practical Guard Exhaustiveness" Example 1: disjoint ranges cover int64
fn main() -> int64 {
    n: int64 <- 42;
    case n of {
        n: int64 if n < 0 -> -1;
        n: int64 if n >= 0 and n < 100 -> n * 2;
        n: int64 if n >= 100 -> n + 100
    }
}
