// guards partition int64 into three ranges; n = 41 gives 141
fn main() -> int64 {
    n: int64 <- 41;
    case n of {
        n: int64 if n < 36 -> n * 2;
        n: int64 if n >= 36 and n < 87 -> n + 100;
        n: int64 if n >= 87 -> n / 2
    }
}
