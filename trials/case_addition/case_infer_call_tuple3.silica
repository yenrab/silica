// Case scrutinee is a custom function call returning a 3-tuple — type inferred from callee.
fn triple(n: int64) -> (int64, int64, int64) {
    (n, n + 1, n + 2)
}

fn main() -> int64 {
    case triple(1) of {
        t: (int64, int64, int64) -> {
            (a: int64, b: int64, c: int64) <- t;
            a + b + c
        }
    }
}
