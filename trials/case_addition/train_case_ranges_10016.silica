// guards partition int64 into three ranges; n = 93 gives 193
fn main() -> int64 {
    n: int64 <- 93;
    case n of {
        n: int64 if n < 6 -> n / 2;
        n: int64 if n >= 6 and n < 86 -> n * 2;
        n: int64 if n >= 86 -> n + 100
    }
}
