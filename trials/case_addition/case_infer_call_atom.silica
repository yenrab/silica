// Case scrutinee is a custom function call returning atom — type inferred from callee.
fn kind(n: int64) -> atom {
    case n == 0 of {
        true -> :zero;
        false -> :nonzero
    }
}

fn main() -> int64 {
    case kind(2) of {
        :zero -> 0;
        :nonzero -> 1;
        _: atom -> 2
    }
}
