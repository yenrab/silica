// guards partition int64 into three ranges; n = 127 gives 63
fn main() -> int64 {
    n: int64 <- 127;
    case n of {
        n: int64 if n < 30 -> n;
        n: int64 if n >= 30 and n < 113 -> n * 2;
        n: int64 if n >= 113 -> n / 2
    }
}
