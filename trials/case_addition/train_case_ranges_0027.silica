// guards partition int64 into three ranges; n = 85 gives 0
fn main() -> int64 {
    n: int64 <- 85;
    case n of {
        n: int64 if n < 38 -> 0;
        n: int64 if n >= 38 and n < 93 -> 0;
        n: int64 if n >= 93 -> 0
    }
}
