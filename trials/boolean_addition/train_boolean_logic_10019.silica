// false or true printed
fn main() -> atom {
    sequence proc[device_io]
        result: boolean <- false or true;
        outcome: atom <- print_bool(result)
    produces
        pure outcome
    end
}
