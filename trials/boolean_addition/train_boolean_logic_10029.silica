// true or false printed
fn main() -> atom {
    sequence proc[device_io] b: boolean <- true;
        a: boolean <- false;
        result: boolean <- b or a;
        outcome: atom <- print_bool(result)
        produces pure outcome end
}
