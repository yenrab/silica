// positive twin of B2: a string argument to a string parameter; length of "abc" doubled is 6
fn double_len(s: string) -> int64 {
    length_bytes(s) * 2
}

fn main() -> int64 {
    double_len("abc")
}
