// string helper greet prints hi cloud

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io] print_string(greet("cloud"));
        println("")
        produces pure :ok end
}
