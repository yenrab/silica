fn bit(b: boolean, w: int64) -> int64 {
    case b of { true -> w; false -> 0 }
}

fn id_boolean(b: boolean) -> boolean {
    b
}

fn tt() -> boolean {
    true
}

fn ff() -> boolean {
    false
}

fn check() -> int64 {
    t: boolean <- tt();
    f: boolean <- ff();
    pt: boolean <- id_boolean(t);
    pf: boolean <- id_boolean(f);
    bit(pt, 1) + bit(pf == false, 2)
}

fn main() -> int64 {
    check()//pt=1, pf==false=2: want 3
}
