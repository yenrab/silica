// string helper greet prints hi world

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("world"));
        println("")
    produces
        pure :ok
    end
}
