// string helper greet prints hi omega

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("omega"));
        println("")
    produces pure :ok end
}
