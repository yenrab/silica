// string helper greet prints hi silica

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("silica"));
        println("")
    produces pure :ok end
}
