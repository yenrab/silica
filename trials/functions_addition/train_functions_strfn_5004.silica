// string helper greet prints hi banana

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io] print_string(greet("banana"));
        println("")
        produces pure :ok end
}
