// string helper greet prints hi delta

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("delta"));
        println("")
    produces pure :ok end
}
