// string helper greet prints hi hello

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io] print_string(greet("hello"));
        println("")
        produces pure :ok end
}
