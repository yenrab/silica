// string helper greet prints hi river

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("river"));
        println("")
    produces pure :ok end
}
