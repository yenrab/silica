// string helper greet prints hi apple

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("apple"));
        println("")
    produces
        pure :ok
    end
}
