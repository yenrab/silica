// qualified call bound then printed; prints 30
use lib_beta;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_beta@twice(15);
        result: atom <- print_int64(v)
    produces pure result end
}
