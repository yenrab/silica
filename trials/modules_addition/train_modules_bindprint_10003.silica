// qualified call bound then printed; prints 20
use lib_math_utils_spec;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_math_utils_spec@add(6, 14);
        result: atom <- print_int64(v)
    produces
        pure result
    end
}
