// qualified call bound then printed; prints 18
use lib_math_utils_spec;

fn main() -> atom {
    sequence proc[device_io] v: int64 <- lib_math_utils_spec@multiply(3, 6);
        result: atom <- print_int64(v)
        produces pure result end
}
