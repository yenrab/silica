// qualified call bound then printed; prints 7
use lib_base;

fn main() -> atom {
    sequence proc[device_io] v: int64 <- lib_base@seven();
        result: atom <- print_int64(v)
        produces pure result end
}
