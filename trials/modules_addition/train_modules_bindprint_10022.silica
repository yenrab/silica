// qualified call bound then printed; prints 47
use lib_arity;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_arity@add3(8, 19, 20);
        result: atom <- print_int64(v)
    produces pure result end
}
