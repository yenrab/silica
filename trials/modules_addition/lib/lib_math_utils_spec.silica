// Filename-based module lib_math_utils_spec (spec §19.1.1). Export surface §19.2.
export add/2;
export multiply/2;

fn add(x: int64, y: int64) -> int64 {
    x + y
}

fn multiply(x: int64, y: int64) -> int64 {
    x * y
}
