// qualified call bound then printed; prints 17
use lib_math_utils_spec;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_math_utils_spec@add(12, 5);
        result: atom <- print_int64(v)
    produces pure result end
}
