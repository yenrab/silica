// qualified call bound then printed; prints 28
use lib_arity;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_arity@add3(10, 15, 3);
        result: atom <- print_int64(v)
    produces
        pure result
    end
}
