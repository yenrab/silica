// expected failure E2018: binding 'count' has no type annotation
// int64 bindings then expression; expect 1
fn main() -> int64 {
    d: int64 <- 42 % 42 % (50 % 47);
    count <- (d + d) * d;
    c: int64 <- count;
    base: int64 <- 21 / 38;
    45 / (42 % 43) + c - c + base - base
}
