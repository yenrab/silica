// expected failure E1050: produces clause without pure
// false or false printed
fn main() -> atom {
    sequence proc[device_io]
        b: boolean <- false;
        a: boolean <- false;
        result: boolean <- b or a;
        outcome: atom <- print_bool(result)
    produces outcome end
}
