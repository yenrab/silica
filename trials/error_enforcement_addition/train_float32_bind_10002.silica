// expected failure E1053: duplicate produces keyword
// float32 bindings 8.0 * 18.0 printed as 144.0
fn main() -> atom {
    sequence proc[device_io]
        a: float32 <- 8.0;
        b: float32 <- 18.0;
        result: atom <- print_float32(a * b)
    produces produces
        pure result
    end
}
