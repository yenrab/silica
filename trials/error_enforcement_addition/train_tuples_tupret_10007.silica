// expected failure E2002: undefined identifier 'y2'
// divmod returns a pair; p + q = 19
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod(37, 2);
    p + q
}
