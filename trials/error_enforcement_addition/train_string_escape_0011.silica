// expected failure E1055: binding 'scratch' is never used
// string escape quote
fn main() -> atom {
    scratch: int64 <- 41;
    sequence proc[device_io]
        q: string <- "say \"hi\"";
        print_string(q);
        println("")
    produces
        pure :ok
    end
}
