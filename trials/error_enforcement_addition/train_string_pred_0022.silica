// expected failure E2001: integer literal passed to ends_with
// ends_with prints true
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "hello world";
        result: atom <- print_bool(ends_with(s, 7))
    produces pure result end
}
