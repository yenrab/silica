// expected failure E2003: half_sum returns float64 but its result feeds print_float32
// float32 helper half_sum; prints 696.671875
fn half_sum(x: float32) -> float64 {
    x * 19.125 + 1.0
}

fn main() -> atom {
    sequence proc[device_io] result: atom <- print_float32(half_sum(36.375))
        produces pure result end
}
