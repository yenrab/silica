// expected failure E1042: function 'main' has no return type
// uint64 bitwise: ((one bor seven) + flags) = 12
fn main() -> {
    one: uint64 <- 6;
    seven: uint64 <- 3;
    flags: uint64 <- 5;
    ((one bor seven) + flags)
}
