// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 10
fn main() -> int8 {
    width: int8 <- 38;
    base: int8 <- 10;
    -width + (base * (21 % base)) + width - width
}
