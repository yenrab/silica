// expected failure E2018: binding 'person' has no type annotation
// print each field of a mixed record
fn main() -> atom {
    sequence proc[device_io]
        person <- { name: "actor", age: 79, active: true };
        print_string(person.name);
        println("");
        print_int64(person.age);
        println("");
        print_bool(person.active);
        println("")
    produces pure :ok end
}
