// expected failure E2018: binding 'result' has no type annotation
// sequence binding RHS literal then produces with no semicolon; want 189
fn main() -> int64 {
    sequence
        result <- 189
    produces
        pure result
    end
}
