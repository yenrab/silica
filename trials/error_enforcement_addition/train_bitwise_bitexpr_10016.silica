// expected failure E1047: named struct declaration 'Pair' is not allowed
// uint64 bitwise: (three bor flags) band one = 6
struct Pair { x: int64, y: int64 }

fn main() -> uint64 {
    three: uint64 <- 2;
    flags: uint64 <- 4;
    one: uint64 <- 15;
    (three bor flags) band one
}
