// expected failure E1042: function 'main' has no return type
// swap((16, 28)); first = 28
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> {
    (first: int64, second: int64) <- swap((16, 28));
    first
}
