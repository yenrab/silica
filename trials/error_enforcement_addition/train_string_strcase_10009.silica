// expected failure E1042: function 'main' has no return type
// case on string literals; "cherry" gives 51
fn main() -> {
    mode: string <- "cherry";
    case mode of {
        "south" -> 51;
        "region" -> 24;
        "cherry" -> 51;
        _: string -> 33
    }
}
