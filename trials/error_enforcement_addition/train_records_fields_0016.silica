// expected failure E2001: string literal in an int64 record field
// inline record field access; x + y = 47
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 37, y: "10" };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv + yv
}
