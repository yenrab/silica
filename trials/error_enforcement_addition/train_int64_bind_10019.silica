// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 17
fn main() -> int64 {
    k: int64 <- 33 * 26;
    width: int64 <- k + (k - k);
    -k + 36 % (17 + 2) + width - width
}
