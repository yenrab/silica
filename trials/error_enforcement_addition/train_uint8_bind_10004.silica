// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 58
fn main() -> uint8 {
    step: uint8 <- 35 % 16 + (51 + 43);
    height: uint8 <- step - 0 / step;
    a: uint8 <- step;
    count: uint8 <- (11 + 38);
    -step + 18 + 33 + 7 + height - height + a - a + count - count
}
