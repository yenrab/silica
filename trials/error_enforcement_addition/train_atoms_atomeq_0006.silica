// expected failure E1045: function 'main' declared without the fn keyword
// atom identity: :red != :red is false
main() -> atom {
    sequence proc[device_io] left: atom <- :red;
        result: atom <- print_bool(left != :red)
        produces pure result end
}
