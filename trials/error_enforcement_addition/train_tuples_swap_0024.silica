// expected failure E1055: binding 'spare' is never used
// swap((22, 24)); second = 22
fn swap(p: (int64, int64)) -> (int64, int64) {
    spare: int64 <- 41;
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((22, 24));
    second
}
