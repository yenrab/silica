// expected failure E2001: string result in an integer case arm
// case on string literals; "banana" gives 70
fn main() -> int64 {
    mode: string <- "banana";
    case mode of {
        "region" -> "region";
        "banana" -> 70;
        "alpha" -> 44;
        _: string -> 63
    }
}
