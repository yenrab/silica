// expected failure E2005: decomposition of a pair into three names
// divmod returns a pair; p + q = 16
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64, r: int64) <- divmod(44, 3);
    p + q
}
