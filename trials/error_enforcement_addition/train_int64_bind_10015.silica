// expected failure E1041: trailing ';' after the block's final expression
// int64 bindings then expression; expect 32
fn main() -> int64 {
    d: int64 <- 23;
    total: int64 <- d + (8 + d);
    32 + total - total;
}
