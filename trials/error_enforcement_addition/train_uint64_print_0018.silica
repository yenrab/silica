// expected failure E1042: function 'main' has no return type
// print_uint64 expr; prints 11
fn main() -> {
    sequence proc[device_io]
        result: atom <- print_uint64((25 - 42 / 3))
    produces
        pure result
    end
}
