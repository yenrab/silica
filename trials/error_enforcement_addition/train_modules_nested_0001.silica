// expected failure E1047: named struct declaration 'Point' is not allowed
// nested module-qualified calls; lib_math_utils_spec@add(lib_base@seven(), 25) = 32
use lib_base, lib_math_utils_spec;

struct Point { x: int64, y: int64 }

fn main() -> int64 {
    lib_math_utils_spec@add(lib_base@seven(), 25)
}
