// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot lo) band three) + flags) = 15
fn main() -> uint64 {
    lo: uint64 <- true;
    three: uint64 <- 11;
    flags: uint64 <- 5;
    (((bnot lo) band three) + flags)
}
