// expected failure E2001: integer literal passed to a bool parameter
// bool and boolean name the same type
fn both(x: bool, y: boolean) -> bool {
    x and y
}

fn main() -> int64 {
    case both(1, false) of {
        true -> 1;
        false -> 0
    }
}
