// expected failure E2013: unary minus on a variable instead of negate_int32
// int32 bindings then expression; expect 59
fn main() -> int32 {
    c: int32 <- 18 / 5;
    count: int32 <- c + 53;
    width: int32 <- 40;
    b: int32 <- c;
    -c + count + count / 17 + width - width + b - b
}
