// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot two) band three) + flags) = 7
fn main() -> uint64 {
    two: uint64 <- true;
    three: uint64 <- 7;
    flags: uint64 <- 1;
    (((bnot two) band three) + flags)
}
