// expected failure E2003: shift amount literal 64 is out of range 0..63
// shifts: (seed shl 4) + (wide shr 1) = 143
fn main() -> uint64 {
    seed: uint64 <- 5;
    wide: uint64 <- 127;
    (seed shl 64) + (wide shr 1)
}
