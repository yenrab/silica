// Error enforcement: List type binding must declare mem(Space) (E2015); spec §4.2.4.
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64] <- empty[int64, mem(normal)]();
        length[int64, mem(normal)](xs)
    produces
        pure 0
    end
}
