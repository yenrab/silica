// expected failure E2005: call to unknown function 'divmod_all'
// divmod returns a pair; p + q = 5
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod_all(37, 9);
    p + q
}
