// expected failure E2001: string result in an integer case arm
// case on string literals; "world" gives 59
fn main() -> int64 {
    mode: string <- "world";
    case mode of {
        "world" -> "world";
        "river" -> 55;
        "banana" -> 10;
        _: string -> 98
    }
}
