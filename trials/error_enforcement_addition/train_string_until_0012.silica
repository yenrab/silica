// expected failure E2018: binding 's' has no type annotation
// substring_until_char from 7 up to ','
fn main() -> atom {
    sequence proc[device_io]
        s <- substring_until_char("banana,chair,cloud", 7, ',');
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
