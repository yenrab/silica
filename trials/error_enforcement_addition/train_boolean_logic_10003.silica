// expected failure E2005: call to unknown function 'print_booz'
// true and false printed
fn main() -> atom {
    sequence proc[device_io]
        b: boolean <- true;
        a: boolean <- false;
        result: boolean <- b and a;
        outcome: atom <- print_booz(result)
    produces pure outcome end
}
