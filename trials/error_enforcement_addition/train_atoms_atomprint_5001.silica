// expected failure E1042: function 'main' has no return type
// print_atom prints the atom with its colon: :fail
fn main() -> {
    sequence proc[device_io] result: atom <- print_atom(:fail)
        produces pure result end
}
