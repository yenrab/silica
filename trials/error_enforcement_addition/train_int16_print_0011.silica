// expected failure E1055: binding 'temp_val' is never used
// print_int16 ignore; prints 55
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        _: atom <- print_int16(55 + 0)
    produces pure :ok end
}
