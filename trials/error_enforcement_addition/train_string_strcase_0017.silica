// expected failure E1043: whitespace between binding name 'mode' and ':'
// case on string literals; "other" gives 81
fn main() -> int64 {
    mode :string <- "other";
    case mode of {
        "banana" -> 80;
        "world" -> 30;
        "cloud" -> 3;
        _: string -> 81
    }
}
