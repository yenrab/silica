// expected failure E1052: duplicate sequence keyword
// print_uint8 var; prints 0
fn main() -> atom {
    sequence sequence proc[device_io] amount: uint8 <- 57 + 55;
        result: atom <- print_uint8(amount % amount)
        produces pure result end
}
