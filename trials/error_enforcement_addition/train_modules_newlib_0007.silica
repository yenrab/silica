// expected failure E1055: binding 'spare' is never used
// root program using the new library module train_lib_pair9; train_lib_pair9@pair9_apply(9, 1) = 82
use train_lib_pair9;

fn main() -> int64 {
    spare: int64 <- 41;
    train_lib_pair9@pair9_apply(9, 1)
}
