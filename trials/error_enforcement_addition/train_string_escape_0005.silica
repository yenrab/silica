// expected failure E1055: binding 'spare' is never used
// string escape newline
fn main() -> atom {
    spare: int64 <- 41;
    sequence proc[device_io] print_string("delta\napple");
        println("")
        produces pure :ok end
}
