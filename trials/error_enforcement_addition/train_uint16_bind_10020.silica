// expected failure E2013: unary minus on a variable instead of negate_uint16
// uint16 bindings then expression; expect 150
fn main() -> uint16 {
    total: uint16 <- 39;
    b: uint16 <- (total + 20 + total * total);
    c: uint16 <- 53;
    -total + (total + 58 + c) + b - b
}
