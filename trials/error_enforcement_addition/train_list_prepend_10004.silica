// expected failure E3002: sequence declares the wrong effect; mem(normal) is used but not declared
// 3 prepends after empty; head is 2
fn main() -> int64 {
    sequence proc[device_io]
        xs0: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        xs1: List[int64, mem(normal)] <- prepend[int64, mem(normal)](18, xs0);
        xs2: List[int64, mem(normal)] <- prepend[int64, mem(normal)](4, xs1);
        xs3: List[int64, mem(normal)] <- prepend[int64, mem(normal)](2, xs2);
        h: int64 <- head[int64, mem(normal)](xs3)
    produces pure h end
}
