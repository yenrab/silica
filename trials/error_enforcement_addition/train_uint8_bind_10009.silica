// expected failure E2003: binding m declared uint32 but used as uint8
// uint8 bindings then expression; expect 20
fn main() -> uint8 {
    m: uint32 <- 20;
    total: uint8 <- 41 * 1;
    m + total - total
}
