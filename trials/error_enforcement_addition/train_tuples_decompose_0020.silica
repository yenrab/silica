// expected failure E2005: decomposition element typed as string against an int64 slot
// decompose a 2-tuple from a binding; a + b = 38
fn main() -> int64 {
    t: (int64, int64) <- (0, 38);
    (a: int64, b: string) <- t;
    a + b
}
