// expected failure E1055: binding 'temp_val' is never used
// uint64 bindings then expression; expect 51
fn main() -> uint64 {
    temp_val: int64 <- 41;
    total: uint64 <- 16 % 60;
    b: uint64 <- (16 + 1);
    step: uint64 <- 52 - 24 + 56 * 3;
    height: uint64 <- 20 % b;
    51 + total - total + step - step + height - height
}
