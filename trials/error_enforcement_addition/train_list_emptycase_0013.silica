// expected failure E2015: list pattern without its mem(Space)
// case on a non-empty list; expect 71
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [30, 32, 37, 27];
        r: int64 <- case xs of {
            []: List[int64] -> 42;
            [h: int64, _: List[int64, mem(normal)]] -> h + 41;
            _: List[int64, mem(normal)] -> 42
        }
    produces
        pure r
    end
}
