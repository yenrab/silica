// expected failure E2018: binding 'result' has no type annotation
// print_uint64 expr; prints 56
fn main() -> atom {
    sequence proc[device_io]
        result <- print_uint64(56 / 1 % (41 * 48))
    produces pure result end
}
