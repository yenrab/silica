// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot hi) band two) + flags) = 8
fn main() -> uint64 {
    hi: uint64 <- true;
    two: uint64 <- 3;
    flags: uint64 <- 5;
    (((bnot hi) band two) + flags)
}
