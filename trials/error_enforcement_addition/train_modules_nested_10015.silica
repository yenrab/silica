// expected failure E1045: function 'main' declared without the fn keyword
// nested module-qualified calls; lib_alpha@bump(lib_beta@twice(13)) = 27
use lib_alpha, lib_beta;

main() -> int64 {
    lib_alpha@bump(lib_beta@twice(13))
}
