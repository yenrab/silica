// E1055: using one binding does not satisfy another unused binding in the same scope.
fn consume(x: int64) -> int64 {
    x
}

fn main() -> int64 {
    kept: int64 <- 1;
    unused_value: int64 <- 2;
    consume(kept)
}
