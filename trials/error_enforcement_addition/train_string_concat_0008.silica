// expected failure E2001: integer literal passed to concatenate
// concatenate of 3 pieces prints gammaappleriver

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate("gamma", "apple"), 5);
        print_string(s);
        println("")
        produces pure :ok end
}
