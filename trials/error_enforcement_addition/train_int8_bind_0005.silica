// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 18
fn main() -> int8 {
    width: int8 <- 6;
    b: int8 <- width;
    total: int8 <- 27 + width;
    -width + 18 + b - b + total - total
}
