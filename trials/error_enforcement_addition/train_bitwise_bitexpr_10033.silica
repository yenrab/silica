// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (hi band bits) band mask = 0
fn main() -> uint64 {
    hi: uint64 <- true;
    bits: uint64 <- 2;
    mask: uint64 <- 5;
    (hi band bits) band mask
}
