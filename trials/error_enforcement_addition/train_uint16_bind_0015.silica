// expected failure E2013: unary minus on a variable instead of negate_uint16
// uint16 bindings then expression; expect 0
fn main() -> uint16 {
    base: uint16 <- 7;
    a: uint16 <- 41;
    -base + 56 % 9 / (19 + 26) + base - base + a - a
}
