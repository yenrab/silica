// expected failure E2003: binding base declared uint16 but used as uint64
// uint64 bindings then expression; expect 39
fn main() -> uint64 {
    base: uint16 <- 39;
    m: uint64 <- 2 + base;
    d: uint64 <- m % base / (base * m);
    c: uint64 <- base / m;
    m + base - m + d - d + c - c
}
