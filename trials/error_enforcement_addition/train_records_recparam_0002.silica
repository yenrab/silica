// expected failure E1046: stray '<' after the last record field
// record passed to a function; perimeter({ width: 10, height: 8 }) = 36
fn perimeter(r: { width: int64, height: int64 }) -> int64 {
    2 * (r.width + r.height)
}

fn main() -> int64 {
    perimeter({ width: 10, height: 8< })
}
