// expected failure E4016: no main/0: entry function renamed to 'start'
// pair returns a pair; p + q = 100
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, y * 2)
}

fn start() -> int64 {
    (p: int64, q: int64) <- pair(1, 49);
    p + q
}
