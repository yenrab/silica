// expected failure E2003: binding step declared uint8 but used as int8
// int8 bindings then expression; expect 27
fn main() -> int8 {
    step: uint8 <- 36;
    width: int8 <- 0;
    k: int8 <- 14 + width;
    13 + k % 56 + step - step
}
