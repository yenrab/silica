// expected failure E1065: parameter list of 'pair' is not closed
// pair returns a pair; p + q = 93
fn pair(x: int64, y: int64 -> (int64, int64) {
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- pair(22, 35);
    p + q
}
