// expected failure E1053: duplicate produces keyword
// substring_until_char from 0 up to '/'
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("stone/omega", 0, '/');
        print_string(s);
        println("")
    produces produces pure :ok end
}
