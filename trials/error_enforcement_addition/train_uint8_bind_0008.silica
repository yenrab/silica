// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 32
fn main() -> uint8 {
    total: uint8 <- 21;
    b: uint8 <- 52 + 9 + total;
    a: uint8 <- 48;
    -total + 32 + b - b + a - a
}
