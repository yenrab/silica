// expected failure E1042: function 'main' has no return type
// nested tuple decomposition; x + y + z = 67
fn main() -> {
    t: ((int64, int64), int64) <- ((38, 0), 29);
    (inner: (int64, int64), z: int64) <- t;
    (x: int64, y: int64) <- inner;
    x + y + z
}
