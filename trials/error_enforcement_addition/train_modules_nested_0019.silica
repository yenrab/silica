// expected failure E1042: function 'main' has no return type
// nested module-qualified calls; lib_math_utils_spec@add(lib_base@seven(), 8) = 15
use lib_base, lib_math_utils_spec;

fn main() -> {
    lib_math_utils_spec@add(lib_base@seven(), 8)
}
