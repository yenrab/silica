// expected failure E2001: string literal used as a substring index
// substring uses character indices [1, 3) of "αβγ"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("αβγ", "1", 3);
        print_string(s);
        println("")
        produces pure :ok end
}
