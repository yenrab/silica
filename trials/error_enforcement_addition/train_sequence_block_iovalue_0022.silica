// expected failure E2003: sum bound as a boolean
// sequence prints and returns the same sum 25
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 4;
        b: int64 <- 21;
        print_int64(a + b);
        println("");
        total: boolean <- a + b
    produces
        pure total
    end
}
