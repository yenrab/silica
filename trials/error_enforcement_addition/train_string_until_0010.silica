// expected failure E1041: trailing ';' after the pure expression
// substring_until_char from 7 up to ' '
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("silica delta beta north", 7, ' ');
        print_string(s);
        println("")
    produces
        pure :ok;
    end
}
