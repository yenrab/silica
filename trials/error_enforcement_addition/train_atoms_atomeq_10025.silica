// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// atom identity: :right == :right is true
fn main() -> atom {
    sequence proc[device_io, mem(normal)] left: atom <- :right;
        result: atom <- print_bool(left == :right)
        produces pure result end
}
