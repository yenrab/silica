// expected failure E1042: function 'main' has no return type
// sequence binding RHS literal then produces with no semicolon; want 20
fn main() {
    sequence
        result: int64 <- 20
    produces
        pure result
    end
}
