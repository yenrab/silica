// expected failure E2003: shift amount literal 65 is out of range 0..63
// shifts: (seed shl 1) + (wide shr 3) = 24
fn main() -> uint64 {
    seed: uint64 <- 2;
    wide: uint64 <- 167;
    (seed shl 65) + (wide shr 3)
}
