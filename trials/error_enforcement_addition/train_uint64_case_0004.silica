// expected failure E2001: string literal in one case arm of an integer case
// case on uint64 value 9; expect 80
fn main() -> uint64 {
    v: uint64 <- 9;
    case v of {
        11 -> "121";
        n: uint64 if n > 18 -> 43;
        _: uint64 -> 80
    }
}
