// expected failure E2018: binding 'total' has no type annotation
// uint64 bindings then expression; expect 28
fn main() -> uint64 {
    total <- 40;
    b: uint64 <- 38;
    count: uint64 <- total % 11;
    6 % b + (56 - 34) + count - count
}
