// expected failure E1049: sequence block without produces
// print_int8 var; prints 29
fn main() -> atom {
    sequence proc[device_io]
        amount: int8 <- 4 + 25;
        result: atom <- print_int8(amount)
    pure result
    end
}
