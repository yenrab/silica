// expected failure E1055: binding 'temp_val' is never used
// concatenate of 2 pieces prints gamma日本語

fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        s: string <- concatenate("gamma", "日本語");
        print_string(s);
        println("")
    produces pure :ok end
}
