// expected failure E2003: comparison against a string binding
// uint8 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        label: string <- "x";
        _: atom <- print_bool(label == (60 - 25) == 5);
        println("");
        result: atom <- print_bool((13 % 18) > (6 * 16))
    produces
        pure result
    end
}
