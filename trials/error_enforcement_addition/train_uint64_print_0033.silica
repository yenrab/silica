// expected failure E1049: sequence block without produces
// print_uint64 var; prints 12
fn main() -> atom {
    sequence proc[device_io]
        amount: uint64 <- 20 + 36;
        result: atom <- print_uint64(amount % 22)
    pure result end
}
