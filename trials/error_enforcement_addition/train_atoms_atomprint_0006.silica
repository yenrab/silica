// expected failure E1051: sequence block without a closing end
// print_atom prints the atom with its colon: :ready
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_atom(:ready)
    produces
        pure result
}
