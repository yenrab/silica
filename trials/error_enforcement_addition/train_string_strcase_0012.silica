// expected failure E1042: function 'main' has no return type
// case on string literals; "hello" gives 32
fn main() -> {
    mode: string <- "hello";
    case mode of {
        "chair" -> 30;
        "banana" -> 91;
        "hello" -> 32;
        _: string -> 90
    }
}
