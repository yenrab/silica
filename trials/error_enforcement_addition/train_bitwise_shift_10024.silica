// expected failure E1041: trailing ';' after the block's final expression
// shifts: (seed shl 5) + (wide shr 3) = 40
fn main() -> uint64 {
    seed: uint64 <- 1;
    wide: uint64 <- 68;
    (seed shl 5) + (wide shr 3);
}
