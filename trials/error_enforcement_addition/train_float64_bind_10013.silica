// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// float64 bindings 20.75 + 3.5 printed as 24.25
fn main() -> atom {
    sequence proc[device_io, mem(normal)] a: float64 <- 20.75;
        b: float64 <- 3.5;
        result: atom <- print_float64(a + b)
        produces pure result end
}
