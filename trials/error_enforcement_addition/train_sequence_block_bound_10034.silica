// expected failure E1055: binding 'temp_val' is never used
// sequence result bound to a variable; expect 131
fn main() -> int64 {
    temp_val: int64 <- 41;
    value: int64 <- sequence produces pure 92 end;
    value + 39
}
