// expected failure E1051: sequence block without a closing end
// atom identity: :down != :down is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :down;
        result: atom <- print_bool(left != :down)
    produces
        pure result
}
