// expected failure E1042: function 'main' has no return type
// tuple parameter typed_param; add_pair((3, 19)) = 22
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

fn main() -> {
    add_pair((3, 19))
}
