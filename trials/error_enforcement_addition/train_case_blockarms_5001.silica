// expected failure E1043: whitespace between binding name 'base' and ':'
// case arms with multi-statement block bodies; expect 16
fn main() -> int64 {
    x: int64 <- 0;
    case x of {
        0 -> {
            base :int64 <- 8;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 18;
            x * step + 1
        }
    }
}
