// expected failure E1043: whitespace between binding name 'result' and ':'
// print_uint16 var; prints 2700
fn main() -> atom {
    sequence proc[device_io]
        value: uint16 <- 4 * 45;
        result :atom <- print_uint16(value * 15)
    produces
        pure result
    end
}
