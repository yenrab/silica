// expected failure E2018: binding 'result' has no type annotation
// chained print_int8 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_int8(1 % 6);
        println("");
        result <- print_int8(32)
    produces
        pure result
    end
}
