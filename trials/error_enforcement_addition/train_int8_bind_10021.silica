// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 2
fn main() -> int8 {
    d: int8 <- 35 / 21 / (43 - 3);
    m: int8 <- (d + 52) % (56 + d);
    b: int8 <- d;
    a: int8 <- d + 15;
    -d + 2 / (6 - 5) + m - m + b - b + a - a
}
