// expected failure E2013: unary minus used instead of negate_int16
// negate_int16 variable; prints -76
fn main() -> atom {
    sequence proc[device_io]
        x: int16 <- 76;
        result: atom <- print_int16(-x)
    produces
        pure result
    end
}
