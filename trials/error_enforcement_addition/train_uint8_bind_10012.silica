// expected failure E2003: binding m declared uint32 but used as uint8
// uint8 bindings then expression; expect 140
fn main() -> uint8 {
    m: uint32 <- 32;
    b: uint8 <- m;
    c: uint8 <- 51;
    m - 0 + (51 + 57) + b - b + c - c
}
