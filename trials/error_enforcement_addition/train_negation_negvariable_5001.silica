// expected failure E2005: negate_uint16 does not exist
// negate_int64 variable; prints 98
fn main() -> atom {
    sequence proc[device_io]
        x: int64 <- -98;
        result: atom <- print_uint16(negate_uint16(x))
    produces pure result end
}
