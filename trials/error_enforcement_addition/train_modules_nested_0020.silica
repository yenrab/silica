// expected failure E1055: binding 'leftover' is never used
// nested module-qualified calls; lib_alpha@bump(lib_beta@twice(29)) = 59
use lib_alpha, lib_beta;

fn main() -> int64 {
    leftover: int64 <- 41;
    lib_alpha@bump(lib_beta@twice(29))
}
