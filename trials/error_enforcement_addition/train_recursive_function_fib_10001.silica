// expected failure E2002: undefined accumulator name in the recursive call
// fibonacci; fib(0) = 0
fn fib(n: int64) -> int64 {
    case n of {
        0 -> 0;
        1 -> 1;
        _: int64 -> fib(m - 1) + fib(n - 2)
    }
}

fn main() -> int64 {
    fib(0)
}
