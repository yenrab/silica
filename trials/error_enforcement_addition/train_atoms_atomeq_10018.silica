// expected failure E2003: atom compared against an integer binding
// atom identity: :busy == :busy is true
fn main() -> atom {
    sequence proc[device_io] left: int64 <- 3;
        result: atom <- print_bool(left == :busy)
        produces pure result end
}
