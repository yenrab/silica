// expected failure E2001: integer literal passed to concat
// concat of 2 pieces prints Zürichomega

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "Zürich";
        b: string <- 5;
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces pure :ok end
}
