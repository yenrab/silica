// expected failure E2003: or applied to an integer operand
// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 8;
    b: int64 <- 0;
    hit: boolean <- a or

        b > 5;
    case hit of {
        true -> 1;
        false -> 0
    }
}
