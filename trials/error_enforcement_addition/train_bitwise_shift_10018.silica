// expected failure E2003: shift amount literal 100 is out of range 0..63
// shifts: (seed shl 2) + (wide shr 2) = 50
fn main() -> uint64 {
    seed: uint64 <- 5;
    wide: uint64 <- 122;
    (seed shl 100) + (wide shr 2)
}
