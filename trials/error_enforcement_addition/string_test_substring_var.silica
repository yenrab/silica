// substring with variable: extract from bound string
fn main() -> atom proc[device_io] {
    src: string <- "Hello World";
    s: string <- substring(src, 0, 5);
    print_string(s);
    println("");
    :ok
}
