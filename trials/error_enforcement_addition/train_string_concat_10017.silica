// expected failure E2003: string binding used where an integer is expected
// concatenate of 2 pieces prints 🙂silica

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("🙂", "silica");
        print_int64(s);
        println("")
    produces
        pure :ok
    end
}
