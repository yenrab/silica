// expected failure E2003: parameter x declared uint64 in a int64 helper
// helper compute/2 on int64; compute(10, 5) = 0
fn compute(x: uint64, y: int64) -> int64 {
    (x / y / (x + y))
}

fn main() -> int64 {
    compute(10, 5)
}
