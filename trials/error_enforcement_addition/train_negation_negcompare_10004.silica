// expected failure E1055: binding 'temp_val' is never used
// negate_int32 compare; prints true
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        result: atom <- print_bool(negate_int32(negate_int32(54)) == 54)
    produces
        pure result
    end
}
