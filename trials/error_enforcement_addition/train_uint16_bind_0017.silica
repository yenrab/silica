// expected failure E2003: binding width declared int64 but used as uint16
// uint16 bindings then expression; expect 0
fn main() -> uint16 {
    width: int64 <- (55 + 48) % (25 % 16);
    step: uint16 <- width;
    width / 2 / (step + step)
}
