// expected failure E2013: unary minus on a variable instead of negate_uint64
// uint64 bindings then expression; expect 52
fn main() -> uint64 {
    base: uint64 <- 47 + 1 + (58 + 43);
    a: uint64 <- 26;
    -base + (a + a) % (23 * 9) + base - base
}
