// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot one) band lo) + three) = 18
fn main() -> uint64 {
    one: uint64 <- true;
    lo: uint64 <- 10;
    three: uint64 <- 10;
    (((bnot one) band lo) + three)
}
