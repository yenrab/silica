// expected failure E2013: unary minus used instead of negate_int64
// negate_int64 variable; prints 20
fn main() -> atom {
    sequence proc[device_io] x: int64 <- -20;
        result: atom <- print_int64(-x)
        produces pure result end
}
