// expected failure E2018: binding 'hi' has no type annotation
// uint64 bitwise: (one band hi) bor three = 1
fn main() -> uint64 {
    one: uint64 <- 1;
    hi <- 7;
    three: uint64 <- 1;
    (one band hi) bor three
}
