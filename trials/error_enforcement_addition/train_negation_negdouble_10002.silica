// expected failure E2005: negate_uint32 does not exist
// negate_int16 double; prints 80
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint32(negate_uint32(negate_int16(80)))
    produces
        pure result
    end
}
