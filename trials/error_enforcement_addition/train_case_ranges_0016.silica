// expected failure E1055: binding 'leftover' is never used
// guards partition int64 into three ranges; n = 118 gives 59
fn main() -> int64 {
    leftover: int64 <- 41;
    n: int64 <- 118;
    case n of {
        n: int64 if n < 4 -> n + 100;
        n: int64 if n >= 4 and n < 89 -> 0;
        n: int64 if n >= 89 -> n / 2
    }
}
