// expected failure E2001: atom literal returned from the recursion base case
// recursive tri_acc on uint32; tri_acc(16, 0) = 136
fn tri_acc(n: uint32, acc: uint32) -> uint32 {
    case n == 0 of {
        true -> :done;
        false -> tri_acc(n - 1, acc + n)
    }
}

fn main() -> uint32 {
    tri_acc(16, 0)
}
