// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot one) band mask) bor bits = 15
fn main() -> uint64 {
    one: uint64 <- true;
    mask: uint64 <- 13;
    bits: uint64 <- 14;
    ((bnot one) band mask) bor bits
}
