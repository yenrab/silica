// expected failure E2005: wrong argument count in call to 'digit_count'
// recursive digit_count on uint32; digit_count(75) = 2
fn digit_count(n: uint32) -> uint32 {
    case n < 10 of {
        true -> 1;
        false -> 1 + digit_count(n / 10)
    }
}

fn main() -> uint32 {
    digit_count()
}
