// expected failure E2005: call to unknown function 'starts_witz'
// starts_with prints true
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "path/to/file.silica";
        result: atom <- print_bool(starts_witz(s, "path"))
    produces pure result end
}
