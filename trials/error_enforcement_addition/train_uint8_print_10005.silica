// expected failure E2003: amount is uint8 but passed to print_uint64
// print_uint8 var; prints 33
fn main() -> atom {
    sequence proc[device_io]
        amount: uint8 <- 27 + 6;
        result: atom <- print_uint64(amount)
    produces
        pure result
    end
}
