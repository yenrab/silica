// expected failure E2003: shift amount literal 100 is out of range 0..63
// shifts: (seed shl 5) + (wide shr 4) = 162
fn main() -> uint64 {
    seed: uint64 <- 5;
    wide: uint64 <- 39;
    (seed shl 100) + (wide shr 4)
}
