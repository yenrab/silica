// expected failure E2018: binding 'result' has no type annotation
// atom identity: :red == :retry is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :red;
        result <- print_bool(left == :retry)
    produces pure result end
}
