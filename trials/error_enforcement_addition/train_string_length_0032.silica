// expected failure E1051: sequence block without a closing end
// length_bytes of a bound string prints 6
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "naïve";
        result: atom <- print_int64(length_bytes(text))
    produces pure result
}
