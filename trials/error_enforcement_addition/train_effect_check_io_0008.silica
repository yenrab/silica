// expected failure E2018: binding 'result' has no type annotation
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int64(71)
    produces
        pure result
    end
}
