// expected failure E1054: extra end after the sequence block
// pure sequence block (two); expect 29
fn main() -> int64 {
    sequence
        a: int64 <- 9;
        b: int64 <- 20;
        total: int64 <- a + b
    produces pure total end end
}
