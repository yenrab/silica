// expected failure E2001: greet returns an integer literal
// string helper greet prints hi actor

fn greet(name: string) -> string {
    42
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("actor"));
        println("")
    produces pure :ok end
}
