// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 0
fn main() -> int64 {
    width: int64 <- 26;
    m: int64 <- width - width;
    total: int64 <- 46 - m;
    a: int64 <- total;
    -width + m * 4 / (6 + 39) + a - a
}
