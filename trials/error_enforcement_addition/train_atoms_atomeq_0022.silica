// expected failure E2003: atom compared against an integer binding
// atom identity: :gamma != :gamma is false
fn main() -> atom {
    sequence proc[device_io]
        left: int64 <- 3;
        result: atom <- print_bool(left != :gamma)
    produces pure result end
}
