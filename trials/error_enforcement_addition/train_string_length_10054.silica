// expected failure E2003: length_bytes result printed with print_string
// length_bytes of a bound string prints 6
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "世界";
        result: atom <- print_string(length_bytes(text))
    produces pure result end
}
