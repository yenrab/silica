// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "cherry" gives 34
fn main() -> int64 {
    mode <- "cherry";
    case mode of {
        "omega" -> 52;
        "cherry" -> 34;
        "region" -> 59;
        _: string -> 100
    }
}
