// expected failure E2002: undefined identifier 'n2'
// guards partition int64 into three ranges; n = 8 gives 16
fn main() -> int64 {
    n: int64 <- 8;
    case n of {
        n: int64 if n2 < 5 -> n - 1;
        n: int64 if n >= 5 and n < 29 -> n * 2;
        n: int64 if n >= 29 -> 0
    }
}
