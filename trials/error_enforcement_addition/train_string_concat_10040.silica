// expected failure E1053: duplicate produces keyword
// concatenate of 2 pieces prints regionalpha

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "region";
        b: string <- "alpha";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces produces pure :ok end
}
