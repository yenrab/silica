// expected failure E2018: binding 'a' has no type annotation
// sequence prints and returns the same sum 74
fn main() -> int64 {
    sequence proc[device_io]
        a <- 38;
        b: int64 <- 36;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces pure total end
}
