// expected failure E2001: string result in an integer case arm
// case on string literals; "cherry" gives 80
fn main() -> int64 {
    mode: string <- "cherry";
    case mode of {
        "apple" -> "apple";
        "cherry" -> 80;
        "stone" -> 57;
        _: string -> 31
    }
}
