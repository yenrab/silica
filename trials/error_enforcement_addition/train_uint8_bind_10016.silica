// expected failure E2003: binding k declared int16 but used as uint8
// uint8 bindings then expression; expect 1
fn main() -> uint8 {
    k: int16 <- 57;
    step: uint8 <- 59;
    total: uint8 <- k;
    b: uint8 <- total;
    39 / 31 + step - step + b - b
}
