// expected failure E1045: function 'main' declared without the fn keyword
// pair returns a pair; p + q = 69
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, y * 2)
}

main() -> int64 {
    (p: int64, q: int64) <- pair(42, 13);
    p + q
}
