// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// float16 bindings 17.25 - 18.5 printed as -1.25
fn main() -> atom {
    sequence proc[mem(normal)] a: float16 <- 17.25;
        b: float16 <- 18.5;
        result: atom <- print_float16(a - b)
        produces pure result end
}
