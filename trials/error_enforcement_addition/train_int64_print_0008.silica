// expected failure E2003: amount is int64 but passed to print_uint64
// print_int64 var; prints 58
fn main() -> atom {
    sequence proc[device_io]
        amount: int64 <- 58;
        result: atom <- print_uint64(amount)
    produces
        pure result
    end
}
