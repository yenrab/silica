// expected failure E2003: blend returns float32 but its result feeds print_float64
// float64 helper blend; prints 7.5
fn blend(x: float64) -> float32 {
    x - 28.5 + 3.5
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float64(blend(32.5))
    produces
        pure result
    end
}
