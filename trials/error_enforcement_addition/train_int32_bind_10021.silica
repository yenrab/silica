// expected failure E2003: binding m declared int16 but used as int32
// int32 bindings then expression; expect 114
fn main() -> int32 {
    m: int16 <- (58 - 39) * 6;
    a: int32 <- (m + m) - m;
    count: int32 <- a / a + a / m;
    a + count - count
}
