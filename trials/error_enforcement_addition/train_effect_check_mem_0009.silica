// expected failure E3001: list built outside any sequence
// Positive: list allocation requires sequence proc[mem(normal)] (spec §9.3.1)
fn main() -> int64 {
    xs: List[int64, mem(normal)] <- [3, 16, 23, 22, 14];
    length[int64, mem(normal)](xs)
}
