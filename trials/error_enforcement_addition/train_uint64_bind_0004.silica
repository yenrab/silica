// expected failure E1042: function 'main' has no return type
// uint64 bindings then expression; expect 3
fn main() -> {
    count: uint64 <- 14;
    c: uint64 <- count;
    total: uint64 <- 7;
    height: uint64 <- 8;
    28 / height % (height + height) + c - c + total - total
}
