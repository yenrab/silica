// expected failure E2018: binding 't' has no type annotation
// nested tuple decomposition; x + y + z = 82
fn main() -> int64 {
    t <- (18, (36, 28));
    (x: int64, inner: (int64, int64)) <- t;
    (y: int64, z: int64) <- inner;
    x + y + z
}
