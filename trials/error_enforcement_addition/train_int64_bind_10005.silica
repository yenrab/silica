// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 50
fn main() -> int64 {
    m: int64 <- 17;
    k: int64 <- (m * 4) + (41 - 55);
    height: int64 <- 50;
    step: int64 <- height;
    -m + step + k - k
}
