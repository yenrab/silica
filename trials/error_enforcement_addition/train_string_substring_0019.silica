// expected failure E2001: string literal used as a substring index
// substring uses character indices [4, 4) of "beta"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("beta", "4", 4);
        print_string(s);
        println("")
        produces pure :ok end
}
