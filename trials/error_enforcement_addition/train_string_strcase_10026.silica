// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "region" gives 12
fn main() -> int64 {
    mode <- "region";
    case mode of {
        "cherry" -> 44;
        "delta" -> 17;
        "region" -> 12;
        _: string -> 30
    }
}
