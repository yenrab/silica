// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((five band flags) + three) = 16
fn main() -> uint64 {
    five: uint64 <- true;
    flags: uint64 <- 3;
    three: uint64 <- 13;
    ((five band flags) + three)
}
