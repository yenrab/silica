// expected failure E2001: integer literal passed to concat
// concat of 4 pieces prints übercafécherrysouth

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("über", "café"), "cherry"), 5);
        print_string(s);
        println("")
    produces pure :ok end
}
