// expected failure E1041: semicolon after the last binding before produces
// sequence binding RHS literal then produces with no semicolon; want 125
fn main() -> int64 {
    sequence
        result: int64 <- 125;
    produces
        pure result
    end
}
