// expected failure E1052: duplicate sequence keyword
// substring uses character indices [3, 4) of "beta"
fn main() -> atom {
    sequence sequence proc[device_io] text: string <- "beta";
        s: string <- substring(text, 3, 4);
        print_string(s);
        println("")
        produces pure :ok end
}
