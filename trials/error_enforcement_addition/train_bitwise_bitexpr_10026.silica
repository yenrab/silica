// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot mask) band one) band lo = 8
fn main() -> uint64 {
    mask: uint64 <- true;
    one: uint64 <- 10;
    lo: uint64 <- 14;
    ((bnot mask) band one) band lo
}
