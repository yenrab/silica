// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot flags) band hi) band three = 12
fn main() -> uint64 {
    flags: uint64 <- true;
    hi: uint64 <- 15;
    three: uint64 <- 12;
    ((bnot flags) band hi) band three
}
