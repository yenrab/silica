// expected failure E2015: list pattern without its mem(Space)
// case on a non-empty list; expect 100
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [12, 35, 34, 2];
        r: int64 <- case xs of {
            []: List[int64] -> 25;
            [h: int64, _: List[int64, mem(normal)]] -> h + 88;
            _: List[int64, mem(normal)] -> 25
        }
    produces
        pure r
    end
}
