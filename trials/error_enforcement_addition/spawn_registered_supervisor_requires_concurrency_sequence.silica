// Enforce that spawn_registered_supervisor must run inside sequence proc[concurrency].
// This call is intentionally outside any sequence block.

fn main() -> supervisor_ref {
    spawn_registered_supervisor(MissingConcurrencySup, 0, :missing_concurrency_sup)
}
