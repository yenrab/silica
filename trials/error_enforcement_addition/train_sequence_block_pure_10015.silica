// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 27
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 5;
        b: int64 <- 22;
        total: int64 <- a + b
    produces
        pure total
    end
}
