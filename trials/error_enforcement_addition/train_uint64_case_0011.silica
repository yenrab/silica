// expected failure E2001: string literal in one case arm of an integer case
// case on uint64 value 20; expect 161
fn main() -> uint64 {
    v: uint64 <- 20;
    case v of {
        33 -> "47";
        _: uint64 -> 161
    }
}
