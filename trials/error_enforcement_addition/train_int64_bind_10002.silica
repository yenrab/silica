// expected failure E1042: function 'main' has no return type
// int64 bindings then expression; expect 1
fn main() -> {
    a: int64 <- (8 + 48);
    height: int64 <- 33 / 21;
    b: int64 <- a / a;
    m: int64 <- a + (0 - 29);
    m / m + 49 % height + b - b
}
