// expected failure E2018: binding 's' has no type annotation
// ends_with prints false
fn main() -> atom {
    sequence proc[device_io]
        s <- "path/to/file.silica";
        result: atom <- print_bool(ends_with(s, "qq"))
    produces
        pure result
    end
}
