// expected failure E2005: negate_uint8 does not exist
// negate_int16 double; prints -9
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint8(negate_uint8(negate_int16(-9)))
    produces
        pure result
    end
}
