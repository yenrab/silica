// expected failure E2003: n is uint8 but passed to print_uint16
// print_uint8 var; prints 10
fn main() -> atom {
    sequence proc[device_io]
        n: uint8 <- 10;
        result: atom <- print_uint16(n)
    produces pure result end
}
