// expected failure E1041: trailing ';' after the block's final expression
// int16 bindings then expression; expect 126
fn main() -> int16 {
    b: int16 <- 42;
    height: int16 <- b + b + b;
    total: int16 <- 0;
    height + total - total;
}
