// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// int8 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io, mem(normal)] _: atom <- print_bool((27 - 4) > 38);
        println("");
        result: atom <- print_bool((47 + 52) >= 53 % 56)
        produces pure result end
}
