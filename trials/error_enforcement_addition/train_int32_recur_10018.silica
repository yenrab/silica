// expected failure E2001: atom literal returned from the recursion base case
// recursive sum_to on int32; sum_to(4) = 10
fn sum_to(n: int32) -> int32 {
    case n == 0 of {
        true -> :zero;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> int32 {
    sum_to(4)
}
