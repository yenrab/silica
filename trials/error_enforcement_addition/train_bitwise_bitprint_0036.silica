// expected failure E1042: function 'main' has no return type
// print_uint64 of 40 bor 4 = 44
fn main() -> {
    sequence proc[device_io]
        a: uint64 <- 40;
        b: uint64 <- 4;
        result: atom <- print_uint64(a bor b)
    produces pure result end
}
