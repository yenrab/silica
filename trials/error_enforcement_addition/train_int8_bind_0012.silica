// expected failure E1055: binding 'temp_val' is never used
// int8 bindings then expression; expect 33
fn main() -> int8 {
    temp_val: int64 <- 41;
    width: int8 <- 41 - 24;
    step: int8 <- width % 53;
    m: int8 <- width;
    (13 + m) + (20 - width) + step - step
}
