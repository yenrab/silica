// expected failure E2005: integer pattern against a boolean scrutinee
// case on a boolean binding; expect 52
fn main() -> int64 {
    flag: boolean <- false;
    case flag of {
        1 -> 80;
        false -> 52
    }
}
