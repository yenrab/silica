// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (flags band bits) band hi = 0
fn main() -> uint64 {
    flags: uint64 <- true;
    bits: uint64 <- 5;
    hi: uint64 <- 2;
    (flags band bits) band hi
}
