// expected failure E2005: negate_uint8 does not exist
// negate_int64 triple; prints -47
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint8(negate_uint8(negate_int64(negate_int64(47))))
    produces
        pure result
    end
}
