// Enforce E3001: list allocation must run inside sequence proc[mem(S)] … produces pure … end.
fn main() -> int64 {
    xs: List[int64, mem(normal)] <- [1, 2, 3];
    length[int64, mem(normal)](xs)
}
