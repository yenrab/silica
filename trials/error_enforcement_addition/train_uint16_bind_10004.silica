// expected failure E2013: unary minus on a variable instead of negate_uint16
// uint16 bindings then expression; expect 5
fn main() -> uint16 {
    k: uint16 <- 48 * 60;
    a: uint16 <- 47 + (k + 34);
    -k + 5 + a - a
}
