// expected failure E1055: binding 'scratch' is never used
// print_uint64 literal; prints 6
fn main() -> atom {
    scratch: int64 <- 41;
    sequence proc[device_io]
        result: atom <- print_uint64(6)
    produces
        pure result
    end
}
