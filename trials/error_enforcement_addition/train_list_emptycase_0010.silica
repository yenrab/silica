// expected failure E2015: list pattern without its mem(Space)
// case on a non-empty list; expect 33
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [1, 0, 14, 18];
        r: int64 <- case xs of {
            []: List[int64] -> 55;
            [h: int64, _: List[int64, mem(normal)]] -> h + 32;
            _: List[int64, mem(normal)] -> 55
        }
    produces
        pure r
    end
}
