// expected failure E2018: binding 'm' has no type annotation
// int32 bindings then expression; expect 0
fn main() -> int32 {
    c: int32 <- 7;
    b: int32 <- 2;
    count: int32 <- c;
    m <- 53 + 44;
    b % 52 / (b * m) + count - count
}
