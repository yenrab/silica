// expected failure E1042: function 'sum_to' has no return type
// recursive sum_to on int8; sum_to(15) = 120
fn sum_to(n: int8) {
    case n == 0 of {
        true -> 0;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> int8 {
    sum_to(15)
}
