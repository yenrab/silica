// expected failure E1047: named struct declaration 'Point' is not allowed
// inline record field access; x + y = 70
struct Point { x: int64, y: int64 }

fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 60, y: 10 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv + yv
}
