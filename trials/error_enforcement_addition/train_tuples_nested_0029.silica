// expected failure E1042: function 'main' has no return type
// nested tuple decomposition; x + y + z = 42
fn main() -> {
    t: (int64, (int64, int64)) <- (8, (19, 15));
    (x: int64, inner: (int64, int64)) <- t;
    (y: int64, z: int64) <- inner;
    x + y + z
}
