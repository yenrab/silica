// expected failure E2003: atom compared against an integer binding
// atom identity: :red == :idle is false
fn main() -> atom {
    sequence proc[device_io]
        left: int64 <- 3;
        result: atom <- print_bool(left == :idle)
    produces pure result end
}
