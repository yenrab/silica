// expected failure E1055: binding 'leftover' is never used
// root program using the new library module train_lib_b10000_clamp6; train_lib_b10000_clamp6@clamp6_apply(16) = 6
use train_lib_b10000_clamp6;

fn main() -> int64 {
    leftover: int64 <- 41;
    train_lib_b10000_clamp6@clamp6_apply(16)
}
