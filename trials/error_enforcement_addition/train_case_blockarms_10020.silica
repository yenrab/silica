// expected failure E1055: block-local binding never used
// case arms with multi-statement block bodies; expect 40
fn main() -> int64 {
    x: int64 <- 3;
    case x of {
        0 -> {
            base: int64 <- 16;
            16 * 2
        }
        _: int64 -> {
            step: int64 <- 13;
            x * step + 1
        }
    }
}
