// expected failure E1055: binding 'leftover' is never used
// false or true printed
fn main() -> atom {
    leftover: int64 <- 41;
    sequence proc[device_io] result: boolean <- false or true;
        outcome: atom <- print_bool(result)
        produces pure outcome end
}
