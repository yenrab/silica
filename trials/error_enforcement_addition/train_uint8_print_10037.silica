// expected failure E1042: function 'main' has no return type
// print_uint8 var; prints 56
fn main() -> {
    sequence proc[device_io]
        amount: uint8 <- 56;
        result: atom <- print_uint8(amount)
    produces
        pure result
    end
}
