// expected failure E1042: function 'main' has no return type
// head on [1, 14, 32] gives 1
fn main() -> {
    sequence proc[mem(normal)] xs: List[int64, mem(normal)] <- [1, 14, 32];
        first: int64 <- head[int64, mem(normal)](xs)
        produces pure first end
}
