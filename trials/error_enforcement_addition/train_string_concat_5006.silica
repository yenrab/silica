// expected failure E2005: call to unknown function 'my_print_string'
// concat of 3 pieces prints southcloudtable

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat("south", "cloud"), "table");
        my_print_string(s);
        println("")
    produces pure :ok end
}
