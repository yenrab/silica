// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// true and true printed
fn main() -> atom {
    sequence proc[mem(normal)] result: boolean <- true and true;
        outcome: atom <- print_bool(result)
        produces pure outcome end
}
