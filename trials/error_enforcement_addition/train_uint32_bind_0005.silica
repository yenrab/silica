// expected failure E2003: binding base declared int32 but used as uint32
// uint32 bindings then expression; expect 0
fn main() -> uint32 {
    base: int32 <- 48;
    count: uint32 <- 16 / 24 % (10 * base);
    28 / 26 / base + count - count
}
