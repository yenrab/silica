// expected failure E1042: function 'combine' has no return type
// helper combine/2 on uint16; combine(20, 20) = 2
fn combine(x: uint16, y: uint16) -> {
    46 / y * (y / x)
}

fn main() -> uint16 {
    combine(20, 20)
}
