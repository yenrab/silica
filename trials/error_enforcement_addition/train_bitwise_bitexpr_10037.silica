// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bits bor five) + one) = 28
fn main() -> uint64 {
    bits: uint64 <- true;
    five: uint64 <- 15;
    one: uint64 <- 13;
    ((bits bor five) + one)
}
