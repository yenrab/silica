// expected failure E1041: trailing ';' after a block arm's final expression
// case arms with multi-statement block bodies; expect 18
fn main() -> int64 {
    x: int64 <- 0;
    case x of {
        0 -> {
            base: int64 <- 9;
            base * 2;
        }
        _: int64 -> {
            step: int64 <- 3;
            x * step + 1
        }
    }
}
