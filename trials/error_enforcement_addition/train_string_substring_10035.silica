// expected failure E2001: string literal used as a substring index
// substring uses character indices [5, 5) of "river"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "river";
        s: string <- substring(text, "5", 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
