// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "apple" gives 4
fn main() -> int64 {
    mode <- "apple";
    case mode of {
        "river" -> 62;
        "apple" -> 4;
        "north" -> 88;
        _: string -> 20
    }
}
