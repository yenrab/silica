// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (bits band one) bor mask = 9
fn main() -> uint64 {
    bits: uint64 <- true;
    one: uint64 <- 11;
    mask: uint64 <- 8;
    (bits band one) bor mask
}
