// expected failure E1042: function 'pair' has no return type
// pair returns a pair; p + q = 94
fn pair(x: int64, y: int64) {
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- pair(7, 43);
    p + q
}
