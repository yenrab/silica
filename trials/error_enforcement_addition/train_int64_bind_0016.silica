// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 51
fn main() -> int64 {
    b: int64 <- 13 * 48 + 37;
    step: int64 <- (b + 47 + 18 * b);
    a: int64 <- step;
    -b + 51 + a - a
}
