// expected failure E1047: named struct declaration 'Config' is not allowed
// pure sequence block (two); expect 15
struct Config { x: int64, y: int64 }

fn main() -> int64 {
    sequence
        a: int64 <- 3;
        b: int64 <- 12;
        total: int64 <- a + b
    produces pure total end
}
