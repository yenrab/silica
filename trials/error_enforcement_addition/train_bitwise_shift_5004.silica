// expected failure E1041: trailing ';' after the block's final expression
// shifts: (seed shl 2) + (wide shr 2) = 43
fn main() -> uint64 {
    seed: uint64 <- 3;
    wide: uint64 <- 126;
    (seed shl 2) + (wide shr 2);
}
