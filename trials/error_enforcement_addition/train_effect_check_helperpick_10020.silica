// expected failure E2005: call to unknown function 'pick_valuex'
// Positive: pure helper chosen from a case arm; the effect runs in the sequence body
fn pick_value(flag: boolean) -> int64 {
    case flag of {
        true -> 5;
        false -> 7
    }
}

fn main() -> atom {
    flag: boolean <- false;
    sequence proc[device_io]
        v: int64 <- pick_valuex(flag);
        result: atom <- print_int64(v)
    produces
        pure result
    end
}
