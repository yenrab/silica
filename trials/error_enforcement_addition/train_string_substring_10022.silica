// expected failure E2001: string literal used as a substring index
// substring uses character indices [2, 5) of "alpha"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("alpha", "2", 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
