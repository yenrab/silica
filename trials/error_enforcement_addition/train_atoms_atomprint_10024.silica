// expected failure E3009: proc[...] written on the function return type instead of the sequence
// print_atom prints the atom with its colon: :right
fn main() -> atom proc[device_io] {
    sequence proc[device_io] state: atom <- :right;
        result: atom <- print_atom(state)
        produces pure result end
}
