// expected failure E4016: no main/0: entry function renamed to 'entry'
// uint64 bitwise: (((bnot flags) band seven) + two) = 15
fn entry() -> uint64 {
    flags: uint64 <- 1;
    seven: uint64 <- 1;
    two: uint64 <- 15;
    (((bnot flags) band seven) + two)
}
