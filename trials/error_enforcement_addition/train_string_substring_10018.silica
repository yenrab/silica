// expected failure E2001: string literal used as a substring index
// substring uses character indices [2, 5) of "cloud"
fn main() -> atom {
    sequence proc[device_io] text: string <- "cloud";
        s: string <- substring(text, "2", 5);
        print_string(s);
        println("")
        produces pure :ok end
}
