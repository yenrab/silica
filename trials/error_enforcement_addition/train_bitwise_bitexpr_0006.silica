// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (lo bor bits) band mask = 2
fn main() -> uint64 {
    lo: uint64 <- true;
    bits: uint64 <- 2;
    mask: uint64 <- 7;
    (lo bor bits) band mask
}
