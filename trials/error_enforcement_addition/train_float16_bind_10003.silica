// expected failure E2003: integer binding mixed into float arithmetic
// float16 bindings 2.5 + 37.0 printed as 39.5
fn main() -> atom {
    sequence proc[device_io] a: float16 <- 2.5;
        b: int64 <- 37;
        result: atom <- print_float16(a + b)
        produces pure result end
}
