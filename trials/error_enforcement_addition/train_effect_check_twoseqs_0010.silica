// expected failure E2005: call to unknown function 'print_int64_all'
// Positive: two sequences with separate effect declarations in one function
fn main() -> int64 {
    sequence proc[device_io]
        print_int64_all(2);
        println("")
    produces pure :ok end;
    n: int64 <- sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [2, 9]
    produces
        pure length[int64, mem(normal)](xs)
    end;
    n
}
