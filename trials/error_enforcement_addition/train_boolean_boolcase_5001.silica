// expected failure E1055: binding 'temp_val' is never used
// case on a boolean binding; expect 20
fn main() -> int64 {
    temp_val: int64 <- 41;
    flag: boolean <- false;
    case flag of {
        true -> 74;
        false -> 20
    }
}
