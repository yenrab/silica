// expected failure E4016: no main/0: entry function renamed to 'mainx'
// length_chars of a bound string prints 1
fn mainx() -> atom {
    sequence proc[device_io]
        text: string <- "🎉";
        result: atom <- print_int64(length_chars(text))
    produces
        pure result
    end
}
