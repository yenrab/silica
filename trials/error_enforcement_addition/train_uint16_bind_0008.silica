// expected failure E2018: binding 'c' has no type annotation
// uint16 bindings then expression; expect 15
fn main() -> uint16 {
    base: uint16 <- 44;
    total: uint16 <- base;
    c <- base;
    59 % base + total - total + c - c
}
