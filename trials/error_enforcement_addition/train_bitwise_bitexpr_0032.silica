// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (flags band one) band three = 0
fn main() -> uint64 {
    flags: uint64 <- true;
    one: uint64 <- 10;
    three: uint64 <- 9;
    (flags band one) band three
}
