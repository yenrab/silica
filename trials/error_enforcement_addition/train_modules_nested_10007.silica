// expected failure E1047: named struct declaration 'Pair' is not allowed
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(9)) = 20
use lib_alpha, lib_beta;

struct Pair { x: int64, y: int64 }

fn main() -> int64 {
    lib_beta@twice(lib_alpha@bump(9))
}
