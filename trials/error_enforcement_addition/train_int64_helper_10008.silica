// expected failure E1055: binding 'temp_val' is never used
// helper combine/2 on int64; combine(8, 4) = 8
fn combine(x: int64, y: int64) -> int64 {
    temp_val: int64 <- 41;
    y % x + y
}

fn main() -> int64 {
    combine(8, 4)
}
