// expected failure E1042: function 'main' has no return type
// string helper greet prints hi stone

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() {
    sequence proc[device_io]
        print_string(greet("stone"));
        println("")
    produces pure :ok end
}
