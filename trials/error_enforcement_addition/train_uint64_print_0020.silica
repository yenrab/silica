// expected failure E2003: x is uint64 but passed to print_int16
// print_uint64 var; prints 0
fn main() -> atom {
    sequence proc[device_io]
        x: uint64 <- 22 / 23;
        result: atom <- print_int16(x)
    produces
        pure result
    end
}
