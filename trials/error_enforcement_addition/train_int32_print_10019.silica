// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// print_int32 var; prints 3
fn main() -> atom {
    sequence proc[mem(normal)]
        x: int32 <- 3;
        result: atom <- print_int32(x - 0)
    produces
        pure result
    end
}
