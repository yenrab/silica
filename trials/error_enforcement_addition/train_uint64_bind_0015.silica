// expected failure E1055: binding 'leftover' is never used
// uint64 bindings then expression; expect 86
fn main() -> uint64 {
    leftover: int64 <- 41;
    c: uint64 <- 48 * 13 % (17 * 7);
    d: uint64 <- (c + 57);
    b: uint64 <- c;
    m: uint64 <- c % d + c % b;
    c - m + d
}
