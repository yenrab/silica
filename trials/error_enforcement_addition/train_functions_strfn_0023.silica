// expected failure E1041: trailing ';' after the block's final expression
// string helper greet prints hi table

fn greet(name: string) -> string {
    concatenate("hi ", name);
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("table"));
        println("")
    produces
        pure :ok
    end
}
