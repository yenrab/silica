// expected failure E2018: binding 'height' has no type annotation
// uint64 bindings then expression; expect 31
fn main() -> uint64 {
    d: uint64 <- 50 + 10;
    height <- (d % 32);
    k: uint64 <- height;
    k / k + 30
}
