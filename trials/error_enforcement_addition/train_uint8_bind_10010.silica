// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 60
fn main() -> uint8 {
    height: uint8 <- 49;
    step: uint8 <- height % 19 * (37 / height);
    -height + height + 11 + (step + step)
}
