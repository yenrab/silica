// expected failure E1042: function 'main' has no return type
// string helper greet prints hi delta

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() {
    sequence proc[device_io] print_string(greet("delta"));
        println("")
        produces pure :ok end
}
