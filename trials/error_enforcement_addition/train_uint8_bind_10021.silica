// expected failure E2003: binding m declared uint16 but used as uint8
// uint8 bindings then expression; expect 14
fn main() -> uint8 {
    m: uint16 <- 58;
    width: uint8 <- m;
    base: uint8 <- 40;
    14 + width - width + base - base
}
