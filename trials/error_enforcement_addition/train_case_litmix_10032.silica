// expected failure E2005: atom pattern against an integer scrutinee
// literal patterns, a guard and a typed wildcard on uint64; v = 10 gives 5
fn main() -> uint64 {
    v: uint64 <- 10;
    case v of {
        :4 -> 4;
        1 -> 33;
        8 -> 25;
        m: uint64 if m > 12 -> 27;
        _: uint64 -> 5
    }
}
