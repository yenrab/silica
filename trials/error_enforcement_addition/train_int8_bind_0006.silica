// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 43
fn main() -> int8 {
    count: int8 <- 47 + 58 / 36;
    k: int8 <- 25;
    -count + count - count / 9 + k - k
}
