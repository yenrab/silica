// expected failure E2003: binding step declared int16 but used as uint32
// uint32 bindings then expression; expect 30
fn main() -> uint32 {
    step: int16 <- 26;
    c: uint32 <- step;
    m: uint32 <- (32 + c);
    30 + m - m
}
