// expected failure E1041: trailing ';' after the block's final expression
// nested module-qualified calls; lib_math_utils_spec@multiply(lib_middle@ten(), 5) = 50
use lib_middle, lib_math_utils_spec;

fn main() -> int64 {
    lib_math_utils_spec@multiply(lib_middle@ten(), 5);
}
