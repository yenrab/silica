// expected failure E1040: missing ';' between two statements
// substring uses character indices [3, 3) of "αβγ"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("αβγ", 3, 3);
        print_string(s)
        println("")
    produces
        pure :ok
    end
}
