// expected failure E2005: call to unknown function 'my_print_atom'
// print_atom prints the atom with its colon: :right
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- my_print_atom(:right)
    produces pure result end
}
