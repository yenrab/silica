// expected failure E2003: binding d declared int16 but used as uint64
// uint64 bindings then expression; expect 48
fn main() -> uint64 {
    d: int16 <- 24;
    height: uint64 <- d;
    m: uint64 <- 21;
    a: uint64 <- height;
    (height + d) % (d + 35) + m - m + a - a
}
