// expected failure E1041: trailing ';' after the block's final expression
// nullary functions calling each other; expect 171
fn base() -> int64 {
    85
}

fn next() -> int64 {
    base() + 1;
}

fn main() -> int64 {
    next() + base()
}
