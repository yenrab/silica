// expected failure E1042: function 'main' has no return type
// int16 bindings then expression; expect 1
fn main() {
    step: int16 <- 13;
    m: int16 <- step;
    total: int16 <- step;
    (47 + 57) / (step + 56) + m - m + total - total
}
