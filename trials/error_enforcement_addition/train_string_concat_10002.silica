// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// concatenate of 2 pieces prints stone日本語

fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        a: string <- "stone";
        b: string <- "日本語";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
