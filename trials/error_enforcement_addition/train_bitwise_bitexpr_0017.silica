// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (five band mask) bor hi = 1
fn main() -> uint64 {
    five: uint64 <- true;
    mask: uint64 <- 3;
    hi: uint64 <- 1;
    (five band mask) bor hi
}
