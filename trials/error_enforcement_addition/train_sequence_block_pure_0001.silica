// expected failure E1055: binding 'temp_val' is never used
// pure sequence block (two); expect 38
fn main() -> int64 {
    temp_val: int64 <- 41;
    sequence
        a: int64 <- 7;
        b: int64 <- 31;
        total: int64 <- a + b
    produces
        pure total
    end
}
