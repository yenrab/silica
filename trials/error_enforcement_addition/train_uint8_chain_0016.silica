// expected failure E1042: function 'main' has no return type
// chained print_uint8 calls separated by println
fn main() {
    sequence proc[device_io]
        _: atom <- print_uint8(58);
        println("");
        result: atom <- print_uint8(4 % 40 + (44 % 45))
    produces
        pure result
    end
}
