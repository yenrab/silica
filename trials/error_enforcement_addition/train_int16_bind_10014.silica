// expected failure E2013: unary minus on a variable instead of negate_int16
// int16 bindings then expression; expect 67
fn main() -> int16 {
    count: int16 <- 53;
    total: int16 <- 48;
    -count + 9 + 16 + (24 + 18) + count - count + total - total
}
