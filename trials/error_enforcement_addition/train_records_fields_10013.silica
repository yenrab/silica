// expected failure E1043: whitespace between binding name 'p' and ':'
// inline record field access; x * y = 164
fn main() -> int64 {
    p :{ x: int64, y: int64 } <- { x: 4, y: 41 };
    p.x * p.y
}
