// expected failure E2001: atom literal returned from the recursion base case
// recursive steps on int64; steps(11, 0) = 11
fn steps(n: int64, acc: int64) -> int64 {
    case n == 0 of {
        true -> :done;
        false -> steps(n - 1, acc + 1)
    }
}

fn main() -> int64 {
    steps(11, 0)
}
