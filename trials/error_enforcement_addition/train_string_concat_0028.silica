// expected failure E2001: integer literal passed to concat
// concat of 2 pieces prints northriver

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("north", 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
