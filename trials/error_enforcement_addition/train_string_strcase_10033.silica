// expected failure E2001: string result in an integer case arm
// case on string literals; "actor" gives 55
fn main() -> int64 {
    mode: string <- "actor";
    case mode of {
        "cloud" -> "cloud";
        "actor" -> 55;
        "north" -> 0;
        _: string -> 78
    }
}
