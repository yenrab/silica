// expected failure E3010: concurrency declared but unused
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    sequence proc[device_io, concurrency]
        result: atom <- print_int64(33)
    produces
        pure result
    end
}
