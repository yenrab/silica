// expected failure E2001: atom literal returned from the recursion base case
// recursive sum_to on int64; sum_to(16) = 136
fn sum_to(n: int64) -> int64 {
    case n == 0 of {
        true -> :zero;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> int64 {
    sum_to(16)
}
