// expected failure E1055: binding 'scratch' is never used
// fibonacci; fib(9) = 34
fn fib(n: int64) -> int64 {
    scratch: int64 <- 41;
    case n of {
        0 -> 0;
        1 -> 1;
        _: int64 -> fib(n - 1) + fib(n - 2)
    }
}

fn main() -> int64 {
    fib(9)
}
