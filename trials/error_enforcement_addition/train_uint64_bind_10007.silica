// expected failure E2018: binding 'width' has no type annotation
// uint64 bindings then expression; expect 54
fn main() -> uint64 {
    width <- 35 + 50 + 23 % 32;
    a: uint64 <- width;
    a / (width / 52)
}
