// expected failure E2018: binding 'b' has no type annotation
// uint16 bindings then expression; expect 53
fn main() -> uint16 {
    b <- 19;
    step: uint16 <- (b - b) % (48 * b);
    53 + step - step
}
