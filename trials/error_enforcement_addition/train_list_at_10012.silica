// expected failure E1042: function 'main' has no return type
// list_at index 0 of [27, 24, 33] is 27
fn main() -> {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [27, 24, 33];
        v: int64 <- list_at[int64, mem(normal)](xs, 0)
    produces pure v end
}
