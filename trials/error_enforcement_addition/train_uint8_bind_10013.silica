// expected failure E2003: binding b declared int16 but used as uint8
// uint8 bindings then expression; expect 11
fn main() -> uint8 {
    b: int16 <- 49 % 13 + 37 / 49;
    height: uint8 <- b * b + (31 + b);
    c: uint8 <- b + 2;
    a: uint8 <- 32 + height + (12 - c);
    25 % (a / c)
}
