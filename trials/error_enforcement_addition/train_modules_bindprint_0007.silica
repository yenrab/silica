// expected failure E1042: function 'main' has no return type
// qualified call bound then printed; prints 18
use lib_alpha;

fn main() -> {
    sequence proc[device_io] v: int64 <- lib_alpha@bump(17);
        result: atom <- print_int64(v)
        produces pure result end
}
