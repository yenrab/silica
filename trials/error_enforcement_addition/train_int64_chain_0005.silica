// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// chained print_int64 calls separated by println
fn main() -> atom {
    sequence proc[mem(normal)] _: atom <- print_int64(3 / 47);
        println("");
        result: atom <- print_int64((56 - 1) / (15 + 40))
        produces pure result end
}
