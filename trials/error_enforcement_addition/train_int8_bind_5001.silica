// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 51
fn main() -> int8 {
    width: int8 <- (59 + 7) / (54 + 47);
    k: int8 <- 55 + width - 22 / 36;
    -width + 51 + k - k
}
