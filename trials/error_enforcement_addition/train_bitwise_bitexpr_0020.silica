// expected failure E2018: binding 'three' has no type annotation
// uint64 bitwise: ((two bor three) + lo) = 9
fn main() -> uint64 {
    two: uint64 <- 4;
    three <- 3;
    lo: uint64 <- 2;
    ((two bor three) + lo)
}
