// expected failure E1041: semicolon after the last binding before produces
// sequence binding RHS literal then produces with no semicolon; want 17
fn main() -> int64 {
    sequence
        result: int64 <- 17;
    produces
        pure result
    end
}
