// expected failure E1042: function 'main' has no return type
// atom identity: :up == :alpha is false
fn main() {
    sequence proc[device_io] left: atom <- :up;
        result: atom <- print_bool(left == :alpha)
        produces pure result end
}
