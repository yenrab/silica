// regression (already diagnosed by silica-999997): expected failure E2001: integer literal passed to print_bool
fn main() -> atom {
    sequence proc[device_io]
        r: atom <- print_bool(5)
    produces pure r end
}
