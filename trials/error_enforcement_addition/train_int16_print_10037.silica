// expected failure E2005: call to unknown function 'print_int16_all'
// print_int16 var; prints 56
fn main() -> atom {
    sequence proc[device_io]
        n: int16 <- 39;
        result: atom <- print_int16_all(17 + n)
    produces
        pure result
    end
}
