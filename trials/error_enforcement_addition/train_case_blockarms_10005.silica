// expected failure E1042: function 'main' has no return type
// case arms with multi-statement block bodies; expect 81
fn main() {
    x: int64 <- 4;
    case x of {
        0 -> {
            base: int64 <- 14;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 20;
            x * step + 1
        }
    }
}
