// expected failure E2003: binding base declared uint8 but used as int16
// int16 bindings then expression; expect 29
fn main() -> int16 {
    base: uint8 <- (29 + 6) * (28 + 29);
    m: int16 <- base;
    29 + m - m
}
