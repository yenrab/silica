// expected failure E1047: named struct declaration 'Config' is not allowed
// case scrutinee is a tuple-returning call; expect 37
struct Config { x: int64, y: int64 }

fn pair(n: int64) -> (int64, int64) {
    (n, n + 1)
}

fn main() -> int64 {
    case pair(11) of {
        t: (int64, int64) -> {
            (x: int64, y: int64) <- t;
            x + y + 14
        }
    }
}
