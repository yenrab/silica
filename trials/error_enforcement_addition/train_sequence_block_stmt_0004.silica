// expected failure E1053: duplicate produces keyword
// effectful sequence used as a statement before the return value 91
fn main() -> int64 {
    sequence proc[device_io]
        print_string("alpha");
        println("")
    produces produces pure :ok end;
    91
}
