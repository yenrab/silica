// expected failure E2005: access to a field the record does not have
// inline record field access; x + y = 61
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 26, y: 35 };
    p.x + p.z
}
