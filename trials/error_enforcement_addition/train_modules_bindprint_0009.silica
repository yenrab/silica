// expected failure E1050: produces clause without pure
// qualified call bound then printed; prints 10
use lib_middle;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_middle@ten();
        result: atom <- print_int64(v)
    produces result end
}
