// expected failure E2018: binding 'count' has no type annotation
// int64 bindings then expression; expect 0
fn main() -> int64 {
    step: int64 <- 49;
    count <- 35 / 31 + 11 * step;
    total: int64 <- 20;
    m: int64 <- count;
    m / count / (count + step) + total - total
}
