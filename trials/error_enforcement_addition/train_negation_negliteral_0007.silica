// expected failure E1047: named struct declaration 'Config' is not allowed
// negate_int8 literal; prints 40
struct Config { x: int64, y: int64 }

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_int8(negate_int8(-40))
    produces
        pure result
    end
}
