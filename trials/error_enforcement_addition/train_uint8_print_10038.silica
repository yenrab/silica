// expected failure E1043: whitespace between binding name 'a' and ':'
// print_uint8 two; prints 30 then 29
fn main() -> atom {
    sequence proc[device_io]
        a :uint8 <- 30 % 39;
        b: uint8 <- 29;
        _: atom <- print_uint8(a);
        println("");
        result: atom <- print_uint8(b)
    produces pure result end
}
