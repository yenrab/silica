// expected failure E1042: function 'minmax' has no return type
// minmax returns a pair; p + q = 51
fn minmax(x: int64, y: int64) {
    case x < y of {
        true -> (x, y);
        false -> (y, x)
    }
}

fn main() -> int64 {
    (p: int64, q: int64) <- minmax(13, 38);
    p + q
}
