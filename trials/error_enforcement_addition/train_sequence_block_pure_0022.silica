// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 52
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 10;
        b: int64 <- 42;
        total: int64 <- a + b
    produces pure total end
}
