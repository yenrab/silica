// regression (already diagnosed by silica-999997): expected failure E3010: mem(normal) declared on a sequence that calls nothing
fn main() -> int64 {
    sequence proc[mem(normal)] produces pure 42 end
}
