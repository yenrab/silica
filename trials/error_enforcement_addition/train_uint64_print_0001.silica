// expected failure E1052: duplicate sequence keyword
// print_uint64 expr; prints 45
fn main() -> atom {
    sequence sequence proc[device_io]
        result: atom <- print_uint64(45)
    produces
        pure result
    end
}
