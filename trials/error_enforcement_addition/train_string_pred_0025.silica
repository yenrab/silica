// expected failure E1045: function 'main' declared without the fn keyword
// ends_with prints true
main() -> atom {
    sequence proc[device_io]
        s: string <- "北京市";
        result: atom <- print_bool(ends_with(s, "市"))
    produces
        pure result
    end
}
