// expected failure E1042: function 'wrap' has no return type
// string helper wrap; prints [region]

fn wrap(s: string) {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(wrap("region"));
        println("")
    produces pure :ok end
}
