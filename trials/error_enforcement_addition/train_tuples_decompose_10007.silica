// expected failure E1041: trailing ';' after the block's final expression
// decompose a 3-tuple from a literal; a + b + c = 104
fn main() -> int64 {
    (a: int64, b: int64, c: int64) <- (37, 33, 34);
    a + b + c;
}
