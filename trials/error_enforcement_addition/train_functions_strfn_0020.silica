// expected failure E2001: greet returns an integer literal
// string helper greet prints hi stone

fn greet(name: string) -> string {
    42
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("stone"));
        println("")
    produces
        pure :ok
    end
}
