// expected failure E2001: string result in an integer case arm
// case on string literals; "cloud" gives 2
fn main() -> int64 {
    mode: string <- "cloud";
    case mode of {
        "cloud" -> "cloud";
        "south" -> 7;
        "river" -> 16;
        _: string -> 58
    }
}
