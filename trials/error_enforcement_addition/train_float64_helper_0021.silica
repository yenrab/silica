// expected failure E1042: function 'half_sum' has no return type
// float64 helper half_sum; prints 73.0
fn half_sum(x: float64) -> {
    x + 29.0 + 4.0
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float64(half_sum(40.0))
    produces pure result end
}
