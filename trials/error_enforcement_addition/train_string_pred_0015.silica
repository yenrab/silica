// expected failure E1043: whitespace between binding name 'suf' and ':'
// case on ends_with; expect 60
fn main() -> int64 {
    s: string <- "silica compiler";
    suf :string <- "r";
    case ends_with(s, suf) of {
        true -> 60;
        false -> 92
    }
}
