// expected failure E2003: exponent parameter declared as a string
// power(2, 2) = 4
fn power(b: int64, e: string) -> int64 {
    case e == 0 of {
        true -> 1;
        false -> b * power(b, e - 1)
    }
}

fn main() -> int64 {
    power(2, 2)
}
