// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// substring uses character indices [5, 5) of "delta"
fn main() -> atom {
    sequence proc[mem(normal)] s: string <- substring("delta", 5, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
