// expected failure E2001: string literal used as a substring index
// substring uses character indices [1, 4) of "actor"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "actor";
        s: string <- substring(text, "1", 4);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
