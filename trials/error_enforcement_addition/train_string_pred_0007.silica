// expected failure E1043: whitespace between binding name 's' and ':'
// case on ends_with; expect 86
fn main() -> int64 {
    s :string <- "北京市";
    suf: string <- "北京市";
    case ends_with(s, suf) of {
        true -> 86;
        false -> 39
    }
}
