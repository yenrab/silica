// expected failure E2003: parameter x declared int64 in a int16 helper
// helper combine/2 on int16; combine(17, 11) = 0
fn combine(x: int64, y: int16) -> int16 {
    (50 / x) / (35 - y)
}

fn main() -> int16 {
    combine(17, 11)
}
