// expected failure E1050: produces clause without pure
// print_atom prints the atom with its colon: :down
fn main() -> atom {
    sequence proc[device_io] state: atom <- :down;
        result: atom <- print_atom(state)
        produces result end
}
