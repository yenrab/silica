// expected failure E4016: no main/0: entry function renamed to 'mainx'
// Positive: both device_io and mem(normal) declared and used in one sequence
fn mainx() -> atom {
    sequence proc[mem(normal), device_io] xs: List[int64, mem(normal)] <- [20, 15];
        n: int64 <- length[int64, mem(normal)](xs);
        result: atom <- print_int64(n)
        produces pure result end
}
