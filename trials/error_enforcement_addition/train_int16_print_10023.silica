// expected failure E1052: duplicate sequence keyword
// print_int16 expr; prints 51
fn main() -> atom {
    sequence sequence proc[device_io]
        result: atom <- print_int16(51)
    produces
        pure result
    end
}
