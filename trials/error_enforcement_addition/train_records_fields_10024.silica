// expected failure E1047: named struct declaration 'Pair' is not allowed
// inline record field access; x * y = 60
struct Pair { x: int64, y: int64 }

fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 30, y: 2 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv * yv
}
