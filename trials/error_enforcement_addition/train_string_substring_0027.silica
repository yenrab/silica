// expected failure E1042: function 'main' has no return type
// substring uses character indices [4, 4) of "south"
fn main() {
    sequence proc[device_io] s: string <- substring("south", 4, 4);
        print_string(s);
        println("")
        produces pure :ok end
}
