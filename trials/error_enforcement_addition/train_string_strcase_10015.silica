// expected failure E2001: string result in an integer case arm
// case on string literals; "omega" gives 75
fn main() -> int64 {
    mode: string <- "omega";
    case mode of {
        "apple" -> "apple";
        "world" -> 68;
        "omega" -> 75;
        _: string -> 63
    }
}
