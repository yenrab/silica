// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 2
fn main() -> int64 {
    base: int64 <- 31 * 35;
    k: int64 <- (46 + 22) * (base + base);
    c: int64 <- 28 / 12;
    -base + 2 + k - k + c - c
}
