// expected failure E2003: binding total declared uint16 but used as int64
// int64 bindings then expression; expect 1
fn main() -> int64 {
    total: uint16 <- 32;
    step: int64 <- total / total;
    d: int64 <- total + 33 + (12 - 27);
    count: int64 <- total;
    (23 + d) / d + step - step + count - count
}
