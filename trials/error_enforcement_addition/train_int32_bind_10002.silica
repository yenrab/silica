// expected failure E2013: unary minus on a variable instead of negate_int32
// int32 bindings then expression; expect 101
fn main() -> int32 {
    m: int32 <- 15;
    a: int32 <- 8;
    step: int32 <- (m * a) % (55 + a);
    -m + (step + (16 + 28))
}
