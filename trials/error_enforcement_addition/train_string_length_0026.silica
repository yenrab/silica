// expected failure E2003: length_chars result printed with print_string
// length_chars of a bound string prints 5
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "gamma";
        result: atom <- print_string(length_chars(text))
    produces
        pure result
    end
}
