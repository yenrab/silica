// expected failure E2018: binding 'result' has no type annotation
// print_int64 expr; prints 8
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int64(8 % 19 % (56 + 53))
    produces pure result end
}
