// expected failure E2018: binding 'wide' has no type annotation
// shifts: (seed shl 2) + (wide shr 4) = 26
fn main() -> uint64 {
    seed: uint64 <- 4;
    wide <- 160;
    (seed shl 2) + (wide shr 4)
}
