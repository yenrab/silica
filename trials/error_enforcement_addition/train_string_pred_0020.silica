// expected failure E2001: integer literal passed to contains
// contains prints true
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "北京市";
        result: atom <- print_bool(contains(s, 7))
    produces pure result end
}
