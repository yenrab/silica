// expected failure E2003: binding height declared int8 but used as uint64
// uint64 bindings then expression; expect 0
fn main() -> uint64 {
    height: int8 <- 45 - 6;
    base: uint64 <- height / height + (height * height);
    (9 / height) % (1 + 48) + base - base
}
