// expected failure E2003: guard expression is an integer, not a boolean
// guards partition int64 into three ranges; n = 34 gives 0
fn main() -> int64 {
    n: int64 <- 34;
    case n of {
        n: int64 if n -> 0;
        n: int64 if n >= 94 and n < 103 -> n / 2;
        n: int64 if n >= 103 -> 0
    }
}
