// expected failure E1049: sequence block without produces
// print_uint32 var; prints 30
fn main() -> atom {
    sequence proc[device_io]
        amount: uint32 <- 7 % 26;
        result: atom <- print_uint32(amount + (28 - 5))
    pure result
    end
}
