// expected failure E2005: wrong argument count in call to 'status'
// helper returning :ok for positive input
fn status(n: int64) -> atom {
    case n > 0 of {
        true -> :ok;
        false -> :red
    }
}

fn main() -> atom {
    status()
}
