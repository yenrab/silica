// expected failure E1053: duplicate produces keyword
// sequence prints and returns the same sum 27
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 7;
        b: int64 <- 20;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces produces pure total end
}
