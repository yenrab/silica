// expected failure E1043: whitespace between binding name 'mode' and ':'
// case on string literals; "cloud" gives 56
fn main() -> int64 {
    mode :string <- "cloud";
    case mode of {
        "hello" -> 0;
        "north" -> 49;
        "cloud" -> 56;
        _: string -> 53
    }
}
