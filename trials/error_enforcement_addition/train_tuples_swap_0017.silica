// expected failure E2005: call to unknown function 'swaz'
// swap((25, 11)); first * 2 + second = 47
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swaz((25, 11));
    first * 2 + second
}
