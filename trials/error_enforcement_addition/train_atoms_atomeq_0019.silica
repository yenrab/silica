// expected failure E1041: trailing ';' after the pure expression
// atom identity: :start != :start is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result: atom <- print_bool(left != :start)
    produces pure result; end
}
