// expected failure E1042: function 'main' has no return type
// uint8 bindings then expression; expect 55
fn main() -> {
    d: uint8 <- 37 - 32 + 39 % 11;
    m: uint8 <- d / d;
    c: uint8 <- (34 % 58) / (m * 11);
    height: uint8 <- m + 37 + d;
    55 + c - c + height - height
}
