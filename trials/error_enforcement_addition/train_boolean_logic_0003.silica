// expected failure E1052: duplicate sequence keyword
// true and false printed
fn main() -> atom {
    sequence sequence proc[device_io] b: boolean <- true;
        a: boolean <- false;
        result: boolean <- b and a;
        outcome: atom <- print_bool(result)
        produces pure outcome end
}
