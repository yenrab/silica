// expected failure E1055: binding 'leftover' is never used
// bool and boolean name the same type
fn both(x: bool, y: boolean) -> bool {
    leftover: int64 <- 41;
    x and y
}

fn main() -> int64 {
    case both(false, true) of {
        true -> 1;
        false -> 0
    }
}
