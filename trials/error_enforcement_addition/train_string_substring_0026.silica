// expected failure E1055: binding 'spare' is never used
// substring uses character indices [0, 1) of "🎉"
fn main() -> atom {
    spare: int64 <- 41;
    sequence proc[device_io] s: string <- substring("🎉", 0, 1);
        print_string(s);
        println("")
        produces pure :ok end
}
