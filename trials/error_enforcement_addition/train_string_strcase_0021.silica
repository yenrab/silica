// expected failure E2005: integer pattern against a string scrutinee
// case on string literals; "actor" gives 81
fn main() -> int64 {
    mode: string <- "actor";
    case mode of {
        0 -> 74;
        "actor" -> 81;
        "delta" -> 0;
        _: string -> 16
    }
}
