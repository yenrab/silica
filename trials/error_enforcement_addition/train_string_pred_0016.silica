// expected failure E2001: integer literal passed to starts_with
// case on starts_with; expect 34
fn main() -> int64 {
    s: string <- "hello world";
    suf: string <- 7;
    case starts_with(s, suf) of {
        true -> 34;
        false -> 87
    }
}
