// expected failure E1055: binding 'unused_value' is never used
// uint32 bindings then expression; expect 36
fn main() -> uint32 {
    unused_value: int64 <- 41;
    total: uint32 <- 58;
    b: uint32 <- total % total % (total + total);
    36 + b - b / 47
}
