// expected failure E2018: binding 'result' has no type annotation
// print_atom prints the atom with its colon: :down
fn main() -> atom {
    sequence proc[device_io]
        result <- print_atom(:down)
    produces pure result end
}
