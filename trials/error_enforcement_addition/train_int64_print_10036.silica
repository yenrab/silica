// expected failure E2018: binding 'result' has no type annotation
// print_int64 literal; prints 47
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int64(47)
    produces
        pure result
    end
}
