// expected failure E3009: proc[...] written on the function return type instead of the sequence
// sequence prints and returns the same sum 48
fn main() -> int64 proc[device_io] {
    sequence proc[device_io] a: int64 <- 30;
        b: int64 <- 18;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
