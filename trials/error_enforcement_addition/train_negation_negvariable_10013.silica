// expected failure E2005: negate_uint32 does not exist
// negate_int32 variable; prints -86
fn main() -> atom {
    sequence proc[device_io]
        x: int32 <- 86;
        result: atom <- print_uint32(negate_uint32(x))
    produces pure result end
}
