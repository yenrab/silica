// expected failure E3009: proc[...] written on the function return type instead of the sequence
// substring uses character indices [5, 5) of "south"
fn main() -> atom proc[device_io] {
    sequence proc[device_io] s: string <- substring("south", 5, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
