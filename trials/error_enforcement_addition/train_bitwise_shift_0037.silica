// expected failure E2018: binding 'wide' has no type annotation
// shifts: (seed shl 3) + (wide shr 3) = 20
fn main() -> uint64 {
    seed: uint64 <- 1;
    wide <- 98;
    (seed shl 3) + (wide shr 3)
}
