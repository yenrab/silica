// expected failure E2003: binding d declared uint8 but used as int16
// int16 bindings then expression; expect 0
fn main() -> int16 {
    d: uint8 <- 40;
    count: int16 <- 12;
    k: int16 <- (count + count) % (d - 14);
    count / 1 - count % 26 + k - k
}
