// expected failure E2001: string literal passed to print_atom
// print_atom prints the atom with its colon: :alpha
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_atom("alpha")
        produces pure result end
}
