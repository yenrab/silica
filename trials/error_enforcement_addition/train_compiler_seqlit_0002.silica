// expected failure E1055: binding 'leftover' is never used
// sequence binding RHS literal then produces with no semicolon; want 58
fn main() -> int64 {
    leftover: int64 <- 41;
    sequence
        result: int64 <- 58
    produces
        pure result
    end
}
