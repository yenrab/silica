// expected failure E2018: binding 'a' has no type annotation
// int32 bindings then expression; expect 22
fn main() -> int32 {
    m: int32 <- 48;
    a <- (11 / m);
    c: int32 <- a / m - (a + a);
    (m + 22 - (38 + 10)) + c - c
}
