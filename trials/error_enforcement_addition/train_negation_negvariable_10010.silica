// expected failure E2005: negate_uint8 does not exist
// negate_int32 variable; prints 79
fn main() -> atom {
    sequence proc[device_io]
        x: int32 <- -79;
        result: atom <- print_uint8(negate_uint8(x))
    produces
        pure result
    end
}
