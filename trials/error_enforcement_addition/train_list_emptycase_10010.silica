// expected failure E2015: list pattern without its mem(Space)
// case on an empty list; expect 1
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64] -> 1;
            [h: int64, _: List[int64, mem(normal)]] -> h + 93;
            _: List[int64, mem(normal)] -> 1
        }
    produces
        pure r
    end
}
