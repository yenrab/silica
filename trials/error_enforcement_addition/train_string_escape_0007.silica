// expected failure E1042: function 'main' has no return type
// string escape hex
fn main() -> {
    sequence proc[device_io]
        h: string <- "\x41\x42";
        print_string(h);
        println("")
    produces
        pure :ok
    end
}
