// expected failure E1043: whitespace between binding name 'result' and ':'
// print_int64 expr; prints 1
fn main() -> atom {
    sequence proc[device_io]
        result :atom <- print_int64(45 / 38)
    produces pure result end
}
