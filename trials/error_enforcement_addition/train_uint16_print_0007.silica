// expected failure E4016: no main/0: entry function renamed to 'main_program'
// print_uint16 var; prints 9
fn main_program() -> atom {
    sequence proc[device_io]
        amount: uint16 <- 9;
        result: atom <- print_uint16(amount + 0)
    produces
        pure result
    end
}
