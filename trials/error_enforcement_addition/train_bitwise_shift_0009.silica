// expected failure E1043: whitespace between binding name 'wide' and ':'
// shifts: (seed shl 2) + (wide shr 2) = 45
fn main() -> uint64 {
    seed: uint64 <- 5;
    wide :uint64 <- 103;
    (seed shl 2) + (wide shr 2)
}
