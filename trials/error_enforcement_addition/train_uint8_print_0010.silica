// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_uint8 ignore; prints 32
fn main() -> atom {
    sequence proc[device_io, mem(normal)] _: atom <- print_uint8(15 + 17)
        produces pure :ok end
}
