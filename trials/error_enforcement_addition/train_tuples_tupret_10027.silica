// expected failure E2005: call to unknown function 'pairx'
// pair returns a pair; p + q = 60
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- pairx(31, 14);
    p + q
}
