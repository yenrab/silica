// expected failure E1055: binding 'spare' is never used
// uint64 bindings then expression; expect 2
fn main() -> uint64 {
    spare: int64 <- 41;
    total: uint64 <- 4;
    b: uint64 <- 1;
    d: uint64 <- 27;
    height: uint64 <- 42 / total;
    40 / 17 + b - b + d - d + height - height
}
