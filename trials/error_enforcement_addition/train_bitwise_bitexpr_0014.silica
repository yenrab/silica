// expected failure E1042: function 'main' has no return type
// uint64 bitwise: (seven band two) band flags = 0
fn main() {
    seven: uint64 <- 8;
    two: uint64 <- 13;
    flags: uint64 <- 5;
    (seven band two) band flags
}
