// expected failure E1055: binding 'temp_val' is never used
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        result: atom <- print_int64(47)
    produces pure result end
}
