// expected failure E2005: call to unknown function 'print_booz'
// atom identity: :fail != :retry is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :fail;
        result: atom <- print_booz(left != :retry)
    produces
        pure result
    end
}
