// expected failure E2005: call to unknown function 'wraz'
// string helper wrap; prints [hello]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(wraz("hello"));
        println("")
    produces
        pure :ok
    end
}
