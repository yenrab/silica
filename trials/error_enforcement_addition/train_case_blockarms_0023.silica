// expected failure E1041: trailing ';' after a block arm's final expression
// case arms with multi-statement block bodies; expect 5
fn main() -> int64 {
    x: int64 <- 2;
    case x of {
        0 -> {
            base: int64 <- 8;
            base * 2;
        }
        _: int64 -> {
            step: int64 <- 2;
            x * step + 1
        }
    }
}
