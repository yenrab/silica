// expected failure E2001: atom literal returned from the recursion base case
// recursive sum_to on int8; sum_to(14) = 105
fn sum_to(n: int8) -> int8 {
    case n == 0 of {
        true -> :zero;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> int8 {
    sum_to(14)
}
