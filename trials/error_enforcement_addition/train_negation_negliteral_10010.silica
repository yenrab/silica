// expected failure E2018: binding 'result' has no type annotation
// negate_int8 literal; prints -16
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int8(negate_int8(16))
    produces
        pure result
    end
}
