// expected failure E2013: unary minus on a variable instead of negate_int16
// int16 bindings then expression; expect 4
fn main() -> int16 {
    b: int16 <- 41;
    count: int16 <- b;
    -b + 4 + count - count
}
