// expected failure E1042: function 'main' has no return type
// pure sequence block (binding); expect 89
fn main() -> {
    sequence
        result: int64 <- 89
    produces
        pure result
    end
}
