// expected failure E2003: shift amount literal 100 is out of range 0..63
// shifts: (seed shl 0) + (wide shr 1) = 69
fn main() -> uint64 {
    seed: uint64 <- 2;
    wide: uint64 <- 134;
    (seed shl 100) + (wide shr 1)
}
