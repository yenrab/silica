// expected failure E1043: whitespace between binding name 'result' and ':'
// print_uint8 var; prints 2
fn main() -> atom {
    sequence proc[device_io]
        amount: uint8 <- (17 - 15);
        result :atom <- print_uint8(amount % 28)
    produces pure result end
}
