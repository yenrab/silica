// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (mask band hi) bor two = 15
fn main() -> uint64 {
    mask: uint64 <- true;
    hi: uint64 <- 12;
    two: uint64 <- 11;
    (mask band hi) bor two
}
