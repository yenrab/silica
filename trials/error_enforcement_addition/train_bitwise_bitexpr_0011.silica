// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot two) band one) band mask = 8
fn main() -> uint64 {
    two: uint64 <- true;
    one: uint64 <- 15;
    mask: uint64 <- 9;
    ((bnot two) band one) band mask
}
