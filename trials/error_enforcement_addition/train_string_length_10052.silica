// expected failure E1055: binding 'scratch' is never used
// length_bytes of a bound string prints 7
fn main() -> atom {
    scratch: int64 <- 41;
    sequence proc[device_io] text: string <- "ñandú";
        result: atom <- print_int64(length_bytes(text))
        produces pure result end
}
