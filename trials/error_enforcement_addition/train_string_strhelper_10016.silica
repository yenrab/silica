// expected failure E1049: sequence block without produces
// string helper wrap; prints [world]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("world"));
        println("")
        pure :ok end
}
