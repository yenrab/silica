// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 59
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 46;
        b: int64 <- 13;
        total: int64 <- a + b
    produces
        pure total
    end
}
