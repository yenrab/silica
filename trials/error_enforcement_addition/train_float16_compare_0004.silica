// expected failure E2018: binding 'done' has no type annotation
// float16 comparisons
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool((29.5 - 1.0) >= 34.75);
        println("");
        done <- print_bool(11.0 != 35.5)
    produces
        pure done
    end
}
