// expected failure E2001: integer literal passed to concatenate
// concatenate of 2 pieces prints riverZürich

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("river", 6);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
