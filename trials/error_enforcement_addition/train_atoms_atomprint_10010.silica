// expected failure E1050: produces clause without pure
// print_atom prints the atom with its colon: :done
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :done;
        result: atom <- print_atom(state)
    produces
        result
    end
}
