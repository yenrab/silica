// expected failure E2005: wrong argument count in call to 'greet'
// string helper greet prints hi south

fn greet(name: string) -> string {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io] print_string(greet());
        println("")
        produces pure :ok end
}
