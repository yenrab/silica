// expected failure E2018: binding 'flag' has no type annotation
// case on a boolean binding; expect 12
fn main() -> int64 {
    flag <- true;
    case flag of {
        true -> 12;
        false -> 12
    }
}
