// expected failure E2018: binding 'result' has no type annotation
// atom identity: :ready != :ready is false
fn main() -> atom {
    sequence proc[device_io] left: atom <- :ready;
        result <- print_bool(left != :ready)
        produces pure result end
}
