// expected failure E2003: binding height declared uint16 but used as uint32
// uint32 bindings then expression; expect 159
fn main() -> uint32 {
    height: uint16 <- (57 + 1 + 21 % 38);
    m: uint32 <- height;
    (height + m) + height / height
}
