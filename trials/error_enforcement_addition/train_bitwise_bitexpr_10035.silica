// expected failure E1047: named struct declaration 'Pair' is not allowed
// uint64 bitwise: (one band mask) bor seven = 14
struct Pair { x: int64, y: int64 }

fn main() -> uint64 {
    one: uint64 <- 2;
    mask: uint64 <- 15;
    seven: uint64 <- 12;
    (one band mask) bor seven
}
