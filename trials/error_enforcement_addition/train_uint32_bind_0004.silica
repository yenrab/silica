// expected failure E2018: binding 'b' has no type annotation
// uint32 bindings then expression; expect 43
fn main() -> uint32 {
    b <- 20;
    total: uint32 <- b / b;
    a: uint32 <- b;
    37 + (total + 5) + a - a
}
