// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (two band flags) band bits = 0
fn main() -> uint64 {
    two: uint64 <- true;
    flags: uint64 <- 2;
    bits: uint64 <- 13;
    (two band flags) band bits
}
