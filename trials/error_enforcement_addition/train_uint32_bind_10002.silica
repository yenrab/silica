// expected failure E2013: unary minus on a variable instead of negate_uint32
// uint32 bindings then expression; expect 57
fn main() -> uint32 {
    step: uint32 <- 34 * (49 * 29);
    b: uint32 <- step;
    -step + 57 + b - b
}
