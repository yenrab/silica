// expected failure E2015: list pattern without its mem(Space)
// case on a non-empty list; expect 86
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [3, 19, 35, 36];
        r: int64 <- case xs of {
            []: List[int64] -> 71;
            [h: int64, _: List[int64, mem(normal)]] -> h + 83;
            _: List[int64, mem(normal)] -> 71
        }
    produces
        pure r
    end
}
