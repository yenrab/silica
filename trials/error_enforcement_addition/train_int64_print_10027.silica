// expected failure E1042: function 'main' has no return type
// print_int64 var; prints 0
fn main() {
    sequence proc[device_io]
        amount: int64 <- 18 / 41;
        result: atom <- print_int64(amount % 32)
    produces pure result end
}
