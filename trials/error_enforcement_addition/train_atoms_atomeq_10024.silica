// expected failure E2005: call to unknown function 'print_boolx'
// atom identity: :up != :hot is true
fn main() -> atom {
    sequence proc[device_io] left: atom <- :up;
        result: atom <- print_boolx(left != :hot)
        produces pure result end
}
