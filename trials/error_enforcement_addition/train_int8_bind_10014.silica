// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 96
fn main() -> int8 {
    total: int8 <- 60;
    count: int8 <- 36;
    m: int8 <- 55 / 40;
    -total + total + 35 + m % count
}
