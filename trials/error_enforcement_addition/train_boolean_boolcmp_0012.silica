// expected failure E1055: binding 'temp_val' is never used
// print_bool(0 <= 8)
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        result: atom <- print_bool(0 <= 8)
    produces
        pure result
    end
}
