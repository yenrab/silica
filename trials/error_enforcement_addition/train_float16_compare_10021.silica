// expected failure E1043: whitespace between binding name 'done' and ':'
// float16 comparisons
fn main() -> atom {
    sequence proc[device_io] _: atom <- print_bool(18.5 <= (34.0 + 0.5));
        println("");
        done :atom <- print_bool((30.5 + 13.5) != 14.5)
        produces pure done end
}
