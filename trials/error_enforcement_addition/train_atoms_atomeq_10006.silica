// expected failure E2018: binding 'result' has no type annotation
// atom identity: :start != :ready is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result <- print_bool(left != :ready)
    produces
        pure result
    end
}
