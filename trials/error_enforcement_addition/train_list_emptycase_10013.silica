// expected failure E1047: named struct declaration 'Pair' is not allowed
// case on an empty list; expect 46
struct Pair { x: int64, y: int64 }

fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        r: int64 <- case xs of {
            []: List[int64, mem(normal)] -> 46;
            [h: int64, _: List[int64, mem(normal)]] -> h + 25;
            _: List[int64, mem(normal)] -> 46
        }
    produces
        pure r
    end
}
