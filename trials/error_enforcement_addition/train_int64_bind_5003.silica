// expected failure E2003: binding width declared uint16 but used as int64
// int64 bindings then expression; expect 46
fn main() -> int64 {
    width: uint16 <- 42;
    height: int64 <- width;
    a: int64 <- 36 / 14 % (width * 4);
    height + 4 % height + a - a
}
