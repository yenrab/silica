// expected failure E2001: string result in an integer case arm
// case on string literals; "cherry" gives 18
fn main() -> int64 {
    mode: string <- "cherry";
    case mode of {
        "actor" -> "actor";
        "cherry" -> 18;
        "stone" -> 10;
        _: string -> 80
    }
}
