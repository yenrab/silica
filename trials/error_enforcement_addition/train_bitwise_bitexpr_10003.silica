// expected failure E1042: function 'main' has no return type
// uint64 bitwise: (((bnot bits) band one) + flags) = 9
fn main() -> {
    bits: uint64 <- 7;
    one: uint64 <- 3;
    flags: uint64 <- 9;
    (((bnot bits) band one) + flags)
}
