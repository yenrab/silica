// expected failure E1055: binding 'leftover' is never used
// uint64 bitwise: (lo band hi) band three = 0
fn main() -> uint64 {
    leftover: int64 <- 41;
    lo: uint64 <- 13;
    hi: uint64 <- 6;
    three: uint64 <- 2;
    (lo band hi) band three
}
