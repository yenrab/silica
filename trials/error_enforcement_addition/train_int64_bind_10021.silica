// expected failure E2003: binding height declared uint8 but used as int64
// int64 bindings then expression; expect 31
fn main() -> int64 {
    height: uint8 <- 33;
    base: int64 <- (height + (height + 8));
    31 + base - base
}
