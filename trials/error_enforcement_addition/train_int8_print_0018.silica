// expected failure E3009: proc[...] written on the function return type instead of the sequence
// print_int8 two; prints 43 then 52
fn main() -> atom proc[device_io] {
    sequence proc[device_io]
        a: int8 <- 14 + 29;
        b: int8 <- 24 + 28;
        _: atom <- print_int8(a);
        println("");
        result: atom <- print_int8(b)
    produces
        pure result
    end
}
