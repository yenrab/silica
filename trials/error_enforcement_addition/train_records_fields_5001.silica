// expected failure E2005: access to a field the record does not have
// inline record field access; x + y = 69
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 32, y: 37 };
    p.x + p.z
}
