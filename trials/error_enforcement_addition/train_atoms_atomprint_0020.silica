// expected failure E2018: binding 'result' has no type annotation
// print_atom prints the atom with its colon: :retry
fn main() -> atom {
    sequence proc[device_io]
        result <- print_atom(:retry)
    produces
        pure result
    end
}
