// expected failure E2003: atom compared against an integer binding
// atom identity: :done != :done is false
fn main() -> atom {
    sequence proc[device_io]
        left: int64 <- 3;
        result: atom <- print_bool(left != :done)
    produces
        pure result
    end
}
