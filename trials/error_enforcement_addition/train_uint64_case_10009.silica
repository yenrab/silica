// expected failure E2001: string literal in one case arm of an integer case
// case on uint64 value 1; expect 65
fn main() -> uint64 {
    v: uint64 <- 1;
    case v of {
        0 -> "159";
        _: uint64 -> 65
    }
}
