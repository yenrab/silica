// expected failure E1050: produces clause without pure
// atom identity: :red != :warm is true
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :red;
        result: atom <- print_bool(left != :warm)
    produces result end
}
