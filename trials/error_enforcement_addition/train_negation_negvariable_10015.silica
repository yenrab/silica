// expected failure E2018: binding 'result' has no type annotation
// negate_int8 variable; prints -97
fn main() -> atom {
    sequence proc[device_io]
        x: int8 <- 97;
        result <- print_int8(negate_int8(x))
    produces pure result end
}
