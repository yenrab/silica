// expected failure E2018: binding 'base' has no type annotation
// case arms with multi-statement block bodies; expect 101
fn main() -> int64 {
    x: int64 <- 5;
    case x of {
        0 -> {
            base <- 20;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 20;
            x * step + 1
        }
    }
}
