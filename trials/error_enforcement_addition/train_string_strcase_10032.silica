// expected failure E2001: string result in an integer case arm
// case on string literals; "other" gives 87
fn main() -> int64 {
    mode: string <- "other";
    case mode of {
        "region" -> "region";
        "beta" -> 15;
        "cherry" -> 25;
        _: string -> 87
    }
}
