// expected failure E2013: unary minus on a variable instead of negate_uint64
// uint64 bindings then expression; expect 4
fn main() -> uint64 {
    m: uint64 <- 4;
    k: uint64 <- m;
    c: uint64 <- k;
    -m + c
}
