// expected failure E1046: stray '-' after the last record field
// record passed to a function; area({ width: 2, height: 6 }) = 12
fn area(r: { width: int64, height: int64- }) -> int64 {
    r.width * r.height
}

fn main() -> int64 {
    area({ width: 2, height: 6 })
}
