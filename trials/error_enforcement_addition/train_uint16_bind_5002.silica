// expected failure E2003: binding d declared uint32 but used as uint16
// uint16 bindings then expression; expect 40
fn main() -> uint16 {
    d: uint32 <- 19;
    c: uint16 <- (d * d % (d + d));
    b: uint16 <- d / d;
    40 % (34 + 22) + c - c + b - b
}
