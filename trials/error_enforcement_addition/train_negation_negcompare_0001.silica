// expected failure E2005: negate_uint8 does not exist
// negate_int16 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_bool(negate_uint8(negate_int16(-62)) == -62)
    produces pure result end
}
