// expected failure E2005: decomposition element typed as string against an int64 slot
// decompose a 2-tuple from a binding; a + b = 47
fn main() -> int64 {
    t: (int64, int64) <- (18, 29);
    (a: int64, b: string) <- t;
    a + b
}
