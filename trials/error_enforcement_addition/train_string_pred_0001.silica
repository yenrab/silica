// expected failure E1042: function 'main' has no return type
// case on starts_with; expect 85
fn main() -> {
    s: string <- "北京市";
    suf: string <- "";
    case starts_with(s, suf) of {
        true -> 85;
        false -> 34
    }
}
