// expected failure E1042: function 'main' has no return type
// print_int64 var; prints 0
fn main() -> {
    sequence proc[device_io]
        x: int64 <- 0 * 42;
        result: atom <- print_int64((x * 46))
    produces
        pure result
    end
}
