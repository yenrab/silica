// expected failure E2005: string literal pattern against an integer scrutinee
// case on uint32 value 10; expect 80
fn main() -> uint32 {
    v: uint32 <- 10;
    case v of {
        "one" -> 148;
        29 -> 95;
        _: uint32 -> 80
    }
}
