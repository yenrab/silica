// expected failure E2003: binding count declared int32 but used as uint8
// uint8 bindings then expression; expect 48
fn main() -> uint8 {
    count: int32 <- 16 % (11 + 31);
    a: uint8 <- count + 10 + (9 + 53);
    48 + a - a
}
