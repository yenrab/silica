// expected failure E2001: string literal used as a substring index
// substring uses character indices [3, 3) of "日本語"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("日本語", "3", 3);
        print_string(s);
        println("")
        produces pure :ok end
}
