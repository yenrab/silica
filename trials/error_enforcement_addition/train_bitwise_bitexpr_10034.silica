// expected failure E2018: binding 'hi' has no type annotation
// uint64 bitwise: ((bnot one) band mask) band hi = 0
fn main() -> uint64 {
    one: uint64 <- 9;
    mask: uint64 <- 11;
    hi <- 8;
    ((bnot one) band mask) band hi
}
