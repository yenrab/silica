// expected failure E1041: trailing ';' after the pure expression
// print_uint64 var; prints 36
fn main() -> atom {
    sequence proc[device_io] amount: uint64 <- (32 + 4);
        result: atom <- print_uint64(amount)
        produces pure result; end
}
