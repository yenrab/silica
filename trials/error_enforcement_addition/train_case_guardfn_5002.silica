// expected failure E1042: function 'is_positive' has no return type
// guard calls a boolean helper; z = 5 gives 10
fn is_positive(v: int64) {
    v > 0
}

fn main() -> int64 {
    z: int64 <- 5;
    case z of {
        z: int64 if is_positive(z) -> z * 2;
        _: int64 -> 0
    }
}
