// expected failure E2003: shift amount literal 100 is out of range 0..63
// shifts: (seed shl 5) + (wide shr 3) = 213
fn main() -> uint64 {
    seed: uint64 <- 6;
    wide: uint64 <- 173;
    (seed shl 100) + (wide shr 3)
}
