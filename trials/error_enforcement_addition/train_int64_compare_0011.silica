// expected failure E2003: comparison against a string binding
// int64 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        label: string <- "x";
        _: atom <- print_bool(label == (36 / 21) <= (6 + 13));
        println("");
        result: atom <- print_bool((53 / 6) <= (20 + 52))
    produces
        pure result
    end
}
