// expected failure E1042: function 'main' has no return type
// case producing atoms then case dispatching on them; grade(5) -> 1
fn grade(n: int64) -> atom {
    case n of {
        0 -> :zero;
        k: int64 if k < 12 -> :low;
        _: int64 -> :high
    }
}

fn main() {
    g: atom <- grade(5);
    case g of {
        :zero -> 0;
        :low -> 1;
        :high -> 2;
        _: atom -> 9
    }
}
