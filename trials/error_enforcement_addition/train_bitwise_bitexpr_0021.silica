// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (bits band one) band mask = 0
fn main() -> uint64 {
    bits: uint64 <- true;
    one: uint64 <- 1;
    mask: uint64 <- 3;
    (bits band one) band mask
}
