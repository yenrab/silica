// expected failure E1051: sequence block without a closing end
// qualified call bound then printed; prints 26
use lib_beta;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_beta@twice(13);
        result: atom <- print_int64(v)
    produces
        pure result
}
