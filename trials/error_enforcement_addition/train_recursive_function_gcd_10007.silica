// expected failure E1055: binding 'temp_val' is never used
// Euclid's gcd; gcd(13, 6) = 1
fn gcd(a: int64, b: int64) -> int64 {
    temp_val: int64 <- 41;
    case b == 0 of {
        true -> a;
        false -> gcd(b, a % b)
    }
}

fn main() -> int64 {
    gcd(13, 6)
}
