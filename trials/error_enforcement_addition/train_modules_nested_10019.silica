// expected failure E1041: trailing ';' after the block's final expression
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(4)) = 10
use lib_alpha, lib_beta;

fn main() -> int64 {
    lib_beta@twice(lib_alpha@bump(4));
}
