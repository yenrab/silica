// expected failure E2003: shift amount literal 64 is out of range 0..63
// shifts: (seed shl 2) + (wide shr 4) = 17
fn main() -> uint64 {
    seed: uint64 <- 2;
    wide: uint64 <- 149;
    (seed shl 64) + (wide shr 4)
}
