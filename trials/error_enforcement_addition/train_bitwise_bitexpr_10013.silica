// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((five bor flags) + mask) = 16
fn main() -> uint64 {
    five: uint64 <- true;
    flags: uint64 <- 11;
    mask: uint64 <- 1;
    ((five bor flags) + mask)
}
