// expected failure E1044: whitespace between ':' and the atom name
// atom identity: :red != :busy is true
fn main() -> atom {
    sequence proc[device_io] left: atom <- : red;
        result: atom <- print_bool(left != :busy)
        produces pure result end
}
