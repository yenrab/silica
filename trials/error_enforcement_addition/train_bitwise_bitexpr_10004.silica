// expected failure E1055: binding 'temp_val' is never used
// uint64 bitwise: (three bor bits) band five = 10
fn main() -> uint64 {
    temp_val: int64 <- 41;
    three: uint64 <- 13;
    bits: uint64 <- 10;
    five: uint64 <- 10;
    (three bor bits) band five
}
