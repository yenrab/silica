// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 35
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 11;
        b: int64 <- 24;
        total: int64 <- a + b
    produces
        pure total
    end
}
