// expected failure E2013: unary minus on a variable instead of negate_uint64
// uint64 bindings then expression; expect 24
fn main() -> uint64 {
    k: uint64 <- 53;
    d: uint64 <- k;
    a: uint64 <- 54;
    c: uint64 <- a / 47;
    -k + 24 * c + 43 / a + d - d
}
