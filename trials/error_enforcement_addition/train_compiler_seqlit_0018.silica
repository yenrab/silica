// expected failure E1043: whitespace between binding name 'result' and ':'
// sequence binding RHS literal then produces with no semicolon; want 152
fn main() -> int64 {
    sequence
        result :int64 <- 152
    produces
        pure result
    end
}
