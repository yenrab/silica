// expected failure E2001: integer literal passed to concatenate
// concatenate of 3 pieces prints naïvestoneapple

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate("naïve", "stone"), 5);
        print_string(s);
        println("")
        produces pure :ok end
}
