// expected failure E2003: string binding used where an integer is expected
// concat of 4 pieces prints cherrytablehellocherry

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("cherry", "table"), "hello"), "cherry");
        print_int64(s);
        println("")
    produces
        pure :ok
    end
}
