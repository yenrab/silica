// expected failure E2013: unary minus on a variable instead of negate_int16
// int16 bindings then expression; expect 31
fn main() -> int16 {
    base: int16 <- 35;
    b: int16 <- 13;
    -base + (18 + b) % base
}
