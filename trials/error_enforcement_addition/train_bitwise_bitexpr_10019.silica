// expected failure E1055: binding 'scratch' is never used
// uint64 bitwise: (((bnot two) band mask) + flags) = 18
fn main() -> uint64 {
    scratch: int64 <- 41;
    two: uint64 <- 8;
    mask: uint64 <- 5;
    flags: uint64 <- 13;
    (((bnot two) band mask) + flags)
}
