// expected failure E1055: binding 'scratch' is never used
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(19)) = 40
use lib_alpha, lib_beta;

fn main() -> int64 {
    scratch: int64 <- 41;
    lib_beta@twice(lib_alpha@bump(19))
}
