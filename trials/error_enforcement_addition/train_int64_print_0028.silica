// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_int64 two; prints 26 then 32
fn main() -> atom {
    sequence proc[device_io, mem(normal)] a: int64 <- 26;
        b: int64 <- 32;
        _: atom <- print_int64(a);
        println("");
        result: atom <- print_int64(b)
        produces pure result end
}
