// expected failure E2003: binding base declared uint16 but used as int64
// int64 bindings then expression; expect 20
fn main() -> int64 {
    base: uint16 <- 8;
    b: int64 <- 36 / base;
    base + 49 / b
}
