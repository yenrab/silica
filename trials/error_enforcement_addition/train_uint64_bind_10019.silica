// expected failure E2018: binding 'count' has no type annotation
// uint64 bindings then expression; expect 26
fn main() -> uint64 {
    count <- 36;
    c: uint64 <- 27 * count;
    height: uint64 <- count - count + 58;
    26 + c - c + height - height
}
