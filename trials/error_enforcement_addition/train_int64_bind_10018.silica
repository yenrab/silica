// expected failure E2003: binding base declared uint64 but used as int64
// int64 bindings then expression; expect 42
fn main() -> int64 {
    base: uint64 <- 23 * 30 * (22 % 57);
    k: int64 <- 59 + 19 + base * 57;
    total: int64 <- base;
    42 - 0 % 5 + k - k + total - total
}
