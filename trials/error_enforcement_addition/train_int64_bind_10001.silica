// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 5
fn main() -> int64 {
    count: int64 <- 31 * 44;
    step: int64 <- count;
    -count + count % 9 % (32 + step)
}
