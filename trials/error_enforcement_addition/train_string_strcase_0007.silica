// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "world" gives 85
fn main() -> int64 {
    mode <- "world";
    case mode of {
        "table" -> 54;
        "world" -> 85;
        "cherry" -> 1;
        _: string -> 25
    }
}
