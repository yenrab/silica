// expected failure E2005: tuple literal element count differs from the declared tuple type
// decompose a 3-tuple from a binding; a + b + c = 106
fn main() -> int64 {
    t: (int64, int64, int64) <- (40, 26, 40, 1);
    (a: int64, b: int64, c: int64) <- t;
    a + b + c
}
