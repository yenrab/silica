// expected failure E1040: missing ';' between two statements
// sequence prints and returns the same sum 69
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 40;
        b: int64 <- 29;
        print_int64(a + b)
        println("");
        total: int64 <- a + b
    produces pure total end
}
