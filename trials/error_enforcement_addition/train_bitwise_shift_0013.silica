// expected failure E2018: binding 'seed' has no type annotation
// shifts: (seed shl 5) + (wide shr 2) = 145
fn main() -> uint64 {
    seed <- 3;
    wide: uint64 <- 198;
    (seed shl 5) + (wide shr 2)
}
