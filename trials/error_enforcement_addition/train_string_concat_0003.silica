// expected failure E2001: integer literal passed to concatenate
// concatenate of 2 pieces prints gammachair

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("gamma", 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
