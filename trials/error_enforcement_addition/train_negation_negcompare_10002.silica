// expected failure E2005: negate_uint8 does not exist
// negate_int32 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_bool(negate_uint8(negate_int32(23)) == 23)
    produces
        pure result
    end
}
