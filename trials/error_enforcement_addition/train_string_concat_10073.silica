// expected failure E2001: integer literal passed to concatenate
// concatenate of 4 pieces prints northhellogammabeta

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("north", "hello"), "gamma"), 4);
        print_string(s);
        println("")
        produces pure :ok end
}
