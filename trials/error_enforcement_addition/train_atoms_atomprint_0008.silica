// expected failure E2018: binding 'result' has no type annotation
// print_atom prints the atom with its colon: :alpha
fn main() -> atom {
    sequence proc[device_io]
        result <- print_atom(:alpha)
    produces
        pure result
    end
}
