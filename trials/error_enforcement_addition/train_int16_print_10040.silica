// expected failure E1049: sequence block without produces
// print_int16 literal; prints 10
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_int16(10)
    pure result end
}
