// expected failure E2005: call to unknown function 'bitx'
// boolean helpers combined into bits; expect 2
fn bit(b: boolean, w: int64) -> int64 {
    case b of { true -> w; false -> 0 }
}

fn tt() -> boolean {
    true
}

fn ff() -> boolean {
    false
}

fn main() -> int64 {
    bit(tt() == ff(), 1) + bitx(ff() == ff(), 2) + bit(tt() and ff(), 4)
}
