// expected failure E1043: whitespace between binding name 'outcome' and ':'
// true or true printed
fn main() -> atom {
    sequence proc[device_io]
        result: boolean <- true or true;
        outcome :atom <- print_bool(result)
    produces
        pure outcome
    end
}
