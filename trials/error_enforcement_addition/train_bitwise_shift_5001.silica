// expected failure E2018: binding 'wide' has no type annotation
// shifts: (seed shl 3) + (wide shr 1) = 55
fn main() -> uint64 {
    seed: uint64 <- 2;
    wide <- 78;
    (seed shl 3) + (wide shr 1)
}
