// expected failure E2005: negate_uint8 does not exist
// negate_int16 literal; prints 57
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_uint8(negate_uint8(-57))
        produces pure result end
}
