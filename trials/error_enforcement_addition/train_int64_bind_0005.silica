// expected failure E1055: binding 'temp_val' is never used
// int64 bindings then expression; expect 57
fn main() -> int64 {
    temp_val: int64 <- 41;
    a: int64 <- 60;
    step: int64 <- 23 % a;
    k: int64 <- 34;
    (step + k)
}
