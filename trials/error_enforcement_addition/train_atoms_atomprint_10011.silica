// expected failure E1045: function 'main' declared without the fn keyword
// print_atom prints the atom with its colon: :blue
main() -> atom {
    sequence proc[device_io] result: atom <- print_atom(:blue)
        produces pure result end
}
