// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 60
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 24;
        b: int64 <- 36;
        total: int64 <- a + b
    produces pure total end
}
