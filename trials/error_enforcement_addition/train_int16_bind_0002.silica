// expected failure E1045: function 'main' declared without the fn keyword
// int16 bindings then expression; expect 14
main() -> int16 {
    height: int16 <- 49;
    b: int16 <- height + height;
    c: int16 <- height * b % (height / height);
    total: int16 <- b - 31;
    12 + 2 + c - c + total - total
}
