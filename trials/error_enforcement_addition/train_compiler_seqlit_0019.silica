// expected failure E1042: function 'main' has no return type
// sequence binding RHS literal then produces with no semicolon; want 88
fn main() -> {
    sequence
        result: int64 <- 88
    produces
        pure result
    end
}
