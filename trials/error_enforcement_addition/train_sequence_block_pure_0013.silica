// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 65
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 24;
        b: int64 <- 41;
        total: int64 <- a + b
    produces pure total end
}
