// expected failure E1055: binding 'scratch' is never used
// effectful sequence used as a statement before the return value 66
fn main() -> int64 {
    scratch: int64 <- 41;
    sequence proc[device_io]
        print_string("beta");
        println("")
    produces pure :ok end;
    66
}
