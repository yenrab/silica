// expected failure E1042: function 'main' has no return type
// literal patterns, a guard and a typed wildcard on uint8; v = 2 gives 8
fn main() -> {
    v: uint8 <- 2;
    case v of {
        11 -> 7;
        2 -> 8;
        10 -> 2;
        m: uint8 if m > 13 -> 30;
        _: uint8 -> 17
    }
}
