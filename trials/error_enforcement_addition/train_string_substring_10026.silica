// expected failure E2001: string literal used as a substring index
// substring uses character indices [4, 5) of "world"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "world";
        s: string <- substring(text, "4", 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
