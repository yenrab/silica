// expected failure E2001: atom literal returned from the recursion base case
// recursive pow2 on uint64; pow2(1) = 2
fn pow2(n: uint64) -> uint64 {
    case n == 0 of {
        true -> :one;
        false -> 2 * pow2(n - 1)
    }
}

fn main() -> uint64 {
    pow2(1)
}
