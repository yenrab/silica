// expected failure E1055: binding 'temp_val' is never used
// print_int32 var; prints 546
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        n: int32 <- 35 + 7;
        result: atom <- print_int32(13 * n)
    produces
        pure result
    end
}
