// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (three band one) band two = 4
fn main() -> uint64 {
    three: uint64 <- true;
    one: uint64 <- 13;
    two: uint64 <- 5;
    (three band one) band two
}
