// expected failure E2005: decomposition element typed as string against an int64 slot
// decompose a 3-tuple from a binding; a + b + c = 52
fn main() -> int64 {
    t: (int64, int64, int64) <- (3, 16, 33);
    (a: int64, b: int64, c: string) <- t;
    a + b + c
}
