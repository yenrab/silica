// expected failure E2003: binding step declared int8 but used as uint8
// uint8 bindings then expression; expect 0
fn main() -> uint8 {
    step: int8 <- 41;
    k: uint8 <- 6;
    k / step % (41 + step)
}
