// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 50
fn main() -> int8 {
    b: int8 <- 2 + 48;
    total: int8 <- 45;
    k: int8 <- 45;
    -b + b + total - total + k - k
}
