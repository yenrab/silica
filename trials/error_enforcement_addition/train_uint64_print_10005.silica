// expected failure E1040: missing ';' between two statements
// print_uint64 two; prints 30 then 2
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 30;
        b: uint64 <- 2;
        _: atom <- print_uint64(a);
        println("")
        result: atom <- print_uint64(b)
    produces pure result end
}
