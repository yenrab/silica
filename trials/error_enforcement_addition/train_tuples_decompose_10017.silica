// expected failure E1055: binding 'temp_val' is never used
// decompose a 2-tuple from a literal; a + b = 22
fn main() -> int64 {
    temp_val: int64 <- 41;
    (a: int64, b: int64) <- (2, 20);
    a + b
}
