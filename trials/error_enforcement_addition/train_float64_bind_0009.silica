// expected failure E2003: integer binding mixed into float arithmetic
// float64 bindings 25.5 - 10.5 printed as 15.0
fn main() -> atom {
    sequence proc[device_io] a: float64 <- 25.5;
        b: int64 <- 10;
        result: atom <- print_float64(a - b)
        produces pure result end
}
