// expected failure E2003: shift amount literal 65 is out of range 0..63
// shifts: (seed shl 2) + (wide shr 2) = 12
fn main() -> uint64 {
    seed: uint64 <- 2;
    wide: uint64 <- 19;
    (seed shl 65) + (wide shr 2)
}
