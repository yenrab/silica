// expected failure E2018: binding 'x' has no type annotation
// case arms with multi-statement block bodies; expect 58
fn main() -> int64 {
    x <- 3;
    case x of {
        0 -> {
            base: int64 <- 1;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 19;
            x * step + 1
        }
    }
}
