// expected failure E2001: integer literal bound to a boolean
// false and false printed
fn main() -> atom {
    sequence proc[device_io]
        b: boolean <- 1;
        a: boolean <- false;
        result: boolean <- b and a;
        outcome: atom <- print_bool(result)
    produces pure outcome end
}
