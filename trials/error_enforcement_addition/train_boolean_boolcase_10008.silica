// expected failure E2018: binding 'flag' has no type annotation
// case on a boolean binding; expect 70
fn main() -> int64 {
    flag <- true;
    case flag of {
        true -> 70;
        false -> 72
    }
}
