// expected failure E1047: named struct declaration 'Point' is not allowed
// nested module-qualified calls; lib_alpha@bump(lib_beta@twice(7)) = 15
use lib_alpha, lib_beta;

struct Point { x: int64, y: int64 }

fn main() -> int64 {
    lib_alpha@bump(lib_beta@twice(7))
}
