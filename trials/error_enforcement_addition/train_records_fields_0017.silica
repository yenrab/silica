// expected failure E2003: record literal missing a declared field
// inline record field access; x - y = 42
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 58 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv - yv
}
