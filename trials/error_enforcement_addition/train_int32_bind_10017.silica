// expected failure E2003: binding width declared int16 but used as int32
// int32 bindings then expression; expect 39
fn main() -> int32 {
    width: int16 <- (13 % 55 % (58 % 17));
    b: int32 <- width;
    count: int32 <- 11;
    39 + b - b + count - count
}
