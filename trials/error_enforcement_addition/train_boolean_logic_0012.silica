// expected failure E1042: function 'main' has no return type
// true or true printed
fn main() {
    sequence proc[device_io]
        result: boolean <- true or true;
        outcome: atom <- print_bool(result)
    produces pure outcome end
}
