// expected failure E2005: call to unknown function 'perimetez'
// record passed to a function; perimeter({ width: 15, height: 1 }) = 32
fn perimeter(r: { width: int64, height: int64 }) -> int64 {
    2 * (r.width + r.height)
}

fn main() -> int64 {
    perimetez({ width: 15, height: 1 })
}
