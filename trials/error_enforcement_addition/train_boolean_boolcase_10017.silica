// expected failure E1055: binding 'scratch' is never used
// case on a boolean binding; expect 100
fn main() -> int64 {
    scratch: int64 <- 41;
    flag: boolean <- false;
    case flag of {
        true -> 18;
        false -> 100
    }
}
