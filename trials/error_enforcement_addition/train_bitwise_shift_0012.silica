// expected failure E2003: shift amount literal 65 is out of range 0..63
// shifts: (seed shl 0) + (wide shr 4) = 15
fn main() -> uint64 {
    seed: uint64 <- 4;
    wide: uint64 <- 186;
    (seed shl 65) + (wide shr 4)
}
