// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "cherry" gives 86
fn main() -> int64 {
    mode <- "cherry";
    case mode of {
        "cherry" -> 86;
        "cloud" -> 26;
        "actor" -> 22;
        _: string -> 60
    }
}
