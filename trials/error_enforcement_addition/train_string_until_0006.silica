// expected failure E2001: string literal used where a char terminator is expected
// substring_until_char from 7 up to '|'
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("banana|chair", 7, "|");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
