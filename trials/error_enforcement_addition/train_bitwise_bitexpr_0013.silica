// expected failure E1047: named struct declaration 'Pair' is not allowed
// uint64 bitwise: (mask band bits) bor three = 4
struct Pair { x: int64, y: int64 }

fn main() -> uint64 {
    mask: uint64 <- 4;
    bits: uint64 <- 13;
    three: uint64 <- 4;
    (mask band bits) bor three
}
