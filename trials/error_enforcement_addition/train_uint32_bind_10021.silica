// expected failure E2003: binding c declared int64 but used as uint32
// uint32 bindings then expression; expect 62
fn main() -> uint32 {
    c: int64 <- 31;
    total: uint32 <- c;
    b: uint32 <- 37;
    (31 + c) % (14 * 20) + total - total + b - b
}
