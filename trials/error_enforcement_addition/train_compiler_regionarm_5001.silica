// expected failure E2002: undefined identifier 'missing_n'
// region handle passed to a helper that allocates inside a case arm; want 83
fn pick(r: region(L1, normal), n: int64) -> int64 {
    sequence proc[mem(normal)]
        result: int64 <- case n > 0 of {
            true -> {
                cell: ref(L1, normal, int64) <- alloc_ref(r, missing_n);
                read_ref(cell)
            };
            false -> 0
        }
    produces
        pure result
    end
}

fn main() -> int64 {
    sequence proc[mem(normal)]
        L1: lifetime <- fresh_lifetime();
        r: region(L1, normal) <- alloc_region(normal);
        v: int64 <- pick(r, 83)
    produces
        pure v
    end
}
