// expected failure E2018: binding 'q' has no type annotation
// string escape quote
fn main() -> atom {
    sequence proc[device_io]
        q <- "say \"hi\"";
        print_string(q);
        println("")
    produces pure :ok end
}
