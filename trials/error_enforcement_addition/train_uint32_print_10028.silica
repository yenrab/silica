// expected failure E2018: binding 'result' has no type annotation
// print_uint32 literal; prints 52
fn main() -> atom {
    sequence proc[device_io]
        result <- print_uint32(52)
    produces
        pure result
    end
}
