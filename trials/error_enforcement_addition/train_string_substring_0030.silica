// expected failure E2018: binding 's' has no type annotation
// substring uses character indices [5, 5) of "naïve"
fn main() -> atom {
    sequence proc[device_io] text: string <- "naïve";
        s <- substring(text, 5, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
