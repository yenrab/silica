// expected failure E1055: binding 'temp_val' is never used
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(23)) = 48
use lib_alpha, lib_beta;

fn main() -> int64 {
    temp_val: int64 <- 41;
    lib_beta@twice(lib_alpha@bump(23))
}
