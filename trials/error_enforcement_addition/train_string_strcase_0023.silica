// expected failure E2005: integer pattern against a string scrutinee
// case on string literals; "other" gives 13
fn main() -> int64 {
    mode: string <- "other";
    case mode of {
        1 -> 57;
        "alpha" -> 92;
        "delta" -> 2;
        _: string -> 13
    }
}
