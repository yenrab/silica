// expected failure E1041: trailing ';' after the block's final expression
// uint64 bindings then expression; expect 0
fn main() -> uint64 {
    count: uint64 <- 47 * 47 + (47 + 36);
    a: uint64 <- count;
    (57 + 15) * (a - count);
}
