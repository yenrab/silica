// expected failure E1044: whitespace between ':' and the atom name
// print_atom prints the atom with its colon: :busy
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- : busy;
        result: atom <- print_atom(state)
    produces
        pure result
    end
}
