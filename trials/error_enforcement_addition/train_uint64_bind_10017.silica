// expected failure E2013: unary minus on a variable instead of negate_uint64
// uint64 bindings then expression; expect 29
fn main() -> uint64 {
    total: uint64 <- 50;
    base: uint64 <- total - 46 + 30 / 37;
    k: uint64 <- 2;
    count: uint64 <- 29;
    -total + 0 + (25 + 4) + base - base + k - k + count - count
}
