// expected failure E2001: string result in an integer case arm
// case on string literals; "alpha" gives 33
fn main() -> int64 {
    mode: string <- "alpha";
    case mode of {
        "gamma" -> "gamma";
        "alpha" -> 33;
        "chair" -> 64;
        _: string -> 48
    }
}
