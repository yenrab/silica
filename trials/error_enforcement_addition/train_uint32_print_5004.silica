// expected failure E2018: binding 'b' has no type annotation
// print_uint32 two; prints 0 then 30
fn main() -> atom {
    sequence proc[device_io] a: uint32 <- 12 / 48;
        b <- 30;
        _: atom <- print_uint32(a);
        println("");
        result: atom <- print_uint32(b)
        produces pure result end
}
