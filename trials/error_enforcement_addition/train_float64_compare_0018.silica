// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// float64 comparisons
fn main() -> atom {
    sequence proc[device_io, mem(normal)] _: atom <- print_bool(30.75 <= 23.75);
        println("");
        done: atom <- print_bool(7.25 * 28.0 < 10.5)
        produces pure done end
}
