// expected failure E4016: no main/0: entry function renamed to 'entry'
// case on string literals; "omega" gives 82
fn entry() -> int64 {
    mode: string <- "omega";
    case mode of {
        "region" -> 26;
        "alpha" -> 41;
        "omega" -> 82;
        _: string -> 72
    }
}
