// expected failure E1055: binding 'leftover' is never used
// print_uint64 of 10 band 5 = 0
fn main() -> atom {
    leftover: int64 <- 41;
    sequence proc[device_io] a: uint64 <- 10;
        b: uint64 <- 5;
        result: atom <- print_uint64(a band b)
        produces pure result end
}
