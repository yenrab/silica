// expected failure E4016: no main/0: entry function renamed to 'start'
// Positive: two sequences with separate effect declarations in one function
fn start() -> int64 {
    sequence proc[device_io]
        print_int64(7);
        println("")
    produces pure :ok end;
    n: int64 <- sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [7, 3]
    produces
        pure length[int64, mem(normal)](xs)
    end;
    n
}
