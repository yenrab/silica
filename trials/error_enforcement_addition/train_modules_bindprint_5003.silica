// expected failure E1042: function 'main' has no return type
// qualified call bound then printed; prints 10
use lib_middle;

fn main() {
    sequence proc[device_io] v: int64 <- lib_middle@ten();
        result: atom <- print_int64(v)
        produces pure result end
}
