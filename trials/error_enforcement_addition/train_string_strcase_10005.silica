// expected failure E2005: integer pattern against a string scrutinee
// case on string literals; "region" gives 23
fn main() -> int64 {
    mode: string <- "region";
    case mode of {
        3 -> 2;
        "region" -> 23;
        "hello" -> 16;
        _: string -> 61
    }
}
