// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((seven bor hi) + mask) = 24
fn main() -> uint64 {
    seven: uint64 <- true;
    hi: uint64 <- 15;
    mask: uint64 <- 9;
    ((seven bor hi) + mask)
}
