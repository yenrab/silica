// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (two band seven) band five = 4
fn main() -> uint64 {
    two: uint64 <- true;
    seven: uint64 <- 12;
    five: uint64 <- 12;
    (two band seven) band five
}
