// expected failure E1042: function 'main' has no return type
// shifts: (seed shl 2) + (wide shr 2) = 59
fn main() -> {
    seed: uint64 <- 4;
    wide: uint64 <- 174;
    (seed shl 2) + (wide shr 2)
}
