// expected failure E2013: unary minus on a variable instead of negate_uint32
// uint32 bindings then expression; expect 0
fn main() -> uint32 {
    m: uint32 <- 21;
    step: uint32 <- (20 % 29 / (1 + m));
    -m + (5 + 1) / m + step - step
}
