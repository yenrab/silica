// expected failure E1041: trailing ';' after the block's final expression
// case over atoms; :retry selects 11
fn main() -> int64 {
    mode: atom <- :retry;
    case mode of {
        :down -> 82;
        :retry -> 11;
        :ready -> 15;
        _: atom -> 42
    };
}
