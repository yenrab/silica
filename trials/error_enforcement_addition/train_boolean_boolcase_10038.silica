// expected failure E1041: trailing ';' after the block's final expression
// case on a boolean binding; expect 75
fn main() -> int64 {
    flag: boolean <- true;
    case flag of {
        true -> 75;
        false -> 19
    };
}
