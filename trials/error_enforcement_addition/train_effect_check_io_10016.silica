// expected failure E3002: device_io used under proc[mem(normal)]
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    sequence proc[mem(normal)] result: atom <- print_int64(76)
        produces pure result end
}
