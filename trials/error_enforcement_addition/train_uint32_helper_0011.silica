// expected failure E1041: trailing ';' after the block's final expression
// helper compute/3 on uint32; compute(6, 11, 17) = 110
fn compute(x: uint32, y: uint32, z: uint32) -> uint32 {
    z % x * (y + y);
}

fn main() -> uint32 {
    compute(6, 11, 17)
}
