// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_uint8 var; prints 0
fn main() -> atom {
    sequence proc[device_io, mem(normal)] value: uint8 <- 57 - 57;
        result: atom <- print_uint8(value * value)
        produces pure result end
}
