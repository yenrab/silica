// expected failure E1045: function 'main' declared without the fn keyword
// tuple parameter typed_param; add_pair((28, 20)) = 48
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

main() -> int64 {
    add_pair((28, 20))
}
