// expected failure E2018: binding 'result' has no type annotation
// print_uint64 var; prints 0
fn main() -> atom {
    sequence proc[device_io] n: uint64 <- 3;
        result <- print_uint64((n - n))
        produces pure result end
}
