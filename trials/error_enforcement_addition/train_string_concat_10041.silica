// expected failure E1054: extra end after the sequence block
// concatenate of 4 pieces prints betaüberomegaüber

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("beta", "über"), "omega"), "über");
        print_string(s);
        println("")
        produces pure :ok end end
}
