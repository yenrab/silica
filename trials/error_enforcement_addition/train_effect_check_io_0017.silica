// expected failure E1055: binding 'scratch' is never used
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    scratch: int64 <- 41;
    sequence proc[device_io] result: atom <- print_int64(81)
        produces pure result end
}
