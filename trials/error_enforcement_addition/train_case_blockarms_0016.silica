// expected failure E1055: block-local binding never used
// case arms with multi-statement block bodies; expect 20
fn main() -> int64 {
    x: int64 <- 0;
    case x of {
        0 -> {
            base: int64 <- 10;
            10 * 2
        }
        _: int64 -> {
            step: int64 <- 14;
            x * step + 1
        }
    }
}
