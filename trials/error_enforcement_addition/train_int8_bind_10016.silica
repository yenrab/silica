// expected failure E2003: binding a declared int32 but used as int8
// int8 bindings then expression; expect 120
fn main() -> int8 {
    a: int32 <- (43 + 44 + 8);
    k: int8 <- a;
    d: int8 <- 57;
    total: int8 <- d;
    (d + (d + 6)) + k - k + total - total
}
