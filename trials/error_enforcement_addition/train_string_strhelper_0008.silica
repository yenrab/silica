// expected failure E1065: parameter list of 'wrap' is not closed
// string helper wrap; prints [river]

fn wrap(s: string -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("river"));
        println("")
        produces pure :ok end
}
