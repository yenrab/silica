// expected failure E2003: binding total declared uint8 but used as uint16
// uint16 bindings then expression; expect 4
fn main() -> uint16 {
    total: uint8 <- (16 % 47);
    count: uint16 <- total * total;
    4 + count - count
}
