// expected failure E2003: binding d declared int8 but used as uint32
// uint32 bindings then expression; expect 0
fn main() -> uint32 {
    d: int8 <- 40;
    count: uint32 <- d;
    56 % count / (d * 40)
}
