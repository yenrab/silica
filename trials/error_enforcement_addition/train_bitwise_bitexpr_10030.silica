// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (flags band bits) bor lo = 3
fn main() -> uint64 {
    flags: uint64 <- true;
    bits: uint64 <- 3;
    lo: uint64 <- 3;
    (flags band bits) bor lo
}
