// expected failure E1041: trailing ';' after the block's final expression
// root program using the new library module train_lib_scale1; train_lib_scale1@scale1_apply(11) = 11
use train_lib_scale1;

fn main() -> int64 {
    train_lib_scale1@scale1_apply(11);
}
