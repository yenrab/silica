// expected failure E2018: binding 'label' has no type annotation
// case expression as a binding's right-hand side; expect 6
fn main() -> int64 {
    x: int64 <- 5;
    label <- case x % 3 of {
        0 -> 0;
        1 -> 7;
        _: int64 -> x
    };
    label + 1
}
