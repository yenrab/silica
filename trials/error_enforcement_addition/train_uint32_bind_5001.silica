// expected failure E2013: unary minus on a variable instead of negate_uint32
// uint32 bindings then expression; expect 92
fn main() -> uint32 {
    step: uint32 <- (4 + 35);
    b: uint32 <- 45;
    k: uint32 <- 18 % 4;
    a: uint32 <- k;
    -step + step % k + (step + 52) + b - b + a - a
}
