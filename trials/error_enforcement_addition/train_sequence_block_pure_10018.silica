// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 89
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 39;
        b: int64 <- 50;
        total: int64 <- a + b
    produces
        pure total
    end
}
