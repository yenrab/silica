// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 122
fn main() -> uint8 {
    width: uint8 <- 25 + 36;
    a: uint8 <- width + width;
    -width + a
}
