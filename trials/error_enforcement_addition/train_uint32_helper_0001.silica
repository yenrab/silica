// expected failure E2001: string literal passed where an integer parameter is expected
// helper combine/3 on uint32; combine(16, 11, 8) = 0
fn combine(x: uint32, y: uint32, z: uint32) -> uint32 {
    z / x / (x + y)
}

fn main() -> uint32 {
    combine("16", 11, 8)
}
