// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (two band five) bor mask = 12
fn main() -> uint64 {
    two: uint64 <- true;
    five: uint64 <- 12;
    mask: uint64 <- 12;
    (two band five) bor mask
}
