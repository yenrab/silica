// expected failure E2005: wrong argument count in call to 'shift'
// float32 helper shift; prints 0.375
fn shift(x: float32) -> float32 {
    x - 5.25 + 1.25
}

fn main() -> atom {
    sequence proc[device_io] result: atom <- print_float32(shift())
        produces pure result end
}
