// expected failure E2005: wrong argument count in call to 'divmod'
// divmod returns a pair; p + q = 6
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod(36);
    p + q
}
