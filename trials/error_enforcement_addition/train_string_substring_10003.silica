// expected failure E2001: string literal used as a substring index
// substring uses character indices [2, 2) of "ñandú"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("ñandú", "2", 2);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
