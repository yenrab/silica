// expected failure E1042: function 'main' has no return type
// list literal of 5 uint32 elements; length is 5
fn main() {
    sequence proc[mem(normal)]
        xs: List[uint32, mem(normal)] <- [37, 40, 18, 16, 39];
        n: int64 <- length[uint32, mem(normal)](xs)
    produces
        pure n
    end
}
