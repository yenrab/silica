// expected failure E1041: trailing ';' after the block's final expression
// uint8 bindings then expression; expect 74
fn main() -> uint8 {
    step: uint8 <- (2 + 12) / (44 + 43);
    total: uint8 <- step;
    b: uint8 <- (total * (6 * 32));
    k: uint8 <- 8;
    38 + (28 + k) + b - b;
}
