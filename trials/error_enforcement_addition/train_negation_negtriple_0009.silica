// expected failure E2005: negate_uint64 does not exist
// negate_int64 triple; prints -25
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint64(negate_uint64(negate_int64(negate_int64(25))))
    produces pure result end
}
