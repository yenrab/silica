// expected failure E1042: function 'main' has no return type
// print_atom prints the atom with its colon: :beta
fn main() {
    sequence proc[device_io]
        result: atom <- print_atom(:beta)
    produces pure result end
}
