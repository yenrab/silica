// expected failure E1041: trailing ';' after the block's final expression
// shifts: (seed shl 5) + (wide shr 4) = 44
fn main() -> uint64 {
    seed: uint64 <- 1;
    wide: uint64 <- 192;
    (seed shl 5) + (wide shr 4);
}
