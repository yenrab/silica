// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot lo) band hi) + flags) = 14
fn main() -> uint64 {
    lo: uint64 <- true;
    hi: uint64 <- 10;
    flags: uint64 <- 6;
    (((bnot lo) band hi) + flags)
}
