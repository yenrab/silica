// expected failure E1041: trailing ';' after the block's final expression
// int8 bindings then expression; expect 52
fn main() -> int8 {
    k: int8 <- 34 % 52;
    step: int8 <- k * (k - k);
    width: int8 <- 9 + k;
    42 - 2 + 12 + step - step + width - width;
}
