// expected failure E1050: produces clause without pure
// substring uses character indices [4, 4) of "Zürich"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("Zürich", 4, 4);
        print_string(s);
        println("")
    produces :ok end
}
