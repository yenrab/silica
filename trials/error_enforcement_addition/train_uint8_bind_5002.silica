// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 3
fn main() -> uint8 {
    height: uint8 <- 58;
    a: uint8 <- height;
    -height + 7 % a / (height / 20)
}
