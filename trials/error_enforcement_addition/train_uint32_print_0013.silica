// expected failure E2018: binding 'result' has no type annotation
// print_uint32 var; prints 0
fn main() -> atom {
    sequence proc[device_io] amount: uint32 <- 29 / 48;
        result <- print_uint32(amount / 16)
        produces pure result end
}
