// expected failure E2005: call to unknown function 'my_print_int64'
// sequence prints and returns the same sum 43
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 32;
        b: int64 <- 11;
        my_print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces pure total end
}
