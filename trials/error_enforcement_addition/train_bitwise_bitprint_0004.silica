// expected failure E1043: whitespace between binding name 'result' and ':'
// print_uint64 of 28 bor 1 = 29
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 28;
        b: uint64 <- 1;
        result :atom <- print_uint64(a bor b)
    produces
        pure result
    end
}
