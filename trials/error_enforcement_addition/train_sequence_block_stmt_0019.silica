// expected failure E1041: trailing ';' after the block's final expression
// effectful sequence used as a statement before the return value 92
fn main() -> int64 {
    sequence proc[device_io]
        print_string("beta");
        println("")
    produces pure :ok end;
    92;
}
