// expected failure E1055: binding 'temp_val' is never used
// function literal bound to a typed variable; f(32) = 35
fn main() -> int64 {
    temp_val: int64 <- 41;
    f: fn(int64) -> int64 <- fn(x: int64) -> int64 { x + 3 };
    f(32)
}
