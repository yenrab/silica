// expected failure E1045: function 'main' declared without the fn keyword
// substring uses character indices [2, 3) of "café"
main() -> atom {
    sequence proc[device_io]
        s: string <- substring("café", 2, 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
