// expected failure E2003: binding step declared int8 but used as uint64
// uint64 bindings then expression; expect 0
fn main() -> uint64 {
    step: int8 <- (54 + 8) / 50;
    a: uint64 <- 51;
    (39 + step) / a
}
