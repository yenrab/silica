// expected failure E1047: named struct declaration 'Point' is not allowed
// uint64 bindings then expression; expect 33
struct Point { x: int64, y: int64 }

fn main() -> uint64 {
    base: uint64 <- 21 + 33 * 35;
    width: uint64 <- base;
    width / base + base / 36
}
