// expected failure E1042: function 'main' has no return type
// pair returns a pair; p + q = 102
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, y * 2)
}

fn main() {
    (p: int64, q: int64) <- pair(35, 33);
    p + q
}
