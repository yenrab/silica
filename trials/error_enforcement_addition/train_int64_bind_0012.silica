// expected failure E2003: binding total declared uint32 but used as int64
// int64 bindings then expression; expect 27
fn main() -> int64 {
    total: uint32 <- 6 + 47 + 52 % 40;
    count: int64 <- total;
    3 + 23 + count / total
}
