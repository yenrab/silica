// expected failure E2005: decomposition of a pair into three names
// pair returns a pair; p + q = 15
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64, r: int64) <- pair(2, 6);
    p + q
}
