// expected failure E1050: produces clause without pure
// print_atom prints the atom with its colon: :pending
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_atom(:pending)
    produces
        result
    end
}
