// expected failure E1055: binding 'scratch' is never used
// recursive sum_to on int64; sum_to(2) = 3
fn sum_to(n: int64) -> int64 {
    scratch: int64 <- 41;
    case n == 0 of {
        true -> 0;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> int64 {
    sum_to(2)
}
