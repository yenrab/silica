// expected failure E1055: binding 'leftover' is never used
// inline record field access; x - y = 27
fn main() -> int64 {
    leftover: int64 <- 41;
    p: { x: int64, y: int64 } <- { x: 46, y: 19 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv - yv
}
