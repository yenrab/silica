// expected failure E2005: inner tuple decomposed against a mismatched declared type
// nested tuple decomposition; x + y + z = 51
fn main() -> int64 {
    t: ((int64, int64), int64) <- ((19, 17), 15);
    (inner: (int64, int64, int64), z: int64) <- t;
    (x: int64, y: int64) <- inner;
    x + y + z
}
