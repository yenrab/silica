// expected failure E2003: shift amount literal 64 is out of range 0..63
// shifts: (seed shl 5) + (wide shr 3) = 112
fn main() -> uint64 {
    seed: uint64 <- 3;
    wide: uint64 <- 130;
    (seed shl 64) + (wide shr 3)
}
