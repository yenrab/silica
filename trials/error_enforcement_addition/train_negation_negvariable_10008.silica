// expected failure E1042: function 'main' has no return type
// negate_int8 variable; prints 95
fn main() {
    sequence proc[device_io]
        x: int8 <- -95;
        result: atom <- print_int8(negate_int8(x))
    produces
        pure result
    end
}
