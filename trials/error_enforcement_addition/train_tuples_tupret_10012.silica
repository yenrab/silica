// expected failure E1055: binding 'spare' is never used
// pair returns a pair; p + q = 110
fn pair(x: int64, y: int64) -> (int64, int64) {
    spare: int64 <- 41;
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- pair(25, 42);
    p + q
}
