// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// sequence prints and returns the same sum 47
fn main() -> int64 {
    sequence proc[device_io, mem(normal)] a: int64 <- 33;
        b: int64 <- 14;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
