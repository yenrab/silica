// expected failure E2018: binding 'z' has no type annotation
// guard calls a boolean helper; z = 22 gives 44
fn is_positive(v: int64) -> boolean {
    v > 0
}

fn main() -> int64 {
    z <- 22;
    case z of {
        z: int64 if is_positive(z) -> z * 2;
        _: int64 -> 0
    }
}
