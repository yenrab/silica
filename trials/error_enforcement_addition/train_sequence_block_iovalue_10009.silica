// expected failure E1050: produces clause without pure
// sequence prints and returns the same sum 12
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 6;
        b: int64 <- 6;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces total end
}
