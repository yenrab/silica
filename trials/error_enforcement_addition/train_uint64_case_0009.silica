// expected failure E2001: string literal in one case arm of an integer case
// case on uint64 value 40; expect 93
fn main() -> uint64 {
    v: uint64 <- 40;
    case v of {
        1 -> "113";
        n: uint64 if n > 37 -> 93;
        _: uint64 -> 154
    }
}
