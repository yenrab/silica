// expected failure E1043: whitespace between binding name 'value' and ':'
// sequence result bound to a variable; expect 21
fn main() -> int64 {
    value :int64 <- sequence produces pure 5 end;
    value + 16
}
