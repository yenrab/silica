// Module checker: `export trait` must equal the filename stem exactly, including letter case (E4017, spec §3.4.8).
// Stem from this file: trait_case_only_name_diff — trait below differs only by ASCII case.
export trait Trait_case_only_name_diff;
