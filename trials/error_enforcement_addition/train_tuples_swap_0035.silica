// expected failure E1055: binding 'leftover' is never used
// swap((47, 39)); first = 39
fn swap(p: (int64, int64)) -> (int64, int64) {
    leftover: int64 <- 41;
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((47, 39));
    first
}
