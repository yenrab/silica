// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 45
fn main() -> int64 {
    step: int64 <- 51;
    c: int64 <- 32 * 35;
    count: int64 <- 10 + c % 45;
    total: int64 <- count;
    -step + (17 + 28) % 48 + step - step + total - total
}
