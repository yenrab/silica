// expected failure E2018: binding 'result' has no type annotation
// negate_int16 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result <- print_bool(negate_int16(negate_int16(19)) == 19)
    produces
        pure result
    end
}
