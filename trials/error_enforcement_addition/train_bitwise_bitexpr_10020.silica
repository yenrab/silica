// expected failure E1055: binding 'temp_val' is never used
// uint64 bitwise: (((bnot one) band three) + hi) = 5
fn main() -> uint64 {
    temp_val: int64 <- 41;
    one: uint64 <- 15;
    three: uint64 <- 7;
    hi: uint64 <- 5;
    (((bnot one) band three) + hi)
}
