// expected failure E1045: function 'main' declared without the fn keyword
// nested tuple decomposition; x + y + z = 21
main() -> int64 {
    t: ((int64, int64), int64) <- ((2, 19), 0);
    (inner: (int64, int64), z: int64) <- t;
    (x: int64, y: int64) <- inner;
    x + y + z
}
