// expected failure E1042: function 'main' has no return type
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(1)) = 4
use lib_alpha, lib_beta;

fn main() -> {
    lib_beta@twice(lib_alpha@bump(1))
}
