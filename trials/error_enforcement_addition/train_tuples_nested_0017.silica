// expected failure E1055: binding 'temp_val' is never used
// nested tuple decomposition; x + y + z = 72
fn main() -> int64 {
    temp_val: int64 <- 41;
    t: ((int64, int64), int64) <- ((30, 28), 14);
    (inner: (int64, int64), z: int64) <- t;
    (x: int64, y: int64) <- inner;
    x + y + z
}
