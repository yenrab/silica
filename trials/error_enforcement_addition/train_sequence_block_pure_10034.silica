// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 58
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 44;
        b: int64 <- 14;
        total: int64 <- a + b
    produces
        pure total
    end
}
