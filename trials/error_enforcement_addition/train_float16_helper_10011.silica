// expected failure E2003: blend returns float64 but its result feeds print_float16
// float16 helper blend; prints 53.25
fn blend(x: float16) -> float64 {
    x + 25.5 + 0.75
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float16(blend(27.0))
    produces
        pure result
    end
}
