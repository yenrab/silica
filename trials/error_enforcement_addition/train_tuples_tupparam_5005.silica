// expected failure E2005: call to unknown function 'add_paiz'
// tuple parameter typed_param; add_pair((41, 10)) = 51
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

fn main() -> int64 {
    add_paiz((41, 10))
}
