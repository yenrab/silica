// expected failure E1055: binding 'leftover' is never used
// case arms with multi-statement block bodies; expect 9
fn main() -> int64 {
    leftover: int64 <- 41;
    x: int64 <- 4;
    case x of {
        0 -> {
            base: int64 <- 14;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 2;
            x * step + 1
        }
    }
}
