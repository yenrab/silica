// expected failure E1055: binding 'temp_val' is never used
// Spec §19.3: use lib_alpha; then a module-qualified call lib_alpha@bump(20) = 21
use lib_alpha;

fn main() -> int64 {
    temp_val: int64 <- 41;
    lib_alpha@bump(20)
}
