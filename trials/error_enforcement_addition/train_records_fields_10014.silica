// expected failure E2005: access to a field the record does not have
// inline record field access; x + y = 95
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 46, y: 49 };
    p.x + p.z
}
