// expected failure E2005: call to unknown function 'my_divmod'
// divmod returns a pair; p + q = 7
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- my_divmod(47, 9);
    p + q
}
