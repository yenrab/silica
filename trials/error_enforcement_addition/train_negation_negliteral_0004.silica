// expected failure E2005: negate_uint8 does not exist
// negate_int8 literal; prints -52
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint8(negate_uint8(52))
    produces pure result end
}
