// expected failure E2003: binding total declared int32 but used as uint16
// uint16 bindings then expression; expect 22
fn main() -> uint16 {
    total: int32 <- 43 % 5;
    b: uint16 <- total;
    a: uint16 <- total * b - (total + 5);
    k: uint16 <- 30;
    14 + 8 % 27 + a - a + k - k
}
