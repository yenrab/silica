// expected failure E2001: string result in an integer case arm
// case on string literals; "cherry" gives 79
fn main() -> int64 {
    mode: string <- "cherry";
    case mode of {
        "banana" -> "banana";
        "cherry" -> 79;
        "table" -> 30;
        _: string -> 50
    }
}
