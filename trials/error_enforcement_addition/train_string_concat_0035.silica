// expected failure E1050: produces clause without pure
// concat of 2 pieces prints chairalpha

fn main() -> atom {
    sequence proc[device_io] a: string <- "chair";
        b: string <- "alpha";
        s: string <- concat(a, b);
        print_string(s);
        println("")
        produces :ok end
}
