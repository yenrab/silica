// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 104
fn main() -> uint8 {
    base: uint8 <- 21 + 11 - 2 / 3;
    step: uint8 <- base;
    -base + base + 58 + (57 - 43) + step - step
}
