// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_int32 two; prints 1170 then 22
fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        a: int32 <- 30 * 39;
        b: int32 <- 22 % 28;
        _: atom <- print_int32(a);
        println("");
        result: atom <- print_int32(b)
    produces pure result end
}
