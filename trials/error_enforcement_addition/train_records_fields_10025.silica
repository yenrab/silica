// expected failure E2001: string literal in an int64 record field
// inline record field access; x + y = 46
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 0, y: "46" };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv + yv
}
