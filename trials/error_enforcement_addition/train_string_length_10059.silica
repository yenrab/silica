// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// length_bytes of a bound string prints 5
fn main() -> atom {
    sequence proc[mem(normal)] text: string <- "cloud";
        result: atom <- print_int64(length_bytes(text))
        produces pure result end
}
