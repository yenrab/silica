// expected failure E4016: no main/0: entry function renamed to 'mainx'
// divmod returns a pair; p + q = 44
fn divmod(x: int64, y: int64) -> (int64, int64) {
    (x / y, x % y)
}

fn mainx() -> int64 {
    (p: int64, q: int64) <- divmod(44, 1);
    p + q
}
