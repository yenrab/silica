// expected failure E4016: no main/0: entry function renamed to 'entry'
// tuple parameter typed_param; add_pair((25, 60)) = 85
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

fn entry() -> int64 {
    add_pair((25, 60))
}
