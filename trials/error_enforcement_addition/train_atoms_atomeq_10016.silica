// expected failure E2018: binding 'result' has no type annotation
// atom identity: :start == :hot is false
fn main() -> atom {
    sequence proc[device_io]
        left: atom <- :start;
        result <- print_bool(left == :hot)
    produces
        pure result
    end
}
