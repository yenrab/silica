// expected failure E2005: inner tuple decomposed against a mismatched declared type
// nested tuple decomposition; x + y + z = 84
fn main() -> int64 {
    t: (int64, (int64, int64)) <- (37, (16, 31));
    (x: int64, inner: (int64, string)) <- t;
    (y: int64, z: int64) <- inner;
    x + y + z
}
