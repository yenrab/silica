// expected failure E2005: wrong argument count in call to 'wrap'
// string helper wrap; prints [chair]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(wrap());
        println("")
    produces pure :ok end
}
