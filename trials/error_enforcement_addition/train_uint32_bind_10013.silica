// expected failure E2013: unary minus on a variable instead of negate_uint32
// uint32 bindings then expression; expect 52
fn main() -> uint32 {
    width: uint32 <- 26 + 16;
    b: uint32 <- width;
    a: uint32 <- 26;
    -width + 52 + b - b + a - a
}
