// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot two) band hi) + one) = 17
fn main() -> uint64 {
    two: uint64 <- true;
    hi: uint64 <- 13;
    one: uint64 <- 8;
    (((bnot two) band hi) + one)
}
