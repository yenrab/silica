// expected failure E1045: function 'main' declared without the fn keyword
// nested tuple decomposition; x + y + z = 47
main() -> int64 {
    t: (int64, (int64, int64)) <- (13, (24, 10));
    (x: int64, inner: (int64, int64)) <- t;
    (y: int64, z: int64) <- inner;
    x + y + z
}
