// expected failure E1053: duplicate produces keyword
// Positive: device_io use belongs in sequence proc[device_io] (spec §9.3.1)
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_int64(98)
        produces produces pure result end
}
