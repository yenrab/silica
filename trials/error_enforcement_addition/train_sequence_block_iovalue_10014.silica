// expected failure E2003: sum bound as a boolean
// sequence prints and returns the same sum 71
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 38;
        b: int64 <- 33;
        print_int64(a + b);
        println("");
        total: boolean <- a + b
        produces pure total end
}
