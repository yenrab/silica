// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// decompose a mixed tuple and print each element
fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        t: (string, int64, atom) <- ("region", 4, :done);
        (name: string, k: int64, tag: atom) <- t;
        print_string(name);
        println("");
        print_int64(k);
        println("");
        print_atom(tag);
        println("")
    produces pure :ok end
}
