// expected failure E2018: binding 'outcome' has no type annotation
// true or false printed
fn main() -> atom {
    sequence proc[device_io]
        result: boolean <- true or false;
        outcome <- print_bool(result)
    produces pure outcome end
}
