// expected failure E1054: extra end after the sequence block
// qualified call bound then printed; prints 36
use lib_beta;

fn main() -> atom {
    sequence proc[device_io]
        v: int64 <- lib_beta@twice(18);
        result: atom <- print_int64(v)
    produces
        pure result
    end
    end
}
