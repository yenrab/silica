// expected failure E2005: integer pattern against an atom scrutinee
// case over atoms; :busy selects 68
fn main() -> int64 {
    mode: atom <- :busy;
    case mode of {
        9 -> 10;
        :cold -> 41;
        :busy -> 68;
        _: atom -> 30
    }
}
