// expected failure E2001: atom literal returned from the recursion base case
// recursive pow2 on int8; pow2(5) = 32
fn pow2(n: int8) -> int8 {
    case n == 0 of {
        true -> :one;
        false -> 2 * pow2(n - 1)
    }
}

fn main() -> int8 {
    pow2(5)
}
