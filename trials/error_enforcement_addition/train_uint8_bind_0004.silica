// expected failure E2003: binding step declared uint64 but used as uint8
// uint8 bindings then expression; expect 18
fn main() -> uint8 {
    step: uint64 <- 54 / 47 + (54 - 45);
    total: uint8 <- step;
    base: uint8 <- 18;
    base / (total / step)
}
