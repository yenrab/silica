// expected failure E1043: whitespace between binding name 'result' and ':'
// float16 bindings 9.5 + 2.0 printed as 11.5
fn main() -> atom {
    sequence proc[device_io] a: float16 <- 9.5;
        b: float16 <- 2.0;
        result :atom <- print_float16(a + b)
        produces pure result end
}
