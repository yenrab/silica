// expected failure E1055: binding 'temp_val' is never used
// uint64 bindings then expression; expect 47
fn main() -> uint64 {
    temp_val: int64 <- 41;
    height: uint64 <- 18 * 10;
    width: uint64 <- 41 / height + height % height;
    base: uint64 <- 9;
    56 - base + (width + width)
}
