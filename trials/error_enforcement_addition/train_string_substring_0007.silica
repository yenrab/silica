// expected failure E2001: string literal used as a substring index
// substring uses character indices [3, 5) of "Zürich"
fn main() -> atom {
    sequence proc[device_io] text: string <- "Zürich";
        s: string <- substring(text, "3", 5);
        print_string(s);
        println("")
        produces pure :ok end
}
