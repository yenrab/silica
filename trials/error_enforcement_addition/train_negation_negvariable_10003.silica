// expected failure E1055: binding 'temp_val' is never used
// negate_int8 variable; prints 94
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io] x: int8 <- -94;
        result: atom <- print_int8(negate_int8(x))
        produces pure result end
}
