// expected failure E1042: function 'main' has no return type
// shifts: (seed shl 1) + (wide shr 4) = 11
fn main() -> {
    seed: uint64 <- 2;
    wide: uint64 <- 120;
    (seed shl 1) + (wide shr 4)
}
