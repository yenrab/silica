// expected failure E2013: unary minus on a variable instead of negate_int8
// int8 bindings then expression; expect 126
fn main() -> int8 {
    count: int8 <- 47;
    d: int8 <- 47 % count;
    step: int8 <- d % count;
    -count + 32 % 41 + (count + count) + step - step
}
