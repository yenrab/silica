// expected failure E2018: binding 'result' has no type annotation
// chained print_int64 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_int64(18);
        println("");
        result <- print_int64(50)
    produces pure result end
}
