// expected failure E1050: produces clause without pure
// chained print_int8 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_int8(33);
        println("");
        result: atom <- print_int8((37 + (43 + 1)))
    produces
        result
    end
}
