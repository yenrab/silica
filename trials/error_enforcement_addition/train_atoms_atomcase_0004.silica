// expected failure E2005: integer pattern against an atom scrutinee
// case over atoms; :retry selects 10
fn main() -> int64 {
    mode: atom <- :retry;
    case mode of {
        9 -> 39;
        :right -> 73;
        :retry -> 10;
        _: atom -> 84
    }
}
