// expected failure E2003: shift amount literal 65 is out of range 0..63
// shifts: (seed shl 1) + (wide shr 1) = 60
fn main() -> uint64 {
    seed: uint64 <- 1;
    wide: uint64 <- 116;
    (seed shl 65) + (wide shr 1)
}
