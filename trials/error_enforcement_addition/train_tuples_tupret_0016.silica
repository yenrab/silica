// expected failure E2002: undefined identifier 'ys'
// minmax returns a pair; p + q = 54
fn minmax(x: int64, y: int64) -> (int64, int64) {
    case x < y of {
        true -> (x, y);
        false -> (ys, x)
    }
}

fn main() -> int64 {
    (p: int64, q: int64) <- minmax(30, 24);
    p + q
}
