// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 2
fn main() -> uint8 {
    base: uint8 <- 40;
    k: uint8 <- 24;
    a: uint8 <- 22 + base;
    m: uint8 <- 41 + base - k;
    -base + 2 % (a - k) + m - m
}
