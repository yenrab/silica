// expected failure E2001: string element inside an integer list literal
// list literal of 3 uint8 elements; length is 3
fn main() -> int64 {
    sequence proc[mem(normal)] xs: List[uint8, mem(normal)] <- ["22", 37, 23];
        n: int64 <- length[uint8, mem(normal)](xs)
        produces pure n end
}
