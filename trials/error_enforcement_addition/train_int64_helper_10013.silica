// expected failure E2005: call to unknown function 'combine_all'
// helper combine/2 on int64; combine(16, 20) = 32
fn combine(x: int64, y: int64) -> int64 {
    (46 / x * (x % y))
}

fn main() -> int64 {
    combine_all(16, 20)
}
