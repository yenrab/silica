// expected failure E2001: integer literal passed to concat
// concat of 2 pieces prints omegaapple

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "omega";
        b: string <- 5;
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
