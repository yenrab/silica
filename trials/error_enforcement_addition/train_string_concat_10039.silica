// expected failure E1050: produces clause without pure
// concatenate of 2 pieces prints 日本語🙂

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate("日本語", "🙂");
        print_string(s);
        println("")
        produces :ok end
}
