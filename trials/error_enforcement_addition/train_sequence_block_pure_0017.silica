// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (binding); expect 139
fn main() -> int64 {
    sequence proc[]
        result: int64 <- 139
    produces
        pure result
    end
}
