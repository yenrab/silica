// expected failure E1055: binding 'temp_val' is never used
// pair returns a pair; p + q = 45
fn pair(x: int64, y: int64) -> (int64, int64) {
    temp_val: int64 <- 41;
    (x + 1, y * 2)
}

fn main() -> int64 {
    (p: int64, q: int64) <- pair(40, 2);
    p + q
}
