// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (two); expect 47
fn main() -> int64 {
    sequence proc[]
        a: int64 <- 16;
        b: int64 <- 31;
        total: int64 <- a + b
    produces pure total end
}
