// expected failure E2018: binding 'width' has no type annotation
// uint64 bindings then expression; expect 46
fn main() -> uint64 {
    width <- 6;
    m: uint64 <- (width - width / 20);
    height: uint64 <- m + width;
    base: uint64 <- height * 12;
    21 + 25 + 3 / 34 + base - base
}
