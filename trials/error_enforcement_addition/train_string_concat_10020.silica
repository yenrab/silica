// expected failure E2018: binding 's' has no type annotation
// concatenate of 2 pieces prints southworld

fn main() -> atom {
    sequence proc[device_io]
        s <- concatenate("south", "world");
        print_string(s);
        println("")
    produces pure :ok end
}
