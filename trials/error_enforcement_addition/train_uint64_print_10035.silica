// expected failure E1052: duplicate sequence keyword
// print_uint64 var; prints 74
fn main() -> atom {
    sequence sequence proc[device_io]
        amount: uint64 <- 23 - 2;
        result: atom <- print_uint64(53 + amount)
    produces pure result end
}
