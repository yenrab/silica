// expected failure E1053: duplicate produces keyword
// length_chars of a bound string prints 4
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "beta";
        result: atom <- print_int64(length_chars(text))
    produces produces
        pure result
    end
}
