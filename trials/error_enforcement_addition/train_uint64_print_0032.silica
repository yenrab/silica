// expected failure E2018: binding 'result' has no type annotation
// print_uint64 var; prints 76
fn main() -> atom {
    sequence proc[device_io]
        value: uint64 <- (44 + 20);
        result <- print_uint64(12 + value)
    produces pure result end
}
