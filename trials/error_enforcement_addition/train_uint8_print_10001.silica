// expected failure E2003: x is uint8 but passed to print_uint32
// print_uint8 var; prints 16
fn main() -> atom {
    sequence proc[device_io]
        x: uint8 <- 16;
        result: atom <- print_uint32(x)
    produces pure result end
}
