// expected failure E2001: string element in the returned tuple
// divmod returns a pair; p + q = 4
fn divmod(x: int64, y: int64) -> (int64, int64) {
    ("x", x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod(12, 3);
    p + q
}
