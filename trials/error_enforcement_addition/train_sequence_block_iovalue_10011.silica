// expected failure E2005: call to unknown function 'my_print_int64'
// sequence prints and returns the same sum 32
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 10;
        b: int64 <- 22;
        my_print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces pure total end
}
