// expected failure E1042: function 'main' has no return type
// substring uses character indices [3, 4) of "stone"
fn main() {
    sequence proc[device_io]
        text: string <- "stone";
        s: string <- substring(text, 3, 4);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
