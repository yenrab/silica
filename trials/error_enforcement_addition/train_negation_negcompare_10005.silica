// expected failure E2005: negate_uint8 does not exist
// negate_int8 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_bool(negate_uint8(negate_int8(96)) == 96)
    produces
        pure result
    end
}
