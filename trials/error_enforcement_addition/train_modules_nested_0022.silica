// expected failure E1041: trailing ';' after the block's final expression
// nested module-qualified calls; lib_math_utils_spec@add(lib_base@seven(), 21) = 28
use lib_base, lib_math_utils_spec;

fn main() -> int64 {
    lib_math_utils_spec@add(lib_base@seven(), 21);
}
