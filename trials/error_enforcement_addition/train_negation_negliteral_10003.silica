// expected failure E2018: binding 'result' has no type annotation
// negate_int64 literal; prints 27
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int64(negate_int64(-27))
    produces
        pure result
    end
}
