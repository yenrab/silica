// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot bits) band mask) bor three = 10
fn main() -> uint64 {
    bits: uint64 <- true;
    mask: uint64 <- 11;
    three: uint64 <- 10;
    ((bnot bits) band mask) bor three
}
