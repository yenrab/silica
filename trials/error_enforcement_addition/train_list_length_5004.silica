// expected failure E2015: List type without its mem(Space)
// list literal of 4 uint8 elements; length is 4
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[uint8] <- [21, 32, 1, 0];
        n: int64 <- length[uint8, mem(normal)](xs)
    produces
        pure n
    end
}
