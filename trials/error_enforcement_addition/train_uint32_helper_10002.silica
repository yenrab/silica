// expected failure E1041: trailing ';' after the block's final expression
// helper combine/2 on uint32; combine(14, 1) = 14
fn combine(x: uint32, y: uint32) -> uint32 {
    y * y * x;
}

fn main() -> uint32 {
    combine(14, 1)
}
