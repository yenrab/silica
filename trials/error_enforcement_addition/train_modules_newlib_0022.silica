// expected failure E1042: function 'main' has no return type
// root program using the new library module train_lib_label1; train_lib_label1@label1_apply("apple") = 6
use train_lib_label1;

fn main() {
    train_lib_label1@label1_apply("apple")
}
