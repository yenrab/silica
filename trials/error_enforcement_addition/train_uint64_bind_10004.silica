// expected failure E2002: undefined identifier 'missing_total'
// uint64 bindings then expression; expect 10
fn main() -> uint64 {
    count: uint64 <- 6 * 52;
    total: uint64 <- count + 2;
    10 + total - missing_total
}
