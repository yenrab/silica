// expected failure E1045: function 'main' declared without the fn keyword
// nested module-qualified calls; lib_beta@twice(lib_alpha@bump(21)) = 44
use lib_alpha, lib_beta;

main() -> int64 {
    lib_beta@twice(lib_alpha@bump(21))
}
