// expected failure E1040: missing ';' between two statements
// chained print_uint8 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_uint8(50);
        println("")
        result: atom <- print_uint8((60 % 20))
    produces
        pure result
    end
}
