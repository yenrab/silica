// regression (already diagnosed by silica-999997): expected failure E3002: device_io used inside a case arm of a mem(normal) sequence
fn main() -> int64 {
    sequence proc[mem(normal)]
        x: int64 <- 1;
        _: atom <- case x of {
            0 -> print_int64(0);
            _: int64 -> print_int64(1)
        }
    produces pure 7 end
}
