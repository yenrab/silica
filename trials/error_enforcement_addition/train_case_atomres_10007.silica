// expected failure E1045: function 'main' declared without the fn keyword
// case producing atoms then case dispatching on them; grade(2) -> 1
fn grade(n: int64) -> atom {
    case n of {
        0 -> :zero;
        k: int64 if k < 14 -> :low;
        _: int64 -> :high
    }
}

main() -> int64 {
    g: atom <- grade(2);
    case g of {
        :zero -> 0;
        :low -> 1;
        :high -> 2;
        _: atom -> 9
    }
}
