// expected failure E1056: empty proc[] on a pure sequence
// pure sequence block (binding); expect 87
fn main() -> int64 {
    sequence proc[]
        result: int64 <- 87
    produces
        pure result
    end
}
