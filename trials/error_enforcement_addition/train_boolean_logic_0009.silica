// expected failure E1053: duplicate produces keyword
// true and true printed
fn main() -> atom {
    sequence proc[device_io]
        result: boolean <- true and true;
        outcome: atom <- print_bool(result)
    produces produces
        pure outcome
    end
}
