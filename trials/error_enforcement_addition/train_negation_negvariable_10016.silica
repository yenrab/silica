// expected failure E1055: binding 'spare' is never used
// negate_int16 variable; prints -82
fn main() -> atom {
    spare: int64 <- 41;
    sequence proc[device_io]
        x: int16 <- 82;
        result: atom <- print_int16(negate_int16(x))
    produces pure result end
}
