// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 243
fn main() -> uint8 {
    c: uint8 <- 14;
    m: uint8 <- 11 * c;
    base: uint8 <- 50;
    step: uint8 <- m;
    -c + 56 + (33 + step) + base - base
}
