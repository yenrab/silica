// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (lo band seven) band two = 2
fn main() -> uint64 {
    lo: uint64 <- true;
    seven: uint64 <- 11;
    two: uint64 <- 10;
    (lo band seven) band two
}
