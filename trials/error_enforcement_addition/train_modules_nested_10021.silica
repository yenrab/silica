// expected failure E1055: binding 'spare' is never used
// nested module-qualified calls; lib_math_utils_spec@add(lib_base@seven(), 5) = 12
use lib_base, lib_math_utils_spec;

fn main() -> int64 {
    spare: int64 <- 41;
    lib_math_utils_spec@add(lib_base@seven(), 5)
}
