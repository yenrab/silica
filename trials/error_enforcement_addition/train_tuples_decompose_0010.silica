// expected failure E1055: binding 'temp_val' is never used
// decompose a 3-tuple from a literal; a * b + c = 58
fn main() -> int64 {
    temp_val: int64 <- 41;
    (a: int64, b: int64, c: int64) <- (1, 23, 35);
    a * b + c
}
