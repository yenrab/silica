// expected failure E2013: unary minus on a variable instead of negate_uint64
// uint64 bindings then expression; expect 5
fn main() -> uint64 {
    base: uint64 <- 30 - 25;
    k: uint64 <- base;
    d: uint64 <- (43 - k / k);
    b: uint64 <- d;
    -base + base + b - b
}
