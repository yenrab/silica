// expected failure E2005: call to unknown function 'print_atomx'
// print_atom prints the atom with its colon: :green
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- :green;
        result: atom <- print_atomx(state)
    produces
        pure result
    end
}
