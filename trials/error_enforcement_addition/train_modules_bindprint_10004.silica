// expected failure E1060: unbalanced braces
// qualified call bound then printed; prints 1
use lib_math_utils_spec;

fn main() -> atom {
    sequence proc[device_io] v: int64 <- lib_math_utils_spec@add(1, 0);
        result: atom <- print_int64(v)
        produces pure result end
}}
