// expected failure E2003: binding k declared int32 but used as uint8
// uint8 bindings then expression; expect 99
fn main() -> uint8 {
    k: int32 <- 41 + 57;
    b: uint8 <- k / k + k;
    c: uint8 <- 3;
    width: uint8 <- 18 % 10;
    b + c - c + width - width
}
