// expected failure E2018: binding 'step' has no type annotation
// case arms with multi-statement block bodies; expect 33
fn main() -> int64 {
    x: int64 <- 2;
    case x of {
        0 -> {
            base: int64 <- 20;
            base * 2
        }
        _: int64 -> {
            step <- 16;
            x * step + 1
        }
    }
}
