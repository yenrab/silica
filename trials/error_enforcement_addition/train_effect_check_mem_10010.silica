// expected failure E2018: binding 'n' has no type annotation
// Positive: list allocation requires sequence proc[mem(normal)] (spec §9.3.1)
fn main() -> int64 {
    sequence proc[mem(normal)] xs: List[int64, mem(normal)] <- [10];
        n <- length[int64, mem(normal)](xs)
        produces pure n end
}
