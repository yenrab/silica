// expected failure E2005: tuple argument with three elements against a pair parameter
// tuple parameter typed_param; add_pair((28, 53)) = 81
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

fn main() -> int64 {
    add_pair((28, 53, 1))
}
