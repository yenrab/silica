// expected failure E2018: binding 'first' has no type annotation
// head on [37, 29, 36] gives 37
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [37, 29, 36];
        first <- head[int64, mem(normal)](xs)
    produces
        pure first
    end
}
