// expected failure E1045: function 'main' declared without the fn keyword
// uint32 bindings then expression; expect 60
main() -> uint32 {
    height: uint32 <- 53;
    d: uint32 <- height / height * (height / 16);
    height * 16 / (59 - 45) + d - d
}
