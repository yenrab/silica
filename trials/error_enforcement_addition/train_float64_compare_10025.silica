// expected failure E1055: binding 'temp_val' is never used
// float64 comparisons
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        _: atom <- print_bool((31.375 + 22.5) > 7.5);
        println("");
        done: atom <- print_bool(31.75 != (21.375 - 32.25))
    produces
        pure done
    end
}
