// expected failure E4016: no main/0: entry function renamed to 'start'
// string escape quote
fn start() -> atom {
    sequence proc[device_io] q: string <- "say \"hi\"";
        print_string(q);
        println("")
        produces pure :ok end
}
