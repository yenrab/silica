// expected failure E2001: string literal used as a substring index
// substring uses character indices [3, 3) of "abcdefgh"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("abcdefgh", "3", 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
