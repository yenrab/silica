// expected failure E2005: negate_uint64 does not exist
// negate_int16 literal; prints -42
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint64(negate_uint64(42))
    produces pure result end
}
