// expected failure E2018: binding 'n' has no type annotation
// guards partition int64 into three ranges; n = 148 gives 147
fn main() -> int64 {
    n <- 148;
    case n of {
        n: int64 if n < 18 -> 0;
        n: int64 if n >= 18 and n < 101 -> n;
        n: int64 if n >= 101 -> n - 1
    }
}
