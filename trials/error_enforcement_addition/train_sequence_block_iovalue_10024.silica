// expected failure E2018: binding 'total' has no type annotation
// sequence prints and returns the same sum 33
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 14;
        b: int64 <- 19;
        print_int64(a + b);
        println("");
        total <- a + b
        produces pure total end
}
