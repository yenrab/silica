// expected failure E1055: binding 'leftover' is never used
// case on a boolean binding; expect 0
fn main() -> int64 {
    leftover: int64 <- 41;
    flag: boolean <- true;
    case flag of {
        true -> 0;
        false -> 11
    }
}
