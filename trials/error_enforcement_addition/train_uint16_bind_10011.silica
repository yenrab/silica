// expected failure E2013: unary minus on a variable instead of negate_uint16
// uint16 bindings then expression; expect 50
fn main() -> uint16 {
    d: uint16 <- 9 + 33;
    base: uint16 <- 50;
    width: uint16 <- base / (4 % d);
    -d + base + width - width
}
