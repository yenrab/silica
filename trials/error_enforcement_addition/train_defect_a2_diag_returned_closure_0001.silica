// expected failure E1067: a closure that captures a local is returned (interim diagnostic for defect A2)
fn make_adder(k: int64) -> fn(int64) -> int64 {
    fn(x: int64) -> int64 { x + k }
}

fn main() -> int64 {
    add_k: fn(int64) -> int64 <- make_adder(10);
    add_k(5)
}
