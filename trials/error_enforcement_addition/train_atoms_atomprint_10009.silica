// expected failure E1051: sequence block without a closing end
// print_atom prints the atom with its colon: :start
fn main() -> atom {
    sequence proc[device_io] state: atom <- :start;
        result: atom <- print_atom(state)
        produces pure result
}
