// expected failure E2001: string bound to an atom binding
// print_atom prints the atom with its colon: :busy
fn main() -> atom {
    sequence proc[device_io]
        state: atom <- "busy";
        result: atom <- print_atom(state)
    produces pure result end
}
