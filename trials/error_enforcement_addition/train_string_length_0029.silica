// expected failure E1042: function 'main' has no return type
// length_chars of a bound string prints 0
fn main() {
    sequence proc[device_io]
        text: string <- "";
        result: atom <- print_int64(length_chars(text))
    produces
        pure result
    end
}
