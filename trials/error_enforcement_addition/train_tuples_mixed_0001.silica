// expected failure E4016: no main/0: entry function renamed to 'main_program'
// decompose a mixed tuple and print each element
fn main_program() -> atom {
    sequence proc[device_io]
        t: (boolean, int64, string) <- (true, 94, "world");
        (flag: boolean, k: int64, name: string) <- t;
        print_bool(flag);
        println("");
        print_int64(k);
        println("");
        print_string(name);
        println("")
    produces pure :ok end
}
