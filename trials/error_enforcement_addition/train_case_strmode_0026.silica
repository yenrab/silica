// expected failure E3002: device_io used in an arm whose sequence declares mem(normal)
// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "other";
    case mode of {
        "quiet" -> {
            sequence proc[device_io]
                print_string("quiet");
                println("");
                print_int64(4);
                println("")
            produces
                pure :ok
            end
        }
        "debug" -> {
            sequence proc[mem(normal)]
                print_int64(6);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
