// expected failure E1041: trailing ';' after the block's final expression
// tuple parameter typed_param; add_pair((31, 14)) = 45
fn add_pair(p: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- p;
    a + b
}

fn main() -> int64 {
    add_pair((31, 14));
}
