// expected failure E2001: atom literal returned from the recursion base case
// recursive pow2 on int32; pow2(0) = 1
fn pow2(n: int32) -> int32 {
    case n == 0 of {
        true -> :one;
        false -> 2 * pow2(n - 1)
    }
}

fn main() -> int32 {
    pow2(0)
}
