// expected failure E1009: use names a module that does not exist
// Spec §19.3: use lib_alpha; then a module-qualified call lib_alpha@bump(10) = 11
use lib_alpha_missing;

fn main() -> int64 {
    lib_alpha@bump(10)
}
