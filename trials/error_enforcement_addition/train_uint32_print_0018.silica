// expected failure E1045: function 'main' declared without the fn keyword
// print_uint32 var; prints 393
main() -> atom {
    sequence proc[device_io]
        n: uint32 <- 18 * 25;
        result: atom <- print_uint32(n - 57)
    produces
        pure result
    end
}
