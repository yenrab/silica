// expected failure E1046: stray '=' after the last record field
// inline record field access; x + y = 38
fn main() -> int64 {
    p: { x: int64, y: int64= } <- { x: 11, y: 27 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv + yv
}
