// expected failure E1054: extra end after the sequence block
// sequence prints and returns the same sum 57
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 17;
        b: int64 <- 40;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end end
}
