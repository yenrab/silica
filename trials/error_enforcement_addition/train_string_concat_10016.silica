// expected failure E4016: no main/0: entry function renamed to 'main_program'
// concatenate of 2 pieces prints stoneüber

fn main_program() -> atom {
    sequence proc[device_io] s: string <- concatenate("stone", "über");
        print_string(s);
        println("")
        produces pure :ok end
}
