// expected failure E1009: use names a module that does not exist
// Spec §19.3: use lib_beta; then a module-qualified call lib_beta@twice(15) = 30
use lib_beta_missing;

fn main() -> int64 {
    lib_beta@twice(15)
}
