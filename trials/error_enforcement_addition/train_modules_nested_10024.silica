// expected failure E1041: trailing ';' after the block's final expression
// nested module-qualified calls; lib_alpha@bump(lib_beta@twice(22)) = 45
use lib_alpha, lib_beta;

fn main() -> int64 {
    lib_alpha@bump(lib_beta@twice(22));
}
