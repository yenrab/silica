// expected failure E2001: integer literal passed to starts_with
// starts_with prints true
fn main() -> atom {
    sequence proc[device_io] s: string <- 7;
        result: atom <- print_bool(starts_with(s, "北京市"))
        produces pure result end
}
