// expected failure E1041: trailing ';' after the block's final expression
// uint64 bitwise: (hi band seven) bor flags = 7
fn main() -> uint64 {
    hi: uint64 <- 7;
    seven: uint64 <- 7;
    flags: uint64 <- 5;
    (hi band seven) bor flags;
}
