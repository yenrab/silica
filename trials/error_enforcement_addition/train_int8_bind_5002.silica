// expected failure E2003: binding d declared int32 but used as int8
// int8 bindings then expression; expect 10
fn main() -> int8 {
    d: int32 <- (28 % 50) % (23 % 33);
    base: int8 <- d;
    base + d + 3 / 41
}
