// expected failure E2018: binding 'result' has no type annotation
// print_int8 var; prints 1
fn main() -> atom {
    sequence proc[device_io]
        amount: int8 <- 7;
        result <- print_int8(amount / amount)
    produces pure result end
}
