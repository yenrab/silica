// expected failure E2003: comparison against a string binding
// int16 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        label: string <- "x";
        _: atom <- print_bool(label == (31 * 48) > 8);
        println("");
        result: atom <- print_bool((0 * 36) > 1 / 33)
    produces
        pure result
    end
}
