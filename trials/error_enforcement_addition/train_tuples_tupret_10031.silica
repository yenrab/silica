// expected failure E2001: string element in the returned tuple
// pair returns a pair; p + q = 71
fn pair(x: int64, y: int64) -> (int64, int64) {
    (x + 1, "y")
}

fn main() -> int64 {
    (p: int64, q: int64) <- pair(46, 12);
    p + q
}
