// expected failure E2003: binding height declared uint8 but used as int32
// int32 bindings then expression; expect 56
fn main() -> int32 {
    height: uint8 <- 31;
    k: int32 <- (height + height) / height;
    step: int32 <- height * (k % 17);
    25 + step - height
}
