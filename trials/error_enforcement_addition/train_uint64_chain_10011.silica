// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// chained print_uint64 calls separated by println
fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        _: atom <- print_uint64(40);
        println("");
        result: atom <- print_uint64(10)
    produces
        pure result
    end
}
