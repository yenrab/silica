// expected failure E2001: string literal used as a substring index
// substring uses character indices [2, 3) of "table"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "table";
        s: string <- substring(text, "2", 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
