// expected failure E2005: integer pattern against a string scrutinee
// case on string literals; "gamma" gives 80
fn main() -> int64 {
    mode: string <- "gamma";
    case mode of {
        3 -> 80;
        "delta" -> 20;
        "omega" -> 64;
        _: string -> 23
    }
}
