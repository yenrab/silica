// expected failure E1050: produces clause without pure
// print_int16 var; prints 393
fn main() -> atom {
    sequence proc[device_io]
        n: int16 <- 13 * 31;
        result: atom <- print_int16(n - 10)
    produces
        result
    end
}
