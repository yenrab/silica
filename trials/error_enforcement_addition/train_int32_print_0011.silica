// expected failure E2018: binding 'result' has no type annotation
// print_int32 var; prints 2
fn main() -> atom {
    sequence proc[device_io]
        value: int32 <- 60 / 34;
        result <- print_int32(value + value)
    produces pure result end
}
