// expected failure E2003: sum bound as a boolean
// sequence prints and returns the same sum 58
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 28;
        b: int64 <- 30;
        print_int64(a + b);
        println("");
        total: boolean <- a + b
        produces pure total end
}
