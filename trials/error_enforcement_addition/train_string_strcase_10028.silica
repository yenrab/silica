// expected failure E2001: string result in an integer case arm
// case on string literals; "north" gives 52
fn main() -> int64 {
    mode: string <- "north";
    case mode of {
        "gamma" -> "gamma";
        "river" -> 84;
        "north" -> 52;
        _: string -> 75
    }
}
