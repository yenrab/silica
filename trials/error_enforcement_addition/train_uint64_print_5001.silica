// expected failure E1045: function 'main' declared without the fn keyword
// print_uint64 var; prints 273
main() -> atom {
    sequence proc[device_io]
        amount: uint64 <- 5 + 2;
        result: atom <- print_uint64(39 * amount)
    produces pure result end
}
