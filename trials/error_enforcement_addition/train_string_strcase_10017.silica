// expected failure E2005: integer pattern against a string scrutinee
// case on string literals; "cloud" gives 94
fn main() -> int64 {
    mode: string <- "cloud";
    case mode of {
        9 -> 64;
        "cloud" -> 94;
        "actor" -> 100;
        _: string -> 19
    }
}
