// expected failure E1042: function 'main' has no return type
// case expression as a binding's right-hand side; expect 4
fn main() -> {
    x: int64 <- 13;
    label: int64 <- case x % 3 of {
        0 -> 3;
        1 -> 3;
        _: int64 -> x
    };
    label + 1
}
