// expected failure E1040: missing ';' between two statements
// concat of 4 pieces prints 日本語bananacherrychair

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("日本語", "banana"), "cherry"), "chair");
        print_string(s)
        println("")
    produces pure :ok end
}
