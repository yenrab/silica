// expected failure E4016: no main/0: entry function renamed to 'entry'
// pure sequence block (two); expect 77
fn entry() -> int64 {
    sequence
        a: int64 <- 42;
        b: int64 <- 35;
        total: int64 <- a + b
    produces
        pure total
    end
}
