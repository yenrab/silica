// expected failure E1055: binding 'temp_val' is never used
// print_int8 var; prints 22
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io] x: int8 <- 55 - 33;
        result: atom <- print_int8(x % 23)
        produces pure result end
}
