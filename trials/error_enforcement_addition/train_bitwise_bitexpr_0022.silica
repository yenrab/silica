// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (five bor lo) band flags = 10
fn main() -> uint64 {
    five: uint64 <- true;
    lo: uint64 <- 8;
    flags: uint64 <- 10;
    (five bor lo) band flags
}
