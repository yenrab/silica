// expected failure E2005: boolean case missing its false arm
// case on starts_with; expect 9
fn main() -> int64 {
    s: string <- "silica compiler";
    suf: string <- "sili";
    case starts_with(s, suf) of {
        true -> 9
    }
}
