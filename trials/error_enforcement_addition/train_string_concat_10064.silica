// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// concatenate of 3 pieces prints worldübersouth

fn main() -> atom {
    sequence proc[device_io, mem(normal)] s: string <- concatenate(concatenate("world", "über"), "south");
        print_string(s);
        println("")
        produces pure :ok end
}
