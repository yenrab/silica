// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_uint64 two; prints 42 then 330
fn main() -> atom {
    sequence proc[device_io, mem(normal)] a: uint64 <- 42;
        b: uint64 <- (15 * 22);
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
        produces pure result end
}
