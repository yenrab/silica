// expected failure E1041: trailing ';' after the block's final expression
// double_len("omega") = 10
fn double_len(s: string) -> int64 {
    length_chars(s) * 2
}

fn main() -> int64 {
    double_len("omega");
}
