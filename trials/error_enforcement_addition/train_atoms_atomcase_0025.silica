// expected failure E2018: binding 'mode' has no type annotation
// case over atoms; :up selects 70
fn main() -> int64 {
    mode <- :up;
    case mode of {
        :idle -> 12;
        :up -> 70;
        :start -> 16;
        _: atom -> 2
    }
}
