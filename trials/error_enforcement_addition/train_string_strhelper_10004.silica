// expected failure E2002: undefined identifier 'sx'
// string helper shout; prints cloud!

fn shout(s: string, n: int64) -> string {
    case n == 0 of {
        true -> sx;
        false -> concatenate(shout(s, n - 1), "!")
    }
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(shout("cloud", 1));
        println("")
    produces
        pure :ok
    end
}
