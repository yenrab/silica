// expected failure E1042: function 'main' has no return type
// case on a boolean binding; expect 46
fn main() -> {
    flag: boolean <- true;
    case flag of {
        true -> 46;
        false -> 60
    }
}
