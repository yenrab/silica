// expected failure E1047: named struct declaration 'Node' is not allowed
// uint64 bitwise: (two band five) band bits = 4
struct Node { x: int64, y: int64 }

fn main() -> uint64 {
    two: uint64 <- 6;
    five: uint64 <- 4;
    bits: uint64 <- 6;
    (two band five) band bits
}
