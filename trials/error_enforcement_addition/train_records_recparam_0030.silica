// expected failure E2005: field name misspelled in the function body
// record passed to a function; perimeter({ width: 2, height: 2 }) = 8
fn perimeter(r: { width: int64, height: int64 }) -> int64 {
    2 * (r.width + r.heigth)
}

fn main() -> int64 {
    perimeter({ width: 2, height: 2 })
}
