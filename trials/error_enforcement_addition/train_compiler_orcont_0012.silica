// expected failure E2018: binding 'hit' has no type annotation
// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 4;
    b: int64 <- 4;
    hit <- a > 8 or

        b > 8;
    case hit of {
        true -> 1;
        false -> 0
    }
}
