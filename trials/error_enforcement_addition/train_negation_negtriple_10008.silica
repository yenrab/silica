// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// negate_int64 triple; prints 4
fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        result: atom <- print_int64(negate_int64(negate_int64(negate_int64(-4))))
    produces
        pure result
    end
}
