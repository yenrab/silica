// expected failure E1050: produces clause without pure
// concatenate of 4 pieces prints silica🎉cherrycloud

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate(concatenate("silica", "🎉"), "cherry"), "cloud");
        print_string(s);
        println("")
    produces :ok end
}
