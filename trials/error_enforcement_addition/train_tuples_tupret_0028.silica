// expected failure E1065: parameter list of 'divmod' is not closed
// divmod returns a pair; p + q = 15
fn divmod(x: int64, y: int64 -> (int64, int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod(41, 3);
    p + q
}
