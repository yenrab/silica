// expected failure E2005: call to unknown function 'print_int16_all'
// print_int16 literal; prints 42
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_int16_all(42)
    produces
        pure result
    end
}
