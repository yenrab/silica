// expected failure E1050: produces clause without pure
// effectful sequence used as a statement before the return value 63
fn main() -> int64 {
    sequence proc[device_io]
        print_string("world");
        println("")
    produces :ok end;
    63
}
