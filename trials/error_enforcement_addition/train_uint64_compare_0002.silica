// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// uint64 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io, mem(normal)] _: atom <- print_bool((14 + 21) == 60);
        println("");
        _: atom <- print_bool(47 * 32 >= 46);
        println("");
        result: atom <- print_bool((48 * 58) > 40 * 59)
        produces pure result end
}
