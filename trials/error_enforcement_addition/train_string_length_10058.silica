// expected failure E1050: produces clause without pure
// length_chars of a bound string prints 5
fn main() -> atom {
    sequence proc[device_io] text: string <- "table";
        result: atom <- print_int64(length_chars(text))
        produces result end
}
