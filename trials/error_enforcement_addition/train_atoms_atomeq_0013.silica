// expected failure E2018: binding 'result' has no type annotation
// atom identity: :beta != :beta is false
fn main() -> atom {
    sequence proc[device_io] left: atom <- :beta;
        result <- print_bool(left != :beta)
        produces pure result end
}
