// expected failure E2001: integer literal passed to ends_with
// case on ends_with; expect 52
fn main() -> int64 {
    s: string <- "path/to/file.silica";
    suf: string <- 7;
    case ends_with(s, suf) of {
        true -> 52;
        false -> 0
    }
}
