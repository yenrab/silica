// expected failure E1045: function 'main' declared without the fn keyword
// print_uint64 two; prints 0 then 41
main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 5 / 47;
        b: uint64 <- 41;
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
    produces
        pure result
    end
}
