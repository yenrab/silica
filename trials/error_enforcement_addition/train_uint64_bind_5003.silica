// expected failure E2002: undefined identifier 'missing_base'
// uint64 bindings then expression; expect 60
fn main() -> uint64 {
    height: uint64 <- 60;
    base: uint64 <- 36 + height;
    step: uint64 <- 50;
    d: uint64 <- height;
    d + missing_base - base + step - step
}
