use StubWbtSet;
use StubHeap;

fn cmp_i64(a: int64, b: int64) -> :less | :equal | :greater {
    :equal
}

fn main() -> int64 {
    _: OrderedSet[int64, mem(normal)] <- StubHeap@empty({ compare_item: cmp_i64 });
    0
}
