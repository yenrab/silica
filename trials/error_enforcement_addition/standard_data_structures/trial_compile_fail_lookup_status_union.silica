// Expected compile failure — lookup status must be exactly :not_found | :found.

fn bad_lookup() -> { status: :missing | :found, value: int64 } {
    { status: :missing, value: 0 }
}

fn main() -> int64 {
    _: { status: :missing | :found, value: int64 } <- bad_lookup();
    0
}
