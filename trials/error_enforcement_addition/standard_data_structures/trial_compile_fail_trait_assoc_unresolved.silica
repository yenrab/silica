// Layer 1 §7.4: unresolved assoc placeholder without collection or witness context (E2017).

use UnresolvedAssocTrait;

fn main() -> int64 {
    set0: { slot: int64 } <- { slot: 0 };
    case UnresolvedAssocTrait@peek(set0, "x") of {
        true -> 1;
        false -> 0
    }
}
