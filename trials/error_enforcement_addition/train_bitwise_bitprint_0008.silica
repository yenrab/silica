// expected failure E1045: function 'main' declared without the fn keyword
// print_uint64 of 48 band 7 = 0
main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 48;
        b: uint64 <- 7;
        result: atom <- print_uint64(a band b)
    produces pure result end
}
