// expected failure E2003: binding height declared int32 but used as uint8
// uint8 bindings then expression; expect 0
fn main() -> uint8 {
    height: int32 <- 29 + 47;
    base: uint8 <- 58 / height;
    count: uint8 <- 5;
    24 % 20 / 56 + base - base + count - count
}
