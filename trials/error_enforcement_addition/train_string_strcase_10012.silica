// expected failure E1043: whitespace between binding name 'mode' and ':'
// case on string literals; "alpha" gives 96
fn main() -> int64 {
    mode :string <- "alpha";
    case mode of {
        "alpha" -> 96;
        "hello" -> 47;
        "world" -> 35;
        _: string -> 70
    }
}
