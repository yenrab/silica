// expected failure E2003: value is int32 but passed to print_uint32
// print_int32 var; prints 2080
fn main() -> atom {
    sequence proc[device_io]
        value: int32 <- 40 * 52;
        result: atom <- print_uint32(value)
    produces
        pure result
    end
}
