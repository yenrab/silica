// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// float32 helper scale; prints 14.75
fn scale(x: float32) -> float32 {
    x - 0.5 + 1.75
}

fn main() -> atom {
    sequence proc[mem(normal)] result: atom <- print_float32(scale(13.5))
        produces pure result end
}
