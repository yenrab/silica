// expected failure E1050: produces clause without pure
// list field accessors is_nil/head; want 9
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [9, 3, 43];
        r: int64 <- case xs.is_nil of {
            true -> 0;
            false -> xs.head
        }
    produces
        r
    end
}
