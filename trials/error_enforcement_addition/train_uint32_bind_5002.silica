// expected failure E2003: binding count declared uint64 but used as uint32
// uint32 bindings then expression; expect 2
fn main() -> uint32 {
    count: uint64 <- 35;
    height: uint32 <- count + count;
    total: uint32 <- height;
    (height + 30) / (count % height) + total - total
}
