// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// concat of 3 pieces prints applestone🎉

fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        a: string <- "apple";
        b: string <- "stone";
        s: string <- concat(a, b);
        s0: string <- concat(s, "🎉");
        print_string(s0);
        println("")
    produces pure :ok end
}
