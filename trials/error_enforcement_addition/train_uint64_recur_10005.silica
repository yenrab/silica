// expected failure E2005: boolean case missing its false branch
// recursive pow2 on uint64; pow2(2) = 4
fn pow2(n: uint64) -> uint64 {
    case n == 0 of {
        true -> 1
    }
}

fn main() -> uint64 {
    pow2(2)
}
