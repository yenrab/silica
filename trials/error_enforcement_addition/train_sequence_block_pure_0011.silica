// expected failure E1055: binding 'spare' is never used
// pure sequence block (two); expect 72
fn main() -> int64 {
    spare: int64 <- 41;
    sequence
        a: int64 <- 28;
        b: int64 <- 44;
        total: int64 <- a + b
    produces
        pure total
    end
}
