// expected failure E2003: sum bound as a boolean
// sequence prints and returns the same sum 14
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 11;
        b: int64 <- 3;
        print_int64(a + b);
        println("");
        total: boolean <- a + b
        produces pure total end
}
