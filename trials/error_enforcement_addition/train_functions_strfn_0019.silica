// expected failure E1045: function 'double_len' declared without the fn keyword
// double_len("delta") = 10
double_len(s: string) -> int64 {
    length_chars(s) * 2
}

fn main() -> int64 {
    double_len("delta")
}
