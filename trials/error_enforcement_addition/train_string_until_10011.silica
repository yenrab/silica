// expected failure E1060: unbalanced braces
// substring_until_char from 6 up to ';'
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("alpha;delta;delta;hello", 6, ';');
        print_string(s);
        println("")
    produces pure :ok end
