// expected failure E1051: sequence block without a closing end
// float64 helper combine; prints 7.15625
fn combine(x: float64) -> float64 {
    x * 5.25 + 1.25
}

fn main() -> atom {
    sequence proc[device_io] result: atom <- print_float64(combine(1.125))
        produces pure result
}
