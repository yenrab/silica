// expected failure E1042: function 'main' has no return type
// print_uint64 of 7 band 44 = 4
fn main() -> {
    sequence proc[device_io]
        a: uint64 <- 7;
        b: uint64 <- 44;
        result: atom <- print_uint64(a band b)
    produces
        pure result
    end
}
