// expected failure E1050: produces clause without pure
// effectful sequence used as a statement before the return value 23
fn main() -> int64 {
    sequence proc[device_io]
        print_string("stone");
        println("")
    produces :ok end;
    23
}
