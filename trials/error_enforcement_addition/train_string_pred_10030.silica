// expected failure E2005: boolean case missing its false arm
// case on contains; expect 3
fn main() -> int64 {
    s: string <- "abcdef";
    suf: string <- "bc";
    case contains(s, suf) of {
        true -> 3
    }
}
