// expected failure E1043: whitespace between binding name 'b' and ':'
// comments must stop at the newline; want 18
// whole-line comment before the function
fn main() -> int64 {
    a: int64 <- 10; // end-of-line after a binding
    // whole-line comment between statements
    b :int64 <- 2; // another
    a / b + 13 // end-of-line after the return expression
}
