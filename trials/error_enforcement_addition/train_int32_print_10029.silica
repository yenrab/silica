// expected failure E3010: sequence declares mem(normal) but nothing in it needs that effect
// print_int32 var; prints 7744
fn main() -> atom {
    sequence proc[device_io, mem(normal)] x: int32 <- 44 + 44;
        result: atom <- print_int32(x * x)
        produces pure result end
}
