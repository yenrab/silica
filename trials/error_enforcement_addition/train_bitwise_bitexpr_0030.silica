// expected failure E1042: function 'main' has no return type
// uint64 bitwise: (((bnot bits) band three) + hi) = 13
fn main() {
    bits: uint64 <- 7;
    three: uint64 <- 4;
    hi: uint64 <- 13;
    (((bnot bits) band three) + hi)
}
