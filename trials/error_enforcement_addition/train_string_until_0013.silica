// expected failure E2005: call to unknown function 'print_stringx'
// substring_until_char from 0 up to ','
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("hello,north,gamma", 0, ',');
        print_stringx(s);
        println("")
    produces
        pure :ok
    end
}
