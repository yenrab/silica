// expected failure E2013: unary minus on a variable instead of negate_int32
// int32 bindings then expression; expect 72
fn main() -> int32 {
    width: int32 <- 45;
    base: int32 <- 30;
    m: int32 <- (54 + base) * (52 + 12);
    -width + 33 + (57 - 18) + width - width + m - m
}
