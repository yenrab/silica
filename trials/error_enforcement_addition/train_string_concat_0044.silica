// expected failure E2003: string binding used where an integer is expected
// concat of 3 pieces prints cloudbetatable

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat("cloud", "beta"), "table");
        print_int64(s);
        println("")
    produces pure :ok end
}
