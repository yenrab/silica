// expected failure E2005: call to unknown function 'starts_with_all'
// starts_with prints false
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "path/to/file.silica";
        result: atom <- print_bool(starts_with_all(s, "xx"))
    produces
        pure result
    end
}
