// expected failure E2018: binding 'wide' has no type annotation
// shifts: (seed shl 2) + (wide shr 1) = 65
fn main() -> uint64 {
    seed: uint64 <- 1;
    wide <- 123;
    (seed shl 2) + (wide shr 1)
}
