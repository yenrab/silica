// expected failure E1042: function 'main' has no return type
// uint8 bindings then expression; expect 86
fn main() -> {
    step: uint8 <- 44;
    width: uint8 <- 19 + step;
    total: uint8 <- 21;
    42 / 18 + (width + total)
}
