// expected failure E1049: sequence block without produces
// contains prints false
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "silica compiler";
        result: atom <- print_bool(contains(s, "lo w"))
    pure result
    end
}
