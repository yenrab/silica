// expected failure E1049: sequence block without produces
// substring uses character indices [1, 3) of "apple"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("apple", 1, 3);
        print_string(s);
        println("")
    pure :ok
    end
}
