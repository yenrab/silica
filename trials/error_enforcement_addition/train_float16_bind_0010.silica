// expected failure E3002: sequence declares the wrong effect; device_io is used but not declared
// float16 bindings 35.0 + 18.0 printed as 53.0
fn main() -> atom {
    sequence proc[mem(normal)]
        a: float16 <- 35.0;
        b: float16 <- 18.0;
        result: atom <- print_float16(a + b)
    produces
        pure result
    end
}
