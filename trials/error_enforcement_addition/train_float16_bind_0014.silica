// expected failure E2003: integer binding mixed into float arithmetic
// float16 bindings 21.0 * 5.0 printed as 105.0
fn main() -> atom {
    sequence proc[device_io]
        a: float16 <- 21.0;
        b: int64 <- 5;
        result: atom <- print_float16(a * b)
    produces
        pure result
    end
}
