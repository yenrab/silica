// expected failure E1041: trailing ';' after the block's final expression
// int8 bindings then expression; expect 4
fn main() -> int8 {
    total: int8 <- (18 % 43) + 54 / 8;
    a: int8 <- total + total;
    4 + a - a;
}
