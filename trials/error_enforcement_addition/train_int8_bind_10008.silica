// expected failure E1042: function 'main' has no return type
// int8 bindings then expression; expect 10
fn main() {
    width: int8 <- (34 + 14) * (17 / 31);
    b: int8 <- 53;
    10 + width - width + b - b
}
