// expected failure E1042: function 'main' has no return type
// int8 bindings then expression; expect 2
fn main() -> {
    b: int8 <- 20 + 45;
    height: int8 <- (46 + 45) / 45;
    base: int8 <- height + height;
    height + b - b + base - base
}
