// expected failure E2002: undefined identifier 'a_'
// uint32 bindings then expression; expect 30
fn main() -> uint32 {
    c: uint32 <- 28;
    m: uint32 <- 46 + c + (c + c);
    a: uint32 <- 11;
    c * 1 + m / 51 + a - a_
}
