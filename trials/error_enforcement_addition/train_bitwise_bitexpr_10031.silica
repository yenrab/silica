// expected failure E1042: function 'main' has no return type
// uint64 bitwise: ((mask band bits) + five) = 8
fn main() {
    mask: uint64 <- 2;
    bits: uint64 <- 13;
    five: uint64 <- 8;
    ((mask band bits) + five)
}
