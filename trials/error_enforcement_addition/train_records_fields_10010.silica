// expected failure E2005: access to a field the record does not have
// inline record field access; x - y = 44
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 56, y: 12 };
    p.x - p.z
}
