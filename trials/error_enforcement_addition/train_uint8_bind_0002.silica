// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 27
fn main() -> uint8 {
    d: uint8 <- 27;
    a: uint8 <- d;
    count: uint8 <- 51 + 10;
    -d + d + a - a + count - count
}
