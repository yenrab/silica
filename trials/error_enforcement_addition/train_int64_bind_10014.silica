// expected failure E2003: binding m declared int8 but used as int64
// int64 bindings then expression; expect 5
fn main() -> int64 {
    m: int8 <- 55 / 27 + 2;
    k: int64 <- 0;
    base: int64 <- 9;
    11 / 9 + (m + k) + base - base
}
