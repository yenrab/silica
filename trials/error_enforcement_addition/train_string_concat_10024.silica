// expected failure E1060: unbalanced braces
// concat of 4 pieces prints southZürichnaïve🙂

fn main() -> atom {
    sequence proc[device_io] s: string <- concat(concat(concat("south", "Zürich"), "naïve"), "🙂");
        print_string(s);
        println("")
        produces pure :ok end
