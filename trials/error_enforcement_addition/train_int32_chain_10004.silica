// expected failure E1043: whitespace between binding name 'result' and ':'
// chained print_int32 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_int32(30);
        println("");
        result :atom <- print_int32(3)
    produces
        pure result
    end
}
