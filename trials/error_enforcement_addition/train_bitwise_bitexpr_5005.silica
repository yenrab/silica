// expected failure E1042: function 'main' has no return type
// uint64 bitwise: ((one bor flags) + bits) = 14
fn main() -> {
    one: uint64 <- 12;
    flags: uint64 <- 13;
    bits: uint64 <- 1;
    ((one bor flags) + bits)
}
