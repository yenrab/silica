// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot three) band mask) band one = 4
fn main() -> uint64 {
    three: uint64 <- true;
    mask: uint64 <- 6;
    one: uint64 <- 7;
    ((bnot three) band mask) band one
}
