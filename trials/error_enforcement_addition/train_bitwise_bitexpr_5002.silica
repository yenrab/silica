// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: ((bnot seven) band two) band bits = 1
fn main() -> uint64 {
    seven: uint64 <- true;
    two: uint64 <- 5;
    bits: uint64 <- 7;
    ((bnot seven) band two) band bits
}
