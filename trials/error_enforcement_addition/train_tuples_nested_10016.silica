// expected failure E1041: trailing ';' after the block's final expression
// nested tuple decomposition; x + y + z = 52
fn main() -> int64 {
    t: ((int64, int64), int64) <- ((30, 4), 18);
    (inner: (int64, int64), z: int64) <- t;
    (x: int64, y: int64) <- inner;
    x + y + z;
}
