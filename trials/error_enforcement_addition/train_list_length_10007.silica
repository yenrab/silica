// expected failure E2015: List type without its mem(Space)
// list literal of 5 int64 elements; length is 5
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64] <- [34, 0, 13, 32, 27];
        n: int64 <- length[int64, mem(normal)](xs)
    produces pure n end
}
