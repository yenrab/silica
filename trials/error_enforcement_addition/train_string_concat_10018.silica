// expected failure E2018: binding 's' has no type annotation
// concat of 4 pieces prints southsilicaactorhello

fn main() -> atom {
    sequence proc[device_io]
        s <- concat(concat(concat("south", "silica"), "actor"), "hello");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
