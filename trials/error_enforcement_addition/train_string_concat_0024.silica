// expected failure E2001: integer literal passed to concat
// concat of 3 pieces prints regionapplebanana

fn main() -> atom {
    sequence proc[device_io] s: string <- concat(concat("region", "apple"), 6);
        print_string(s);
        println("")
        produces pure :ok end
}
