// expected failure E2018: binding 'c' has no type annotation
// uint32 bindings then expression; expect 61
fn main() -> uint32 {
    count: uint32 <- 4;
    c <- count;
    m: uint32 <- 11 / c;
    42 + 19 + m - m
}
