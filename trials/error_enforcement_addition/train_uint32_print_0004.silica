// expected failure E1055: binding 'temp_val' is never used
// print_uint32 two; prints 0 then 39
fn main() -> atom {
    temp_val: int64 <- 41;
    sequence proc[device_io]
        a: uint32 <- 0 - 0;
        b: uint32 <- 39;
        _: atom <- print_uint32(a);
        println("");
        result: atom <- print_uint32(b)
    produces pure result end
}
