// expected failure E1065: parameter list of 'grade' is not closed
// case producing atoms then case dispatching on them; grade(3) -> 1
fn grade(n: int64 -> atom {
    case n of {
        0 -> :zero;
        k: int64 if k < 14 -> :low;
        _: int64 -> :high
    }
}

fn main() -> int64 {
    g: atom <- grade(3);
    case g of {
        :zero -> 0;
        :low -> 1;
        :high -> 2;
        _: atom -> 9
    }
}
