// expected failure E1042: function 'greet' has no return type
// string helper greet prints hi cherry

fn greet(name: string) -> {
    concatenate("hi ", name)
}

fn main() -> atom {
    sequence proc[device_io]
        print_string(greet("cherry"));
        println("")
    produces pure :ok end
}
