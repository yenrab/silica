// expected failure E2001: string literal used where a char terminator is expected
// substring_until_char from 5 up to ','
fn main() -> atom {
    sequence proc[device_io] s: string <- substring_until_char("beta,cherry,north", 5, ",");
        print_string(s);
        println("")
        produces pure :ok end
}
