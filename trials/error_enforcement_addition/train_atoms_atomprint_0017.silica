// expected failure E2005: call to unknown function 'print_atomx'
// print_atom prints the atom with its colon: :retry
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_atomx(:retry)
    produces pure result end
}
