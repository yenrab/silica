// expected failure E2013: unary minus on a variable instead of negate_int64
// int64 bindings then expression; expect 44
fn main() -> int64 {
    d: int64 <- 8 + 36;
    count: int64 <- 42;
    k: int64 <- count % d;
    step: int64 <- 19 * k * count;
    -d + d + step - step
}
