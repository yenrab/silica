// expected failure E1042: function 'main' has no return type
// print_int64 var; prints 113
fn main() -> {
    sequence proc[device_io]
        n: int64 <- 49 + 34;
        result: atom <- print_int64(n + 30)
    produces pure result end
}
