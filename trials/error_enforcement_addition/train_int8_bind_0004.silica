// expected failure E1042: function 'main' has no return type
// int8 bindings then expression; expect 84
fn main() {
    m: int8 <- 28;
    step: int8 <- m % m;
    count: int8 <- m;
    (9 + 47) + count % 37 + step - step
}
