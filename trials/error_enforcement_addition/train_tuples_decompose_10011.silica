// expected failure E2005: tuple literal element count differs from the declared tuple type
// decompose a 2-tuple from a binding; a + b = 11
fn main() -> int64 {
    t: (int64, int64) <- (7, 4, 1);
    (a: int64, b: int64) <- t;
    a + b
}
