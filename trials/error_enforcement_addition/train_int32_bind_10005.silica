// expected failure E2003: binding count declared uint32 but used as int32
// int32 bindings then expression; expect 0
fn main() -> int32 {
    count: uint32 <- 27 % 6 + 49 / 60;
    b: int32 <- 48 + count;
    (b - count) / (27 * b)
}
