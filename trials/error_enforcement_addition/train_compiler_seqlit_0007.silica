// expected failure E4016: no main/0: entry function renamed to 'entry'
// sequence binding RHS literal then produces with no semicolon; want 4
fn entry() -> int64 {
    sequence
        result: int64 <- 4
    produces
        pure result
    end
}
