// expected failure E1055: binding 'temp_val' is never used
// Spec §19.3: use lib_math_utils_spec; then a module-qualified call lib_math_utils_spec@add(18, 5) = 23
use lib_math_utils_spec;

fn main() -> int64 {
    temp_val: int64 <- 41;
    lib_math_utils_spec@add(18, 5)
}
