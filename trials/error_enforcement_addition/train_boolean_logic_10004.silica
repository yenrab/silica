// expected failure E1054: extra end after the sequence block
// false and true printed
fn main() -> atom {
    sequence proc[device_io] result: boolean <- false and true;
        outcome: atom <- print_bool(result)
        produces pure outcome end end
}
