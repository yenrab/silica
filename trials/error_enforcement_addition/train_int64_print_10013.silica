// expected failure E2003: amount is int64 but passed to print_int32
// print_int64 var; prints 86
fn main() -> atom {
    sequence proc[device_io]
        amount: int64 <- 36 + 50;
        result: atom <- print_int32(amount)
    produces pure result end
}
