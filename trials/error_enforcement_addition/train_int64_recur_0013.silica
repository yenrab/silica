// expected failure E1041: trailing ';' after the block's final expression
// recursive pow2 on int64; pow2(2) = 4
fn pow2(n: int64) -> int64 {
    case n == 0 of {
        true -> 1;
        false -> 2 * pow2(n - 1)
    }
}

fn main() -> int64 {
    pow2(2);
}
