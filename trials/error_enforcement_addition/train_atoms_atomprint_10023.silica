// expected failure E1042: function 'main' has no return type
// print_atom prints the atom with its colon: :pending
fn main() {
    sequence proc[device_io] state: atom <- :pending;
        result: atom <- print_atom(state)
        produces pure result end
}
