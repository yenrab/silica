// expected failure E1055: binding 'temp_val' is never used
// list literal of 2 uint32 elements; length is 2
fn main() -> int64 {
    temp_val: int64 <- 41;
    sequence proc[mem(normal)] xs: List[uint32, mem(normal)] <- [28, 8];
        n: int64 <- length[uint32, mem(normal)](xs)
        produces pure n end
}
