// expected failure E1055: binding 'spare' is never used
// qualified call bound then printed; prints 6
use lib_beta;

fn main() -> atom {
    spare: int64 <- 41;
    sequence proc[device_io]
        v: int64 <- lib_beta@twice(3);
        result: atom <- print_int64(v)
    produces
        pure result
    end
}
