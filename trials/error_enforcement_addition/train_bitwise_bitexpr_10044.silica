// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (((bnot bits) band hi) + flags) = 12
fn main() -> uint64 {
    bits: uint64 <- true;
    hi: uint64 <- 5;
    flags: uint64 <- 11;
    (((bnot bits) band hi) + flags)
}
