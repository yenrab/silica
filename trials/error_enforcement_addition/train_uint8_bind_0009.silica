// expected failure E2013: unary minus on a variable instead of negate_uint8
// uint8 bindings then expression; expect 33
fn main() -> uint8 {
    step: uint8 <- 44 + 13 + 55;
    m: uint8 <- (step + 18);
    count: uint8 <- 33 % step;
    -step + count + m - m
}
