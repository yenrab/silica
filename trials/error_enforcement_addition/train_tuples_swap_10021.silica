// expected failure E1041: trailing ';' after the block's final expression
// swap((32, 55)); first = 55
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((32, 55));
    first;
}
