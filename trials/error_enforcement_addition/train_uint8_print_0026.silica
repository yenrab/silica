// expected failure E2018: binding 'result' has no type annotation
// print_uint8 var; prints 47
fn main() -> atom {
    sequence proc[device_io]
        x: uint8 <- (36 % 56);
        result <- print_uint8(x + 11)
    produces
        pure result
    end
}
