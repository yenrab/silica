// expected failure E2003: binding count declared int8 but used as int64
// int64 bindings then expression; expect 36
fn main() -> int64 {
    count: int8 <- 29 * 3;
    d: int64 <- 5;
    step: int64 <- count;
    (12 * (count / 24)) + d - d + step - step
}
