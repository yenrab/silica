// expected failure E2018: binding 'mode' has no type annotation
// case on string literals; "region" gives 22
fn main() -> int64 {
    mode <- "region";
    case mode of {
        "cloud" -> 26;
        "omega" -> 51;
        "region" -> 22;
        _: string -> 60
    }
}
