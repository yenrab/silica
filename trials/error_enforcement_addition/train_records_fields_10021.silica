// expected failure E2005: access to a field the record does not have
// inline record field access; x - y = 25
fn main() -> int64 {
    p: { x: int64, y: int64 } <- { x: 40, y: 15 };
    xv: int64 <- p.x;
    yv: int64 <- p.z;
    xv - yv
}
