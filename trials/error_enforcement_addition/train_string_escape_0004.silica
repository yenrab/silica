// expected failure E1042: function 'main' has no return type
// string escape tab_bytes
fn main() {
    sequence proc[device_io]
        t: string <- "a\tb";
        result: atom <- print_int64(length_bytes(t))
    produces pure result end
}
