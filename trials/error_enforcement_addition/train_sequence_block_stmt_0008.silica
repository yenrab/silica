// expected failure E4016: no main/0: entry function renamed to 'start'
// effectful sequence used as a statement before the return value 6
fn start() -> int64 {
    sequence proc[device_io]
        print_string("alpha");
        println("")
    produces pure :ok end;
    6
}
