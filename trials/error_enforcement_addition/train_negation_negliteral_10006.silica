// expected failure E1051: sequence block without a closing end
// negate_int8 literal; prints -10
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_int8(negate_int8(10))
    produces
        pure result
}
