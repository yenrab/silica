// expected failure E2018: binding 's' has no type annotation
// substring_until_char from 0 up to ';'
fn main() -> atom {
    sequence proc[device_io]
        s <- substring_until_char("chair;chair;delta", 0, ';');
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
