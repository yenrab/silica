// expected failure E1055: binding 'scratch' is never used
// case arms with multi-statement block bodies; expect 40
fn main() -> int64 {
    scratch: int64 <- 41;
    x: int64 <- 0;
    case x of {
        0 -> {
            base: int64 <- 20;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 3;
            x * step + 1
        }
    }
}
