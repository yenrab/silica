// expected failure E1042: function 'main' has no return type
// negate_int16 compare; prints true
fn main() {
    sequence proc[device_io]
        result: atom <- print_bool(negate_int16(negate_int16(64)) == 64)
    produces
        pure result
    end
}
