// expected failure E1040: missing ';' between two statements
// effectful sequence used as a statement before the return value 2
fn main() -> int64 {
    sequence proc[device_io]
        print_string("actor")
        println("")
    produces pure :ok end;
    2
}
