// expected failure E1043: whitespace between binding name 'h' and ':'
// string escape hex
fn main() -> atom {
    sequence proc[device_io]
        h :string <- "\x41\x42";
        print_string(h);
        println("")
    produces pure :ok end
}
