// expected failure E1042: function 'main' has no return type
// uint8 bindings then expression; expect 51
fn main() {
    base: uint8 <- 31;
    height: uint8 <- base;
    (height + 20 + 26 / base)
}
