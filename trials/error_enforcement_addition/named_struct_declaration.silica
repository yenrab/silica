// E1047: named struct/record declarations are not allowed; records are inline only.

struct PidRegNode { name: atom, process: actor_ref, next_left: PidRegNode, next_right: PidRegNode }

fn main() -> int64 {
    0
}
