// expected failure E1055: binding 'temp_val' is never used
// uint16 bindings then expression; expect 1
fn main() -> uint16 {
    temp_val: int64 <- 41;
    count: uint16 <- 30 % 38 % 56;
    k: uint16 <- 10;
    m: uint16 <- 30;
    34 / m + (k - k) + count - count
}
