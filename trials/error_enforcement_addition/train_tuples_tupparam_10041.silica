// expected failure E1055: binding 'spare' is never used
// tuple parameter typed_param; add_pair((34, 7)) = 41
fn add_pair(p: (int64, int64)) -> int64 {
    spare: int64 <- 41;
    (a: int64, b: int64) <- p;
    a + b
}

fn main() -> int64 {
    add_pair((34, 7))
}
