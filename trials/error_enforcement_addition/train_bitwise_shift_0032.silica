// expected failure E2018: binding 'seed' has no type annotation
// shifts: (seed shl 2) + (wide shr 2) = 20
fn main() -> uint64 {
    seed <- 2;
    wide: uint64 <- 51;
    (seed shl 2) + (wide shr 2)
}
