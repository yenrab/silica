// expected failure E2003: parameter x declared uint8 in a int64 helper
// helper compute/2 on int64; compute(10, 6) = 22
fn compute(x: uint8, y: int64) -> int64 {
    x + (y + y)
}

fn main() -> int64 {
    compute(10, 6)
}
