// expected failure E2005: atom pattern against an integer scrutinee
// literal patterns, a guard and a typed wildcard on uint64; v = 0 gives 14
fn main() -> uint64 {
    v: uint64 <- 0;
    case v of {
        :9 -> 39;
        11 -> 41;
        2 -> 10;
        m: uint64 if m > 5 -> 20;
        _: uint64 -> 14
    }
}
