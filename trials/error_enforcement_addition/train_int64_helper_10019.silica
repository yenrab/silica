// expected failure E1041: trailing ';' after the block's final expression
// helper compute/2 on int64; compute(5, 3) = 46
fn compute(x: int64, y: int64) -> int64 {
    26 + x + y * x
}

fn main() -> int64 {
    compute(5, 3);
}
