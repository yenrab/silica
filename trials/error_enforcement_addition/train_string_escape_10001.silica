// expected failure E1050: produces clause without pure
// string escape newline
fn main() -> atom {
    sequence proc[device_io]
        print_string("river\nactor");
        println("")
    produces :ok end
}
