// expected failure E3009: proc[...] written on the function return type instead of the sequence
// concat of 3 pieces prints überworlddelta

fn main() -> atom proc[device_io] {
    sequence proc[device_io] s: string <- concat(concat("über", "world"), "delta");
        print_string(s);
        println("")
        produces pure :ok end
}
