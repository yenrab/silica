// expected failure E2013: unary minus on a variable instead of negate_uint32
// uint32 bindings then expression; expect 50
fn main() -> uint32 {
    m: uint32 <- 31 + 29;
    a: uint32 <- m % 37;
    total: uint32 <- a - 14 + (45 - a);
    -m + 50 + total - total
}
