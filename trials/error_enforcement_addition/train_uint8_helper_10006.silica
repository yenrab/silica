// expected failure E2005: call to unknown function 'adjust_all'
// helper adjust/2 on uint8; adjust(4, 2) = 0
fn adjust(x: uint8, y: uint8) -> uint8 {
    (10 % x / (y + 47))
}

fn main() -> uint8 {
    adjust_all(4, 2)
}
