// expected failure E2005: negate_uint32 does not exist
// negate_int32 literal; prints -54
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint32(negate_uint32(54))
    produces
        pure result
    end
}
