// expected failure E1055: binding 'scratch' is never used
// uint64 bitwise: (flags bor one) band bits = 7
fn main() -> uint64 {
    scratch: int64 <- 41;
    flags: uint64 <- 2;
    one: uint64 <- 15;
    bits: uint64 <- 7;
    (flags bor one) band bits
}
