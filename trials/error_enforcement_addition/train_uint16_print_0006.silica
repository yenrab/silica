// expected failure E2003: amount is uint16 but passed to print_int64
// print_uint16 var; prints 1
fn main() -> atom {
    sequence proc[device_io] amount: uint16 <- 40 / 23;
        result: atom <- print_int64(amount)
        produces pure result end
}
