// expected failure E1047: named struct declaration 'Node' is not allowed
// uint16 bindings then expression; expect 2
struct Node { x: int64, y: int64 }

fn main() -> uint16 {
    count: uint16 <- (27 * 5);
    width: uint16 <- (23 * count);
    m: uint16 <- 19 / 25;
    (m + 58 % 7) + width - width
}
