// expected failure E1042: function 'main' has no return type
// shifts: (seed shl 4) + (wide shr 2) = 58
fn main() -> {
    seed: uint64 <- 3;
    wide: uint64 <- 42;
    (seed shl 4) + (wide shr 2)
}
