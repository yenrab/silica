// expected failure E2003: string binding used where an integer is expected
// concatenate of 3 pieces prints appleworldnorth

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("apple", "world"), "north");
        print_int64(s);
        println("")
    produces pure :ok end
}
