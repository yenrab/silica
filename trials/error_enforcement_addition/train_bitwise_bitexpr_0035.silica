// expected failure E2001: boolean literal used as a bitwise operand
// uint64 bitwise: (flags band hi) band three = 0
fn main() -> uint64 {
    flags: uint64 <- true;
    hi: uint64 <- 12;
    three: uint64 <- 4;
    (flags band hi) band three
}
