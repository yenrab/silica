// expected failure E1042: function 'divmod' has no return type
// divmod returns a pair; p + q = 17
fn divmod(x: int64, y: int64) {
    (x / y, x % y)
}

fn main() -> int64 {
    (p: int64, q: int64) <- divmod(17, 1);
    p + q
}
