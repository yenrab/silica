// expected failure E1047: named struct declaration 'Pair' is not allowed
// uint64 bitwise: (seven band flags) band mask = 8
struct Pair { x: int64, y: int64 }

fn main() -> uint64 {
    seven: uint64 <- 12;
    flags: uint64 <- 8;
    mask: uint64 <- 13;
    (seven band flags) band mask
}
