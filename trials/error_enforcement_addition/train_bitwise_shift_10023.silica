// expected failure E1042: function 'main' has no return type
// shifts: (seed shl 3) + (wide shr 4) = 46
fn main() -> {
    seed: uint64 <- 5;
    wide: uint64 <- 110;
    (seed shl 3) + (wide shr 4)
}
