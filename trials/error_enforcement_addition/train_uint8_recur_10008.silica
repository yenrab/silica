// expected failure E2001: atom literal returned from the recursion base case
// recursive steps on uint8; steps(45, 0) = 45
fn steps(n: uint8, acc: uint8) -> uint8 {
    case n == 0 of {
        true -> :done;
        false -> steps(n - 1, acc + 1)
    }
}

fn main() -> uint8 {
    steps(45, 0)
}
