// expected failure E2018: binding 'done' has no type annotation
// float16 comparisons
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool((29.5 + 32.0) > 29.0);
        println("");
        done <- print_bool(27.5 >= 18.0)
    produces pure done end
}
