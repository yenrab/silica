// expected failure E2001: integer literal passed to concatenate
// concatenate of 2 pieces prints cloudbeta

fn main() -> atom {
    sequence proc[device_io] a: string <- "cloud";
        b: string <- 4;
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
