// expected failure E1047: named struct declaration 'Point' is not allowed
// uint64 bitwise: (((bnot flags) band lo) + one) = 10
struct Point { x: int64, y: int64 }

fn main() -> uint64 {
    flags: uint64 <- 5;
    lo: uint64 <- 5;
    one: uint64 <- 10;
    (((bnot flags) band lo) + one)
}
