// E1063: List[ must be closed with a matching ] (incomplete List[ is rejected early).
fn main() -> int64 {
    x: int64 <- 1;
    case x == 0 of {
        true -> 0;
        false -> List[ {
            1
        }
    }
}
