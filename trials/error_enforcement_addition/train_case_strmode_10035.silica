// expected failure E2005: integer pattern against a string scrutinee
// string case with an effectful sequence in each arm
fn main() -> atom {
    mode: string <- "verbose";
    case mode of {
        "debug" -> {
            sequence proc[device_io]
                print_string("debug");
                println("");
                print_int64(0);
                println("")
            produces
                pure :ok
            end
        }
        2 -> {
            sequence proc[device_io]
                print_int64(2);
                println("")
            produces
                pure :ok
            end
        }
        _: string -> {
            sequence proc[device_io]
                print_string("default");
                println("")
            produces
                pure :ok
            end
        }
    }
}
