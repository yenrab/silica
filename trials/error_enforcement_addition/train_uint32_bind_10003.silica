// expected failure E2003: binding k declared int64 but used as uint32
// uint32 bindings then expression; expect 26
fn main() -> uint32 {
    k: int64 <- 9;
    d: uint32 <- k * k;
    d % d + (38 - 12)
}
