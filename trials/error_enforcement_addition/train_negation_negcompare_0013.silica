// expected failure E1051: sequence block without a closing end
// negate_int32 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_bool(negate_int32(negate_int32(5)) == 5)
    produces
        pure result
}
