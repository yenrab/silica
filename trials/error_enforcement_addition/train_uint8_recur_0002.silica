// expected failure E2001: atom literal returned from the recursion base case
// recursive sum_to on uint8; sum_to(15) = 120
fn sum_to(n: uint8) -> uint8 {
    case n == 0 of {
        true -> :zero;
        false -> n + sum_to(n - 1)
    }
}

fn main() -> uint8 {
    sum_to(15)
}
