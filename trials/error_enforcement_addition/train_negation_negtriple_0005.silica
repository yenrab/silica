// expected failure E2005: negate_uint64 does not exist
// negate_int64 triple; prints -14
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint64(negate_uint64(negate_int64(negate_int64(14))))
    produces
        pure result
    end
}
