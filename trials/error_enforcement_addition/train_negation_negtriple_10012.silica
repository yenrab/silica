// expected failure E2018: binding 'result' has no type annotation
// negate_int16 triple; prints -9
fn main() -> atom {
    sequence proc[device_io]
        result <- print_int16(negate_int16(negate_int16(negate_int16(9))))
    produces
        pure result
    end
}
