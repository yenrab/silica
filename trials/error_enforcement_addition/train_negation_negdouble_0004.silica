// expected failure E2005: negate_uint64 does not exist
// negate_int32 double; prints 95
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_uint64(negate_uint64(negate_int32(95)))
    produces
        pure result
    end
}
