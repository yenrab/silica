// expected failure E1042: function 'main' has no return type
// inline record field access; x + y = 34
fn main() {
    p: { x: int64, y: int64 } <- { x: 16, y: 18 };
    xv: int64 <- p.x;
    yv: int64 <- p.y;
    xv + yv
}
