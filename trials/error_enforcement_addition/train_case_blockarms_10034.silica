// expected failure E1043: whitespace between binding name 'x' and ':'
// case arms with multi-statement block bodies; expect 10
fn main() -> int64 {
    x :int64 <- 1;
    case x of {
        0 -> {
            base: int64 <- 9;
            base * 2
        }
        _: int64 -> {
            step: int64 <- 9;
            x * step + 1
        }
    }
}
