// expected failure E1042: function 'main' has no return type
// nested module-qualified calls; lib_alpha@bump(lib_beta@twice(4)) = 9
use lib_alpha, lib_beta;

fn main() {
    lib_alpha@bump(lib_beta@twice(4))
}
