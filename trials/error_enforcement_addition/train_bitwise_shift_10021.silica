// expected failure E2018: binding 'seed' has no type annotation
// shifts: (seed shl 4) + (wide shr 1) = 29
fn main() -> uint64 {
    seed <- 1;
    wide: uint64 <- 26;
    (seed shl 4) + (wide shr 1)
}
