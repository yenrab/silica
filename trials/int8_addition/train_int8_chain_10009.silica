// chained print_int8 calls separated by println
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_int8(8 + 10);
        println("");
        result: atom <- print_int8(3 % 18 / (25 - 12))
    produces pure result end
}
