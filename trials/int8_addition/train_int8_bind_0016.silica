// int8 bindings then expression; expect 37
fn main() -> int8 {
    step: int8 <- 54 % 45 + 32 / 7;
    total: int8 <- step;
    a: int8 <- 21;
    base: int8 <- step;
    (24 + (0 + step)) + total - total + a - a + base - base
}
