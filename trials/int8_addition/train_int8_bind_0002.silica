// int8 bindings then expression; expect 23
fn main() -> int8 {
    height: int8 <- 38;
    step: int8 <- 40 + height - (4 + height);
    c: int8 <- 60 / height;
    base: int8 <- 52 + (12 + c);
    7 + base / 4 + step - step
}
