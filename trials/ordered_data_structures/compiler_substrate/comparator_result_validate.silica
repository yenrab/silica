// Layer 1 §7.7: invalid comparator atoms map to :invalid_comparator_result.

fn main() -> int64 {
    good: atom <- comparator_result_validate(:equal);
    bad: atom <- comparator_result_validate(:ok);
    case good == :equal of {
        false -> 1;
        true -> case bad == :invalid_comparator_result of {
            true -> 0;
            false -> 2
        }
    }
}
