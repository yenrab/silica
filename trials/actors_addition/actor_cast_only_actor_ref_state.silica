// Minimal: cast-only behavior; state and return are actor_ref. Body is only cast(peer, msg).
fn relay(m: atom, peer: actor_ref) -> (:no_reply, actor_ref) {
    sequence proc[concurrency]
        _: boolean <- cast(peer, m impl ActorMessage {})
    produces
        pure (:no_reply, peer)
    end
}

fn main() -> atom {
    :ok
}
