// negate_int64 variable; prints 94
fn main() -> atom {
    sequence proc[device_io]
        x: int64 <- -94;
        result: atom <- print_int64(negate_int64(x))
    produces
        pure result
    end
}
