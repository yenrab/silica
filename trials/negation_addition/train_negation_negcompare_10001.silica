// negate_int16 compare; prints true
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_bool(negate_int16(negate_int16(-52)) == -52)
    produces
        pure result
    end
}
