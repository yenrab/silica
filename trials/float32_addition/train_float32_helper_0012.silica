// float32 helper shift; prints -1.75
fn shift(x: float32) -> float32 {
    x - 34.25 + 3.5
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(shift(29.0))
    produces pure result end
}
