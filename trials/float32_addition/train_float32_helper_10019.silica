// float32 helper combine; prints 0.75
fn combine(x: float32) -> float32 {
    x - 35.0 + 0.75
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(combine(35.0))
    produces pure result end
}
