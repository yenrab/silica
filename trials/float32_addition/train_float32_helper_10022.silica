// float32 helper combine; prints 30.25
fn combine(x: float32) -> float32 {
    x + 24.0 + 0.25
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(combine(6.0))
    produces pure result end
}
