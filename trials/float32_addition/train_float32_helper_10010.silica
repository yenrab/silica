// float32 helper combine; prints 57.25
fn combine(x: float32) -> float32 {
    x + 25.5 + 1.75
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(combine(30.0))
    produces
        pure result
    end
}
