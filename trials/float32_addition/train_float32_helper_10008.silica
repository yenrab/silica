// float32 helper half_sum; prints 1.25
fn half_sum(x: float32) -> float32 {
    x - 26.5 + 2.5
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(half_sum(25.25))
    produces pure result end
}
