// float32 helper shift; prints 30.0
fn shift(x: float32) -> float32 {
    x + 13.5 + 4.0
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(shift(12.5))
    produces
        pure result
    end
}
