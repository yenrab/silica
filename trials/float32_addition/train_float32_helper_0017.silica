// float32 helper half_sum; prints -8.75
fn half_sum(x: float32) -> float32 {
    x - 24.25 + 1.5
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(half_sum(14.0))
    produces
        pure result
    end
}
