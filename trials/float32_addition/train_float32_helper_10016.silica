// float32 helper half_sum; prints 32.25
fn half_sum(x: float32) -> float32 {
    x + 20.25 + 5.0
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float32(half_sum(7.0))
    produces pure result end
}
