# compile_defects_ref_opt_binding_annotation_addition

One valid Silica program that today's compiler rejects (E1040, project-bees SD-5); one per directory because a compile
rejection fails the whole directory's compile step (see the compile_defects_addition README). Move `sd5_ref_opt_binding_annotation`
back to `memory_region_addition` once the compiler accepts it.
