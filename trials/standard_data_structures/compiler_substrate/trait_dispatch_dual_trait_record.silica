// Layer 1 §7.4: one concrete record implements two independent traits.

use SizeTrait;
use ScaleTrait;

fn main() -> int64 {
    cell: { payload: int64 } <- { payload: 11 };
    SizeTrait@read_size(cell) + ScaleTrait@read_scale(cell)
}
