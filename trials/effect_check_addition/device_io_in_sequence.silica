// Positive: device_io use belongs in sequence proc[device_io] … produces pure … end (spec §9.3.1).
fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_int64(1)
    produces
        pure result
    end
}
