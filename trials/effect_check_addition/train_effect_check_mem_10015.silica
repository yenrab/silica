// Positive: list allocation requires sequence proc[mem(normal_writeback)] (spec §9.3.1)
fn main() -> int64 {
    sequence proc[mem(normal_writeback)]
        xs: List[int64, mem(normal_writeback)] <- [40, 12];
        n: int64 <- length[int64, mem(normal_writeback)](xs)
    produces
        pure n
    end
}
