// Positive: device_io only in a nested case arm inside sequence proc[device_io]
// … produces pure … end still counts as used (spec §9.3.1 / E3010 must not fire).
fn main() -> atom {
    outer: int64 <- 1;
    inner: int64 <- 2;
    sequence proc[device_io]
        result: atom <- case outer of {
            0 -> print_int64(0);
            _: int64 -> case inner of {
                2 -> print_int64(2);
                _: int64 -> print_int64(9)
            }
        }
    produces
        pure result
    end
}
