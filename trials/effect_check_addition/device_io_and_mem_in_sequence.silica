// Positive: both device_io and mem(normal) declared and used in one sequence block.
fn main() -> atom {
    sequence proc[device_io, mem(normal)]
        xs: List[int64, mem(normal)] <- [7];
        n: int64 <- length[int64, mem(normal)](xs);
        result: atom <- print_int64(n)
    produces
        pure result
    end
}
