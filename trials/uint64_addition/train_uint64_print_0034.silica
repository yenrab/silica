// print_uint64 two; prints 0 then 31
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- (50 / 57);
        b: uint64 <- 31 % 59;
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
    produces pure result end
}
