// print_uint64 two; prints 30 then 1
fn main() -> atom {
    sequence proc[device_io] a: uint64 <- 0 + 30;
        b: uint64 <- 32 % 31;
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
        produces pure result end
}
