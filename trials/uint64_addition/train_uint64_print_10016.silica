// print_uint64 two; prints 15 then 51
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 15;
        b: uint64 <- 51;
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
    produces
        pure result
    end
}
