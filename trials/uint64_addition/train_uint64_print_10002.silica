// print_uint64 two; prints 73 then 0
fn main() -> atom {
    sequence proc[device_io]
        a: uint64 <- 25 + 48;
        b: uint64 <- 0;
        _: atom <- print_uint64(a);
        println("");
        result: atom <- print_uint64(b)
    produces pure result end
}
