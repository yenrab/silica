// uint64 bindings then expression; expect 0
fn main() -> uint64 {
    c: uint64 <- (60 - 29) % 53;
    total: uint64 <- (16 * 10) - c;
    count: uint64 <- (15 * c);
    47 / (42 * 41) + total - total + count - count
}
