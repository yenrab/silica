// uint64 bindings then expression; expect 47
fn main() -> uint64 {
    height: uint64 <- 18 * 10;
    width: uint64 <- 41 / height + height % height;
    base: uint64 <- 9;
    56 - base + (width + width)
}
