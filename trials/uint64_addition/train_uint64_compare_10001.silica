// uint64 comparisons printed with print_bool
fn main() -> atom {
    sequence proc[device_io]
        _: atom <- print_bool(3 % 2 != 22);
        println("");
        result: atom <- print_bool(26 * 50 < 21)
    produces
        pure result
    end
}
