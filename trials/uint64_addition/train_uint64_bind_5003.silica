// uint64 bindings then expression; expect 60
fn main() -> uint64 {
    height: uint64 <- 60;
    base: uint64 <- 36 + height;
    step: uint64 <- 50;
    d: uint64 <- height;
    d + base - base + step - step
}
