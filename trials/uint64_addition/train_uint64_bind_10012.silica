// uint64 bindings then expression; expect 22
fn main() -> uint64 {
    m: uint64 <- 46 * 34;
    total: uint64 <- 48 - 43;
    height: uint64 <- m;
    count: uint64 <- height + (24 - 9);
    22 + total - total + count - count
}
