// defect A4: tuple parameter after a scalar, both items used; pick(5, (3, 10)) = 18
fn pick(acc: int64, x: (int64, int64)) -> int64 {
    (a: int64, b: int64) <- x;
    acc + a + b
}

fn main() -> int64 {
    pick(5, (3, 10))
}
