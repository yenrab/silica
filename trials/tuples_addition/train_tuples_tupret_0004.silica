// minmax returns a pair; p + q = 75
fn minmax(x: int64, y: int64) -> (int64, int64) {
    case x < y of {
        true -> (x, y);
        false -> (y, x)
    }
}

fn main() -> int64 {
    (p: int64, q: int64) <- minmax(39, 36);
    p + q
}
