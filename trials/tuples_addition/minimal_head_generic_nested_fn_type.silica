// Minimal repro: nested fn inside generic brackets for head[...]
fn main() -> int64 {
    child: {
        start: fn(msg: int64, state: int64) -> int64
    } <- { start: fn(msg: int64, state: int64) -> int64{ 0 } };
    0
}

