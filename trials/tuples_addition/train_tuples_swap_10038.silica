// swap((47, 24)); first * 2 + second = 95
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((47, 24));
    first * 2 + second
}
