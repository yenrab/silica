// swap((51, 55)); first * 2 + second = 161
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((51, 55));
    first * 2 + second
}
