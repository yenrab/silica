// swap((28, 42)); first * 2 + second = 112
fn swap(p: (int64, int64)) -> (int64, int64) {
    (x: int64, y: int64) <- p;
    (y, x)
}

fn main() -> int64 {
    (first: int64, second: int64) <- swap((28, 42));
    first * 2 + second
}
