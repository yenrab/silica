// A pair with a string element as a record field value, read back and decomposed.
fn main() -> int64 {
    labelled: { start: (int64, string) } <- { start: (5, "go") };
    st: (int64, string) <- labelled.start;
    (a: int64, b: string) <- st;
    a + length_bytes(b)
}
