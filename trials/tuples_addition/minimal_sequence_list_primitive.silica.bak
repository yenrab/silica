fn main() -> int64 {
    (flags, children) <- sequence proc[mem(normal)]
        flags_and_children: (
        atom,
        List[{ x: int64 }, mem(normal)]
        ) <- (:a, [{x: 0}])
    produces
        pure flags_and_children
    end;
    1
}

