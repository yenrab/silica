fn main() -> int64 {
    (_flags, _children) <- sequence proc[mem(normal)]
        flags_and_children: (
        atom,
        List[int64, mem(normal)]
        ) <- (:a, [0])
    produces
        pure flags_and_children
    end;
    1
}
