// decompose a mixed tuple and print each element
fn main() -> atom {
    sequence proc[device_io]
        t: (int64, string, boolean) <- (15, "chair", true);
        (k: int64, name: string, flag: boolean) <- t;
        print_int64(k);
        println("");
        print_string(name);
        println("");
        print_bool(flag);
        println("")
    produces pure :ok end
}
