fn main() -> int64 {
    list: List[int64, mem(normal)] <- sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [1, 2, 3]
    produces
        pure xs
    end;
    _used_list: List[int64, mem(normal)] <- list;
    0
}
