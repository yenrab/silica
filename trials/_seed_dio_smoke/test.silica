fn make_pair() -> (int64, int64) proc[DeviceIO] {
    sequence proc[DeviceIO]
        _: int8 <- println("hi");
        p: (int64, int64) <- (7, 35)
    produces
        pure p
    end
}

fn main() -> int64 {
    (a: int64, b: int64) <- make_pair();
    a + b
}
