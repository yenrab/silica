// Large int64 in variable, printed via print_int64
fn main() -> atom {
    sequence proc[device_io] x: int64 <- 1000000000000000000;
        result: atom <- print_int64(x)
        produces pure result end
}
