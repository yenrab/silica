// int64 bindings then expression; expect 0
fn main() -> int64 {
    step: int64 <- 49;
    count: int64 <- 35 / 31 + 11 * step;
    total: int64 <- 20;
    m: int64 <- count;
    m / count / (count + step) + total - total
}
