// print_int64 var; prints 1
fn main() -> atom {
    sequence proc[device_io]
        value: int64 <- 58 - 35;
        result: atom <- print_int64(value / value)
    produces
        pure result
    end
}
