// float16 helper combine; prints 48.0
fn combine(x: float16) -> float16 {
    x + 22.75 + 5.5
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float16(combine(19.75))
    produces
        pure result
    end
}
