// Ensures float64->int conversion for termination numbers works.
// main returns float64; compiler emits FCVTZS W0, D0 so exit code = trunc(15.1) = 15.
fn main() -> float16 {
    2.5 + 3.2 * 3.9375
}
