// string helper wrap; prints [apple]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("apple"));
        println("")
        produces pure :ok end
}
