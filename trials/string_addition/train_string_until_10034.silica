// substring_until_char from 7 up to ';'
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("cherry;cherry;gamma", 7, ';');
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
