// substring uses character indices [2, 4) of "hello"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "hello";
        s: string <- substring(text, 2, 4);
        print_string(s);
        println("")
    produces pure :ok end
}
