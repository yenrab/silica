// substring with emoji: extract "🙂" from "Hi🙂!" (character-based indices)
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("Hi🙂!", 2, 3);
        print_string(s);
        println("")
        produces pure :ok end
}
