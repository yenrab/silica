// concat of 4 pieces prints river日本語omegaalpha

fn main() -> atom {
    sequence proc[device_io] a: string <- "river";
        b: string <- "日本語";
        s: string <- concat(a, b);
        s0: string <- concat(s, "omega");
        s1: string <- concat(s0, "alpha");
        print_string(s1);
        println("")
        produces pure :ok end
}
