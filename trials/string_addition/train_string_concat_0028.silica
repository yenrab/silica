// concat of 2 pieces prints northriver

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("north", "river");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
