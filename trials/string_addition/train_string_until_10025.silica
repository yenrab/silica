// substring_until_char from 0 up to '|'
fn main() -> atom {
    sequence proc[device_io] s: string <- substring_until_char("beta|banana|silica", 0, '|');
        print_string(s);
        println("")
        produces pure :ok end
}
