// substring uses character indices [5, 5) of "delta"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("delta", 5, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
