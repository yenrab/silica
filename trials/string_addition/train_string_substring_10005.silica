// substring uses character indices [5, 5) of "table"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("table", 5, 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
