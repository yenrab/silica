// case on string literals; "other" gives 100
fn main() -> int64 {
    mode: string <- "other";
    case mode of {
        "south" -> 82;
        "world" -> 99;
        "cloud" -> 0;
        _: string -> 100
    }
}
