// substring uses character indices [4, 4) of "café"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("café", 4, 4);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
