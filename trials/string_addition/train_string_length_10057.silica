// length_chars of a bound string prints 7
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "👨‍👩‍👧‍👦";
        result: atom <- print_int64(length_chars(text))
    produces
        pure result
    end
}
