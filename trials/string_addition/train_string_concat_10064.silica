// concatenate of 3 pieces prints worldübersouth

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate("world", "über"), "south");
        print_string(s);
        println("")
        produces pure :ok end
}
