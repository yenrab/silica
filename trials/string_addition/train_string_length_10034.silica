// length_chars of a bound string prints 3
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "日本語";
        result: atom <- print_int64(length_chars(text))
    produces pure result end
}
