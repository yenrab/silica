// substring uses character indices [0, 2) of "日本語"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("日本語", 0, 2);
        print_string(s);
        println("")
    produces pure :ok end
}
