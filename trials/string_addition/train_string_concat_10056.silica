// concatenate of 4 pieces prints naïvecaféalphanorth

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("naïve", "café"), "alpha"), "north");
        print_string(s);
        println("")
        produces pure :ok end
}
