// concatenate of 3 pieces prints 🎉worldñandú

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("🎉", "world"), "ñandú");
        print_string(s);
        println("")
    produces pure :ok end
}
