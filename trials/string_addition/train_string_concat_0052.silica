// concatenate of 2 pieces prints über🎉

fn main() -> atom {
    sequence proc[device_io] a: string <- "über";
        b: string <- "🎉";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
