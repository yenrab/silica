// concat of 2 pieces prints deltaregion

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("delta", "region");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
