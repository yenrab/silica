// concatenate of 2 pieces prints naïveomega

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("naïve", "omega");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
