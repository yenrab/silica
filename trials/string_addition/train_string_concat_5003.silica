// concat of 4 pieces prints tableriver🎉river

fn main() -> atom {
    sequence proc[device_io] s: string <- concat(concat(concat("table", "river"), "🎉"), "river");
        print_string(s);
        println("")
        produces pure :ok end
}
