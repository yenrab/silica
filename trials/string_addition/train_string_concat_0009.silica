// concatenate of 2 pieces prints cafétable

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "café";
        b: string <- "table";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces pure :ok end
}
