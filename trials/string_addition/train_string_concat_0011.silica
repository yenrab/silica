// concatenate of 4 pieces prints αβγbananacherrysilica

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate(concatenate("αβγ", "banana"), "cherry"), "silica");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
