// concat of 3 pieces prints worldriver日本語

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "world";
        b: string <- "river";
        s: string <- concat(a, b);
        s0: string <- concat(s, "日本語");
        print_string(s0);
        println("")
    produces pure :ok end
}
