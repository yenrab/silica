// substring uses character indices [3, 3) of "über"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("über", 3, 3);
        print_string(s);
        println("")
    produces pure :ok end
}
