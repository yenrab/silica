// concatenate of 2 pieces prints naïveZürich

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("naïve", "Zürich");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
