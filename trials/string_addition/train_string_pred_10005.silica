// starts_with prints true
fn main() -> atom {
    sequence proc[device_io] s: string <- "silica compiler";
        result: atom <- print_bool(starts_with(s, "sili"))
        produces pure result end
}
