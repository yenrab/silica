// concat of 2 pieces prints gammatable

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "gamma";
        b: string <- "table";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
