// substring uses character indices [0, 0) of "🎉"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "🎉";
        s: string <- substring(text, 0, 0);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
