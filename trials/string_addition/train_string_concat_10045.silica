// concatenate of 2 pieces prints riverbanana

fn main() -> atom {
    sequence proc[device_io] a: string <- "river";
        b: string <- "banana";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
