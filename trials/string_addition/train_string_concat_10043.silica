// concat of 4 pieces prints cloudαβγnaïvedelta

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("cloud", "αβγ"), "naïve"), "delta");
        print_string(s);
        println("")
    produces pure :ok end
}
