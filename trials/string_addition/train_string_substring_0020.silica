// substring uses character indices [4, 6) of "Zürich"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("Zürich", 4, 6);
        print_string(s);
        println("")
    produces pure :ok end
}
