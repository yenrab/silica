// concat of 4 pieces prints southsilicaactorhello

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("south", "silica"), "actor"), "hello");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
