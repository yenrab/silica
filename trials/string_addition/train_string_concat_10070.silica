// concatenate of 3 pieces prints naïvestoneapple

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate("naïve", "stone"), "apple");
        print_string(s);
        println("")
        produces pure :ok end
}
