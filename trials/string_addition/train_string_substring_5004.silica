// substring uses character indices [3, 4) of "cloud"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "cloud";
        s: string <- substring(text, 3, 4);
        print_string(s);
        println("")
    produces pure :ok end
}
