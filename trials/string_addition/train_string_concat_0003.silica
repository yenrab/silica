// concatenate of 2 pieces prints gammachair

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("gamma", "chair");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
