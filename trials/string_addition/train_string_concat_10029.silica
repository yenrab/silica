// concatenate of 4 pieces prints übergammaZürichtable

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("über", "gamma"), "Zürich"), "table");
        print_string(s);
        println("")
        produces pure :ok end
}
