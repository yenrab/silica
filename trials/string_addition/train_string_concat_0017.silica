// concat of 2 pieces prints Zürichchair

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("Zürich", "chair");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
