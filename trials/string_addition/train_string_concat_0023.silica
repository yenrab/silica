// concatenate of 2 pieces prints cherrystone

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "cherry";
        b: string <- "stone";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
