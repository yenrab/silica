// concat of 4 pieces prints 🙂rivertableregion

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("🙂", "river"), "table"), "region");
        print_string(s);
        println("")
    produces pure :ok end
}
