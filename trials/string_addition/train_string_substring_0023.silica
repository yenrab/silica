// substring uses character indices [1, 6) of "region"
fn main() -> atom {
    sequence proc[device_io] text: string <- "region";
        s: string <- substring(text, 1, 6);
        print_string(s);
        println("")
        produces pure :ok end
}
