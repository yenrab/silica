// concat of 4 pieces prints übercafécherrysouth

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("über", "café"), "cherry"), "south");
        print_string(s);
        println("")
    produces pure :ok end
}
