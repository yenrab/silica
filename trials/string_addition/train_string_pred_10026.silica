// ends_with prints true
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "silica compiler";
        result: atom <- print_bool(ends_with(s, "ler"))
    produces
        pure result
    end
}
