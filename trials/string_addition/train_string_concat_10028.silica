// concat of 2 pieces prints 日本語world

fn main() -> atom {
    sequence proc[device_io] a: string <- "日本語";
        b: string <- "world";
        s: string <- concat(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
