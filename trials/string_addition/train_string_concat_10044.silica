// concat of 2 pieces prints omegaapple

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "omega";
        b: string <- "apple";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
