// concatenate of 3 pieces prints worldhellobeta

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("world", "hello"), "beta");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
