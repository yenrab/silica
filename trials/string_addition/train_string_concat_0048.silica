// concat of 4 pieces prints worldZürich日本語beta

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("world", "Zürich"), "日本語"), "beta");
        print_string(s);
        println("")
    produces pure :ok end
}
