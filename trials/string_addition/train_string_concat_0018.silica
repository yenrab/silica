// concatenate of 4 pieces prints world世界🙂ñandú

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate(concatenate("world", "世界"), "🙂"), "ñandú");
        print_string(s);
        println("")
    produces pure :ok end
}
