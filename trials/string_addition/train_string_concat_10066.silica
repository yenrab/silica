// concat of 4 pieces prints cherryworldactorworld

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("cherry", "world"), "actor"), "world");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
