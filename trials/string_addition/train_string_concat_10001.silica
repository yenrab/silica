// concat of 2 pieces prints riveralpha

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "river";
        b: string <- "alpha";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces pure :ok end
}
