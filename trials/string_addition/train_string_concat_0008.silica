// concatenate of 3 pieces prints gammaappleriver

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate("gamma", "apple"), "river");
        print_string(s);
        println("")
        produces pure :ok end
}
