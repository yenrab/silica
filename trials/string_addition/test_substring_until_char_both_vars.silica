// substring_until_char with both string and character as variables
fn main() -> atom {
    sequence proc[device_io] src: string <- "apple,banana";
        sep: char <- ',';
        s: string <- substring_until_char(src, 0, sep);
        print_string(s);
        println("")
        produces pure :ok end
}
