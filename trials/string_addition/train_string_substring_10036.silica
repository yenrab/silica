// substring uses character indices [3, 5) of "omega"
fn main() -> atom {
    sequence proc[device_io] text: string <- "omega";
        s: string <- substring(text, 3, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
