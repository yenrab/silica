// concatenate of 3 pieces prints cherrynaïvecafé

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("cherry", "naïve"), "café");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
