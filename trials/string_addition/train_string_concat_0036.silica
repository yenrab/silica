// concatenate of 3 pieces prints Zürichsilicatable

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("Zürich", "silica"), "table");
        print_string(s);
        println("")
    produces pure :ok end
}
