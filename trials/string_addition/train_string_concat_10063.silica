// concatenate of 4 pieces prints omegaregion日本語river

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("omega", "region"), "日本語"), "river");
        print_string(s);
        println("")
        produces pure :ok end
}
