// case on string literals; "actor" gives 55
fn main() -> int64 {
    mode: string <- "actor";
    case mode of {
        "cloud" -> 64;
        "actor" -> 55;
        "north" -> 0;
        _: string -> 78
    }
}
