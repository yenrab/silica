// substring uses character indices [0, 2) of "gamma"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("gamma", 0, 2);
        print_string(s);
        println("")
    produces pure :ok end
}
