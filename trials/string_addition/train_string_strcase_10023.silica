// case on string literals; "cloud" gives 2
fn main() -> int64 {
    mode: string <- "cloud";
    case mode of {
        "cloud" -> 2;
        "south" -> 7;
        "river" -> 16;
        _: string -> 58
    }
}
