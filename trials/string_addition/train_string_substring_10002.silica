// substring uses character indices [5, 5) of "north"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "north";
        s: string <- substring(text, 5, 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
