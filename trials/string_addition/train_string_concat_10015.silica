// concat of 4 pieces prints northnaïvecherryomega

fn main() -> atom {
    sequence proc[device_io] a: string <- "north";
        b: string <- "naïve";
        s: string <- concat(a, b);
        s0: string <- concat(s, "cherry");
        s1: string <- concat(s0, "omega");
        print_string(s1);
        println("")
        produces pure :ok end
}
