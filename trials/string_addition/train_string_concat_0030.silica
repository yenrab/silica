// concat of 4 pieces prints cloudsouthñandúactor

fn main() -> atom {
    sequence proc[device_io] a: string <- "cloud";
        b: string <- "south";
        s: string <- concat(a, b);
        s0: string <- concat(s, "ñandú");
        s1: string <- concat(s0, "actor");
        print_string(s1);
        println("")
        produces pure :ok end
}
