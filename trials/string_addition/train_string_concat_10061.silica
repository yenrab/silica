// concat of 2 pieces prints 🙂über

fn main() -> atom {
    sequence proc[device_io] a: string <- "🙂";
        b: string <- "über";
        s: string <- concat(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
