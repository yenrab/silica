// substring uses character indices [2, 3) of "banana"
fn main() -> atom {
    sequence proc[device_io] text: string <- "banana";
        s: string <- substring(text, 2, 3);
        print_string(s);
        println("")
        produces pure :ok end
}
