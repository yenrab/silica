// substring uses character indices [2, 3) of "café"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("café", 2, 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
