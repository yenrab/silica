// substring uses character indices [2, 2) of "actor"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "actor";
        s: string <- substring(text, 2, 2);
        print_string(s);
        println("")
    produces pure :ok end
}
