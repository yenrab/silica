// concat of 4 pieces prints 🙂ñandú🎉north

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("🙂", "ñandú"), "🎉"), "north");
        print_string(s);
        println("")
    produces pure :ok end
}
