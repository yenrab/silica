// length_chars of a bound string prints 6
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "banana";
        result: atom <- print_int64(length_chars(text))
    produces
        pure result
    end
}
