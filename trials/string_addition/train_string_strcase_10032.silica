// case on string literals; "other" gives 87
fn main() -> int64 {
    mode: string <- "other";
    case mode of {
        "region" -> 70;
        "beta" -> 15;
        "cherry" -> 25;
        _: string -> 87
    }
}
