// concatenate of 3 pieces prints southgammaalpha

fn main() -> atom {
    sequence proc[device_io] a: string <- "south";
        b: string <- "gamma";
        s: string <- concatenate(a, b);
        s0: string <- concatenate(s, "alpha");
        print_string(s0);
        println("")
        produces pure :ok end
}
