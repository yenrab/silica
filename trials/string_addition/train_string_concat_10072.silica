// concatenate of 3 pieces prints übernorthZürich

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("über", "north"), "Zürich");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
