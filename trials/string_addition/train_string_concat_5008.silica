// concatenate of 4 pieces prints 🙂bananaappleüber

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate(concatenate(concatenate("🙂", "banana"), "apple"), "über");
        print_string(s);
        println("")
        produces pure :ok end
}
