// concatenate of 2 pieces prints naïveapple

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("naïve", "apple");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
