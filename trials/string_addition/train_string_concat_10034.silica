// concat of 2 pieces prints stonegamma

fn main() -> atom {
    sequence proc[device_io] a: string <- "stone";
        b: string <- "gamma";
        s: string <- concat(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
