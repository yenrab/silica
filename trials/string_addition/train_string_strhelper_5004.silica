// string helper wrap; prints [delta]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("delta"));
        println("")
        produces pure :ok end
}
