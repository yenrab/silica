// concatenate of 2 pieces prints 日本語ñandú

fn main() -> atom {
    sequence proc[device_io] a: string <- "日本語";
        b: string <- "ñandú";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
