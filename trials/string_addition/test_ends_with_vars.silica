// ends_with with bound string variables
fn main() -> int64 {
    s: string <- "path/to/foo.silica";
    suf: string <- ".silica";
    case ends_with(s, suf) of {
        true -> 1;
        false -> 0
    }
}
