// substring uses character indices [2, 3) of "αβγ"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("αβγ", 2, 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
