// concatenate of 2 pieces prints silicachair

fn main() -> atom {
    sequence proc[device_io] a: string <- "silica";
        b: string <- "chair";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
