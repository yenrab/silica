// concat of 2 pieces prints helloregion

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "hello";
        b: string <- "region";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces pure :ok end
}
