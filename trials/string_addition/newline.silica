fn main() -> atom {
    sequence proc[device_io] s: string <- "🙂";
        print_string(s);
        println("🎉");
        print_string("hello");
        println(" friends")
        produces pure :ok end
}
