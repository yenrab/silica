// string helper wrap; prints [cherry]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("cherry"));
        println("")
        produces pure :ok end
}
