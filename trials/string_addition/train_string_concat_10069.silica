// concat of 3 pieces prints 日本語riveralpha

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat("日本語", "river"), "alpha");
        print_string(s);
        println("")
    produces pure :ok end
}
