// concatenate of 2 pieces prints 🙂silica

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("🙂", "silica");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
