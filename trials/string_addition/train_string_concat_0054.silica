// concatenate of 2 pieces prints 🙂naïve

fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate("🙂", "naïve");
        print_string(s);
        println("")
        produces pure :ok end
}
