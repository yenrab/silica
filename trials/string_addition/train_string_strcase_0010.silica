// case on string literals; "omega" gives 75
fn main() -> int64 {
    mode: string <- "omega";
    case mode of {
        "omega" -> 75;
        "banana" -> 73;
        "region" -> 58;
        _: string -> 95
    }
}
