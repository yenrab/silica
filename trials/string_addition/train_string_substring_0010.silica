// substring uses character indices [4, 5) of "ñandú"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "ñandú";
        s: string <- substring(text, 4, 5);
        print_string(s);
        println("")
    produces pure :ok end
}
