// concatenate of 2 pieces prints regionriver

fn main() -> atom {
    sequence proc[device_io] a: string <- "region";
        b: string <- "river";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
