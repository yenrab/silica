// concat of 2 pieces prints ñandúüber

fn main() -> atom {
    sequence proc[device_io] a: string <- "ñandú";
        b: string <- "über";
        s: string <- concat(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
