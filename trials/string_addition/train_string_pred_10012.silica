// ends_with prints true
fn main() -> atom {
    sequence proc[device_io]
        s: string <- "abcdef";
        result: atom <- print_bool(ends_with(s, "def"))
    produces
        pure result
    end
}
