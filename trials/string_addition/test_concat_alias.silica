// concat/2 alias of concatenate — spec §5.3.2
fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("Hello", " World");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
