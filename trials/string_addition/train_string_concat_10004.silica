// concat of 4 pieces prints cloudüberstoneregion

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat(concat("cloud", "über"), "stone"), "region");
        print_string(s);
        println("")
    produces pure :ok end
}
