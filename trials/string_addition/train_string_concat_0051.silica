// concat of 2 pieces prints Zürichomega

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "Zürich";
        b: string <- "omega";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces pure :ok end
}
