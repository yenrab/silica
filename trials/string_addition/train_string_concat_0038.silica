// concat of 4 pieces prints river🙂naïve🙂

fn main() -> atom {
    sequence proc[device_io] a: string <- "river";
        b: string <- "🙂";
        s: string <- concat(a, b);
        s0: string <- concat(s, "naïve");
        s1: string <- concat(s0, "🙂");
        print_string(s1);
        println("")
        produces pure :ok end
}
