// concatenate of 2 pieces prints southworld

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("south", "world");
        print_string(s);
        println("")
    produces pure :ok end
}
