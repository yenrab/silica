// concat of 2 pieces prints naïveworld

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("naïve", "world");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
