// case on string literals; "region" gives 23
fn main() -> int64 {
    mode: string <- "region";
    case mode of {
        "delta" -> 2;
        "region" -> 23;
        "hello" -> 16;
        _: string -> 61
    }
}
