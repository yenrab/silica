// concat of 2 pieces prints hellocherry

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("hello", "cherry");
        print_string(s);
        println("")
    produces pure :ok end
}
