// concatenate of 2 pieces prints riverZürich

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("river", "Zürich");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
