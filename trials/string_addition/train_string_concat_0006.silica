// concat of 2 pieces prints northnaïve

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("north", "naïve");
        print_string(s);
        println("")
    produces pure :ok end
}
