// concatenate of 4 pieces prints tableñandúZürichbeta

fn main() -> atom {
    sequence proc[device_io] a: string <- "table";
        b: string <- "ñandú";
        s: string <- concatenate(a, b);
        s0: string <- concatenate(s, "Zürich");
        s1: string <- concatenate(s0, "beta");
        print_string(s1);
        println("")
        produces pure :ok end
}
