// concatenate of 2 pieces prints actoractor

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("actor", "actor");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
