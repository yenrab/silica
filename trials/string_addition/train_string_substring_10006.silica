// substring uses character indices [5, 5) of "actor"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "actor";
        s: string <- substring(text, 5, 5);
        print_string(s);
        println("")
    produces pure :ok end
}
