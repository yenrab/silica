// concatenate of 4 pieces prints cherrynaïveriveralpha

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate(concatenate("cherry", "naïve"), "river"), "alpha");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
