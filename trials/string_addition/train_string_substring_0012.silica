// substring uses character indices [0, 0) of "🎉"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("🎉", 0, 0);
        print_string(s);
        println("")
    produces pure :ok end
}
