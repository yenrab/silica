// concat of 3 pieces prints actorhellonaïve

fn main() -> atom {
    sequence proc[device_io] s: string <- concat(concat("actor", "hello"), "naïve");
        print_string(s);
        println("")
        produces pure :ok end
}
