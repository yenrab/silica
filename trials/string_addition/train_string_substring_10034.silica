// substring uses character indices [2, 5) of "alpha"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "alpha";
        s: string <- substring(text, 2, 5);
        print_string(s);
        println("")
    produces pure :ok end
}
