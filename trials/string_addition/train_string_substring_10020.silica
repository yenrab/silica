// substring uses character indices [3, 3) of "river"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("river", 3, 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
