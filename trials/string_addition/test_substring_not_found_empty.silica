// substring not found: start == end yields empty string
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("Hello", 2, 2);
        print_string(s);
        println("")
        produces pure :ok end
}
