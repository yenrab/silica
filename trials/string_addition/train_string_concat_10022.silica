// concat of 2 pieces prints northriver

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "north";
        b: string <- "river";
        s: string <- concat(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
