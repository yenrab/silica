// substring uses character indices [4, 5) of "north"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "north";
        s: string <- substring(text, 4, 5);
        print_string(s);
        println("")
    produces pure :ok end
}
