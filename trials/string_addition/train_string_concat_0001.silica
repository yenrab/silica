// concat of 3 pieces prints applenaïveñandú

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat(concat("apple", "naïve"), "ñandú");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
