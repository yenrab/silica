// concat of 3 pieces prints regionapplebanana

fn main() -> atom {
    sequence proc[device_io] s: string <- concat(concat("region", "apple"), "banana");
        print_string(s);
        println("")
        produces pure :ok end
}
