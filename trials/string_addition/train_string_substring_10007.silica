// substring uses character indices [0, 4) of "apple"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("apple", 0, 4);
        print_string(s);
        println("")
        produces pure :ok end
}
