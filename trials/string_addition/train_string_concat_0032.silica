// concatenate of 3 pieces prints silicastonecherry

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate(concatenate("silica", "stone"), "cherry");
        print_string(s);
        println("")
    produces pure :ok end
}
