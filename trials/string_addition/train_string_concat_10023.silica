// concatenate of 2 pieces prints omegabanana

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "omega";
        b: string <- "banana";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
