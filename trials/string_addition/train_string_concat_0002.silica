// concatenate of 2 pieces prints 世界table

fn main() -> atom {
    sequence proc[device_io] a: string <- "世界";
        b: string <- "table";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
