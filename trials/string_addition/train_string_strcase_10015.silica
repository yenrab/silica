// case on string literals; "omega" gives 75
fn main() -> int64 {
    mode: string <- "omega";
    case mode of {
        "apple" -> 4;
        "world" -> 68;
        "omega" -> 75;
        _: string -> 63
    }
}
