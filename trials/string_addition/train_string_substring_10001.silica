// substring uses character indices [0, 1) of "🙂"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "🙂";
        s: string <- substring(text, 0, 1);
        print_string(s);
        println("")
    produces pure :ok end
}
