// concat of 2 pieces prints cherrychair

fn main() -> atom {
    sequence proc[device_io] s: string <- concat("cherry", "chair");
        print_string(s);
        println("")
        produces pure :ok end
}
