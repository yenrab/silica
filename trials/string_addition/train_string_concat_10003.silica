// concat of 2 pieces prints cafénorth

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concat("café", "north");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
