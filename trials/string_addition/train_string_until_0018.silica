// substring_until_char from 6 up to ';'
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring_until_char("south;beta;beta;table", 6, ';');
        print_string(s);
        println("")
    produces pure :ok end
}
