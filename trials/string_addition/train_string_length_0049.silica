// length_bytes of a bound string prints 25
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "👨‍👩‍👧‍👦";
        result: atom <- print_int64(length_bytes(text))
    produces pure result end
}
