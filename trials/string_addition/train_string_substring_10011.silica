// substring uses character indices [5, 5) of "Zürich"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("Zürich", 5, 5);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
