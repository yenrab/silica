// concatenate of 2 pieces prints worldsouth

fn main() -> atom {
    sequence proc[device_io]
        a: string <- "world";
        b: string <- "south";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
