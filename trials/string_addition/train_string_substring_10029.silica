// substring uses character indices [4, 4) of "apple"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "apple";
        s: string <- substring(text, 4, 4);
        print_string(s);
        println("")
    produces pure :ok end
}
