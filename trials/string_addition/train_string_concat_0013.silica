// concatenate of 2 pieces prints actorriver

fn main() -> atom {
    sequence proc[device_io]
        s: string <- concatenate("actor", "river");
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
