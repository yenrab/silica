// case on starts_with; expect 41
fn main() -> int64 {
    s: string <- "path/to/file.silica";
    suf: string <- "path";
    case starts_with(s, suf) of {
        true -> 41;
        false -> 100
    }
}
