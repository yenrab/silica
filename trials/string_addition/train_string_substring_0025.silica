// substring uses character indices [2, 4) of "cloud"
fn main() -> atom {
    sequence proc[device_io]
        s: string <- substring("cloud", 2, 4);
        print_string(s);
        println("")
    produces pure :ok end
}
