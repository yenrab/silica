// substring uses character indices [1, 2) of "αβγ"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("αβγ", 1, 2);
        print_string(s);
        println("")
        produces pure :ok end
}
