// substring uses character indices [1, 3) of "apple"
fn main() -> atom {
    sequence proc[device_io]
        text: string <- "apple";
        s: string <- substring(text, 1, 3);
        print_string(s);
        println("")
    produces
        pure :ok
    end
}
