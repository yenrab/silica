// string helper wrap; prints [chair]

fn wrap(s: string) -> string {
    concatenate("[", concatenate(s, "]"))
}

fn main() -> atom {
    sequence proc[device_io] print_string(wrap("chair"));
        println("")
        produces pure :ok end
}
