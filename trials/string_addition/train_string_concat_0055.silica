// concatenate of 2 pieces prints gammaapple

fn main() -> atom {
    sequence proc[device_io] a: string <- "gamma";
        b: string <- "apple";
        s: string <- concatenate(a, b);
        print_string(s);
        println("")
        produces pure :ok end
}
