export trait UnresolvedAssocTrait;
export peek/2;

required {
    fn peek(set: UnresolvedAssocTrait, item: ItemType) -> boolean;
}

impl fn peek(
    set: { slot: int64 },
    item: ItemType
) -> boolean {
    false
}
