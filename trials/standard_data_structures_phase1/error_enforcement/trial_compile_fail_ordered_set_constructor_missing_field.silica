use StubWbtSet;

fn cmp_i64(a: int64, b: int64) -> :less | :equal | :greater {
    :equal
}

fn main() -> int64 {
    // Wrong field name — compare_item is required for OrderedSet / StubWbtSet@empty.
    _: OrderedSet[int64, mem(normal)] <- StubWbtSet@empty({ compare_key: cmp_i64 });
    0
}
