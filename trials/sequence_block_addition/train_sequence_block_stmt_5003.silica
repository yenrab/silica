// effectful sequence used as a statement before the return value 30
fn main() -> int64 {
    sequence proc[device_io]
        print_string("apple");
        println("")
    produces pure :ok end;
    30
}
