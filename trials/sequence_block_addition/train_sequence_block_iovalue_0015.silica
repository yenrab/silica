// sequence prints and returns the same sum 72
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 37;
        b: int64 <- 35;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
