// sequence prints and returns the same sum 43
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 25;
        b: int64 <- 18;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces
        pure total
    end
}
