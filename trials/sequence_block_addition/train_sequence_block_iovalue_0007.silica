// sequence prints and returns the same sum 24
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 22;
        b: int64 <- 2;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces
        pure total
    end
}
