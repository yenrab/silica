// sequence prints and returns the same sum 50
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 17;
        b: int64 <- 33;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces
        pure total
    end
}
