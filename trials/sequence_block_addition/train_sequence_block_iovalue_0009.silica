// sequence prints and returns the same sum 45
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 5;
        b: int64 <- 40;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
