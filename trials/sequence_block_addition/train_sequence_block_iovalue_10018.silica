// sequence prints and returns the same sum 7
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 4;
        b: int64 <- 3;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
