// sequence prints and returns the same sum 70
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 35;
        b: int64 <- 35;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
