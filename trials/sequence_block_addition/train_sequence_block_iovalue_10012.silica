// sequence prints and returns the same sum 29
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 28;
        b: int64 <- 1;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
