// sequence prints and returns the same sum 40
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 32;
        b: int64 <- 8;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces
        pure total
    end
}
