// sequence prints and returns the same sum 44
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 33;
        b: int64 <- 11;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces pure total end
}
