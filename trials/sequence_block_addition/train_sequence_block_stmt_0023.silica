// effectful sequence used as a statement before the return value 50
fn main() -> int64 {
    sequence proc[device_io]
        print_string("river");
        println("")
    produces pure :ok end;
    50
}
