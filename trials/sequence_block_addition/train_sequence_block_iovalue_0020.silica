// sequence prints and returns the same sum 35
fn main() -> int64 {
    sequence proc[device_io]
        a: int64 <- 5;
        b: int64 <- 30;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
    produces
        pure total
    end
}
