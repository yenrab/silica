// sequence prints and returns the same sum 22
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 12;
        b: int64 <- 10;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
