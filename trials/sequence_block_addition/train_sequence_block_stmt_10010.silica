// effectful sequence used as a statement before the return value 40
fn main() -> int64 {
    sequence proc[device_io]
        print_string("banana");
        println("")
    produces pure :ok end;
    40
}
