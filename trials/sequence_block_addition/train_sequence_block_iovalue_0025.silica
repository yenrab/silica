// sequence prints and returns the same sum 34
fn main() -> int64 {
    sequence proc[device_io] a: int64 <- 14;
        b: int64 <- 20;
        print_int64(a + b);
        println("");
        total: int64 <- a + b
        produces pure total end
}
