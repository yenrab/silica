// defect A1: float64 after an int64 parameter; pick(3, 2.25) = 2.25
fn pick(n: int64, y: float64) -> float64 {
    y
}

fn main() -> atom {
    sequence proc[device_io]
        r: atom <- print_float64(pick(3, 2.25))
    produces pure r end
}
