// float64 helper combine; prints 70.0
fn combine(x: float64) -> float64 {
    x + 26.0 + 4.0
}

fn main() -> atom {
    sequence proc[device_io]
        result: atom <- print_float64(combine(40.0))
    produces
        pure result
    end
}
