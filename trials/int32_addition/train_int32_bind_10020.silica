// int32 bindings then expression; expect 0
fn main() -> int32 {
    height: int32 <- 2;
    m: int32 <- height;
    base: int32 <- height;
    count: int32 <- height + height + (1 - base);
    base * base / 53 + m - m + count - count
}
