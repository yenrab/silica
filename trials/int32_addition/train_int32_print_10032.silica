// print_int32 two; prints 3 then 46
fn main() -> atom {
    sequence proc[device_io]
        a: int32 <- 29 / 9;
        b: int32 <- 42 + 4;
        _: atom <- print_int32(a);
        println("");
        result: atom <- print_int32(b)
    produces pure result end
}
