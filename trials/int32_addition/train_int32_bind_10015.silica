// int32 bindings then expression; expect 0
fn main() -> int32 {
    d: int32 <- 47;
    step: int32 <- 44;
    count: int32 <- step;
    base: int32 <- (count + step);
    d / 20 / (12 + 17) + base - base
}
