// defect A7: int32 (z % 11 % (x % y)) with (5, 3, 3) is 1 (a clobbered operand became a zero divisor)
fn mix(x: int32, y: int32, z: int32) -> int32 {
    (z % 11 % (x % y))
}

fn main() -> atom {
    sequence proc[device_io]
        r: atom <- print_int32(mix(5, 3, 3))
    produces pure r end
}
