// print_int32 two; prints 0 then 15
fn main() -> atom {
    sequence proc[device_io]
        a: int32 <- 32 / 54;
        b: int32 <- 53 - 38;
        _: atom <- print_int32(a);
        println("");
        result: atom <- print_int32(b)
    produces
        pure result
    end
}
