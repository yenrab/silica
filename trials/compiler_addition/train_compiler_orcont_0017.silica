// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 1;
    b: int64 <- 7;
    hit: boolean <- a > 1 or

        b > 1;
    case hit of {
        true -> 1;
        false -> 0
    }
}
