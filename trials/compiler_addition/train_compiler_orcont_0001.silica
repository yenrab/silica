// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 9;
    b: int64 <- 9;
    hit: boolean <- a > 4 or

        b > 4;
    case hit of {
        true -> 1;
        false -> 0
    }
}
