// comments must stop at the newline; want 48
// whole-line comment before the function
fn main() -> int64 {
    a: int64 <- 38; // end-of-line after a binding
    // whole-line comment between statements
    b: int64 <- 1; // another
    a / b + 10 // end-of-line after the return expression
}
