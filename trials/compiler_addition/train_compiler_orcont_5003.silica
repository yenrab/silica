// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 6;
    b: int64 <- 3;
    hit: boolean <- a > 9 or

        b > 9;
    case hit of {
        true -> 1;
        false -> 0
    }
}
