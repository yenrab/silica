// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 9;
    b: int64 <- 8;
    hit: boolean <- a > 0 or

        b > 0;
    case hit of {
        true -> 1;
        false -> 0
    }
}
