// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 5;
    b: int64 <- 0;
    hit: boolean <- a > 6 or

        b > 6;
    case hit of {
        true -> 1;
        false -> 0
    }
}
