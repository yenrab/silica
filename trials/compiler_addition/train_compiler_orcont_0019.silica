// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 0;
    b: int64 <- 0;
    hit: boolean <- a > 7 or

        b > 7;
    case hit of {
        true -> 1;
        false -> 0
    }
}
