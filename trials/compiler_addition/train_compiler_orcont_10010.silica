// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 2;
    b: int64 <- 0;
    hit: boolean <- a > 3 or

        b > 3;
    case hit of {
        true -> 1;
        false -> 0
    }
}
