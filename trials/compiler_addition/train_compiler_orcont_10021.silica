// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 4;
    b: int64 <- 2;
    hit: boolean <- a > 0 or

        b > 0;
    case hit of {
        true -> 1;
        false -> 0
    }
}
