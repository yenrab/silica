// line-ending `or` followed by a blank line stays one expression
fn main() -> int64 {
    a: int64 <- 7;
    b: int64 <- 2;
    hit: boolean <- a > 2 or

        b > 2;
    case hit of {
        true -> 1;
        false -> 0
    }
}
