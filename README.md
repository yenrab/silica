# Silica Project

This project contains the Silica compiler and experiments.

## Building the Compiler

To compile the compiler:

```bash
cd AArch64
make clean && make compiler
```

## Building the Experiments

To compile the experiments:

```bash
cd AArch64/experiments
make clean && make
```