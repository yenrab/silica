# Drop this file into any project subdirectory as `Makefile`.
# Sources here are compiled by the parent config-driven batch (silica.config).
COMPONENT_NAME := $(notdir $(patsubst %/,%,$(dir $(abspath $(lastword $(MAKEFILE_LIST))))))
include $(dir $(abspath $(lastword $(MAKEFILE_LIST))))../subdir_stub.mk
