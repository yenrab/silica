// Silica I/O Library

export
    // Console I/O functions
    println/1,

    // File I/O functions
    append_file/2, read_lines/1, file_exists/1, delete_file/1,
    get_file_size/1, create_directory/1, remove_directory/1,
    list_directory/1, join_path/2, get_extension/1,
    get_filename/1, get_parent/1;

// I/O functions are implemented as compiler intrinsics
// No declarations needed - intrinsic calls are recognized by name

// Path utilities
fn join_path(base: string, component: string) -> string {
    // Implementation would use intrinsics - return joined path
    ""
}

fn get_extension(path: string) -> string {
    // Implementation would use intrinsics - return file extension
    ""
}

fn get_filename(path: string) -> string {
    // Implementation would use intrinsics - return filename
    ""
}

fn get_parent(path: string) -> string {
    // Implementation would use intrinsics - return parent directory
    ""
}