

fn main() -> int64 proc[DeviceIO] {
    original: string <- "+ 2";
    first_char: string <- substring(original, 0, 1);
    print(first_char);
    println("");
    case first_char of {
        "+" -> 1;
        "-" -> 2;
        _: string -> 0
    }
}