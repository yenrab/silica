// Case expression with guards test
fn is_positive(x: int) -> boolean {
    x > 0
}

fn test_guards(x: int) -> int {
    case x of {
        y: int if is_positive(y) -> y * 2;
        _: int -> 0             // Fallback
    }
}

fn main() -> int {
    test_guards(15)  // Should return 30 (15 * 2) - first guard matches
}
