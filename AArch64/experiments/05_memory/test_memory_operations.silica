// Test basic memory operations with binding and sequencing

fn main() -> int64 {
    do
        // Create a region with explicit type annotation
        r:region(normal) <- region();
        42
    end
}
