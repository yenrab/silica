// Test Phase 1.2: Memory & Effects System

fn main() -> int64 {
    do
        // Test Phase 1.2: Memory & Effects System with explicit complex types
        r:region(normal) <- region();
        reference:ref(r, normal, int64) <- alloc_ref(r, 42);
        read_ref(reference)
    end
}
