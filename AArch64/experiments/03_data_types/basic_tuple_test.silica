// Basic tuple test without generics

fn test_basic_tuples() -> int {
    do
        // Create basic tuples
        pair:(int, int) <- (42, 24);
        triple:(int, int, int) <- (1, 2, 3);
        
        // Return success
        42
    end
}

fn main() -> int {
    test_basic_tuples()
}
