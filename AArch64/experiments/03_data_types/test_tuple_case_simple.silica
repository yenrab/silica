// Test tuple patterns in case expressions

fn test_tuple_case() -> int {
    case (1, 2) of {
        (1, 2) -> 100;
        (2, 3) -> 200;
        _:(int,int) -> 300;
    }
}

fn main() -> int {
    test_tuple_case()  // Should return 100 since (1, 2) matches first pattern
}
