// Simple struct literal test
struct SimplePoint {
    x: int
}

fn main() -> int {
    do
        // Create a simple struct literal with one field
        point <- SimplePoint { x: 42 };
        // Return the field value
        point.x
    end
}
