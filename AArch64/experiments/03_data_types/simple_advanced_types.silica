// Test basic advanced type features

// Type alias with generics
type Pair<T, U> = (T, U);

// Sum type
type MySum = int | bool;

fn main() -> int {
    // Test type alias
    let p: Pair<int, bool> = (42, true);
    
    // Test sum type parsing (basic test)
    let x: MySum = 42;
    
    42
}
