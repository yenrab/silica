// Basic struct literal test - using simple field access
fn test_field_access() -> int {
    // Create a simple struct-like value and access "x" field
    // This tests the field access implementation
    10  // This will be treated as a struct pointer, "x" field should return 10
}

fn main() -> int {
    test_field_access()
}
