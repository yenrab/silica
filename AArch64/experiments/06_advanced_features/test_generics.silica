// Test basic generic functions (monomorphization infrastructure)
// Note: Full generic instantiation is implemented but type inference
// for generic calls needs refinement
fn identity<T>(x: T) -> T {
    x
}

fn main() -> int {
    // For now, test that generic function declarations parse correctly
    42
}
