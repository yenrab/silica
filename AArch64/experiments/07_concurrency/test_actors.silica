// Test actor syntax parsing and functionality
// Demonstrates that send() and recv() syntax is properly parsed and type-checked

fn test_actor_operations() -> unit {
    // Test send syntax - returns unit
    send(0, 42)
}

fn test_recv_operation() -> int {
    // Test recv syntax - returns int (message type)
    recv()
}

fn main() -> int {
    // Note: Actual execution would require concurrency capabilities
    // This test verifies parsing, type checking, and code generation
    42
}
