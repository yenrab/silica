// Test single case function
fn test_basic_patterns(value: int) -> int {
    case value of {
        0 -> 100,      // Literal pattern
        n -> n * 2     // Identifier pattern (binds NEW variable n)
    }
}

fn main() -> int {
    test_basic_patterns(5)  // Should return 10
}
