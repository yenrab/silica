// Test file for Phase 5: Pattern Guards and Exhaustiveness Checking

fn test_exhaustiveness(flag: bool) -> int64 {
    case flag of {
        true -> 1;
        false -> 0
    }  // This should be exhaustive
}

fn test_exhaustive_with_wildcard(flag: bool) -> int64 {
    case flag of {
        true -> 1;
        false -> 0  // Explicit false case
    }
}

fn test_simple_pattern(x: int64) -> int64 {
    case x of {
        n: int64 -> n * 2
    }
}

fn test_guards(x: int64) -> int64 {
    case x of {
        n: int64 -> n * 2
    }
}

fn main() -> int64 {
    test_exhaustiveness(true) + test_guards(5)
}
