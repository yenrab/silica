-- Test basic function types for parameters and return values

fn take_int(x: int) -> int {
    print_int(x);
    x
}

fn return_int() -> int {
    42
}

fn main() -> int {
    take_int(123);
    i <- return_int();
    take_int(i);
    0
}