// Test functions with complex parameter and return types

// Test functions with complex parameter types
fn take_string(s: string) -> int64 proc[DeviceIO] {
    print(s);
    0  // Return success status
}

fn take_tuple(t: (int64, boolean)) -> int64 proc[DeviceIO] {
    (x, b) <- t;
    print_int(x);
    print_bool(b);
    0  // Return success status
}

// Test functions with complex return types
fn return_string() -> string {
    "Hello, World!"
}

fn return_tuple() -> (int64, boolean) {
    (42, true)
}

// Helper function that processes a string
fn process_string(s: string) -> string {
    s  // For now just return the string
}

// Function that uses helper functions
fn use_helpers() -> int64 {
    s: string <- return_string();
    processed_s: string <- process_string(s);
    status1: int64 <- take_string(processed_s);

    0  // Return status
}

// Main function
fn main() -> int64 {
    // Test functions with complex parameter types
    status1: int64 <- take_string("Direct string");
    status2: int64 <- take_tuple((123, true));

    // Test functions with complex return types
    s:string <- return_string();
    t:(int64, boolean) <- return_tuple();

    // Test processing functions
    status3: int64 <- take_string(s);
    status4: int64 <- take_tuple(t);

    // Test helper functions
    status5: int64 <- use_helpers();

    // Return combined status
    status1 + status2 + status3 + status4 + status5
}