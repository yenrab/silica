// Demonstration that actor states support various Silica types
// This shows the general solution implemented

fn main() -> int proc[concurrency] {
    // Example: Boolean state converted to integer
    // true becomes 1, false becomes 0
    spawn(true, fn(msg: int, state: int) -> int { state + msg })
}
