// Test spawning actors with integer initial states

fn main() -> int proc[concurrency] {
    spawn(0, fn(msg: int, state: int) -> int { state });
    spawn(42, fn(msg: int, state: int) -> int { state });
    42 // Return value
}
