// Test Silica Process/Actor spawning
// Tests basic actor spawning

fn add_behavior(msg: int, state: int) -> int {
    state + msg
}

fn main() -> int {
    // Test basic actor spawning
    //spawn(0, 42);
    0
}