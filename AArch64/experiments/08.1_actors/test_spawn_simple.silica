fn main() -> int proc[concurrency] {
    // Test boolean literal as initial state (should be converted to 0)
    spawn(false, fn(msg: int, state: int) -> int { state + msg });

    42 // Return value
}
