// Test tuple states
fn main() -> int proc[concurrency] {
    spawn((1, 2), fn(msg: int, state: (int, int)) -> (int, int) { state });

    spawn(((true,"hello"), 2), fn(msg: int, state: ((boolean,string), int)) -> ((boolean,string), int) { state })
}
