// Test spawning actors with different types of initial states

fn add_behavior(msg: int64, state: int64) -> int64 {
    state + msg
}

fn main() -> int64 proc[concurrency] {
    // Test 1: Integer literal state
    spawn(42, add_behavior);
    0
}
