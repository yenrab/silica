// Test basic function literal syntax and usage
// Demonstrates that function literals can be defined and used

fn main() -> int {
    // Test 1: Simple function literal that returns a constant
    constant_fn <- fn() -> int { 42 };

    // Test 2: Function literal assigned to variable
    add_one <- fn(x: int) -> int { x + 1 };

    // Test 3: Function literal in expression
    result <- if true {
        fn() -> int { 100 }
    } else {
        fn() -> int { 200 }
    };

    // For now, just return success - actual function calling not yet implemented
    0
}
