// Test Phase 1.1: Core Language Features

// Test pattern alternatives
fn test_pattern_alternatives(x: int) -> int {
    case x of {
        0 | 1 | 2 -> 10;
        3 | 4 -> 20;
        _ -> 30;
    }
}

// Test guard expressions  
fn test_guards(x: int) -> int {
    case x of {
        y if y > 10 -> y * 2;
        z if z % 2 == 0 -> z + 1;
        _ -> 0;
    }
}

fn main() -> int {
    test_pattern_alternatives(2)
}
