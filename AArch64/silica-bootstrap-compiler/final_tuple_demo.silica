// Final tuple decomposition demonstration

fn main() -> int {
    do
        // Decompose a pair
        (first, second) <- (42, 24);
        
        // Decompose a triple
        (x, y, z) <- (100, 200, 300);
        
        // Return a constant to show it compiled
        42
    end
}
