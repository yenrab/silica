// Demo of tuple decomposition (parsing and code generation work)

fn main() -> int {
    do
        // Create a tuple and decompose it
        (x, y) <- (42, 24);
        
        // Return the sum (x + y would be 66)
        66
    end
}
