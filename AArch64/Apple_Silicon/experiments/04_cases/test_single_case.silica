// Test single case function
fn test_basic_patterns(value: int64) -> int64 {
    case value of {
        0 -> 100;      // Literal pattern
        n: int64 -> n * 2     // Identifier pattern (binds NEW variable n)
    }
}

fn main() -> int64 {
    test_basic_patterns(5)  // Should return 10
}
