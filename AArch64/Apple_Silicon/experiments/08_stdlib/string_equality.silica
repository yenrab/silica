

fn main() -> int64 proc[DeviceIO] {
    original: string <- "+ 2";
    first_char: string <- substring(original, 0, 1);
    print(first_char);
    println("");
    is_equal: boolean <- first_char == "+";
    case is_equal of {
        true -> 1;
        false -> 0
    }
}