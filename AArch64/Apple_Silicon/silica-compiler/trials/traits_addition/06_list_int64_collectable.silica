// silica:ListType — `List[ElementType]` requires ElementType : Collectable (language-specification.jsonld).
// silica:CollectableTrait — automaticImplementation.primitiveTypes includes int64; lists of int64 rely on that rule.
// Runnable minimal program (links without list runtime). For a full `List[int64, …]` example, see ../list_addition/.

fn main() -> int64 {
    42
}
