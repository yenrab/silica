// silica:CollectableTrait — traitDefinition: `trait Collectable { }`
// silica:CollectableTrait — languageRule: automatic implementations for primitives, tuples, inline records, etc.;
//   "No explicit impl Collectable for ... declarations are needed or allowed for these built-in cases."
// This file intentionally contains NO impl Collectable — only documents the rule. Runnable minimal program.

fn main() -> atom {
    atom
}
