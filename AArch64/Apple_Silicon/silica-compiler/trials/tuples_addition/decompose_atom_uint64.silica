fn main() -> atom proc[device_io] {
    some_tuple:(atom, uint64) <- (:ok, 12345678901234567890);
    println("about to decompose tuple");
    (a_value, b_value:uint64) <- some_tuple;
    println("decomposed tuple");
    print_atom(a_value);
    print_uint64(b_value);
    a_value
}
