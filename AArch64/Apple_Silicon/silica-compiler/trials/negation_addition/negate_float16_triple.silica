// Not in silica.config: float16 AArch64 assembly not yet valid for this pipeline.
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_bool(negate_float16(negate_float16(negate_float16(1.0))) == -1.0);
        produces pure result end
}
