// Double negation restores the original value; print_bool verifies.
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_bool(negate_int64(negate_int64(-404)) == -404);
        produces pure result end
}
