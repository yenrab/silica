// Spec §16 / silica:ActorModel: cast(actor_ref, message) -> atom (fire-and-forget)
fn main() -> atom {
    sequence proc[concurrency]
        peer: actor_ref(int64) <- spawn(0);
        tag: atom <- cast(peer, 42);
    produces pure tag end
}
