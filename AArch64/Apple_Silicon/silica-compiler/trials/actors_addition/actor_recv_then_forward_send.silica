// Spec §15–§16: recv() then send(actor_ref, message) — forward mailbox payload to another actor_ref
fn main() -> atom {
    sequence proc[concurrency]
        inbound: int64 <- recv();
        peer: actor_ref(int64) <- spawn(0);
        out: atom <- send(peer, inbound);
    produces pure out end
}
