// Spec §16: repeated send to the same actor_ref (sequential message delivery in sequence block)
fn main() -> atom {
    sequence proc[concurrency]
        worker: actor_ref(int64) <- spawn(0);
        send(worker, 1);
        send(worker, 2);
        send(worker, 3);
    produces pure :ok end
}
