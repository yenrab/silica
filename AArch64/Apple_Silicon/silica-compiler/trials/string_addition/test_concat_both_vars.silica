fn main() -> atom {
    sequence proc[device_io] a: string <- "Hello";
        b: string <- " World";
        out: string <- concatenate(a, b);
        print_string(out);
        println("");
        produces pure :ok end
}
