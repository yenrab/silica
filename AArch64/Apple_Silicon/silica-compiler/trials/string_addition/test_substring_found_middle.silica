// substring found: extract "World" from middle of "Hello World"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("Hello World", 6, 11);
        print_string(s);
        println("");
        produces pure :ok end
}
