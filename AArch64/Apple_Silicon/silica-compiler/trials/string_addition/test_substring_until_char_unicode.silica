// substring_until_char with unicode string: extract until comma
fn main() -> atom {
    sequence proc[device_io] s: string <- substring_until_char("Hello世界,abc", 0, ',');
        print_string(s);
        println("");
        produces pure :ok end
}
