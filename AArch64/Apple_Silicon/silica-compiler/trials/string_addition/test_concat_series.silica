fn main() -> atom {
    sequence proc[device_io] a: string <- "A";
        b: string <- "B";
        c: string <- concatenate(a, b);
        d: string <- concatenate(c, "C");
        e: string <- concatenate("0", d);
        f: string <- concatenate(e, "9");
        print_string(f);
        println("");
        produces pure :ok end
}
