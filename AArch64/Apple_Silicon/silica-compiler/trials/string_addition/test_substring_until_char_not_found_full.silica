// substring_until_char not found: char 'z' absent in "abc", returns full string
fn main() -> atom {
    sequence proc[device_io] s: string <- substring_until_char("abc", 0, 'z');
        print_string(s);
        println("");
        produces pure :ok end
}
