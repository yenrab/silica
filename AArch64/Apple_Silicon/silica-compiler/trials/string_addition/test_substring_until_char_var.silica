// substring_until_char with variable: extract until char from bound string
fn main() -> atom proc[device_io] {
    src: string <- "apple,banana";
    s: string <- substring_until_char(src, 0, ',');
    print_string(s);
    println("");
    :ok
}
