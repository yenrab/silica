// substring_until_char: entire string and terminator are non-ASCII Unicode
fn main() -> atom proc[device_io] {
    s: string <- substring_until_char("世界αβγ🙂余", 0, '🙂');
    print_string(s);
    println("");
    :ok
}
