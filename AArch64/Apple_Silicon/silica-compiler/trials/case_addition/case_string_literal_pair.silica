// Spec §3.3.3 / §3.5: literal_pattern on string with typed wildcard
fn main() -> int64 {
    s: string <- "hello";
    case s of {
        "hello" -> 1;
        "world" -> 2;
        _: string -> 0
    }
}
