// Spec §3.3.3: int64 scrutinee with guards and typed wildcard fallback
fn main() -> int64 {
    x: int64 <- -7;
    case x of {
        n: int64 if n > 0 -> n * 2;
        n: int64 if n < 0 -> n * 3;
        _: int64 -> 0
    }
}
