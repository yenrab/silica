// Trial: alloc_ref with flat tuple (int64, int64, int64)
// silica-specification §9.2.2, §22.2

fn main() -> int64 {
    sequence proc[mem(normal)]
        L1: lifetime <- fresh_lifetime();
        r: region(L1, normal) <- alloc_region(normal);
        triple: (int64, int64, int64) <- (1, 2, 3);
        ref: ref(L1, normal, (int64, int64, int64)) <- alloc_ref(r, triple);
    produces
        pure 0
    end
}
