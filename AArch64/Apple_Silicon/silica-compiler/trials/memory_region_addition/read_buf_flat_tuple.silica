// Trial: read_buf with flat tuple element type
// silica-specification §22.3

fn main() -> int64 {
    sequence proc[mem(normal)]
        r: region(R, normal) <- alloc_region(normal);
        buf: buf(R, normal, (int64, int64), 4) <- alloc_buf(r, 4);
        value: (int64, int64) <- read_buf(buf, 0);
    produces
        pure 0
    end
}
