// Trial: alloc_region(normal) - normal write-back cacheable memory
// silica-specification §4.4.1, §22.1

fn main() -> int64 {
    sequence proc[mem(normal)]
        r: region(R, normal) <- alloc_region(normal);
    produces
        pure 0
    end
}
