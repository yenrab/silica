// Trial: alloc_ref with unit type
// silica-specification §9.2.2, §22.2

fn main() -> int64 {
    sequence proc[mem(normal)]
        r: region(R, normal) <- alloc_region(normal);
        ref: ref(R, normal, unit) <- alloc_ref(r, ());
    produces
        pure 0
    end
}
