// Trial: alloc_region(atomic) - atomic memory for concurrent access
// silica-specification §4.4.1, §22.1

fn main() -> int64 {
    sequence proc[mem(atomic)]
        r: region(R, atomic) <- alloc_region(atomic);
    produces
        pure 0
    end
}
