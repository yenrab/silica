// Trial: alloc_buf in normal_noncacheable space (e.g. for DMA)
// silica-specification §22.1, §22.3

fn main() -> int64 {
    sequence proc[mem(normal_noncacheable)]
        r: region(R, normal_noncacheable) <- alloc_region(normal_noncacheable);
        buf: buf(R, normal_noncacheable, int64, 256) <- alloc_buf(r, 256);
    produces
        pure 0
    end
}
