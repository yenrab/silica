// Trial: alloc_buf with float64 element type
// silica-specification §12.3.2, §22.1, §22.3

fn main() -> int64 {
    sequence proc[mem(normal)]
        r: region(R, normal) <- alloc_region(normal);
        buf: buf(R, normal, float64, 8) <- alloc_buf(r, 8);
    produces
        pure 0
    end
}
