// Trial: alloc_atomic with int64
// silica-specification §22.1 - alloc_atomic returns proc[mem(space), atomic]

fn main() -> int64 {
    sequence proc[mem(atomic)]
        r: region(R, atomic) <- alloc_region(atomic);
        counter: atomic_ref(R, atomic, int64) <- alloc_atomic(r, 0);
    produces
        pure 0
    end
}
