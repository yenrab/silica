// Trial: alloc_ref in atomic memory space
// silica-specification §4.4.1, §22.2

fn main() -> int64 {
    sequence proc[mem(atomic)]
        r: region(R, atomic) <- alloc_region(atomic);
        ref: ref(R, atomic, int64) <- alloc_ref(r, 42);
    produces
        pure 0
    end
}
