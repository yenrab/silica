// Trial: alloc_ref with atom primitive
// silica-specification §9.2.2, §22.2

fn main() -> int64 {
    sequence proc[mem(normal)]
        r: region(R, normal) <- alloc_region(normal);
        ref: ref(R, normal, atom) <- alloc_ref(r, :ok);
    produces
        pure 0
    end
}
