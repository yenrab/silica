// Trial: alloc_ref with float64 primitive
// silica-specification §9.2.2, §22.2

fn main() -> int64 {
    sequence proc[mem(normal)]
        L1: lifetime <- fresh_lifetime();
        r: region(L1, normal) <- alloc_region(normal);
        ref: ref(L1, normal, float64) <- alloc_ref(r, 3.14);
    produces
        pure 0
    end
}
