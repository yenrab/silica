// Spec §5.6.2: remove_head (head-only removal; returns empty if list was empty)
fn main() -> int64 {
    xs: List[int64] <- [10, 20, 30]: List[int64];
    rest: List[int64] <- remove_head[int64](xs);
    head[int64](rest)//should be 20
}
