// print_uint64 of expression result
fn main() -> atom {
    sequence proc[device_io] a: uint64 <- 10;
        b: uint64 <- 20;
        result: atom <- print_uint64(a + b);
        produces pure result end
}
