// Linear non-tail recursion: base^exp for exp >= 0; 2^7 = 128 (exit code fits byte)
fn pow_int(base: int64, exp: int64) -> int64 {
    case exp <= 0 of {
        true -> 1;
        false -> base * pow_int(base, exp - 1)
    }
}

fn main() -> int64 {
    pow_int(2, 7)
}
