// Effect capability enforcement
// Validates that operations have required effect capabilities

// Push capability onto stack
fn push_capability(context: ref(R, normal, EffectContext), capability: Capability) -> unit proc[mem(normal)] {
    // TODO: Implement capability stack push
}

// Pop capability from stack
fn pop_capability(context: ref(R, normal, EffectContext)) -> OptionCapability proc[mem(normal)] {
    // TODO: Implement capability stack pop
}

// Check if effect is allowed by current capabilities
fn check_capability(context: ref(R, normal, EffectContext), required_effect: Effect) -> ResultUnitEffectError proc[mem(normal)] {
    // TODO: Implement capability checking
}

// Check if effect is subsumed by another effect
fn is_subeffect(effect1: Effect, effect2: Effect) -> bool proc[mem(normal)] {
    // TODO: Implement subeffect checking
}

// Get active effects from capability stack
fn get_active_effects(context: ref(R, normal, EffectContext)) -> ListEffect proc[mem(normal)] {
    // TODO: Implement active effects retrieval
}
