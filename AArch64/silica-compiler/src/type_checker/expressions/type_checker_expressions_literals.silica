// Literal expression type checking (Expr kind 0)

use parser_ast;
use type_checker_core;

export check_literal_expr/1;

// Check type of integer literal expression (Expr kind 0)
fn check_literal_expr(expr: Expr) -> ResultStringTypeError proc[mem(normal)] {
    result_string_ok("int64")
}
