

fn main() -> int64 proc[DeviceIO] {
    len: int64 <- len("");
    print_int64(len);
    println("");
    str: string <- "";
    zerolen: int64 <- len(str);
    print_int64(zerolen);
    println("");
    42
}
