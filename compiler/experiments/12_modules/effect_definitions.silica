// Effect definitions module
// Defines effect aliases for cross-module usage
// Effects are automatically available when this module is imported

// Effect alias for I/O operations
effect io_effect = [DeviceIO];

// Effect alias for memory operations
effect memory_effect = [mem(normal)];

// Effect alias combining I/O and memory
effect io_memory_effect = [DeviceIO, mem(normal)];
