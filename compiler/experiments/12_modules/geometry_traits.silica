// Geometry traits module
// Defines traits for geometric operations
// Traits are automatically available when this module is imported

// Trait for shapes that can compute area
trait Shape {
    fn area(self: Self) -> int64;
}

// Trait for movable objects
trait Movable {
    fn move_by(self: Self, dx: int64, dy: int64) -> Self;
}
