// Test demonstrating all supported actor state types

fn main() -> int64 proc[concurrency] {
    // Test tuple state - should use the tuple's allocated memory directly
    spawn((1, 2), fn(msg: int64, state: (int64, int64)) -> (int64, int64) { state });
    0  // Return value
}
