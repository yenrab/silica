// Phase 3 steps 3.1–3.2 — DenseMatrixGraphDirected[int64, mem(normal)].
use graph_dense_directed;

fn fail_unless_zero(value: int64) -> int64 {
    case value of {
        0 -> 0;
        _: int64 -> 1
    }
}

fn main() -> int64 {
    sequence proc[]
        graph: {
            region: region(L1, normal),
            node_count: int64,
            edge_count: int64,
            present: buf(L1, normal, int64, 9),
            weights: buf(L1, normal, int64, 9)
        } <- graph_dense_directed@empty[int64, mem(normal)](3);
        initial_valid: int64 <- graph_dense_directed@validate[int64, mem(normal)](graph);
        added: { ok: int64, graph: {
            region: region(L1, normal),
            node_count: int64,
            edge_count: int64,
            present: buf(L1, normal, int64, 9),
            weights: buf(L1, normal, int64, 9)
        } } <- graph_dense_directed@set_edge_checked[int64, mem(normal)](graph, 0, 1, 42);
        added_graph: {
            region: region(L1, normal),
            node_count: int64,
            edge_count: int64,
            present: buf(L1, normal, int64, 9),
            weights: buf(L1, normal, int64, 9)
        } <- added.graph;
        after_valid: int64 <- graph_dense_directed@validate[int64, mem(normal)](added_graph);
        has01: int64 <- graph_dense_directed@has_edge[int64, mem(normal)](added_graph, 0, 1);
        has10: int64 <- graph_dense_directed@has_edge[int64, mem(normal)](added_graph, 1, 0);
        out0: int64 <- graph_dense_directed@out_degree[int64, mem(normal)](added_graph, 0);
        weight01: { ok: int64, value: int64 } <- graph_dense_directed@weight_at_checked[int64, mem(normal)](added_graph, 0, 1);
        failures: int64 <-
            ((1 - initial_valid) * 1) +
            ((1 - added.ok) * 2) +
            ((1 - after_valid) * 4) +
            ((1 - has01) * 8) +
            (has10 * 16) +
            ((1 - out0) * 32) +
            ((1 - weight01.ok) * 64) +
            (fail_unless_zero(weight01.value - 42) * 128)
    produces
        pure failures
    end
}
