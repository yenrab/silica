// Phase 3 steps 3.1–3.2 — directed weighted int64 DenseMatrixGraph.
// Runtime is no-op for now; the linked generated module provides compile/golden coverage.
use graph_dense_directed_weighted_int64_normal;

fn main() -> int64 {
    0
}
