use graph_csr_directed;

fn fail_unless_one(value: int64) -> int64 {
    case value of { 1 -> 0; _: int64 -> 1 }
}

fn main() -> int64 {
    sequence proc[mem(normal)]
        n0_empty: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        n1_empty: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        n2_empty: List[int64, mem(normal)] <- empty[int64, mem(normal)]();
        n0_neighbors: List[int64, mem(normal)] <- prepend[int64, mem(normal)](1, n0_empty);
        n1_neighbors: List[int64, mem(normal)] <- prepend[int64, mem(normal)](2, n1_empty);
        csr: {
            region: region(L1, normal),
            node_count: int64,
            edge_count: int64,
            offsets: buf(L1, normal, int64, 4),
            neighbors: buf(L1, normal, int64, 2)
        } <- graph_csr_directed@freeze[mem(normal)](n0_neighbors, n1_neighbors, n2_empty);
        has01: int64 <- graph_csr_directed@has_edge[mem(normal)](csr, 0, 1);
        has12: int64 <- graph_csr_directed@has_edge[mem(normal)](csr, 1, 2);
        failures: int64 <- fail_unless_one(has01) + fail_unless_one(has12) * 2
    produces pure failures end
}
