// Enforce that spawn must run inside sequence proc[concurrency].
// This call is intentionally outside any sequence block.

fn worker(msg: int64, state: int64) -> (:reply, int64, int64) {
    (:reply, msg + state, state)
}

fn main() -> actor_ref {
    spawn(0, worker)
}
