// Statement separator regression: tuple parameter after record parameter must not
// be mistaken for a missing function return type.

fn forwarder(state: { sink: actor_ref }, pair: (int64, int64), msg: int64) -> (:no_reply, { sink: actor_ref }) {
    sequence proc[concurrency]
        _: boolean <- cast(state.sink, msg impl ActorMessage {})
    produces
        pure :ok
    end
    (:no_reply, state)
}
