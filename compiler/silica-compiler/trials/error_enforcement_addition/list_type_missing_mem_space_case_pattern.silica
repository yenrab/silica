// Error enforcement: case list patterns must use List[..., mem(Space)] (E2015).
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [1, 2, 3];
        n: int64 <- case xs of {
            []: List[int64] -> 0;
            [h: int64, t: List[int64, mem(normal)]] -> h
        }
    produces
        pure n
    end
}
