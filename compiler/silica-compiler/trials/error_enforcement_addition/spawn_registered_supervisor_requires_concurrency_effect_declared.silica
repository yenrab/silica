// Enforce E3002 with concrete location:
// spawn_registered_supervisor is inside a sequence block, but concurrency is not declared.

fn main() -> int64 {
    sequence proc[device_io]
        sup: supervisor_ref <- spawn_registered_supervisor(MissingConcurrencySup, 0, :reg_sup)
    produces
        pure 0
    end
}
