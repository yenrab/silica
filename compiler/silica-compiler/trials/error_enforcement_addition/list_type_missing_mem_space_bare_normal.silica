// Error enforcement: bare memory-space name in List type is rejected; require mem(Space) (E2015).
fn main() -> int64 {
    sequence proc[mem(normal)]
        xs: List[int64, normal] <- [1, 2];
        length[int64, mem(normal)](xs)
    produces
        pure 0
    end
}
