// E1055: a sequence-local binding must be used before produces or returned by produces.
fn main() -> int64 {
    sequence proc[]
        unused_value: int64 <- 1
    produces
        pure 0
    end
}
