// Error enforcement (E4016): standalone module has functions but neither main/0 nor any export.
// No import declarations here — files that import other modules are treated as fragments and skip this rule.
fn helper(x: int64) -> int64 {
    x + 1
}

fn other(y: int64) -> int64 {
    helper(y) * 2
}
