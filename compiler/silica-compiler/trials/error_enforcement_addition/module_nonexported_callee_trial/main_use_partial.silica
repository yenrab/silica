// Calls exported fn, then a module-qualified fn that exists in the library but is not exported.
use lib_one_export;

fn main() -> int64 {
    a: int64 <- lib_one_export@published_inc(3);
    lib_one_export@not_exported_double(a)
}

