// E1040: two proc-region statements; the first must end with ';' before the next.

fn main() -> int64 {
    sequence proc[device_io]
        print_int64(1)
        print_int64(2)
    produces
        pure 0
    end
}
