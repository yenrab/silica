// substring_until_char not found: char 'x' absent, returns rest of string
fn main() -> atom proc[device_io] {
    s: string <- substring_until_char("Hello World", 0, 'x');
    print_string(s);
    println("");
    :ok
}
