// E1056: a pure sequence block must not declare empty proc[] — omit proc when no effects are required (spec §9.3.1).

fn main() -> int64 {
    sequence proc[]
        x: int64 <- 1
    produces
        pure x
    end
}
