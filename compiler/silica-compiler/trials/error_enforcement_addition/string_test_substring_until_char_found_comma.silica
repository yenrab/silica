// substring_until_char found: extract "apple" until comma
fn main() -> atom proc[device_io] {
    s: string <- substring_until_char("apple,banana,cherry", 0, ',');
    print_string(s);
    println("");
    :ok
}
