use btree_set_nodeid;

fn compare_bad(left: int64, right: int64) -> :less | :equal | :greater {
    :bogus
}

fn main() -> int64 {
    set0: {
        root_id: int64,
        node_count: int64,
        order: int64,
        nodes: List[{ id: int64, key_count: int64, is_leaf: int64, keys: List[int64, mem(normal)], children: List[int64, mem(normal)] }, mem(normal)],
        compare_item: fn(int64, int64) -> :less | :equal | :greater
    } <- btree_set_nodeid@empty({ compare_item: compare_bad });
    0
}
