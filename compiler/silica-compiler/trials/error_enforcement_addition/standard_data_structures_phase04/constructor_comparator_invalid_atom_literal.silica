use btree_set_phase04;

fn compare_bad(left: int64, right: int64) -> :less | :equal | :greater {
    :bogus
}

fn main() -> int64 {
    set0: { compare_item: fn(int64, int64) -> :less | :equal | :greater } <- btree_set_phase04@empty({ compare_item: compare_bad });
    0
}
