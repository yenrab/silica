fn main() -> atom {
    sequence proc[device_io] suffix: string <- "xyz";
        result: string <- concatenate("abc", suffix);
        print_string(result);
        println("")
        produces pure :ok end
}
