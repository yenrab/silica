// substring found: extract "Hello" from "Hello World"
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("Hello World", 0, 5);
        print_string(s);
        println("")
        produces pure :ok end
}
