// substring_until_char found: extract "Hello" until space
fn main() -> atom {
    sequence proc[device_io] s: string <- substring_until_char("Hello World", 0, ' ');
        print_string(s);
        println("")
        produces pure :ok end
}
