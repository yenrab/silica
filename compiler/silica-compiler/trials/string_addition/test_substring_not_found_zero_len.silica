// substring not found: zero-length range [0, 0)
fn main() -> atom {
    sequence proc[device_io] s: string <- substring("abc", 0, 0);
        print_string(s);
        println("")
        produces pure :ok end
}
