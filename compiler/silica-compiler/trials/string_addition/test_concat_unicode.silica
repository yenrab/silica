fn main() -> atom {
    sequence proc[device_io] s: string <- concatenate("Hello ", "世界");
        print_string(s);
        t: string <- concatenate("αβγ", "123");
        print_string(t);
        println("")
        produces pure :ok end
}
