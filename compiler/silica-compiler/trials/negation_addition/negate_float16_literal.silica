// Not in silica.config: float16 AArch64 assembly not yet valid for this pipeline.
fn main() -> atom {
    sequence proc[device_io] result: atom <- print_bool(negate_float16(2.5) == -2.5)
        produces pure result end
}
