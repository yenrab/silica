// Not in silica.config: float16 AArch64 assembly not yet valid for this pipeline.
fn main() -> atom {
    sequence proc[device_io] x: float16 <- 4.25;
        result: atom <- print_bool(negate_float16(x) == -4.25);
        produces pure result end
}
