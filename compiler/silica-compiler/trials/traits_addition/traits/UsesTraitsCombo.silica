use Sizeable;
use Doubling;

export trait UsesTraitsCombo;
export scale_twice/1;

required {
    fn scale_twice(x: UsesTraitsCombo) -> int64;
}

impl fn scale_twice(n: int64) -> int64 {
    Doubling@double(n + Sizeable@size(0))
}
