# Opt-in: integrate compares .sout to .scout with compare_scout_multiset.sh (sorted lines + Phase F banner glue split).
# Add this sibling file for any trial where stderr/stdout interleaving or glued ":atom=== Silica Actor Failure ===" lines differ.
