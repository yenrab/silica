# Multiset compare; when FailureReporter wins see phase_f8_acceptance.scout.
# When unwind falls back to stderr first (ordering), see phase_f8_acceptance.scout.alt — compare_scout_multiset_oneof.sh accepts either.
