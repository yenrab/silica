# Basenames (one per line) whose supervisor prints a full line `done` before main reaches
# `wait_for_exit`, while integrate feeds `exit` on stdin. Immediate here-docs can race: `exit` may be
# consumed before async work finishes, tearing down the process (empty .sout, SIGBUS under load).
# run_integration_binary.sh runs these via run_integration_exit_after_marker.py (PTY + delayed stdin).
#
# Optional second column (tab-separated): exit-marker line to wait for instead of `done`.

phase_f5_unwind_stderr_fallback
phase_f8_acceptance
phase_f7_bootstrap_ordering
phase_f_failure_reporter_cast_alignment
phase_b_ingress_before_cast
phase_c_child_exit_notification
phase_e4a_permanent_one_for_one
phase_e4c_temporary
phase_e4d_restart_storm
phase_e4e_one_for_all
phase_e4f_rest_for_one
