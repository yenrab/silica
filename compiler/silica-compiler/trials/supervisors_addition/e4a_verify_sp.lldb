# Stop at second region_alloc entry then step until init epilogue corrupts LR
breakpoint set --name silica_rt_region_alloc --ignore-count 1
run
breakpoint set --address $pc ; delete  # no-op workaround
breakpoint set --address 0
