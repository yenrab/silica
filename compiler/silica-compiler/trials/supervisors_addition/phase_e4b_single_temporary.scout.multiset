# Opt-in: integrate compares .sout to .scout with compare_scout_multiset.sh (sorted lines + Phase F banner glue split).
# Cast-driven supervisor stderr (unwind report) vs stdout ordering is nondeterministic; two standalone "0" lines (reason tag + exit code).
