fn main() -> int64 {
    (flags, children) <- sequence proc[mem(normal)]
        flags_and_children: ( int64, List[int64, mem(normal)] ) <- (0, [0])
    produces
        pure flags_and_children
    end;
    1
}
