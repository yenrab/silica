fn main() -> int64 {
    ret_list: List[int64, mem(normal)] <- sequence proc[mem(normal)]
        list: List[int64, mem(normal)] <- [0]
    produces
        pure list
    end;
    children: (int64, List[int64, mem(normal)]) <- (0, ret_list);
    (flag, a_list): (int64, List[int64, mem(normal)]) <- children;
    0
}


