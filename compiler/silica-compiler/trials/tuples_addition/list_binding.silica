fn main() -> int64 {
    packaged: (atom, List[int64, mem(normal)]) <- sequence proc[mem(normal)]
        xs: List[int64, mem(normal)] <- [0];
        tuple_out: ( atom, List[int64, mem(normal)] ) <- (:a, xs)
    produces
        pure tuple_out
    end;
    _used_packaged: (atom, List[int64, mem(normal)]) <- packaged;
    1
}
