
fn main() -> int64 {
    (flags, children): (int64, int64) <- sequence
        flags_and_children: (int64, int64) <- (0, 0)
    produces
        pure flags_and_children
    end;
    flags + children
}
