// Tuple with atom as first element and binary fn as second; call the fn for a concrete int64 result.
fn mul_fn(x: int64, y: int64) -> int64 {
    x * y
}

fn main() -> int64 {
    t: (atom, fn(x: int64, y: int64) -> int64) <- (:a, mul_fn);
    (tag, f) <- t;
    f(6, 7)
}
