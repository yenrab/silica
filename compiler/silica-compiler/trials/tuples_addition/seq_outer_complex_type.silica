
fn worker(msg: int64, state: int64) -> (:reply, int64, int64) {
    (:reply, msg + state, state)
}

fn main() -> int64 {
    (_flags, _children): (int64, int64) <- sequence
        flags_and_children: (int64, int64) <- (0, 0)
    produces
        pure flags_and_children
    end;
    0
}
