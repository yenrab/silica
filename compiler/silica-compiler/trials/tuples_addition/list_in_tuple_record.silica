fn main() -> int64 {
    list: List[int64,mem(normal)] <- sequence proc[mem(normal)]
        xs: List[int64,mem(normal)] <- [0]
    produces
        pure xs
    end;
    x: { start: (int64, List[int64,mem(normal)]), } <- { start: (0, list) };
    1
}
