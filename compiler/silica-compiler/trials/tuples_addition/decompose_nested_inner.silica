fn main() -> string {
    t: ((uint8, string), uint64) <- ((1, "hello"), 42);
    (inner: (uint8, string), n: uint64) <- t;
    _used_n: uint64 <- n;
    (b: uint8, s: string) <- inner;
    _used_b: uint8 <- b;
    s
}
