fn main() -> int64 {
    (flags, children) <- sequence proc[mem(normal)]
        flags_and_children: (
        List[int64, mem(normal)],
        atom
        ) <- ([0], :a)
    produces
        pure flags_and_children
    end;
    1
}
