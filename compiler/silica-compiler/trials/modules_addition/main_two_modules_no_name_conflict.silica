// Spec §19.3.3: importing multiple modules is fine when their export sets do not overlap on name+arity.
use lib_alpha, lib_math_utils_spec;

fn main() -> int64 {
    lib_alpha@bump(lib_math_utils_spec@add(1, 1))
}
