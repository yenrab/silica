.text
    ; use lib_beta
.global main


main:     STP X29, X30, [SP, #-16]!

    MOV X0, #5
    ; unsupported term kind ?
    ADRP X9, twice@PAGE
    ADD X9, X9, twice@PAGEOFF
    BLR X9
    LDP X29, X30, [SP], #16
    RET
