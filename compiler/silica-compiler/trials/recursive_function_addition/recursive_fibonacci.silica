// Tree-shaped recursion: fib(10) = 55 (no lists)
fn fib(n: int64) -> int64 {
    case n <= 1 of {
        true -> n;
        false -> fib(n - 1) + fib(n - 2)
    }
}

fn main() -> int64 {
    fib(10)
}
