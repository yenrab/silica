// Tail recursion: sum 1..n via accumulator; sum_to(10, 0) = 55
fn sum_to(n: int64, acc: int64) -> int64 {
    case n <= 0 of {
        true -> acc;
        false -> sum_to(n - 1, acc + n)
    }
}

fn main() -> int64 {
    sum_to(10, 0)
}
