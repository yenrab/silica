// Linear tail recursion: fib(n) via two accumulators; fib(10) = fib_iter(10, 0, 1) = 55
fn fib_iter(n: int64, a: int64, b: int64) -> int64 {
    case n <= 0 of {
        true -> a;
        false -> fib_iter(n - 1, b, a + b)
    }
}

fn main() -> int64 {
    fib_iter(10, 0, 1)
}
