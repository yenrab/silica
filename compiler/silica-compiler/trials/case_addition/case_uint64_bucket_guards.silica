// Guards on successive branches; final unguarded pattern covers the remainder (spec §3.3.3)
fn main() -> uint64 {
    n: uint64 <- 50;
    case n of {
        k: uint64 if k < 10 -> k;
        k: uint64 if k < 100 -> k + 1000;
        m: uint64 -> m + 10000
    }
}
