fn cmp_fn(a: int64, b: int64) -> :less | :equal | :greater {
    :equal
}

fn f(n: int64) -> int64 {
    node: int64 <- n;
    raw_cmp <- cmp_fn(n, node);
    case raw_cmp of {
        :equal -> 1;
        _: atom -> 0
    }
}

fn main() -> int64 {
    f(1)
}
