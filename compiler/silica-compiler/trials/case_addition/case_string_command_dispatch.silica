// Spec (actor command example): string literals + _: string for unknown commands
fn main() -> int64 {
    cmd: string <- "multiply";
    case cmd of {
        "add" -> 10;
        "multiply" -> 42;
        _: string -> -1
    }
}
