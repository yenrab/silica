// Spec §3.3.3: guard calls a helper after the pattern binds
fn is_positive(x: int64) -> boolean {
    x > 0
}

fn main() -> int64 {
    y: int64 <- 8;
    case y of {
        z: int64 if is_positive(z) -> z * 2;
        _: int64 -> 0
    }
}
