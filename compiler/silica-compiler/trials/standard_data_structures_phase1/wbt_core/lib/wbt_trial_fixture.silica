// Trial-only §8A.2–§8A.3 fixtures (int64): malformed roots via alloc_rec; production nodes via smart_node.

use wbt_set;
use wbt_map;

export build_set_i64/2;
export build_map_i64/2;
export build_set_singleton_i64/3;
export build_map_singleton_i64/4;
export build_set_cached_size_i64/4;
export build_map_cached_size_i64/5;
export build_set_smart_leaf_i64/3;
export build_map_smart_leaf_i64/4;
export build_set_smart_left_heavy_i64/2;
export build_set_smart_balanced3_i64/2;
export build_map_smart_left_heavy_i64/2;
export build_map_smart_balanced3_i64/2;
export build_set_smart_top_i64/5;
export build_map_smart_top_i64/6;
export build_set_right_spine3_i64/2;
export build_set_right_inner_leaf_i64/2;
export build_set_left_spine3_i64/2;
export build_set_left_inner_leaf_i64/2;
export build_map_right_spine3_i64/2;
export build_map_right_inner_leaf_i64/2;
export build_map_left_spine3_i64/2;
export build_map_left_inner_leaf_i64/2;

fn build_set_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n10: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (10, 1, :none, :none));
        n40: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (40, 1, :none, :none));
        n60: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (60, 1, :none, :none));
        n90: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (90, 1, :none, :none));
        n25: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (25, 3, n10, n40));
        n75: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (75, 3, n60, n90));
        root: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (50, 7, n25, n75));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: root,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n10: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (10, 100, 1, :none, :none));
        n40: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (40, 400, 1, :none, :none));
        n60: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (60, 600, 1, :none, :none));
        n90: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (90, 900, 1, :none, :none));
        n25: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (25, 250, 3, n10, n40));
        n75: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (75, 750, 3, n60, n90));
        root: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (50, 500, 7, n25, n75));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: root,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_set_singleton_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    item: int64
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        root: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (item, 1, :none, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: root,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_singleton_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    key: int64,
    value: int64
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        root: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <- alloc_rec(r, (key, value, 1, :none, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: root,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

// Malformed cached-size roots used only to exercise checked read-only weight.
fn build_set_cached_size_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    item: int64,
    cached_size: int64
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        root: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (item, cached_size, :none, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: root,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_cached_size_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    key: int64,
    value: int64,
    cached_size: int64
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        root: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (key, value, cached_size, :none, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: root,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_set_smart_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    item: int64
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, :none, :none));
        scratch_payload: (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: :none,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: wbt_set@smart_node(witness, item, witness.root, witness.root),
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_smart_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    key: int64,
    value: int64
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, 0, :none, :none));
        scratch_payload: (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: :none,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: wbt_map@smart_node(witness, key, value, witness.root, witness.root),
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_set_smart_top_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    item: int64,
    left: ref?(L, normal, rec),
    right: ref?(L, normal, rec)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, :none, :none));
        scratch_payload: (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: :none,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: wbt_set@smart_node(witness, item, left, right),
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_smart_top_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal),
    key: int64,
    value: int64,
    left: ref?(L, normal, rec),
    right: ref?(L, normal, rec)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, 0, :none, :none));
        scratch_payload: (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: :none,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: wbt_map@smart_node(witness, key, value, left, right),
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_set_smart_left_heavy_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, :none, :none));
        scratch_payload: (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: :none,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: wbt_set@smart_node(
                witness,
                25,
                wbt_set@smart_node(witness, 10, witness.root, witness.root),
                witness.root
            ),
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_set_smart_balanced3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, :none, :none));
        scratch_payload: (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: :none,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: wbt_set@smart_node(
                witness,
                50,
                wbt_set@smart_node(witness, 40, witness.root, witness.root),
                wbt_set@smart_node(witness, 60, witness.root, witness.root)
            ),
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_smart_left_heavy_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, 0, :none, :none));
        scratch_payload: (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: :none,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: wbt_map@smart_node(
                witness,
                25,
                2500,
                wbt_map@smart_node(witness, 10, 1000, witness.root, witness.root),
                witness.root
            ),
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_smart_balanced3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        scratch: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (0, 0, 0, :none, :none));
        scratch_payload: (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec)) <- read_ref(scratch);
        witness: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: :none,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        };
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: wbt_map@smart_node(
                witness,
                50,
                5000,
                wbt_map@smart_node(witness, 10, 1000, witness.root, witness.root),
                wbt_map@smart_node(witness, 90, 9000, witness.root, witness.root)
            ),
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}


// Right spine of 4 via alloc_rec: sizes 1,2,3,4 — triggers single left (rl empty, rr heavy).
fn build_set_right_spine3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n40: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (40, 1, :none, :none));
        n30: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 2, :none, n40));
        n20: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 3, :none, n30));
        n10: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 4, :none, n20));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: n10,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

// Double left: 10 / right=20(left=15(left=12)). sizes: 12=1,15=2,20=3,10=4. w_rl=3,w_rr=1 → double.
fn build_set_right_inner_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n12: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (12, 1, :none, :none));
        n15: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (15, 2, n12, :none));
        n20: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 3, n15, :none));
        n10: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 4, :none, n20));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: n10,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

// Left spine of 4: single right.
fn build_set_left_spine3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n10: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 1, :none, :none));
        n20: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 2, n10, :none));
        n30: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 3, n20, :none));
        n40: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (40, 4, n30, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: n40,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

// Double right: 30 / left=20(right=25(right=28)).
fn build_set_left_inner_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_item: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_item_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n28: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (28, 1, :none, :none));
        n25: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (25, 2, :none, n28));
        n20: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 3, :none, n25));
        n30: ref(L, normal, (int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 4, n20, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- {
            root: n30,
            compare_item: cmp,
            region: r,
            specialization_key: 0,
            compare_item_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_right_spine3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n40: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (40, 400, 1, :none, :none));
        n30: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 300, 2, :none, n40));
        n20: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 200, 3, :none, n30));
        n10: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 100, 4, :none, n20));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: n10,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_right_inner_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n12: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (12, 120, 1, :none, :none));
        n15: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (15, 150, 2, n12, :none));
        n20: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 200, 3, n15, :none));
        n10: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 100, 4, :none, n20));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: n10,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_left_spine3_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n10: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (10, 100, 1, :none, :none));
        n20: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 200, 2, n10, :none));
        n30: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 300, 3, n20, :none));
        n40: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (40, 400, 4, n30, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: n40,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}

fn build_map_left_inner_leaf_i64(
    cmp: fn(int64, int64) -> :less | :equal | :greater,
    r: region(L, normal)
) -> {
    root: ref?(L, normal, rec),
    compare_key: fn(int64, int64) -> :less | :equal | :greater,
    compare_value: fn(int64, int64) -> :less | :equal | :greater,
    region: region(L, normal),
    specialization_key: int64,
    compare_key_ordering_bundle: int64,
    compare_value_ordering_bundle: int64
} {
    sequence proc[mem(normal)]
        n28: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (28, 280, 1, :none, :none));
        n25: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (25, 250, 2, :none, n28));
        n20: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (20, 200, 3, :none, n25));
        n30: ref(L, normal, (int64, int64, int64, ref?(L, normal, rec), ref?(L, normal, rec))) <-
            alloc_rec(r, (30, 300, 4, n20, :none));
        out: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- {
            root: n30,
            compare_key: cmp,
            compare_value: cmp,
            region: r,
            specialization_key: 0,
            compare_key_ordering_bundle: 0,
            compare_value_ordering_bundle: 0
        }
    produces
        pure out
    end
}
