// Layer 2 §8A.7: delete_min/max on singleton and {1,2,3}; extracted binding + residual + old root.

use wbt_set;
use wbt_map;

fn cmp_i64(a: int64, b: int64) -> :less | :equal | :greater {
    case a < b of { true -> :less; false -> case a == b of { true -> :equal; false -> :greater } }
}

fn set_extreme_ok() -> boolean {
    sequence
        empty0: {
            root: ref?(L, normal, rec),
            compare_item: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_item_ordering_bundle: int64
        } <- wbt_set@empty({ compare_item: cmp_i64 });
        s1: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            inserted: boolean
        } <- wbt_set@insert(empty0, 9);
        emin_s: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            status: :not_found | :found,
            item: int64
        } <- wbt_set@delete_min(s1.set, 0);
        emax_s: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            status: :not_found | :found,
            item: int64
        } <- wbt_set@delete_max(s1.set, 0);
        a1: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            inserted: boolean
        } <- wbt_set@insert(empty0, 2);
        a2: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            inserted: boolean
        } <- wbt_set@insert(a1.set, 1);
        a3: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            inserted: boolean
        } <- wbt_set@insert(a2.set, 3);
        emin: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            status: :not_found | :found,
            item: int64
        } <- wbt_set@delete_min(a3.set, 0);
        emax: {
            set: {
                root: ref?(L, normal, rec),
                compare_item: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_item_ordering_bundle: int64
            },
            status: :not_found | :found,
            item: int64
        } <- wbt_set@delete_max(a3.set, 0);
        sz_sing: int64 <- wbt_set@size(emin_s.set);
        sz_min: int64 <- wbt_set@size(emin.set);
        sz_max: int64 <- wbt_set@size(emax.set);
        old_has1: boolean <- wbt_set@contains(a3.set, 1);
        old_has3: boolean <- wbt_set@contains(a3.set, 3);
        new_has1: boolean <- wbt_set@contains(emin.set, 1);
        new_has3: boolean <- wbt_set@contains(emax.set, 3)
    produces
        pure case emin_s.status of {
            :not_found -> false;
            :found -> case emin_s.item == 9 and sz_sing == 0 of {
                false -> false;
                true -> case emax_s.item == 9 of {
                    false -> false;
                    true -> case emin.status of {
                        :not_found -> false;
                        :found -> case emin.item == 1 and sz_min == 2 of {
                            false -> false;
                            true -> case emax.status of {
                                :not_found -> false;
                                :found -> case emax.item == 3 and sz_max == 2 of {
                                    false -> false;
                                    true -> case old_has1 and old_has3 of {
                                        false -> false;
                                        true -> case new_has1 of {
                                            true -> false;
                                            false -> case new_has3 of { true -> false; false -> true }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    end
}

fn map_extreme_ok() -> boolean {
    sequence
        empty0: {
            root: ref?(L, normal, rec),
            compare_key: fn(int64, int64) -> :less | :equal | :greater,
            compare_value: fn(int64, int64) -> :less | :equal | :greater,
            region: region(L, normal),
            specialization_key: int64,
            compare_key_ordering_bundle: int64,
            compare_value_ordering_bundle: int64
        } <- wbt_map@empty({ compare_key: cmp_i64, compare_value: cmp_i64 });
        s1: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            inserted: boolean,
            replaced: boolean
        } <- wbt_map@insert(empty0, 9, 90);
        emin_s: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            status: :not_found | :found,
            key: int64,
            value: int64
        } <- wbt_map@delete_min(s1.map, 0, 0);
        a1: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            inserted: boolean,
            replaced: boolean
        } <- wbt_map@insert(empty0, 2, 20);
        a2: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            inserted: boolean,
            replaced: boolean
        } <- wbt_map@insert(a1.map, 1, 10);
        a3: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            inserted: boolean,
            replaced: boolean
        } <- wbt_map@insert(a2.map, 3, 30);
        emin: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            status: :not_found | :found,
            key: int64,
            value: int64
        } <- wbt_map@delete_min(a3.map, 0, 0);
        emax: {
            map: {
                root: ref?(L, normal, rec),
                compare_key: fn(int64, int64) -> :less | :equal | :greater,
                compare_value: fn(int64, int64) -> :less | :equal | :greater,
                region: region(L, normal),
                specialization_key: int64,
                compare_key_ordering_bundle: int64,
                compare_value_ordering_bundle: int64
            },
            status: :not_found | :found,
            key: int64,
            value: int64
        } <- wbt_map@delete_max(a3.map, 0, 0);
        sz_min: int64 <- wbt_map@size(emin.map);
        old_g1: { status: :not_found | :found, value: int64 } <- wbt_map@get(a3.map, 1, 0);
        old_g3: { status: :not_found | :found, value: int64 } <- wbt_map@get(a3.map, 3, 0)
    produces
        pure case emin_s.status of {
            :not_found -> false;
            :found -> case emin_s.key == 9 and emin_s.value == 90 of {
                false -> false;
                true -> case emin.status of {
                    :not_found -> false;
                    :found -> case emin.key == 1 and emin.value == 10 and sz_min == 2 of {
                        false -> false;
                        true -> case emax.status of {
                            :not_found -> false;
                            :found -> case emax.key == 3 and emax.value == 30 of {
                                false -> false;
                                true -> case old_g1.status of {
                                    :not_found -> false;
                                    :found -> case old_g1.value == 10 of {
                                        false -> false;
                                        true -> case old_g3.status of {
                                            :not_found -> false;
                                            :found -> old_g3.value == 30
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    end
}

fn main() -> int64 {
    sequence
        s_ok: boolean <- set_extreme_ok();
        m_ok: boolean <- map_extreme_ok()
    produces
        pure case s_ok of {
            false -> 1;
            true -> case m_ok of { false -> 2; true -> 0 }
        }
    end
}
