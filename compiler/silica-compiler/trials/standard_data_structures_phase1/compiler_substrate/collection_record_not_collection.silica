// Layer 1 §7.6: inline records without a collection bracket type do not get constructor arena lowering.

fn cmp_i64(a: int64, b: int64) -> :less | :equal | :greater {
    :equal
}

fn main() -> int64 {
    _: { compare_item: fn(int64, int64) -> :less | :equal | :greater } <- { compare_item: cmp_i64 };
    0
}
