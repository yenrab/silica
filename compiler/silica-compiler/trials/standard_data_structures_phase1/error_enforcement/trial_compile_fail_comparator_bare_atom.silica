// Expected compile failure — comparator return type must be :less | :equal | :greater, not bare atom.

use StubWbtSet;

fn bad_cmp(_a: int64, _b: int64) -> atom {
    :equal
}

fn main() -> int64 {
    _: OrderedSet[int64, mem(normal)] <- StubWbtSet@empty({ compare_item: bad_cmp });
    0
}
