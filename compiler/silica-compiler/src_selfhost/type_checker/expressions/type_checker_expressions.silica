{-

   Copyright 2026 Lee Scott Barney

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not USE this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-}

// Expression type-check dispatcher: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } kind 0-4
// Delegates to literals, identifiers; handles binary ops and calls inline (recursive).
// Comparisons include ordering/index equality on atoms (<, >, <=, >=, ==, !=) per §4.1.7.

use parser_ast;
use type_checker_core;
use type_checker_expressions_literals;
use type_checker_expressions_identifiers;
use type_checker_expressions_atoms;
use type_checker_expressions_float32_calls;
use type_checker_expressions_string_calls;
use boolean_type_checker_expressions;
use type_checker_supervisor;
use type_checker_tuple_decompose_helpers;
use type_checker_memory_regions;
use type_checker_recursive_tuples;
use type_checker_lists;
use type_checker_collections;
use instantiated_fn_name;
use type_checker_expressions_actors;
use type_checker_record_types;
use type_checker_traits;
use type_checker_callee_env;
use string_parse;

export check_expr/4;
export dispatch_actual_fits_impl_parameter/3;

fn is_primitive_numeric_type_name(t: string) -> bool {
    t == "int8" or t == "int16" or t == "int32" or t == "int64" or t == "uint8" or t == "uint16" or t == "uint32" or t == "uint64" or t == "float16" or t == "float32" or t == "float64"
}

// { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } kind 12: unary minus (-) on an expression; disallowed — use negate_<type>(...) instead.
fn check_unary_minus_forbidden(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext) -> TypeCheckResult {
    inner_t: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
    case len(inner_t) == 0 of {
        true -> type_checker_core@type_check_err("E2013", expr.location, "Unary minus (-) is not allowed; use negate_<type>(...) instead of the `-` symbol, where <type> matches the operand's numeric type (operand type could not be inferred)", "sect6");
        false -> case is_primitive_numeric_type_name(inner_t) of {
            true -> {
                suggest: string <- concat("negate_", inner_t);
                type_checker_core@type_check_err("E2013", expr.location, concat(concat("Unary minus (-) is not allowed; use ", suggest), "(...) instead of the `-` symbol"), "sect6")
            };
            false -> type_checker_core@type_check_err("E2013", expr.location, concat(concat("Unary minus (-) is not allowed; use negate_<type>(...) instead of the `-` symbol (operand type is ", inner_t), ", not a primitive numeric)"), "sect6")
        }
    }
}

// Arithmetic: +, -, *, /, % (mod) - operands int64/int32/int16/int8/uint64/uint32/uint16/uint8, result matches operands
fn is_arithmetic_op_lexeme(op: string) -> bool {
    op == "+" or op == "-" or op == "*" or op == "/" or op == "%"
}

// Phase 1 bitwise operations are intentionally uint64-only.
fn is_bitwise_binary_op_lexeme(op: string) -> bool {
    op == "bor" or op == "band" or op == "shl" or op == "shr"
}

fn is_shift_op_lexeme(op: string) -> bool {
    op == "shl" or op == "shr"
}

fn check_uint64_result(expected_type: int64, location: { file: string, line: int64, column: int64, offset: int64 }, op_label: string) -> TypeCheckResult {
    case expected_type of {
        "uint64" -> type_checker_core@type_check_ok();
        "discard" -> type_checker_core@type_check_ok();
        _: string -> type_checker_core@type_check_err("E2003", location, concat(concat(op_label, " returns uint64, expected "), expected_type), "sect6")
    }
}

fn shift_amount_literal_out_of_range(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case expr.kind == 0 of {
        true -> {
            amount: int64 <- string_parse@string_to_int64(expr.value);
            amount < 0 or amount >= 64
        };
        false -> false
    }
}

fn check_bnot_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "uint64", context, world);
    case arg_result.is_ok of {
        false -> arg_result;
        true -> check_uint64_result(expected_type, expr.location, "bnot")
    }
}

fn check_bitwise_binary_op(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    op: string <- expr.value;
    left_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "uint64", context, world);
    case left_result.is_ok of {
        false -> left_result;
        true -> case is_shift_op_lexeme(op) of {
            true -> case shift_amount_literal_out_of_range(expr.right_expr) of {
                true -> type_checker_core@type_check_err("E2003", expr.right_expr.location, "shift amount literal must be in range 0..63", "sect6");
                false -> {
                    right_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, "int64", context, world);
                    case right_result.is_ok of {
                        false -> right_result;
                        true -> check_uint64_result(expected_type, expr.location, concat("bitwise ", op))
                    }
                }
            };
            false -> {
                right_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, "uint64", context, world);
                case right_result.is_ok of {
                    false -> right_result;
                    true -> check_uint64_result(expected_type, expr.location, concat("bitwise ", op))
                }
            }
        }
    }
}

// Comparison: <, >, <=, >=, ==, != — operands boolean, numeric primitives, string, or atom; result bool
fn is_comparison_op_lexeme(op: string) -> bool {
    op == "<" or op == ">" or op == "<=" or op == ">=" or op == "==" or op == "!="
}

// Try checking both operands with operand_type; return result if both ok.
fn check_comparison_operands(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, operand_type: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    left_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, operand_type, context, world);
    case left_result.is_ok of {
        false -> left_result;
        true -> check_expr(expr.right_expr, operand_type, context, world)
    }
}

fn is_comparison_operand_type(t: string) -> bool {
    t == "boolean" or t == "int64" or t == "int32" or t == "int16" or t == "int8" or t == "uint64" or t == "uint32" or t == "uint16" or t == "uint8" or t == "float64" or t == "float32" or t == "float16" or t == "string" or t == "atom"
}

fn inferred_comparison_operand_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext) -> string {
    left_t: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
    right_t: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.right_expr, context);
    case len(left_t) > 0 of {
        true -> case is_comparison_operand_type(left_t) of {
            false -> "";
            true -> case len(right_t) > 0 of {
                true -> case left_t == right_t of { true -> left_t; false -> "" };
                false -> left_t
            }
        };
        false -> case len(right_t) > 0 of {
            true -> case is_comparison_operand_type(right_t) of { true -> right_t; false -> "" };
            false -> ""
        }
    }
}

// Try each supported comparison operand type; first match wins.
// Signed: int64, int32, int16, int8. Unsigned: uint64, uint32, uint16, uint8. Also float32, float16, string, atom (ordering by atom table index), boolean.
fn bool_result(expected_type: int64, location: { file: string, line: int64, column: int64, offset: int64 }) -> TypeCheckResult {
    case expected_type of {
        "boolean" -> type_checker_core@type_check_ok();
        "discard" -> type_checker_core@type_check_ok();
        _: string -> type_checker_core@type_check_err("E2003", location, concat("comparison op expects boolean result, got ", expected_type), "sect6")
    }
}

fn check_comparison_result(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    inferred_operand: string <- inferred_comparison_operand_type(expr, context);
    case len(inferred_operand) > 0 of {
        true -> {
            ri: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, inferred_operand, context, world);
            case ri.is_ok of {
                false -> ri;
                true -> bool_result(expected_type, expr.location)
            }
        };
        false -> {
            rb: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "boolean", context, world);
            case rb.is_ok of {
                true -> bool_result(expected_type, expr.location);
                false -> check_comparison_result_rest(expr, expected_type, context, rb, world)
            }
        }
    }
}

fn check_comparison_result_rest(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, first_err: TypeCheckResult, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    r64: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "int64", context, world);
    case r64.is_ok of {
        true -> bool_result(expected_type, expr.location);
        false -> {
            r32: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "int32", context, world);
            case r32.is_ok of {
                true -> bool_result(expected_type, expr.location);
                false -> {
                    r16: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "int16", context, world);
                    case r16.is_ok of {
                        true -> bool_result(expected_type, expr.location);
                        false -> {
                            r8: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "int8", context, world);
                            case r8.is_ok of {
                                true -> bool_result(expected_type, expr.location);
                                false -> {
                                    ru64: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "uint64", context, world);
                                    case ru64.is_ok of {
                                        true -> bool_result(expected_type, expr.location);
                                        false -> {
                                            ru32: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "uint32", context, world);
                                            case ru32.is_ok of {
                                                true -> bool_result(expected_type, expr.location);
                                                false -> {
                                                    ru16: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "uint16", context, world);
                                                    case ru16.is_ok of {
                                                        true -> bool_result(expected_type, expr.location);
                                                        false -> {
                                                            ru8: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "uint8", context, world);
                                                            case ru8.is_ok of {
                                                                true -> bool_result(expected_type, expr.location);
                                                                false -> {
                                                                    rf32: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "float32", context, world);
                                                                    case rf32.is_ok of {
                                                                        true -> bool_result(expected_type, expr.location);
                                                                        false -> {
                                                                            rf16: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "float16", context, world);
                                                                            case rf16.is_ok of {
                                                                                true -> bool_result(expected_type, expr.location);
                                                                                false -> {
                                                                                    rs: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "string", context, world);
                                                                                    case rs.is_ok of {
                                                                                        true -> bool_result(expected_type, expr.location);
                                                                                        false -> {
                                                                                            ra: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_comparison_operands(expr, "atom", context, world);
                                                                                            case ra.is_ok of {
                                                                                                true -> bool_result(expected_type, expr.location);
                                                                                                false -> first_err
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Logical: and, or - operands boolean, result boolean
fn is_logical_op_lexeme(op: string) -> bool {
    op == "and" or op == "or"
}

fn check_binary_op(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    op: string <- expr.value;
    case is_arithmetic_op_lexeme(op) of {
        true -> {
            operand_type: string <- case expected_type of {
                "int64" -> "int64";
                "int32" -> "int32";
                "int16" -> "int16";
                "int8" -> "int8";
                "uint64" -> "uint64";
                "uint32" -> "uint32";
                "uint16" -> "uint16";
                "uint8" -> "uint8";
                "float64" -> "float64";
                "float32" -> "float32";
                "float16" -> "float16";
                _: string -> "int64"
            };
            left_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, operand_type, context, world);
            case left_result.is_ok of {
                false -> left_result;
                true -> {
                    right_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, operand_type, context, world);
                    case right_result.is_ok of {
                        false -> right_result;
                        true -> case expected_type of {
                            "int64" -> type_checker_core@type_check_ok();
                            "int32" -> type_checker_core@type_check_ok();
                            "int16" -> type_checker_core@type_check_ok();
                            "int8" -> type_checker_core@type_check_ok();
                            "uint64" -> type_checker_core@type_check_ok();
                            "uint32" -> type_checker_core@type_check_ok();
                            "uint16" -> type_checker_core@type_check_ok();
                            "uint8" -> type_checker_core@type_check_ok();
                            "float64" -> type_checker_core@type_check_ok();
                            "float32" -> type_checker_core@type_check_ok();
                            "float16" -> type_checker_core@type_check_ok();
                            "discard" -> type_checker_core@type_check_ok();
                            _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("arithmetic op expects integer or float result (int64/.../float64/float32/float16), got ", expected_type), "sect6")
                        }
                    }
                }
            }
        };
        false -> case is_bitwise_binary_op_lexeme(op) of {
            true -> check_bitwise_binary_op(expr, expected_type, context, world);
            false -> case is_comparison_op_lexeme(op) of {
            true -> check_comparison_result(expr, expected_type, context, world);
            false -> case is_logical_op_lexeme(op) of {
                true -> {
                    left_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "boolean", context, world);
                    case left_result.is_ok of {
                        false -> left_result;
                        true -> {
                            right_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, "boolean", context, world);
                            case right_result.is_ok of {
                                false -> right_result;
                            true -> case expected_type of {
                                "boolean" -> type_checker_core@type_check_ok();
                                "discard" -> type_checker_core@type_check_ok();
                                _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("logical op expects boolean result, got ", expected_type), "sect6")
                            }
                            }
                        }
                    }
                };
                false -> type_checker_core@type_check_err("E2004", expr.location, concat("unknown operator: ", op), "sect6")
            }
        }
        }
    }
}

// Multi-arg calls use kind-4 continuations with name "" (see extract_call_rest). Second argument is inner of that node.
fn call_second_arg_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
        true -> expr.right_expr.inner;
        false -> expr.right_expr
    }
}

// Third argument of a 3-arg call like write_buf(buf, idx, val).
// Parser encodes as call(inner=buf, right=pair("", inner=idx, right=pair("", inner=val, right=dummy))).
fn call_third_arg_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
        true -> case expr.right_expr.right_expr.kind == 4 and expr.right_expr.right_expr.name == "" of {
            true -> expr.right_expr.right_expr.inner;
            false -> expr.right_expr.right_expr
        };
        false -> expr.right_expr
    }
}

// Fourth argument of spawn_linked(initial, behavior, agent_type, core_id).
fn call_fourth_arg_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
        true -> case expr.right_expr.right_expr.kind == 4 and expr.right_expr.right_expr.name == "" of {
            true -> case expr.right_expr.right_expr.right_expr.kind == 4 and expr.right_expr.right_expr.right_expr.name == "" of {
                true -> expr.right_expr.right_expr.right_expr.inner;
                false -> expr.right_expr.right_expr.right_expr
            };
            false -> expr.right_expr.right_expr
        };
        false -> expr.right_expr
    }
}

// Memory region built-ins per spec §4.4, §9.2.2, §12, §22
fn check_region_builtin_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.name == "alloc_region" of {
        true -> {
            mem_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_memory_regions@validate_memory_region_type(expected_type, expr.location);
            case mem_result.is_ok of {
                false -> mem_result;
                true -> case expr.inner.kind == 1 of {
                    true -> {
                        arg_space: string <- expr.inner.name;
                        expected_space: string <- type_checker_memory_regions@get_space_from_region_type(expected_type);
                        case type_checker_memory_regions@is_valid_memory_space(arg_space) of {
                            false -> type_checker_core@type_check_err("E2010", expr.location, concat(concat("alloc_region: invalid memory space '", arg_space), "'"), "sect4.4.1");
                            true -> case arg_space == expected_space of {
                                false -> type_checker_core@type_check_err("E2010", expr.location, concat(concat("alloc_region: space '", arg_space), concat("' does not match expected type ", expected_type)), "sect4.4.1");
                                true -> type_checker_core@type_check_ok()
                            }
                        }
                    };
                    false -> type_checker_core@type_check_err("E2010", expr.location, "alloc_region: argument must be memory space identifier", "sect4.4.1")
                }
            }
        };
        false -> case expr.name == "alloc_ref" of {
            true -> {
                region_type: string <- type_checker_memory_regions@ref_type_to_region_type(expected_type);
                elem_type: string <- type_checker_memory_regions@extract_element_type_from_ref(expected_type);
                case len(region_type) == 0 of {
                    true -> type_checker_core@type_check_err("E2010", expr.location, concat("alloc_ref result must be ref(R, Space, T), got ", expected_type), "sect4.4.2");
                    false -> {
                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, region_type, context, world);
                        case r1.is_ok of { false -> r1; true -> check_expr(call_second_arg_expr(expr), elem_type, context, world) }
                    }
                }
            };
            false -> case expr.name == "alloc_rec" of {
                true -> {
                    region_type: string <- type_checker_memory_regions@ref_type_to_region_type(expected_type);
                    elem_type: string <- type_checker_memory_regions@extract_element_type_from_ref(expected_type);
                    case len(region_type) == 0 of {
                        true -> type_checker_core@type_check_err("E2010", expr.location, concat("alloc_rec result must be ref(R, Space, T), got ", expected_type), "recursive_tuple§2.3");
                        false -> {
                            rec_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_recursive_tuples@validate_alloc_rec_element_type(elem_type, expr.location);
                            case rec_result.is_ok of {
                                false -> rec_result;
                                true -> {
                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, region_type, context, world);
                                    case r1.is_ok of { false -> r1; true -> check_expr(call_second_arg_expr(expr), elem_type, context, world) }
                                }
                            }
                        }
                    }
                };
                false -> case expr.name == "read_ref" of {
                true -> {
                    ref_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                    elem_type: string <- type_checker_memory_regions@extract_element_type_from_ref(ref_type);
                    case len(elem_type) == 0 of {
                        true -> type_checker_core@type_check_err("E2010", expr.location, "read_ref: argument must have ref(R, Space, T) type", "sect4.4.2");
                        false -> case elem_type == expected_type of {
                            false -> case type_checker_tuple_decompose_helpers@types_equal_surface(elem_type, expected_type) of {
                                false -> type_checker_core@type_check_err("E2010", expr.location, concat(concat("read_ref: element type ", elem_type), concat(" does not match expected ", expected_type)), "sect4.4.2");
                                true -> check_expr(expr.inner, ref_type, context, world)
                            };
                            true -> check_expr(expr.inner, ref_type, context, world)
                        }
                    }
                };
                false -> case expr.name == "write_ref" of {
                    true -> {
                        ref_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                        elem_type: string <- type_checker_memory_regions@extract_element_type_from_ref(ref_type);
                        case len(elem_type) == 0 of {
                            true -> type_checker_core@type_check_err("E2010", expr.location, "write_ref: first argument must have ref(R, Space, T) type", "sect4.4.2");
                            false -> {
                                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, ref_type, context, world);
                                case r1.is_ok of {
                                    false -> r1;
                                    true -> {
                                        r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), elem_type, context, world);
                                        case r2.is_ok of {
                                            false -> r2;
                                            true -> case expected_type == "atom" or expected_type == "discard" of {
                                                true -> type_checker_core@type_check_ok();
                                                false -> type_checker_core@type_check_err("E2003", expr.location, "write_ref returns atom", "sect4.4.2")
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    };
                    false -> case expr.name == "alloc_buf" of {
                        true -> {
                            region_type: string <- type_checker_memory_regions@buf_type_to_region_type(expected_type);
                            case len(region_type) == 0 of {
                                true -> type_checker_core@type_check_err("E2010", expr.location, concat("alloc_buf result must be buf(R, Space, T, N), got ", expected_type), "sect4.4.3");
                                false -> {
                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, region_type, context, world);
                                    case r1.is_ok of { false -> r1; true -> check_expr(call_second_arg_expr(expr), "int64", context, world) }
                                }
                            }
                        };
                        false -> case expr.name == "read_buf" or expr.name == "buf_load" of {
                            true -> {
                                buf_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                                elem_type: string <- type_checker_memory_regions@extract_element_type_from_buf(buf_type);
                                case len(elem_type) == 0 of {
                                    true -> type_checker_core@type_check_err("E2010", expr.location, "read_buf/buf_load: first argument must have buf(R, Space, T, N) type", "sect4.4.3");
                                    false -> case elem_type == expected_type of {
                                        false -> type_checker_core@type_check_err("E2010", expr.location, concat(concat("read_buf/buf_load: element type ", elem_type), concat(" does not match expected ", expected_type)), "sect4.4.3");
                                        true -> {
                                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, buf_type, context, world);
                                            case r1.is_ok of { false -> r1; true -> check_expr(call_second_arg_expr(expr), "int64", context, world) }
                                        }
                                    }
                                }
                            };
                            false -> case expr.name == "write_buf" or expr.name == "buf_store" of {
                                true -> {
                                    buf_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                                    elem_type: string <- type_checker_memory_regions@extract_element_type_from_buf(buf_type);
                                    case len(elem_type) == 0 of {
                                        true -> type_checker_core@type_check_err("E2010", expr.location, "write_buf/buf_store: first argument must have buf(R, Space, T, N) type", "sect4.4.3");
                                        false -> case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
                                            false -> type_checker_core@type_check_err("E2005", expr.location, "write_buf/buf_store requires 3 arguments: (buf, index, value)", "sect12.3.2");
                                            true -> {
                                                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, buf_type, context, world);
                                                case r1.is_ok of {
                                                    false -> r1;
                                                    true -> {
                                                        r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "int64", context, world);
                                                        case r2.is_ok of {
                                                            false -> r2;
                                                            true -> {
                                                                r3: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_third_arg_expr(expr), elem_type, context, world);
                                                                case r3.is_ok of {
                                                                    false -> r3;
                                                                    true -> case expected_type == "atom" or expected_type == "discard" of {
                                                                        true -> type_checker_core@type_check_ok();
                                                                        false -> type_checker_core@type_check_err("E2003", expr.location, "write_buf/buf_store returns atom", "sect12.3.2")
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                };
                                false -> case expr.name == "fresh_lifetime" of {
                                        true -> case expected_type == "lifetime" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("fresh_lifetime returns lifetime, got ", expected_type), "sect22.1")
                                        };
                                        false -> case expr.name == "canonical_arena_lookup" of {
                                            true -> {
                                                mem_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_memory_regions@validate_memory_region_type(expected_type, expr.location);
                                                case mem_result.is_ok of {
                                                    false -> mem_result;
                                                    true -> check_expr(expr.inner, "int64", context, world)
                                                }
                                            };
                                            false -> case expr.name == "canonical_arena_same" of {
                                                true -> {
                                                    r1_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                                                    r2_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(call_second_arg_expr(expr), context);
                                                    space1: string <- type_checker_memory_regions@get_space_from_region_type(r1_type);
                                                    space2: string <- type_checker_memory_regions@get_space_from_region_type(r2_type);
                                                    case len(space1) == 0 or len(space2) == 0 of {
                                                        true -> type_checker_core@type_check_err("E2010", expr.location, "canonical_arena_same: both arguments must be region(R, Space) values", "common_contract§7");
                                                        false -> case space1 == space2 of {
                                                            false -> type_checker_core@type_check_err("E2010", expr.location, concat(concat("canonical_arena_same: memory spaces must match (", space1), concat(" vs ", concat(space2, ")"))), "common_contract§7");
                                                            true -> case expected_type == "boolean" of {
                                                                false -> type_checker_core@type_check_err("E2003", expr.location, "canonical_arena_same returns boolean", "common_contract§7");
                                                                true -> {
                                                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, r1_type, context, world);
                                                                    case r1.is_ok of {
                                                                        false -> r1;
                                                                        true -> check_expr(call_second_arg_expr(expr), r2_type, context, world)
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                };
                                                false -> case expr.name == "ref_eq" of {
                                                    true -> {
                                                        r1_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                                                        r2_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(call_second_arg_expr(expr), context);
                                                        e1: string <- type_checker_memory_regions@extract_element_type_from_ref(r1_type);
                                                        e2: string <- type_checker_memory_regions@extract_element_type_from_ref(r2_type);
                                                        case len(e1) == 0 or len(e2) == 0 of {
                                                            true -> type_checker_core@type_check_err("E2010", expr.location, "ref_eq: both arguments must be ref(R, Space, T) values", "sect4.4.2");
                                                            false -> case expected_type == "boolean" of {
                                                                false -> type_checker_core@type_check_err("E2003", expr.location, "ref_eq returns boolean", "sect4.4.2");
                                                                true -> {
                                                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, r1_type, context, world);
                                                                    case r1.is_ok of {
                                                                        false -> r1;
                                                                        true -> check_expr(call_second_arg_expr(expr), r2_type, context, world)
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    };
                                                    false -> case expr.name == "ref_in_region" of {
                                                        true -> {
                                                            ref_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                                                            region_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(call_second_arg_expr(expr), context);
                                                            elem: string <- type_checker_memory_regions@extract_element_type_from_ref(ref_type);
                                                            space_r: string <- type_checker_memory_regions@get_space_from_region_type(region_type);
                                                            case len(elem) == 0 of {
                                                                true -> type_checker_core@type_check_err("E2010", expr.location, "ref_in_region: first argument must be ref(R, Space, T)", "sect4.4.2");
                                                                false -> case len(space_r) == 0 of {
                                                                    true -> type_checker_core@type_check_err("E2010", expr.location, "ref_in_region: second argument must be region(R, Space)", "sect4.4.2");
                                                                    false -> case expected_type == "boolean" of {
                                                                        false -> type_checker_core@type_check_err("E2003", expr.location, "ref_in_region returns boolean", "sect4.4.2");
                                                                        true -> {
                                                                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, ref_type, context, world);
                                                                            case r1.is_ok of {
                                                                                false -> r1;
                                                                                true -> check_expr(call_second_arg_expr(expr), region_type, context, world)
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        };
                                                        false -> type_checker_core@type_check_err("E2005", expr.location, concat("unknown region built-in: ", expr.name), "sect22")
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Built-in: print_int64/32/16/8, print_uint64/32/16/8 - arg matches type, returns atom
// Built-in: print_atom(atom:x) - arg atom, returns atom (:ok on success, :fail on failure)
fn check_print_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, arg_type: string, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, arg_type, context, world);
    case arg_result.is_ok of {
        false -> arg_result;
        true -> case expected_type of {
            "atom" -> type_checker_core@type_check_ok();
            "discard" -> type_checker_core@type_check_ok();
            _: string -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("print_", concat(arg_type, " expects atom result, got ")), expected_type), "sect6")
        }
    }
}

// Built-in: negate_<primitive>/1 — signed int and float only. negate_uint* are not builtins (unknown function, E2005). negate_unit is rejected below.
fn negate_builtin_arg_type(name: string) -> string {
    case name of {
        "negate_int8" -> "int8";
        "negate_int16" -> "int16";
        "negate_int32" -> "int32";
        "negate_int64" -> "int64";
        "negate_float16" -> "float16";
        "negate_float32" -> "float32";
        "negate_float64" -> "float64";
        _: string -> ""
    }
}

fn check_negate_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, num_type: string, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, num_type, context, world);
    case arg_result.is_ok of {
        false -> arg_result;
        true -> case expected_type of {
            "discard" -> type_checker_core@type_check_ok();
            _: string -> case expected_type == num_type of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat(expr.name, " returns "), num_type), ", expected "), expected_type), "sect6")
            }
        }
    }
}

// §4.5 actor_ref, §15 spawn, §16 call/cast/send, self; recv forbidden §15.1.2;
// CPU affinity / topology helpers (spec appendix): pin_actor_*, unpin_actor, remove_actor, set_actor_priority, get_cpu_topology, get_*_cores, get_core_capabilities.
fn actor_runtime_builtin_return_type(name: string) -> string {
    case name of {
        "pin_actor_to_core" -> "(int64,affinity_error)";
        "pin_actor_to_efficiency_core" -> "(int64,affinity_error)";
        "pin_actor_to_performance_core" -> "(int64,affinity_error)";
        "pin_actor_realtime" -> "(int64,affinity_error)";
        "remove_actor" -> "(int64,affinity_error)";
        "pin_actor_to_numa_node" -> "(int64,affinity_error)";
        "unpin_actor" -> "atom";
        "register_actor" -> "atom";
        "get_registry_actor" -> "actor_ref";
        "get_pid_registry" -> "actor_ref";
        "set_actor_priority" -> "atom";
        "get_cpu_topology" -> "{ cores: List[{ id: int64, core_type: atom, capabilities: List[string,normal], frequency_mhz: int64 },normal], numa_nodes: List[{ id: int64, cores: List[int64,normal], memory_ranges: List[{ start_address: int64, size: int64, latency: int64 },normal] },normal], cache_hierarchy: { levels: List[{ level: int64, size_kb: int64, line_size: int64, associativity: int64, shared_cores: List[int64,normal] },normal] } }";
        "get_efficiency_cores" -> "List[int64,normal]";
        "get_performance_cores" -> "List[int64,normal]";
        "get_core_capabilities" -> "{ id: int64, core_type: atom, capabilities: List[string,normal], frequency_mhz: int64 }";
        "get_actor_memory_usage" -> "int64";
        "migrate_actor" -> "atom";
        "move" -> "atom";
        "register_failure_reporter" -> "atom";
        _: string -> ""
    }
}

fn is_actor_runtime_helper_name(name: string) -> bool {
    len(actor_runtime_builtin_return_type(name)) > 0
}

// §4.5 actor_ref, §15 spawn, §16 call/cast/send, self; recv forbidden §15.1.2.
// §15.4.8.5–§15.4.8.7: link, monitor, demonitor (Phase G: behavior-only for link/monitor/demonitor; E2091).
fn is_actor_concurrency_builtin(name: string) -> bool {
    name == "spawn" or name == "spawn_dangerous" or name == "spawn_registered" or name == "spawn_dangerous_registered" or name == "spawn_registered_supervisor" or name == "spawn_linked" or name == "call_supervisor" or name == "add_child" or name == "link" or name == "monitor" or name == "demonitor" or name == "send" or name == "cast" or name == "cast_registered" or name == "cast_dangerous_registered" or name == "call" or name == "call_registered" or name == "self" or name == "recv" or name == "supervision_ingress_enqueue" or name == "supervision_wait_and_drain_one" or name == "failure_reason_tag" or name == "failure_reason_atom" or name == "failure_agent_type" or name == "failure_release" or name == "start_child" or name == "child_table_first_ref" or name == "child_table_row_ref" or name == "kill_abnormal" or name == "wait_for_exit" or is_actor_runtime_helper_name(name)
}

fn check_runtime_builtin_return(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, inferred: string, expected_type: int64) -> TypeCheckResult {
    disp: string <- type_checker_core@user_call_base_function_name(expr.name);
    case expected_type == "discard" of {
        true -> type_checker_core@type_check_ok();
        false -> case type_checker_lists@types_equal_strip(inferred, expected_type) of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat(disp, " returns "), inferred), ", expected "), expected_type), "sect15")
        }
    }
}

fn check_actor_runtime_helper_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    eff: string <- type_checker_core@user_call_base_function_name(expr.name);
    inferred: string <- actor_runtime_builtin_return_type(eff);
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case eff == "get_cpu_topology" of {
        true -> case ac == 0 of {
            false -> type_checker_core@type_check_err("E2005", expr.location, "get_cpu_topology expects 0 arguments", "sect15");
            true -> check_runtime_builtin_return(expr, inferred, expected_type)
        };
        false -> case eff == "get_efficiency_cores" of {
            true -> case ac == 0 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "get_efficiency_cores expects 0 arguments", "sect15");
                true -> check_runtime_builtin_return(expr, inferred, expected_type)
            };
            false -> case eff == "get_performance_cores" of {
                true -> case ac == 0 of {
                    false -> type_checker_core@type_check_err("E2005", expr.location, "get_performance_cores expects 0 arguments", "sect15");
                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                };
                false -> case eff == "get_core_capabilities" of {
                    true -> case ac == 1 of {
                        false -> type_checker_core@type_check_err("E2005", expr.location, "get_core_capabilities expects 1 argument: (core_id: int64)", "sect15");
                        true -> {
                            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                            case r0.is_ok of {
                                false -> r0;
                                true -> check_runtime_builtin_return(expr, inferred, expected_type)
                            }
                        }
                    };
                    false -> case eff == "pin_actor_to_core" of {
                        true -> case ac == 2 of {
                            false -> type_checker_core@type_check_err("E2005", expr.location, "pin_actor_to_core expects 2 arguments: (actor_ref, core_id)", "sect15");
                            true -> {
                                r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                case r0.is_ok of {
                                    false -> r0;
                                    true -> {
                                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "int64", context, world);
                                        case r1.is_ok of {
                                            false -> r1;
                                            true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                        }
                                    }
                                }
                            }
                        };
                        false -> case eff == "pin_actor_realtime" of {
                            true -> case ac == 2 of {
                                false -> type_checker_core@type_check_err("E2005", expr.location, "pin_actor_realtime expects 2 arguments: (actor_ref, priority)", "sect15");
                                true -> {
                                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                    case r0.is_ok of {
                                        false -> r0;
                                        true -> {
                                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "int64", context, world);
                                            case r1.is_ok of {
                                                false -> r1;
                                                true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                            }
                                        }
                                    }
                                }
                            };
                            false -> case eff == "set_actor_priority" of {
                                true -> case ac == 2 of {
                                    false -> type_checker_core@type_check_err("E2005", expr.location, "set_actor_priority expects 2 arguments: (actor_ref, priority_level)", "sect15");
                                    true -> {
                                        r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                        case r0.is_ok of {
                                            false -> r0;
                                            true -> {
                                                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "discard", context, world);
                                                case r1.is_ok of {
                                                    false -> r1;
                                                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                }
                                            }
                                        }
                                    }
                                };
                                false -> case eff == "pin_actor_to_numa_node" of {
                                    true -> case ac == 2 of {
                                        false -> type_checker_core@type_check_err("E2005", expr.location, "pin_actor_to_numa_node expects 2 arguments: (actor_ref, numa_node)", "sect15");
                                        true -> {
                                            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                            case r0.is_ok of {
                                                false -> r0;
                                                true -> {
                                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "discard", context, world);
                                                    case r1.is_ok of {
                                                        false -> r1;
                                                        true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                    }
                                                }
                                            }
                                        }
                                    };
                                    false -> case eff == "pin_actor_to_efficiency_core" of {
                                        true -> case ac == 1 of {
                                            false -> type_checker_core@type_check_err("E2005", expr.location, "pin_actor_to_efficiency_core expects 1 argument: (actor_ref)", "sect15");
                                            true -> {
                                                r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                case r0.is_ok of {
                                                    false -> r0;
                                                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                }
                                            }
                                        };
                                        false -> case eff == "pin_actor_to_performance_core" of {
                                            true -> case ac == 1 of {
                                                false -> type_checker_core@type_check_err("E2005", expr.location, "pin_actor_to_performance_core expects 1 argument: (actor_ref)", "sect15");
                                                true -> {
                                                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                    case r0.is_ok of {
                                                        false -> r0;
                                                        true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                    }
                                                }
                                            };
                                            false -> case eff == "unpin_actor" of {
                                                true -> case ac == 1 of {
                                                    false -> type_checker_core@type_check_err("E2005", expr.location, "unpin_actor expects 1 argument: (actor_ref)", "sect15");
                                                    true -> {
                                                        r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                        case r0.is_ok of {
                                                            false -> r0;
                                                            true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                        }
                                                    }
                                                };
                                                false -> case eff == "remove_actor" of {
                                                    true -> case ac == 1 of {
                                                        false -> type_checker_core@type_check_err("E2005", expr.location, "remove_actor expects 1 argument: (actor_ref)", "sect15");
                                                        true -> {
                                                            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "discard", context, world);
                                                            case r0.is_ok of {
                                                                false -> r0;
                                                                true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                            }
                                                        }
                                                    };
                                                    false -> case eff == "register_actor" of {
                                                        true -> case ac == 1 of {
                                                            false -> type_checker_core@type_check_err("E2005", expr.location, "register_actor expects 1 argument: (actor_ref)", "sect15");
                                                            true -> {
                                                                r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                                case r0.is_ok of {
                                                                    false -> r0;
                                                                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                }
                                                            }
                                                        };
                                                        false -> case eff == "get_actor_memory_usage" of {
                                                        true -> case ac == 1 of {
                                                            false -> type_checker_core@type_check_err("E2005", expr.location, "get_actor_memory_usage expects 1 argument: (actor_ref)", "sect15");
                                                            true -> {
                                                                r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                                case r0.is_ok of {
                                                                    false -> r0;
                                                                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                }
                                                            }
                                                        };
                                                        false -> case eff == "register_failure_reporter" of {
                                                            true -> case ac == 1 of {
                                                                false -> type_checker_core@type_check_err("E2005", expr.location, "register_failure_reporter expects 1 argument: (failure_reporter_actor: actor_ref)", "sect15.4.13.4");
                                                                true -> {
                                                                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                                    case r0.is_ok of {
                                                                        false -> r0;
                                                                        true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                    }
                                                                }
                                                            };
                                                            false -> case eff == "migrate_actor" of {
                                                                true -> case ac == 2 of {
                                                                    false -> type_checker_core@type_check_err("E2005", expr.location, "migrate_actor expects 2 arguments: (actor_ref, target_core: int64)", "sect15");
                                                                    true -> {
                                                                        r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                                        case r0.is_ok of {
                                                                            false -> r0;
                                                                            true -> {
                                                                                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "int64", context, world);
                                                                                case r1.is_ok of {
                                                                                    false -> r1;
                                                                                    true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                };
                                                                false -> case eff == "move" of {
                                                                    true -> case ac == 3 of {
                                                                        false -> type_checker_core@type_check_err("E2005", expr.location, "move expects 3 arguments: (actor_ref, from_core: int64, to_core: int64)", "sect15");
                                                                        true -> {
                                                                            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                                            case r0.is_ok of {
                                                                                false -> r0;
                                                                                true -> {
                                                                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "int64", context, world);
                                                                                    case r1.is_ok of {
                                                                                        false -> r1;
                                                                                        true -> {
                                                                                            r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_third_arg_expr(expr), "int64", context, world);
                                                                                            case r2.is_ok of {
                                                                                                false -> r2;
                                                                                                true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    };
                                                                    false -> case eff == "get_registry_actor" of {
                                                                        true -> case ac == 0 of {
                                                                            false -> type_checker_core@type_check_err("E2005", expr.location, "get_registry_actor expects 0 arguments", "sect15");
                                                                            true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                        };
                                                                        false -> case eff == "get_pid_registry" of {
                                                                            true -> case ac == 0 of {
                                                                                false -> type_checker_core@type_check_err("E2005", expr.location, "get_pid_registry expects 0 arguments", "sect15");
                                                                                true -> check_runtime_builtin_return(expr, inferred, expected_type)
                                                                            };
                                                                            false -> type_checker_core@type_check_err("E2005", expr.location, "unknown actor runtime helper", "sect15")
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_self_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 0 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "self expects 0 arguments (§15.1.3)", "sect15");
        true -> case expected_type == "actor_ref" of {
            true -> type_checker_core@type_check_ok();
            false -> case expected_type == "discard" of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2003", expr.location, concat("self returns actor_ref, expected ", expected_type), "sect15")
            }
        }
    }
}

// §15.1.1: optional core placement is exactly one logical core id (`uint64`), or `core_id(uint64)`.
// Lists, `core_set`, `performance_cores`, and `efficiency_cores` are rejected here (not a range or set).
// `which_arg` is e.g. "third argument" for spawn, "fourth argument" for spawn_linked (§15.4.8.3).
fn check_spawn_core_id_placement(place_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], builtin_label: string, which_arg: string) -> TypeCheckResult proc[DeviceIO] {
    case place_e.kind == 20 of {
        true -> type_checker_core@type_check_err("E2005", place_e.location, concat(concat(concat(builtin_label, ": "), which_arg), " must be a single uint64 core id (not a list of cores) §15.1.1"), "sect15");
        false -> case place_e.kind == 1 and (place_e.name == "performance_cores" or place_e.name == "efficiency_cores") of {
            true -> type_checker_core@type_check_err("E2005", place_e.location, concat(concat(concat(builtin_label, ": "), which_arg), " must be a uint64 core id (not performance_cores or efficiency_cores) §15.1.1"), "sect15");
            false -> case place_e.kind == 5 of {
                true -> type_checker_core@type_check_err("E2005", place_e.location, concat(concat(concat(builtin_label, ": "), which_arg), " must be uint64 or core_id(uint64), got atom §15.1.1"), "sect15");
                false -> check_expr(place_e, "uint64", context, world)
            }
        }
    }
}

fn check_actor_spawn_result_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, builtin_label: string, spawn_result_type: string) -> TypeCheckResult {
    case expected_type == spawn_result_type of {
        true -> type_checker_core@type_check_ok();
        false -> case expected_type == "discard" of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(builtin_label, " returns "), spawn_result_type), concat(", expected ", expected_type)), "sect15")
        }
    }
}

fn check_actor_spawn_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], builtin_label: string, spawn_result_type: string) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 or ac == 3 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, concat(concat(builtin_label, " expects 2 or 3 arguments: (initial_state, behavior_fn [, core_id]) §15.1.1"), ""), "sect15");
        true -> {
            init_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
            beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
            case beh_e.kind == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": behavior must be a function identifier"), "sect15");
                true -> {
                    (beh_found: bool, beh_type: string) <- type_checker_core@lookup_symbol_found(context.env, beh_e.name);
                    case beh_found of {
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat(concat(builtin_label, ": unknown behavior function: "), beh_e.name), "sect15");
                        true -> case is_function_type_user_call(beh_type) of {
                            false -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": behavior must be a function"), "sect15");
                            true -> {
                                plain_spawn_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_spawn_plain_not_dangerous_spawn(builtin_label, beh_e, expr, world);
                                case plain_spawn_ok.is_ok of {
                                    false -> plain_spawn_ok;
                                    true -> {
                                        dangerous_spawn_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_spawn_dangerous_requires_dangerous_behavior(builtin_label, beh_e, expr, world);
                                        case dangerous_spawn_ok.is_ok of {
                                            false -> dangerous_spawn_ok;
                                            true -> {
                                sig_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_expressions_actors@validate_spawn_behavior_function_type(builtin_label, beh_type, expr.location);
                                    case sig_ok.is_ok of {
                                    false -> sig_ok;
                                    true -> {
                                        state_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_type);
                                        case len(state_t) == 0 of {
                                            true -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": could not parse behavior state parameter type"), "sect15");
                                            false -> {
                                                r_init: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(init_e, state_t, context, world);
                                                case r_init.is_ok of {
                                                    false -> r_init;
                                                    true -> case ac == 3 of {
                                                        true -> {
                                                            place_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_placement_expr(expr);
                                                            r_pl: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_spawn_core_id_placement(place_e, context, world, builtin_label, "third argument");
                                                            case r_pl.is_ok of {
                                                                false -> r_pl;
                                                                true -> check_actor_spawn_result_type(expr, expected_type, builtin_label, spawn_result_type)
                                                            }
                                                        };
                                                        false -> check_actor_spawn_result_type(expr, expected_type, builtin_label, spawn_result_type)
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Spawn: optional `core_id(x)` wrapper — x must be uint64; result is uint64 (same as spawn's third argument).
// `core_set` and list / performance_cores / efficiency_cores placement forms are not supported for spawn.
fn check_spawn_affinity_constructor_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case expr.name == "core_id" of {
        true -> case ac == 1 of {
            false -> type_checker_core@type_check_err("E2005", expr.location, "core_id expects 1 argument (core id: uint64) §15.1.1", "sect15");
            true -> {
                r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "uint64", context, world);
                case r0.is_ok of {
                    false -> r0;
                    true -> case expected_type == "discard" of {
                        true -> type_checker_core@type_check_ok();
                        false -> case expected_type == "uint64" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("core_id(...) has type uint64, expected ", expected_type), "sect15")
                        }
                    }
                }
            }
        };
        false -> case expr.name == "core_set" of {
            true -> case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "core_set expects 1 argument (core set: List[int64,normal]) §15.1.1", "sect15");
                true -> case expected_type == "uint64" of {
                    true -> type_checker_core@type_check_err("E2005", expr.location, "spawn's third argument is a uint64 core id, not core_set(...) §15.1.1", "sect15");
                    false -> {
                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "List[int64,normal]", context, world);
                        case r1.is_ok of {
                            false -> r1;
                            true -> case expected_type == "discard" of {
                                true -> type_checker_core@type_check_ok();
                                false -> case expected_type == "List[int64,normal]" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("core_set(...) has type List[int64,normal], expected ", expected_type), "sect15")
                                }
                            }
                        }
                    }
                }
            };
            false -> type_checker_core@type_check_err("E2005", expr.location, "internal: not a spawn affinity constructor", "sect15")
        }
    }
}


fn string_starts_with_prefix(s: string, prefix: string) -> bool {
    case len(prefix) == 0 of {
        true -> true;
        false -> case len(s) < len(prefix) of {
            true -> false;
            false -> substring(s, 0, len(prefix)) == prefix
        }
    }
}

fn is_dangerous_module_name_for_spawn(name: string) -> bool {
    case len(name) >= 10 of {
        true -> substring(name, 0, 10) == "dangerous_";
        false -> false
    }
}

fn is_dangerous_qualified_call_for_spawn(name: string) -> bool {
    case type_checker_core@call_name_is_module_qualified(name) of {
        false -> false;
        true -> {
            (qmod: string, _: string) <- type_checker_core@split_module_qualified_call_name(name);
            is_dangerous_module_name_for_spawn(qmod)
        }
    }
}

fn effect_list_has_external_danger_for_spawn(effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) -> bool {
    case effects.is_nil of {
        true -> false;
        false -> case effects.head.name == "external_danger" of {
            true -> true;
            false -> effect_list_has_external_danger_for_spawn(effects.tail)
        }
    }
}

fn statement_chain_has_external_danger_for_spawn(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case expr.kind < 0 of {
        true -> false;
        false -> case expr.kind == 11 of {
            true -> effect_list_has_external_danger_for_spawn(expr.sequence_effects);
            false -> case expr.kind == 6 of {
                true -> statement_chain_has_external_danger_for_spawn(expr.right_expr);
                false -> case expr.kind == 10 of {
                    true -> statement_chain_has_external_danger_for_spawn(expr.right_expr);
                    false -> false
                }
            }
        }
    }
}

fn expr_has_dangerous_qualified_call_for_spawn(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case expr.kind < 0 of {
        true -> false;
        false -> case expr.kind == 4 of {
            true -> case is_dangerous_qualified_call_for_spawn(expr.name) of {
                true -> true;
                false -> expr_has_dangerous_qualified_call_pair_for_spawn(expr.inner, expr.right_expr)
            };
            false -> expr_has_dangerous_qualified_call_pair_for_spawn(expr.inner, expr.right_expr)
        }
    }
}

fn expr_has_dangerous_qualified_call_pair_for_spawn(inner: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, right: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case expr_has_dangerous_qualified_call_for_spawn(inner) of {
        true -> true;
        false -> expr_has_dangerous_qualified_call_for_spawn(right)
    }
}

fn find_fn_body_in_decls_loop(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string) -> (bool, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_expr());
        false -> case decls.head.tag == 0 and decls.head.name == fn_name of {
            true -> (true, decls.head.body);
            false -> find_fn_body_in_decls_loop(decls.tail, fn_name)
        }
    }
}

fn find_fn_body_in_world(world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], fn_name: string) -> (bool, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) {
    case world.is_nil of {
        true -> (false, parser_ast@dummy_expr());
        false -> {
            (found: bool, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) <- find_fn_body_in_decls_loop(world.head.program.declarations, fn_name);
            case found of {
                true -> (true, body);
                false -> find_fn_body_in_world(world.tail, fn_name)
            }
        }
    }
}

fn behavior_uses_dangerous_code(fn_name: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    (found: bool, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) <- find_fn_body_in_world(world, fn_name);
    case found of {
        false -> false;
        true -> case statement_chain_has_external_danger_for_spawn(body) of {
            true -> true;
            false -> expr_has_dangerous_qualified_call_for_spawn(body)
        }
    }
}

fn atom_literal_has_dangerous_registry_prefix(name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case name_e.kind == 5 of {
        true -> string_starts_with_prefix(name_e.value, ":dangerous_");
        false -> false
    }
}

fn check_spawn_plain_not_dangerous_spawn(builtin_label: string, beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    case builtin_label == "spawn" of {
        true -> {
            dangerous_behavior: bool <- behavior_uses_dangerous_code(beh_e.name, world);
            case dangerous_behavior of {
                true -> type_checker_core@type_check_err("E4044", expr.location, "spawn cannot start a behavior that uses external_danger or dangerous_* code; use spawn_dangerous and bind the result as dangerous_actor_ref", "sect4");
                false -> type_checker_core@type_check_ok()
            }
        };
        false -> type_checker_core@type_check_ok()
    }
}

fn check_spawn_dangerous_requires_dangerous_behavior(builtin_label: string, beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    case builtin_label == "spawn_dangerous" of {
        true -> {
            dangerous_behavior: bool <- behavior_uses_dangerous_code(beh_e.name, world);
            case dangerous_behavior of {
                false -> type_checker_core@type_check_err("E4045", expr.location, "spawn_dangerous requires a behavior whose body uses sequence proc[external_danger] or dangerous_* code", "sect4");
                true -> type_checker_core@type_check_ok()
            }
        };
        false -> type_checker_core@type_check_ok()
    }
}

fn check_cast_registered_registry_rules(name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> TypeCheckResult {
    case atom_literal_has_dangerous_registry_prefix(name_e) of {
        true -> type_checker_core@type_check_err("E4048", name_e.location, "cast_registered must not use registry names whose spelling begins with :dangerous_; use cast_dangerous_registered (spec 4.9)", "sect4");
        false -> type_checker_core@type_check_ok()
    }
}

fn check_cast_dangerous_registered_registry_rules(name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> TypeCheckResult {
    case atom_literal_has_dangerous_registry_prefix(name_e) of {
        true -> type_checker_core@type_check_ok();
        false -> type_checker_core@type_check_err("E4049", name_e.location, "cast_dangerous_registered requires the registration atom to begin with :dangerous_ (spec 4.9)", "sect4")
    }
}

fn check_spawn_registered_registry_rules(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    eff: string <- type_checker_core@user_call_base_function_name(expr.name);
    case eff == "spawn_dangerous_registered" of {
        true -> case atom_literal_has_dangerous_registry_prefix(name_e) of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E4046", name_e.location, "spawn_dangerous_registered requires the registration atom to begin with :dangerous_", "sect4")
        };
        false -> case eff == "spawn_registered" of {
            true -> {
                dangerous_behavior: bool <- behavior_uses_dangerous_code(beh_e.name, world);
                case dangerous_behavior of {
                    true -> type_checker_core@type_check_err("E4043", name_e.location, "spawn_registered: actors whose behavior uses dangerous code must be registered with spawn_dangerous_registered and an atom whose spelling begins with 'dangerous_' (spec 4.7)", "sect4");
                    false -> type_checker_core@type_check_ok()
                }
            };
            false -> type_checker_core@type_check_ok()
        }
    }
}

fn check_actor_spawn_registered_family_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], builtin_label: string, spawn_result_type: string) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 3 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, concat(concat(builtin_label, " expects 3 arguments: (initial_state, behavior_fn, name: atom)"), ""), "sect15");
        true -> {
            init_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
            beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
            name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_third_arg_expr(expr);
            case beh_e.kind == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": behavior must be a function identifier"), "sect15");
                true -> {
                    (beh_found: bool, beh_type: string) <- type_checker_core@lookup_symbol_found(context.env, beh_e.name);
                    case beh_found of {
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat(concat(builtin_label, ": unknown behavior function: "), beh_e.name), "sect15");
                        true -> case is_function_type_user_call(beh_type) of {
                            false -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": behavior must be a function"), "sect15");
                            true -> {
                                sig_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_expressions_actors@validate_spawn_behavior_function_type(builtin_label, beh_type, expr.location);
                                case sig_ok.is_ok of {
                                    false -> sig_ok;
                                    true -> {
                                        state_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_type);
                                        case len(state_t) == 0 of {
                                            true -> type_checker_core@type_check_err("E2005", expr.location, concat(builtin_label, ": could not parse behavior state parameter type"), "sect15");
                                            false -> {
                                                r_init: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(init_e, state_t, context, world);
                                                case r_init.is_ok of {
                                                    false -> r_init;
                                                    true -> {
                                                        r_name: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(name_e, "atom", context, world);
                                                        case r_name.is_ok of {
                                                            false -> r_name;
                                                            true -> {
                                                                reg_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_spawn_registered_registry_rules(expr, name_e, beh_e, world);
                                                                case reg_ok.is_ok of {
                                                                    false -> reg_ok;
                                                                    true -> check_actor_spawn_result_type(expr, expected_type, builtin_label, spawn_result_type)
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_spawn_registered_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    check_actor_spawn_registered_family_call(expr, expected_type, context, world, "spawn_registered", "actor_ref")
}

fn check_actor_spawn_dangerous_registered_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    check_actor_spawn_registered_family_call(expr, expected_type, context, world, "spawn_dangerous_registered", "dangerous_actor_ref")
}

fn check_supervisor_spawn_result_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, builtin_label: string) -> TypeCheckResult {
    case expected_type == "supervisor_ref" of {
        true -> type_checker_core@type_check_ok();
        false -> case expected_type == "discard" of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(builtin_label, " returns supervisor_ref, expected "), expected_type), "sect15")
        }
    }
}

fn check_spawn_registered_supervisor_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 3 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "spawn_registered_supervisor expects 3 arguments: (SupervisorImplType, initial_state, name: atom)", "sect15.4.13");
        true -> {
            sup_type_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- expr.inner;
            init_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
            name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_third_arg_expr(expr);
            case sup_type_e.kind == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "spawn_registered_supervisor: first argument must name the supervisor implementation type", "sect15.4.13");
                true -> {
                    r_init: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(init_e, "discard", context, world);
                    case r_init.is_ok of {
                        false -> r_init;
                        true -> {
                            r_name: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(name_e, "atom", context, world);
                            case r_name.is_ok of {
                                false -> r_name;
                                true -> check_supervisor_spawn_result_type(expr, expected_type, "spawn_registered_supervisor")
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_call_supervisor_result_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64) -> TypeCheckResult {
    inferred: string <- type_checker_expressions_actors@supervisor_reply_record_type();
    case expected_type == "discard" of {
        true -> type_checker_core@type_check_ok();
        false -> case type_checker_tuple_decompose_helpers@types_equal_surface(inferred, expected_type) of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat("call_supervisor returns ", concat(inferred, concat(", expected ", expected_type))), "sect15.4.13")
        }
    }
}

fn check_call_supervisor_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "call_supervisor expects 2 arguments: (supervisor_ref, SupervisorMessage)", "sect15.4.13");
        true -> {
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "supervisor_ref", context, world);
            r0_actor: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- case r0.is_ok of {
                true -> r0;
                false -> check_expr(expr.inner, "actor_ref", context, world)
            };
            case r0_actor.is_ok of {
                false -> r0_actor;
                true -> {
                    msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, "discard", context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> {
                            rw: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_supervisor@supervisor_walk_validate_child_spec_agent_type_atoms(msg_e);
                            case rw.is_ok of {
                                false -> rw;
                                true -> check_call_supervisor_result_type(expr, expected_type)
                            }
                        }
                    }
                }
            }
        }
    }
}

// §15.4.8.3: spawn_linked(initial_state, behavior_fn, agent_type: atom [, core_id]) -> actor_ref.
// Supervisors plan Phase D: behavior-only call site (§15.4.8.3).
fn check_actor_spawn_linked_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    type_checker_core@type_check_err("E2091", expr.location, "spawn_linked is no longer part of the public supervisor surface; use spawn_registered for actors or spawn_registered_supervisor for supervisors", "sect15.4.13")
}

fn check_actor_spawn_linked_call_removed_old(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "spawn_linked may only be called from within an actor behavior function (§15.4.8.3; supervisors plan Phase D)", "sect15.4.8.3");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 3 or ac == 4 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "spawn_linked expects 3 or 4 arguments: (initial_state, behavior_fn, agent_type: atom [, core_id]) §15.4.8.3", "sect15");
                true -> {
            init_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
            beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
            agent_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_third_arg_expr(expr);
            case beh_e.kind == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "spawn_linked: behavior must be a function identifier", "sect15");
                true -> {
                    (beh_found: bool, beh_type: string) <- type_checker_core@lookup_symbol_found(context.env, beh_e.name);
                    case beh_found of {
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat("spawn_linked: unknown behavior function: ", beh_e.name), "sect15");
                        true -> case is_function_type_user_call(beh_type) of {
                            false -> type_checker_core@type_check_err("E2005", expr.location, "spawn_linked: behavior must be a function", "sect15");
                            true -> {
                                sig_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_expressions_actors@validate_spawn_behavior_function_type("spawn_linked", beh_type, expr.location);
                                case sig_ok.is_ok of {
                                    false -> sig_ok;
                                    true -> {
                                        state_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_type);
                                        case len(state_t) == 0 of {
                                            true -> type_checker_core@type_check_err("E2005", expr.location, "spawn_linked: could not parse behavior state parameter type", "sect15");
                                            false -> {
                                                r_init: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(init_e, state_t, context, world);
                                                case r_init.is_ok of {
                                                    false -> r_init;
                                                    true -> {
                                                        r_atom: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(agent_e, "atom", context, world);
                                                        case r_atom.is_ok of {
                                                            false -> r_atom;
                                                            true -> case ac == 4 of {
                                                                true -> {
                                                                    place_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_fourth_arg_expr(expr);
                                                                    r_pl: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_spawn_core_id_placement(place_e, context, world, "spawn_linked", "fourth argument");
                                                                    case r_pl.is_ok of {
                                                                        false -> r_pl;
                                                                        true -> check_actor_spawn_result_type(expr, expected_type, "spawn_linked", "actor_ref")
                                                                    }
                                                                };
                                                                false -> check_actor_spawn_result_type(expr, expected_type, "spawn_linked", "actor_ref")
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
        }
    }
}

// §15.4.8.5: link(target: actor_ref) -> :ok proc[concurrency]. Supervisors plan Phase D: behavior-only.
fn check_actor_link_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "link may only be called from within an actor behavior function (§15.4.8.5; supervisors plan Phase D)", "sect15.4.8.5");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "link expects 1 argument: (target: actor_ref) §15.4.8.5", "sect15");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("link returns atom (:ok), expected ", expected_type), "sect15")
                        }
                    }
                }
            }
        }
    }
}

fn actor_ref_supervisor_marker_name(var_name: string) -> string {
    concat("__actor_sup__", var_name)
}

fn check_add_child_parent_supervisor(parent_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, location: { file: string, line: int64, column: int64, offset: int64 }) -> TypeCheckResult {
    case parent_e.kind == 1 of {
        false -> type_checker_core@type_check_ok();
        true -> {
            (marked: bool, _: string) <- type_checker_core@lookup_symbol_found(context.env, actor_ref_supervisor_marker_name(parent_e.name));
            case marked of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2091", location, "add_child parent must be an actor_ref spawned from a state type implementing Supervisor", "sect15.4.13")
            }
        }
    }
}

// Explicit supervisor parent/child registration: add_child(parent: actor_ref, child: actor_ref) -> :ok.
// The parent ref is accepted only when this type-checking pass can prove it came from a Supervisor impl spawn.
fn check_add_child_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "add_child expects 2 arguments: (parent: actor_ref implementing Supervisor, child: actor_ref)", "sect15.4.13");
        true -> {
            parent_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- expr.inner;
            child_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
            rp: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(parent_e, "actor_ref", context, world);
            case rp.is_ok of {
                false -> rp;
                true -> {
                    rs: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_add_child_parent_supervisor(parent_e, context, expr.location);
                    case rs.is_ok of {
                        false -> rs;
                        true -> {
                            rc: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(child_e, "actor_ref", context, world);
                            case rc.is_ok of {
                                false -> rc;
                                true -> case expected_type == "atom" or expected_type == "discard" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("add_child returns atom (:ok), expected ", expected_type), "sect15.4.13")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// §15.4.8.6: monitor(target: actor_ref) -> monitor_ref proc[concurrency].
// Supervisors plan Phase G: `DOWN` to standard mailbox only — call site must be the monitoring actor
// (same static rule as `link` §15.4.8.5: behavior function where `self()` is defined).
fn check_actor_monitor_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "monitor may only be called from within an actor behavior function (§15.4.8.6; DOWN to standard mailbox; supervisors plan Phase G)", "sect15.4.8.6");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "monitor expects 1 argument: (target: actor_ref) §15.4.8.6", "sect15");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "monitor_ref" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("monitor returns monitor_ref, expected ", expected_type), "sect15")
                        }
                    }
                }
            }
        }
    }
}

// §15.4.8.6: demonitor(ref: monitor_ref) -> :ok proc[concurrency]. Phase G: same behavior-only surface as `monitor`.
fn check_actor_demonitor_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "demonitor may only be called from within an actor behavior function (§15.4.8.6; supervisors plan Phase G)", "sect15.4.8.6");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "demonitor expects 1 argument: (ref: monitor_ref) §15.4.8.6", "sect15");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "monitor_ref", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("demonitor returns atom (:ok), expected ", expected_type), "sect15")
                        }
                    }
                }
            }
        }
    }
}

// supervisors_implementation_development_plan Phase B: enqueue synthetic int64 on target's supervision ingress (trial / Phase C precursor).
fn check_supervision_ingress_enqueue_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "supervision_ingress_enqueue may only be called from within an actor behavior function (supervisors plan Phase B)", "sect15.4.9");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 2 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "supervision_ingress_enqueue expects 2 arguments: (target: actor_ref, payload: int64)", "sect15");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> {
                            p_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(p_e, "int64", context, world);
                            case r1.is_ok of {
                                false -> r1;
                                true -> case expected_type == "atom" or expected_type == "discard" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("supervision_ingress_enqueue returns atom (:ok), expected ", expected_type), "sect15")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// supervisors_implementation_development_plan Phase C: behavior-only accessors over the structured exit
// notification delivered by `_silica_rt_actor_deliver_exit` (§15.4.10–§15.4.11).
fn check_supervision_wait_and_drain_one_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "supervision_wait_and_drain_one may only be called from within an actor behavior function (supervisors plan Phase C)", "sect15.4.10");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 0 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "supervision_wait_and_drain_one expects 0 arguments", "sect15.4.10");
                true -> case expected_type == "int64" or expected_type == "discard" of {
                    true -> type_checker_core@type_check_ok();
                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("supervision_wait_and_drain_one returns int64 (failure payload pointer), expected ", expected_type), "sect15.4.10")
                }
            }
        }
    }
}

fn check_child_table_first_ref_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "child_table_first_ref may only be called from within an actor behavior function (supervisors plan Phase E e4a)", "sect15.4.13");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 0 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "child_table_first_ref expects 0 arguments", "sect15.4.13");
                true -> case expected_type == "int64" or expected_type == "actor_ref" or expected_type == "discard" of {
                    true -> type_checker_core@type_check_ok();
                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("child_table_first_ref returns int64 (actor_ref as pointer), expected ", expected_type), "sect15.4.13")
                }
            }
        }
    }
}

fn check_child_table_row_ref_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "child_table_row_ref may only be called from within an actor behavior function (supervisors plan Phase E e4f)", "sect15.4.13");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "child_table_row_ref expects 1 argument: (row_index: int64)", "sect15.4.13");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "int64" or expected_type == "actor_ref" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("child_table_row_ref returns int64 (actor_ref as pointer), expected ", expected_type), "sect15.4.13")
                        }
                    }
                }
            }
        }
    }
}

// Phase E e4b: kill_abnormal(child_ref) -> atom :ok — trial-only injection point that flips the target ACB's
// pending_exit_reason_tag to 1 (abnormal) before driving the alive=0 + cond_signal teardown. Used to exercise
// :transient restart policy without depending on a divide-by-zero / segfault path that does not yet exist on
// the user surface (supervisors_implementation_development_plan Phase E e4b).
// Phase I — wait_for_exit/0 — main/0 only (supervisors_implementation_development_plan.md §4).
fn check_wait_for_exit_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_main_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "wait_for_exit may only be called from within main/0 (Phase I)", "sect15.4");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 0 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "wait_for_exit expects 0 arguments", "sect15.4");
                true -> case expected_type == "int64" or expected_type == "discard" of {
                    true -> type_checker_core@type_check_ok();
                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("wait_for_exit returns int64, expected ", expected_type), "sect15.4")
                }
            }
        }
    }
}

fn check_kill_abnormal_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "kill_abnormal may only be called from within an actor behavior function (supervisors plan Phase E e4b)", "sect15.4.13");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "kill_abnormal expects 1 argument: (target: actor_ref | atom)", "sect15.4.13");
                true -> {
                    r0_actor: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                    r0_any: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- case r0_actor.is_ok of {
                        true -> r0_actor;
                        false -> check_expr(expr.inner, "atom", context, world)
                    };
                    case r0_any.is_ok of {
                        false -> r0_any;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("kill_abnormal returns atom, expected ", expected_type), "sect15.4.13")
                        }
                    }
                }
            }
        }
    }
}

fn check_failure_reason_tag_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "failure_reason_tag may only be called from within an actor behavior function (supervisors plan Phase C)", "sect15.4.11");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "failure_reason_tag expects 1 argument: (payload: int64)", "sect15.4.11");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "int64" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("failure_reason_tag returns int64, expected ", expected_type), "sect15.4.11")
                        }
                    }
                }
            }
        }
    }
}

fn check_failure_reason_atom_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "failure_reason_atom may only be called from within an actor behavior function (supervisors plan Phase C)", "sect15.4.11");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "failure_reason_atom expects 1 argument: (payload: int64)", "sect15.4.11");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("failure_reason_atom returns atom, expected ", expected_type), "sect15.4.11")
                        }
                    }
                }
            }
        }
    }
}

fn check_failure_agent_type_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "failure_agent_type may only be called from within an actor behavior function (supervisors plan Phase C)", "sect15.4.11");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "failure_agent_type expects 1 argument: (payload: int64)", "sect15.4.11");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("failure_agent_type returns atom, expected ", expected_type), "sect15.4.11")
                        }
                    }
                }
            }
        }
    }
}

fn check_failure_release_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "failure_release may only be called from within an actor behavior function (supervisors plan Phase C)", "sect15.4.11");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "failure_release expects 1 argument: (payload: int64)", "sect15.4.11");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> case expected_type == "atom" or expected_type == "discard" of {
                            true -> type_checker_core@type_check_ok();
                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("failure_release returns atom (:ok), expected ", expected_type), "sect15.4.11")
                        }
                    }
                }
            }
        }
    }
}

// Phase E e3b2: start_child(child_spec) -> actor_ref — supervisor-only dynamic child spawn (§15.4.13.3).
fn check_start_child_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case type_checker_expressions_actors@type_context_has_behavior_marker(context) of {
        false -> type_checker_core@type_check_err("E2091", expr.location, "start_child may only be called from within an actor behavior function (supervisors plan Phase E e3b2, spec sect15.4.13.3)", "sect15.4.13.3");
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 1 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "start_child expects 1 argument: (spec: child_spec record)", "sect15.4.13.3");
                true -> {
                    r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "discard", context, world);
                    case r0.is_ok of {
                        false -> r0;
                        true -> {
                            rw: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_supervisor@supervisor_walk_validate_child_spec_agent_type_atoms(expr.inner);
                            case rw.is_ok of {
                                false -> rw;
                                true -> case expected_type == "actor_ref" or expected_type == "discard" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("start_child returns actor_ref, expected ", expected_type), "sect15.4.13.3")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_message_satisfies_am(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext) -> TypeCheckResult {
    has_postfix: bool <- type_checker_expressions_actors@call_has_impl_am_marker(expr);
    case has_postfix of {
        true -> type_checker_core@type_check_ok();
        false -> type_checker_core@type_check_err("E2005", msg_e.location, "message must satisfy ActorMessage (§16.3.2); use `expr impl ActorMessage {}`", "sect16")
    }
}

// cast/send/call accept actor_ref or dangerous_actor_ref as the target (same runtime handle; distinct types at bind sites).
fn check_actor_message_target(ref_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ref_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(ref_e, context);
    case ref_type == "actor_ref" or ref_type == "dangerous_actor_ref" of {
        true -> type_checker_core@type_check_ok();
        false -> check_expr(ref_e, "actor_ref", context, world)
    }
}

fn check_actor_call_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "call expects 2 arguments: (actor_ref, message) §16.1.1", "sect16");
        true -> {
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_target(expr.inner, context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    beh_type: string <- type_checker_expressions_actors@lookup_actor_behavior_type(expr, context);
                    msg_expected: string <- case len(beh_type) > 0 of {
                        true -> type_checker_expressions_actors@parse_behavior_msg_param_type(beh_type);
                        false -> "discard"
                    };
                    msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, msg_expected, context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> {
                            am_check: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_satisfies_am(expr, msg_e, context);
                            case am_check.is_ok of {
                                false -> am_check;
                                true -> {
                                    reply_type: string <- case len(beh_type) > 0 of {
                                        true -> type_checker_expressions_actors@parse_behavior_reply_type(beh_type);
                                        false -> ""
                                    };
                                    case len(reply_type) > 0 of {
                                        true -> case expected_type == reply_type or expected_type == "discard" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("call returns ", reply_type), concat(", expected ", expected_type)), "sect16")
                                        };
                                        false -> type_checker_core@type_check_ok()
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_call_registered_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "call_registered expects 2 arguments: (registered_name: atom, message) §16.1.1", "sect16");
        true -> {
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "atom", context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, "discard", context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> check_actor_message_satisfies_am(expr, msg_e, context)
                    }
                }
            }
        }
    }
}

fn check_actor_cast_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "cast expects 2 arguments: (actor_ref, message) §16.1.2", "sect16");
        true -> {
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_target(expr.inner, context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    beh_type: string <- type_checker_expressions_actors@lookup_actor_behavior_type(expr, context);
                    msg_expected: string <- case len(beh_type) > 0 of {
                        true -> type_checker_expressions_actors@parse_behavior_msg_param_type(beh_type);
                        false -> "discard"
                    };
                    msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, msg_expected, context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> {
                            am_check: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_satisfies_am(expr, msg_e, context);
                            case am_check.is_ok of {
                                false -> am_check;
                                true -> case expected_type == "boolean" or expected_type == "discard" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("cast returns boolean, expected ", expected_type), "sect16")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_cast_registered_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "cast_registered expects 2 arguments: (registered_name: atom, message) §16.1.2", "sect16");
        true -> {
            name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- expr.inner;
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(name_e, "atom", context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    reg_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_cast_registered_registry_rules(name_e);
                    case reg_ok.is_ok of {
                        false -> reg_ok;
                        true -> {
                            msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, "discard", context, world);
                            case r1.is_ok of {
                                false -> r1;
                                true -> {
                                    am_check: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_satisfies_am(expr, msg_e, context);
                                    case am_check.is_ok of {
                                        false -> am_check;
                                        true -> case expected_type == "boolean" or expected_type == "discard" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("cast_registered returns boolean, expected ", expected_type), "sect16")
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_cast_dangerous_registered_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "cast_dangerous_registered expects 2 arguments: (registered_name: atom, message) §16.1.2", "sect16");
        true -> {
            name_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- expr.inner;
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(name_e, "atom", context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    reg_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_cast_dangerous_registered_registry_rules(name_e);
                    case reg_ok.is_ok of {
                        false -> reg_ok;
                        true -> {
                            msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, "discard", context, world);
                            case r1.is_ok of {
                                false -> r1;
                                true -> {
                                    am_check: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_satisfies_am(expr, msg_e, context);
                                    case am_check.is_ok of {
                                        false -> am_check;
                                        true -> case expected_type == "boolean" or expected_type == "discard" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> type_checker_core@type_check_err("E2003", expr.location, concat("cast_dangerous_registered returns boolean, expected ", expected_type), "sect16")
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_send_builtin(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 2 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, "send expects 2 arguments: (actor_ref, message) §16.1", "sect16");
        true -> {
            r0: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
            case r0.is_ok of {
                false -> r0;
                true -> {
                    beh_type: string <- type_checker_expressions_actors@lookup_actor_behavior_type(expr, context);
                    msg_expected: string <- case len(beh_type) > 0 of {
                        true -> type_checker_expressions_actors@parse_behavior_msg_param_type(beh_type);
                        false -> "discard"
                    };
                    msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- call_second_arg_expr(expr);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(msg_e, msg_expected, context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> {
                            am_check: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_actor_message_satisfies_am(expr, msg_e, context);
                            case am_check.is_ok of {
                                false -> am_check;
                                true -> case expected_type == "atom" or expected_type == "discard" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat("send returns atom, expected ", expected_type), "sect16")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_actor_concurrency_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    eff: string <- type_checker_core@user_call_base_function_name(expr.name);
    case is_actor_runtime_helper_name(eff) of {
        true -> check_actor_runtime_helper_call(expr, expected_type, context, world);
        false -> case eff == "recv" of {
            true -> type_checker_core@type_check_err("E2005", expr.location, "recv() is runtime-internal only; user code cannot call recv (§15.1.2)", "sect15");
            false -> case eff == "self" of {
                true -> check_actor_self_call(expr, expected_type, context);
                false -> case eff == "spawn" of {
                    true -> check_actor_spawn_call(expr, expected_type, context, world, "spawn", "actor_ref");
                    false -> case eff == "spawn_dangerous" of {
                        true -> check_actor_spawn_call(expr, expected_type, context, world, "spawn_dangerous", "dangerous_actor_ref");
                    false -> case eff == "spawn_dangerous_registered" of {
                        true -> check_actor_spawn_dangerous_registered_call(expr, expected_type, context, world);
                        false -> case eff == "spawn_registered" of {
                        true -> check_actor_spawn_registered_call(expr, expected_type, context, world);
                        false -> case eff == "spawn_registered_supervisor" of {
                            true -> check_spawn_registered_supervisor_call(expr, expected_type, context, world);
                            false -> case eff == "spawn_linked" of {
                                true -> check_actor_spawn_linked_call(expr, expected_type, context, world);
                                false -> case eff == "call_supervisor" of {
                                    true -> check_call_supervisor_builtin(expr, expected_type, context, world);
                                    false -> case eff == "link" of {
                            true -> check_actor_link_call(expr, expected_type, context, world);
                            false -> case eff == "add_child" of {
                                true -> check_add_child_call(expr, expected_type, context, world);
                                false -> case eff == "monitor" of {
                                    true -> check_actor_monitor_call(expr, expected_type, context, world);
                                    false -> case eff == "demonitor" of {
                                        true -> check_actor_demonitor_call(expr, expected_type, context, world);
                                    false -> case eff == "supervision_ingress_enqueue" of {
                                        true -> check_supervision_ingress_enqueue_call(expr, expected_type, context, world);
                                        false -> case eff == "supervision_wait_and_drain_one" of {
                                            true -> check_supervision_wait_and_drain_one_call(expr, expected_type, context, world);
                                            false -> case eff == "failure_reason_tag" of {
                                                true -> check_failure_reason_tag_call(expr, expected_type, context, world);
                                            false -> case eff == "failure_reason_atom" of {
                                                true -> check_failure_reason_atom_call(expr, expected_type, context, world);
                                                false -> case eff == "failure_agent_type" of {
                                                    true -> check_failure_agent_type_call(expr, expected_type, context, world);
                                                    false -> case eff == "failure_release" of {
                                                        true -> check_failure_release_call(expr, expected_type, context, world);
                                                        false -> case eff == "start_child" of {
                                                            true -> check_start_child_call(expr, expected_type, context, world);
                                                            false -> case eff == "child_table_first_ref" of {
                                                                true -> check_child_table_first_ref_call(expr, expected_type, context, world);
                                                                false -> case eff == "child_table_row_ref" of {
                                                                    true -> check_child_table_row_ref_call(expr, expected_type, context, world);
                                                                    false -> case eff == "kill_abnormal" of {
                                                                        true -> check_kill_abnormal_call(expr, expected_type, context, world);
                                                                        false -> case eff == "wait_for_exit" of {
                                                                            true -> check_wait_for_exit_call(expr, expected_type, context, world);
                                                                            false -> case eff == "call" of {
                                                                                true -> check_actor_call_builtin(expr, expected_type, context, world);
                                                                                false -> case eff == "call_registered" of {
                                                                                    true -> check_actor_call_registered_builtin(expr, expected_type, context, world);
                                                                                    false -> case eff == "cast" of {
                                                                                        true -> check_actor_cast_builtin(expr, expected_type, context, world);
                                                                                        false -> case eff == "cast_registered" of {
                                                                                            true -> check_actor_cast_registered_builtin(expr, expected_type, context, world);
                                                                                            false -> case eff == "cast_dangerous_registered" of {
                                                                                                true -> check_actor_cast_dangerous_registered_builtin(expr, expected_type, context, world);
                                                                                                false -> check_actor_send_builtin(expr, expected_type, context, world)
                                                                                            }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                    }
                                                                }
                                                            }
                                                        }
                                                    }}
                                                }
                                            }
                                        }
                                    }
                                    }
                                }
                            }}
                                }
                            }
                        }
                        }
                        }
                    }
                }
            }
        }
    }
}

fn is_float_special_const_name(name: string) -> bool {
    name == "float16_nan" or name == "float16_inf" or name == "float16_neg_inf" or name == "float32_nan" or name == "float32_inf" or name == "float32_neg_inf" or name == "float64_nan" or name == "float64_inf" or name == "float64_neg_inf"
}

fn float_special_const_type(name: string) -> string {
    case substring(name, 0, 7) == "float16" of {
        true -> "float16";
        false -> case substring(name, 0, 7) == "float32" of {
            true -> "float32";
            false -> "float64"
        }
    }
}

fn is_float_predicate_name(name: string) -> bool {
    name == "float16_is_nan" or name == "float16_is_inf" or name == "float16_is_finite" or name == "float32_is_nan" or name == "float32_is_inf" or name == "float32_is_finite" or name == "float64_is_nan" or name == "float64_is_inf" or name == "float64_is_finite"
}

fn float_predicate_arg_type(name: string) -> string {
    case substring(name, 0, 7) == "float16" of {
        true -> "float16";
        false -> case substring(name, 0, 7) == "float32" of {
            true -> "float32";
            false -> "float64"
        }
    }
}

fn check_float_special_const_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64) -> TypeCheckResult {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    ret: string <- float_special_const_type(expr.name);
    case ac == 0 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " expects 0 arguments"), "sect6");
        true -> case expected_type == ret or expected_type == "discard" of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(expr.name, " returns "), ret), concat(", expected ", expected_type)), "sect6")
        }
    }
}

fn check_float_predicate_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac == 1 of {
        false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " expects 1 argument"), "sect6");
        true -> {
            arg_t: string <- float_predicate_arg_type(expr.name);
            r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, arg_t, context, world);
            case r.is_ok of {
                false -> r;
                true -> case expected_type == "bool" or expected_type == "boolean" or expected_type == "discard" of {
                    true -> type_checker_core@type_check_ok();
                    false -> type_checker_core@type_check_err("E2003", expr.location, concat(expr.name, concat(" returns bool, expected ", expected_type)), "sect6")
                }
            }
        }
    }
}

// Ordering identity built-ins (Layer 1 §7.2).
fn is_ordering_identity_builtin_call(call_name: string) -> bool {
    call_name == "ordering_identity_fresh_env" or call_name == "ordering_identity_materialize" or call_name == "ordering_identity_same" or call_name == "ordering_identity_bundle_make"
}

fn is_collection_result_builtin_call(call_name: string) -> bool {
    call_name == "comparator_result_valid" or call_name == "comparator_result_validate"
}

fn is_checked_int64_builtin_call(call_name: string) -> bool {
    call_name == "checked_int64_add" or call_name == "checked_int64_mul" or call_name == "checked_int64_add1" or call_name == "checked_int64_byte_size"
}

fn checked_int64_result_type_ok(expected_type: int64) -> bool {
    expected_type == "(boolean, int64)"
}

fn check_checked_int64_builtin_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case checked_int64_result_type_ok(expected_type) of {
        false -> type_checker_core@type_check_err("E2003", expr.location, concat("checked int64 built-in returns (boolean, int64), got ", expected_type), "common_contract§9");
        true -> case expr.name of {
            "checked_int64_add1" -> check_expr(expr.inner, "int64", context, world);
            "checked_int64_byte_size" -> {
                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                case r1.is_ok of {
                    false -> r1;
                    true -> check_expr(call_second_arg_expr(expr), "int64", context, world)
                }
            };
            _: string -> {
                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                case r1.is_ok of {
                    false -> r1;
                    true -> check_expr(call_second_arg_expr(expr), "int64", context, world)
                }
            }
        }
    }
}

fn check_collection_result_builtin_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "atom", context, world);
    case arg_result.is_ok of {
        false -> arg_result;
        true -> case expr.name of {
            "comparator_result_valid" -> case expected_type == "boolean" of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2003", expr.location, concat("comparator_result_valid returns boolean, got ", expected_type), "common_contract§4")
            };
            "comparator_result_validate" -> case expected_type == "atom" of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2003", expr.location, concat("comparator_result_validate returns atom, got ", expected_type), "common_contract§4")
            };
            _: string -> type_checker_core@type_check_err("E2005", expr.location, concat("unknown collection result built-in: ", expr.name), "common_contract§4")
        }
    }
}

fn type_str_has_arrow_token(s: string, i: int64) -> bool {
    case i + 4 > len(s) of {
        true -> false;
        false -> case substring(s, i, i + 4) == " -> " of {
            true -> true;
            false -> type_str_has_arrow_token(s, i + 1)
        }
    }
}

fn is_fn_type_string(type_str: string) -> bool {
    case len(type_str) >= 3 and substring(type_str, 0, 3) == "fn(" of {
        true -> true;
        false -> case len(type_str) >= 4 and substring(type_str, 0, 4) == "fn (" of {
            true -> true;
            false -> type_str_has_arrow_token(type_str, 0)
        }
    }
}

fn check_ordering_identity_builtin_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.name of {
        "ordering_identity_fresh_env" -> case expected_type == "int64" of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", expr.location, concat("ordering_identity_fresh_env returns int64, got ", expected_type), "common_contract§5")
        };
        "ordering_identity_materialize" -> {
            fn_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
            case is_fn_type_string(fn_type) of {
                false -> type_checker_core@type_check_err("E2010", expr.location, concat("ordering_identity_materialize: argument must be a function value, got ", fn_type), "common_contract§5");
                true -> case expected_type == "int64" of {
                    false -> type_checker_core@type_check_err("E2003", expr.location, "ordering_identity_materialize returns int64", "common_contract§5");
                    true -> check_expr(expr.inner, fn_type, context, world)
                }
            }
        };
        "ordering_identity_same" -> {
            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
            case r1.is_ok of {
                false -> r1;
                true -> {
                    r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "int64", context, world);
                    case r2.is_ok of {
                        false -> r2;
                        true -> case expected_type == "boolean" of {
                            false -> type_checker_core@type_check_err("E2003", expr.location, "ordering_identity_same returns boolean", "common_contract§5");
                            true -> type_checker_core@type_check_ok()
                        }
                    }
                }
            }
        };
        "ordering_identity_bundle_make" -> {
            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
            case r1.is_ok of {
                false -> r1;
                true -> {
                    r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), "atom", context, world);
                    case r2.is_ok of {
                        false -> r2;
                        true -> case expected_type == "int64" of {
                            false -> type_checker_core@type_check_err("E2003", expr.location, "ordering_identity_bundle_make returns int64", "common_contract§5");
                            true -> type_checker_core@type_check_ok()
                        }
                    }
                }
            }
        };
        _: string -> type_checker_core@type_check_err("E2005", expr.location, concat("unknown ordering identity built-in: ", expr.name), "common_contract§5")
    }
}

fn check_call_builtins_dispatch(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case is_float_special_const_name(expr.name) of {
        true -> check_float_special_const_call(expr, expected_type);
        false -> case is_float_predicate_name(expr.name) of {
            true -> check_float_predicate_call(expr, expected_type, context, world);
            false -> case is_checked_int64_builtin_call(expr.name) of {
        true -> check_checked_int64_builtin_call(expr, expected_type, context, world);
        false -> case is_collection_result_builtin_call(expr.name) of {
        true -> check_collection_result_builtin_call(expr, expected_type, context, world);
        false -> case is_ordering_identity_builtin_call(expr.name) of {
        true -> check_ordering_identity_builtin_call(expr, expected_type, context, world);
        false -> case type_checker_memory_regions@is_region_builtin_call(expr.name) of {
        true -> check_region_builtin_call(expr, expected_type, context, world);
        false -> case is_actor_concurrency_builtin(expr.name) of {
            true -> check_actor_concurrency_call(expr, expected_type, context, world);
            false -> case expr.name == "bnot" of {
            true -> check_bnot_call(expr, expected_type, context, world);
            false -> case expr.name == "negate_unit" of {
            true -> type_checker_core@type_check_err("E2003", expr.location, "negation is not supported for unit", "sect6");
            false -> case len(negate_builtin_arg_type(expr.name)) > 0 of {
                    true -> check_negate_call(expr, negate_builtin_arg_type(expr.name), expected_type, context, world);
                    false -> case expr.name == "print_int64" of {
        true -> check_print_call(expr, "int64", expected_type, context, world);
        false -> case expr.name == "print_int32" of {
            true -> check_print_call(expr, "int32", expected_type, context, world);
            false -> case expr.name == "print_int16" of {
                true -> check_print_call(expr, "int16", expected_type, context, world);
                false -> case expr.name == "print_int8" of {
                    true -> check_print_call(expr, "int8", expected_type, context, world);
                    false -> case expr.name == "print_uint64" of {
                        true -> check_print_call(expr, "uint64", expected_type, context, world);
                        false -> case expr.name == "print_uint32" of {
                            true -> check_print_call(expr, "uint32", expected_type, context, world);
                            false -> case expr.name == "print_uint16" of {
                                true -> check_print_call(expr, "uint16", expected_type, context, world);
                                false -> case expr.name == "print_uint8" of {
                                    true -> check_print_call(expr, "uint8", expected_type, context, world);
                                    false -> case expr.name == "print_bool" of {
                                        true -> {
                                            arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "boolean", context, world);
                                            case arg_result.is_ok of {
                                                false -> arg_result;
                                                true -> case expected_type of {
                                                    "boolean" -> type_checker_core@type_check_ok();
                                                    "atom" -> type_checker_core@type_check_ok();
                                                    "discard" -> type_checker_core@type_check_ok();
                                                    _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("print_bool expects boolean or atom result, got ", expected_type), "sect6")
                                                }
                                            }
                                        };
                                        false -> case expr.name == "print_atom" of {
                                            true -> {
                                                arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "atom", context, world);
                                                case arg_result.is_ok of {
                                                    false -> arg_result;
                                                    true -> case expected_type of {
                                                        "atom" -> type_checker_core@type_check_ok();
                                                        "discard" -> type_checker_core@type_check_ok();
                                                        _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("print_atom expects atom result, got ", expected_type), "sect6")
                                                    }
                                                }
                                            };
                                            false -> case expr.name == "atom_bits" of {
                                                true -> {
                                                    ra: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "atom", context, world);
                                                    case ra.is_ok of {
                                                        false -> ra;
                                                        true -> case expected_type of {
                                                            "int64" -> type_checker_core@type_check_ok();
                                                            "discard" -> type_checker_core@type_check_ok();
                                                            _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("atom_bits expects int64 result, got ", expected_type), "sect6")
                                                        }
                                                    }
                                                };
                                                false -> case expr.name == "actor_ref_bits" of {
                                                    true -> {
                                                        rr: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "actor_ref", context, world);
                                                        case rr.is_ok of {
                                                            false -> rr;
                                                            true -> case expected_type of {
                                                                "int64" -> type_checker_core@type_check_ok();
                                                                "discard" -> type_checker_core@type_check_ok();
                                                                _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("actor_ref_bits expects int64 result, got ", expected_type), "sect6")
                                                            }
                                                        }
                                                    };
                                                    false -> case expr.name == "actor_ref_of_word" of {
                                                        true -> {
                                                            rw: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "int64", context, world);
                                                            case rw.is_ok of {
                                                                false -> rw;
                                                                true -> case expected_type of {
                                                                    "actor_ref" -> type_checker_core@type_check_ok();
                                                                    "discard" -> type_checker_core@type_check_ok();
                                                                    _: string -> type_checker_core@type_check_err("E2003", expr.location, concat("actor_ref_of_word expects actor_ref result, got ", expected_type), "sect6")
                                                                }
                                                            }
                                                        };
                                                        false -> case expr.name == "print_float64" of {
                                                            true -> check_print_call(expr, "float64", expected_type, context, world);
                                                            false -> case expr.name == "print_float32" of {
                                                                true -> {
                                                                    arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "float32", context, world);
                                                                    type_checker_expressions_float32_calls@check_print_float32_call(expr, expected_type, arg_result)
                                                                };
                                                                false -> case expr.name == "print_float16" of {
                                                                    true -> {
                                                                        arg_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "float16", context, world);
                                                                        type_checker_expressions_float32_calls@check_print_float32_call(expr, expected_type, arg_result)
                                                                    };
                                                                    false -> case expr.name == "print_string" of {
                                                                        true -> check_print_call(expr, "string", expected_type, context, world);
                                                                        false -> case expr.name == "print" of {
                                                                        true -> check_print_call(expr, "string", expected_type, context, world);
                                                                        false -> case expr.name == "println" of {
                                                                        true -> check_print_call(expr, "string", expected_type, context, world);
                                                                        false -> case expr.name == "print_string_stderr" of {
                                                                            true -> check_print_call(expr, "string", expected_type, context, world);
                                                                            false -> case expr.name == "println_stderr" of {
                                                                                true -> check_print_call(expr, "string", expected_type, context, world);
                                                                                false -> case expr.name == "read_lines" of {
                                                                                    true -> {
                                                                                        rl: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                        type_checker_expressions_string_calls@check_read_lines_call(expr, expected_type, rl)
                                                                                    };
                                                                                    false -> case expr.name == "file_exists" of {
                                                                                        true -> {
                                                                                            fe: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                            type_checker_expressions_string_calls@check_file_exists_call(expr, expected_type, fe)
                                                                                        };
                                                                                        false -> case expr.name == "delete_file" of {
                                                                                            true -> {
                                                                                                df: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                type_checker_expressions_string_calls@check_delete_file_call(expr, expected_type, df)
                                                                                            };
                                                                                            false -> case expr.name == "append_file" of {
                                                                                                true -> {
                                                                                                    af1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                    af2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "string", context, world);
                                                                                                    type_checker_expressions_string_calls@check_append_file_call(expr, expected_type, af1, af2)
                                                                                                };
                                                                                                false -> case expr.name == "concat" of {
                                                                            true -> {
                                                                                a1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                a2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "string", context, world);
                                                                                type_checker_expressions_string_calls@check_concatenate_call(expr, expected_type, a1, a2)
                                                                            };
                                                                                false -> case expr.name == "concatenate" of {
                                                                                true -> {
                                                                                    a1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                    a2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "string", context, world);
                                                                                    type_checker_expressions_string_calls@check_concatenate_call(expr, expected_type, a1, a2)
                                                                                };
                                                                                false -> case expr.name == "substring" of {
                                                                                    true -> case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
                                                                                        true -> {
                                                                                            s1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                            s2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "int64", context, world);
                                                                                            s3: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.right_expr.inner, "int64", context, world);
                                                                                            type_checker_expressions_string_calls@check_substring_call(expr, expected_type, s1, s2, s3)
                                                                                        };
                                                                                        false -> type_checker_core@type_check_err("E2005", expr.location, "substring requires 3 arguments: (s: string, start: int64, end: int64)", "sect6")
                                                                                    };
                                                                                    false -> case expr.name == "substring_until_char" of {
                                                                                        true -> case expr.right_expr.kind == 4 and expr.right_expr.name == "" of {
                                                                                            true -> {
                                                                                                u1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                u2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "int64", context, world);
                                                                                                u3: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.right_expr.inner, "char", context, world);
                                                                                                type_checker_expressions_string_calls@check_substring_until_char_call(expr, expected_type, u1, u2, u3)
                                                                                            };
                                                                                            false -> type_checker_core@type_check_err("E2005", expr.location, "substring_until_char requires 3 arguments: (s: string, start: int64, char: char)", "sect6")
                                                                                        };
                                                                                        false -> case expr.name == "length_bytes" of {
                                                                                            true -> {
                                                                                                lb: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                type_checker_expressions_string_calls@check_length_bytes_call(expr, expected_type, lb)
                                                                                            };
                                                                                                    false -> case expr.name == "length_chars" of {
                                                                                                    true -> {
                                                                                                        lc: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                        type_checker_expressions_string_calls@check_length_chars_call(expr, expected_type, lc)
                                                                                                    };
                                                                                                    false -> case expr.name == "starts_with" or expr.name == "ends_with" or expr.name == "contains" of {
                                                                                                        true -> {
                                                                                                            p1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                                                                                                            p2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, "string", context, world);
                                                                                                            type_checker_expressions_string_calls@check_string_predicate_call(expr, expected_type, p1, p2)
                                                                                                        };
                                                                                                    false -> case type_checker_lists@is_list_primitive_name(expr.name) of {
                                                                                                        true -> check_list_primitive_call(expr, expected_type, context, world);
                                                                                                        false -> case expr.name == "core_id" or expr.name == "core_set" of {
                                                                                                            true -> check_spawn_affinity_constructor_call(expr, expected_type, context, world);
                                                                                                            false -> check_user_defined_call(expr, expected_type, context, world)
                                                                                                        }
                                                                                                    }
                                                                                                    }
                                                                                                }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                        }
                                                                                        }
                                                                                    }
                                                                                }
                                                                            }
                                                                    }
                                                                }
                                                                }
                                                            }
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
            }
        }
        }
        }
    }
    }
    }
    }
    }
    }
    }
    }
        }
    }
}

// Mirror of type_checker_core lookup_fn_type_by_name_and_module + lookup_function_type_for_call.
// Qualified calls: in-TU string return; also first_at for routing before check_call (must appear above check_call).
fn first_at_in_expr_call_name(s: string, i: int64) -> int64 {
    case i >= len(s) of {
        true -> -1;
        false -> case substring(s, i, i + 1) == "@" of {
            true -> i;
            false -> first_at_in_expr_call_name(s, i + 1)
        }
    }
}

fn lookup_fn_type_by_name_and_module_local(env: ListSymbolEntry, fn_name: string, required_module: string) -> string {
    case env.is_nil of {
        true -> "";
        false -> case instantiated_fn_name@decl_name_matches(env.head.name, fn_name) and env.head.is_effect_alias == false of {
            true -> case len(required_module) == 0 of {
                true -> case env.head.source_module == "" of {
                    true -> env.head.type_id;
                    false -> lookup_fn_type_by_name_and_module_local(env.tail, fn_name, required_module)
                };
                false -> case env.head.source_module == required_module of {
                    true -> env.head.type_id;
                    false -> lookup_fn_type_by_name_and_module_local(env.tail, fn_name, required_module)
                }
            };
            false -> lookup_fn_type_by_name_and_module_local(env.tail, fn_name, required_module)
        }
    }
}

fn lookup_function_type_for_call_local(env: ListSymbolEntry, composite_name: string) -> string {
    at: int64 <- first_at_in_expr_call_name(composite_name, 0);
    case at < 0 of {
        true -> lookup_fn_type_by_name_and_module_local(env, composite_name, "");
        false -> lookup_fn_type_by_name_and_module_local(env, substring(composite_name, at + 1, len(composite_name)), substring(composite_name, 0, at))
    }
}

fn check_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    // Module-qualified calls (spec §19.3.2): use first_at in this TU. Routing via
    // type_checker_core@call_name_is_module_qualified misclassified on bootstrap (deep builtin chain / stack).
    // Supervisors Phase A: `spawn` / `spawn_linked` are intrinsics (§15.4.8.3); module@spawn_linked must type-check like the unqualified form.
    case first_at_in_expr_call_name(expr.name, 0) >= 0 of {
        true -> {
            base: string <- type_checker_core@user_call_base_function_name(expr.name);
            case base == "spawn" or base == "spawn_dangerous" or base == "spawn_registered" or base == "spawn_dangerous_registered" or base == "spawn_registered_supervisor" or base == "spawn_linked" or base == "call_supervisor" or base == "add_child" or base == "link" or base == "monitor" or base == "demonitor" or base == "supervision_ingress_enqueue" or base == "supervision_wait_and_drain_one" or base == "failure_reason_tag" or base == "failure_reason_atom" or base == "failure_agent_type" or base == "failure_release" or base == "start_child" or base == "child_table_first_ref" or base == "child_table_row_ref" or base == "kill_abnormal" or base == "wait_for_exit" or base == "get_registry_actor" or base == "get_pid_registry" or base == "call_registered" or base == "cast_registered" or base == "cast_dangerous_registered" of {
                true -> check_actor_concurrency_call(expr, expected_type, context, world);
                false -> check_user_defined_call(expr, expected_type, context, world)
            }
        };
        false -> check_call_builtins_dispatch(expr, expected_type, context, world)
    }
}

// User-defined function call: lookup in symbol table, parse function type "(T1,T2) -> R", check args and result
// Top-level `->` only: parameter types may contain nested `fn(...) -> ...`; naive substring match on ` -> ` splits there first.
fn outer_arrow_token_pos_call(func_type: string) -> int64 {
    type_checker_expressions_identifiers@find_arrow_idf(func_type, 0, 0, 0, 0)
}

fn trim_trailing_spaces_user_call_from(s: string, i: int64) -> string {
    case i <= 0 of {
        true -> "";
        false -> case substring(s, i - 1, i) == " " of {
            true -> trim_trailing_spaces_user_call_from(s, i - 1);
            false -> substring(s, 0, i)
        }
    }
}

fn trim_trailing_spaces_user_call(s: string) -> string {
    trim_trailing_spaces_user_call_from(s, len(s))
}

fn params_part_before_outer_arrow_call(func_type: string) -> string {
    arrow_at: int64 <- outer_arrow_token_pos_call(func_type);
    case arrow_at < 0 of {
        true -> "";
        false -> trim_trailing_spaces_user_call(trim_leading_user_call(substring(func_type, 0, arrow_at)))
    }
}

fn is_function_type_user_call(type_str: string) -> bool {
    outer_arrow_token_pos_call(type_str) >= 0
}

// Find first comma at depth 0 (top level). Respects parentheses, brackets, and braces in type strings
// so that a single tuple parameter like (int64, int64), ref(R, Space, T), or { x: int64, y: int64 } counts as 1 param.
fn find_top_level_comma_user_call(s: string, i: int64, depth: int64) -> int64 {
    case i >= len(s) of {
        true -> len(s);
        false -> case substring(s, i, i + 1) == "(" or substring(s, i, i + 1) == "[" or substring(s, i, i + 1) == "{" of {
            true -> find_top_level_comma_user_call(s, i + 1, depth + 1);
            false -> case substring(s, i, i + 1) == ")" or substring(s, i, i + 1) == "]" or substring(s, i, i + 1) == "}" of {
                true -> case depth <= 0 of {
                    true -> len(s);
                    false -> find_top_level_comma_user_call(s, i + 1, depth - 1)
                };
                false -> case substring(s, i, i + 1) == "," of {
                    true -> case depth == 0 of {
                        true -> i;
                        false -> find_top_level_comma_user_call(s, i + 1, depth)
                    };
                    false -> find_top_level_comma_user_call(s, i + 1, depth)
                }
            }
        }
    }
}

fn trim_leading_user_call(s: string) -> string {
    trim_leading_user_call_from(s, 0)
}

fn trim_leading_user_call_from(s: string, i: int64) -> string {
    case i >= len(s) of {
        true -> "";
        false -> case substring(s, i, i + 1) == " " of {
            true -> trim_leading_user_call_from(s, i + 1);
            false -> substring(s, i, len(s))
        }
    }
}

fn parse_function_param_count(func_type: string) -> int64 {
    arrow_pos: int64 <- outer_arrow_token_pos_call(func_type);
    case arrow_pos < 0 of {
        true -> 0;
        false -> {
            params_part: string <- params_part_before_outer_arrow_call(func_type);
            case len(params_part) < 1 of {
                true -> 0;
                false -> case substring(params_part, 0, 1) == "(" and substring(params_part, len(params_part) - 1, len(params_part)) == ")" of {
                    true -> {
                        inner: string <- trim_leading_user_call(substring(params_part, 1, len(params_part) - 1));
                        case len(inner) == 0 of {
                            true -> 0;
                            false -> parse_param_count_inner(inner)
                        }
                    };
                    false -> {
                        inner_np: string <- trim_leading_user_call(params_part);
                        case len(inner_np) == 0 of {
                            true -> 0;
                            false -> parse_param_count_inner(inner_np)
                        }
                    }
                }
            }
        }
    }
}

fn parse_param_count_inner(inner: string) -> int64 {
    comma1: int64 <- find_top_level_comma_user_call(inner, 0, 0);
    case comma1 >= len(inner) of {
        true -> 1;
        false -> {
            rest: string <- trim_leading_user_call(substring(inner, comma1 + 1, len(inner)));
            case len(rest) == 0 of {
                true -> 2;
                false -> 1 + parse_param_count_inner(rest)
            }
        }
    }
}

fn parse_function_return_type(func_type: string) -> string {
    arrow_pos: int64 <- outer_arrow_token_pos_call(func_type);
    case arrow_pos < 0 of {
        true -> "";
        false -> trim_leading_user_call(substring(func_type, arrow_pos + 2, len(func_type)))
    }
}

fn get_params_inner_from_func_type(func_type: string) -> string {
    arrow_pos: int64 <- outer_arrow_token_pos_call(func_type);
    case arrow_pos < 0 of {
        true -> "";
        false -> {
            params_part: string <- params_part_before_outer_arrow_call(func_type);
            case len(params_part) < 1 of {
                true -> "";
                false -> case substring(params_part, 0, 1) == "(" and substring(params_part, len(params_part) - 1, len(params_part)) == ")" of {
                    true -> trim_leading_user_call(substring(params_part, 1, len(params_part) - 1));
                    false -> trim_leading_user_call(params_part)
                }
            }
        }
    }
}

fn get_first_param_type(params_inner: string) -> (string, string) {
    case len(params_inner) == 0 of {
        true -> ("", "");
        false -> {
            comma_pos: int64 <- find_top_level_comma_user_call(params_inner, 0, 0);
            case comma_pos >= len(params_inner) of {
                true -> (trim_leading_user_call(params_inner), "");
                false -> {
                    first: string <- trim_leading_user_call(substring(params_inner, 0, comma_pos));
                    rest: string <- trim_leading_user_call(substring(params_inner, comma_pos + 1, len(params_inner)));
                    (first, rest)
                }
            }
        }
    }
}

// Args after the first: kind-4 chain nodes only; anything else (non--1, non-4) does not add args (avoids counting 2 for a single-arg call when the tail is not dummy).
fn count_additional_call_args_user_call(er: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> int64 {
    case er.kind == -1 of {
        true -> 0;
        false -> case er.kind == 4 of {
            true -> 1 + count_additional_call_args_user_call(er.right_expr);
            false -> 0
        }
    }
}

fn count_call_args_user_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> int64 {
    case expr.inner.kind == -1 of {
        true -> 0;
        false -> 1 + count_additional_call_args_user_call(expr.right_expr)
    }
}

// Local helper (same as type_checker_modules / type_checker_callee_env list_param_length). Avoid importing type_checker_modules (declarations_functions → … → expressions cycle).
fn list_param_length_call(ps: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) -> int64 {
    case ps.is_nil of {
        true -> 0;
        false -> 1 + list_param_length_call(ps.tail)
    }
}

// First tag-0 match with same name/arity (legacy fallback when receiver type is unknown or no overload fits).
fn find_function_decl_by_name_arity_call(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_decl());
        false -> case decls.head.tag == 0 of {
            true -> case instantiated_fn_name@decl_name_matches(decls.head.name, fn_name) and list_param_length_call(decls.head.parameters) == arity of {
                true -> (true, decls.head);
                false -> find_function_decl_by_name_arity_call(decls.tail, fn_name, arity)
            };
            false -> find_function_decl_by_name_arity_call(decls.tail, fn_name, arity)
        }
    }
}

fn qualified_call_first_arg_matches_decl(inferred: string, decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    source_params: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@list_param_reverse_with_acc(decl.parameters, parser_ast@nil_list_param());
    case source_params.is_nil of {
        true -> false;
        false -> {
            expect: string <- type_checker_traits@trim_type_text(source_params.head.type_name);
            act: string <- type_checker_traits@trim_type_text(inferred);
            case act == expect of {
                true -> true;
                false -> case type_checker_lists@formal_type_accepts_actual(act, expect, world) of {
                    true -> true;
                    false -> dispatch_actual_fits_impl_parameter(act, expect, world)
                }
            }
        }
    }
}

fn qualified_call_first_arg_exact_matches_decl(inferred: string, decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) -> bool {
    source_params: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@list_param_reverse_with_acc(decl.parameters, parser_ast@nil_list_param());
    case source_params.is_nil of {
        true -> false;
        false -> {
            expect: string <- type_checker_traits@trim_type_text(source_params.head.type_name);
            act: string <- type_checker_traits@trim_type_text(inferred);
            case type_checker_lists@types_equal_strip(act, expect) of {
                true -> true;
                false -> type_checker_tuple_decompose_helpers@types_equal_surface(act, expect)
            }
        }
    }
}

fn qualified_call_return_matches_expected(expected_ty: string, decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case len(expected_ty) == 0 of {
        true -> true;
        false -> case expected_ty == "discard" of {
            true -> true;
            false -> {
                ret: string <- type_checker_traits@trim_type_text(decl.return_type_name);
                exp: string <- type_checker_traits@trim_type_text(expected_ty);
                case type_checker_lists@formal_type_accepts_actual(ret, exp, world) of {
                    true -> true;
                    false -> case type_checker_lists@formal_type_accepts_actual(exp, ret, world) of {
                        true -> true;
                        false -> type_checker_lists@types_equal_strip(ret, exp)
                    }
                }
            }
        }
    }
}

fn decl_exact_matches_qualified_call_target(decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case decl.tag == 0 of {
        false -> false;
        true -> case instantiated_fn_name@decl_name_matches(decl.name, fn_name) and list_param_length_call(decl.parameters) == arity of {
            false -> false;
            true -> case len(recv_ty) == 0 of {
                true -> false;
                false -> case qualified_call_first_arg_exact_matches_decl(recv_ty, decl) of {
                    false -> false;
                    true -> qualified_call_return_matches_expected(expected_ty, decl, world)
                }
            }
        }
    }
}

fn decl_matches_qualified_call_target(decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case decl.tag == 0 of {
        false -> false;
        true -> case instantiated_fn_name@decl_name_matches(decl.name, fn_name) and list_param_length_call(decl.parameters) == arity of {
            false -> false;
            true -> case len(recv_ty) == 0 of {
                true -> qualified_call_return_matches_expected(expected_ty, decl, world);
                false -> case qualified_call_first_arg_matches_decl(recv_ty, decl, world) of {
                    false -> false;
                    true -> qualified_call_return_matches_expected(expected_ty, decl, world)
                }
            }
        }
    }
}

fn find_tag0_decl_for_qualified_call_exact(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_decl());
        false -> case decl_exact_matches_qualified_call_target(decls.head, fn_name, arity, recv_ty, expected_ty, world) of {
            true -> (true, decls.head);
            false -> find_tag0_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
        }
    }
}

fn find_tag0_decl_for_qualified_call_compatible(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_decl());
        false -> case decl_matches_qualified_call_target(decls.head, fn_name, arity, recv_ty, expected_ty, world) of {
            true -> (true, decls.head);
            false -> find_tag0_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
        }
    }
}

fn find_tag0_decl_for_qualified_call(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case len(recv_ty) == 0 of {
        true -> find_tag0_decl_for_qualified_call_compatible(decls, fn_name, arity, recv_ty, expected_ty, world);
        false -> {
            (ef: bool, ed: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag0_decl_for_qualified_call_exact(decls, fn_name, arity, recv_ty, expected_ty, world);
            case ef of {
                true -> (true, ed);
                false -> find_tag0_decl_for_qualified_call_compatible(decls, fn_name, arity, recv_ty, expected_ty, world)
            }
        }
    }
}

fn find_tag7_decl_for_qualified_call_exact(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_decl());
        false -> case decls.head.tag == 7 of {
            true -> case instantiated_fn_name@decl_name_matches(decls.head.name, fn_name) and list_param_length_call(decls.head.parameters) == arity of {
                true -> case len(recv_ty) == 0 of {
                    true -> find_tag7_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world);
                    false -> case qualified_call_first_arg_exact_matches_decl(recv_ty, decls.head) of {
                        true -> case qualified_call_return_matches_expected(expected_ty, decls.head, world) of {
                            true -> (true, decls.head);
                            false -> find_tag7_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
                        };
                        false -> find_tag7_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
                    }
                };
                false -> find_tag7_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
            };
            false -> find_tag7_decl_for_qualified_call_exact(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
        }
    }
}

fn find_tag7_decl_for_qualified_call_compatible(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case decls.is_nil of {
        true -> (false, parser_ast@dummy_decl());
        false -> case decls.head.tag == 7 of {
            true -> case instantiated_fn_name@decl_name_matches(decls.head.name, fn_name) and list_param_length_call(decls.head.parameters) == arity of {
                true -> case len(recv_ty) == 0 of {
                    true -> case qualified_call_return_matches_expected(expected_ty, decls.head, world) of {
                        true -> (true, decls.head);
                        false -> find_tag7_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
                    };
                    false -> case qualified_call_first_arg_matches_decl(recv_ty, decls.head, world) of {
                        true -> case qualified_call_return_matches_expected(expected_ty, decls.head, world) of {
                            true -> (true, decls.head);
                            false -> find_tag7_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
                        };
                        false -> find_tag7_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
                    }
                };
                false -> find_tag7_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
            };
            false -> find_tag7_decl_for_qualified_call_compatible(decls.tail, fn_name, arity, recv_ty, expected_ty, world)
        }
    }
}

fn find_tag7_decl_for_qualified_call(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], fn_name: string, arity: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    case len(recv_ty) == 0 of {
        true -> find_tag7_decl_for_qualified_call_compatible(decls, fn_name, arity, recv_ty, expected_ty, world);
        false -> {
            (ef: bool, ed: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag7_decl_for_qualified_call_exact(decls, fn_name, arity, recv_ty, expected_ty, world);
            case ef of {
                true -> (true, ed);
                false -> find_tag7_decl_for_qualified_call_compatible(decls, fn_name, arity, recv_ty, expected_ty, world)
            }
        }
    }
}

export find_tag0_decl_for_qualified_call/6;
export find_function_decl_by_name_arity_call/3;
export qualified_call_first_arg_matches_decl/3;
export context_after_sequence_body/2;
export recv_type_for_trait_link_mangle/3;

fn param_types_str_from_params(params: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) -> string {
    case params.is_nil of {
        true -> "";
        false -> case params.tail.is_nil of {
            true -> params.head.type_name;
            false -> concat(concat(param_types_str_from_params(params.tail), ","), params.head.type_name)
        }
    }
}

fn build_function_type_for_decl_call(decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) -> string {
    params_str: string <- param_types_str_from_params(decl.parameters);
    case decl.parameters.is_nil of {
        true -> concat("() -> ", decl.return_type_name);
        false -> concat(concat(concat("(", params_str), ") -> "), decl.return_type_name)
    }
}

fn inferred_types_concat_comma(bucket: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) -> string {
    case bucket.is_nil of {
        true -> "";
        false -> case bucket.tail.is_nil of {
            true -> bucket.head.type_name;
            false -> concat(concat(bucket.head.type_name, ","), inferred_types_concat_comma(bucket.tail))
        }
    }
}

fn call_site_actual_matches_formal(inferred: string, formal: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    fin: string <- type_checker_traits@trim_type_text(inferred);
    fdecl: string <- type_checker_traits@trim_type_text(formal);
    case fin == fdecl of {
        true -> true;
        false -> case call_site_record_types_compatible(fin, fdecl, world) of {
            true -> true;
            false -> case type_checker_collections@is_collection_trait_type(fin) of {
                true -> type_checker_collections@collection_type_fits_concrete_record(fin, fdecl, world);
                false -> case type_checker_collections@is_collection_trait_type(fdecl) of {
                    true -> type_checker_collections@collection_type_fits_concrete_record(fdecl, fin, world);
                    false -> case type_checker_tuple_decompose_helpers@types_equal_surface(fin, fdecl) of {
                        true -> true;
                        false -> case type_checker_expressions_identifiers@function_types_compatible(fin, fdecl) of {
                            true -> true;
                            false -> case type_checker_lists@formal_type_accepts_actual(fin, fdecl, world) of {
                                true -> true;
                                false -> type_checker_traits@actual_type_satisfies_trait_expectation(fin, fdecl, world)
                            }
                        }
                    }
                }
            }
        }
    }
}

fn call_site_record_types_compatible(actual: string, formal: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case type_checker_record_types@record_type_starts(actual) of {
        false -> false;
        true -> case type_checker_record_types@record_type_starts(formal) of {
            false -> false;
            true -> {
                nformal: int64 <- type_checker_record_types@count_record_fields(formal);
                nactual: int64 <- type_checker_record_types@count_record_fields(actual);
                case nformal == nactual of {
                    false -> false;
                    true -> {
                        inner: string <- type_checker_record_types@record_case_inner_body(formal);
                        call_site_record_fields_match_loop(inner, actual, world)
                    }
                }
            }
        }
    }
}

fn call_site_record_fields_match_loop(rem: string, actual: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case len(rem) == 0 of {
        true -> true;
        false -> {
            (seg: string, rest: string) <- type_checker_record_types@record_case_field_step(rem);
            (fname: string, ftype: string) <- type_checker_record_types@record_field_segment_name_sub(seg);
            case len(fname) == 0 of {
                true -> false;
                false -> {
                    atype: string <- type_checker_record_types@find_field_type_in_record(actual, fname);
                    case len(atype) == 0 of {
                        true -> false;
                        false -> case type_checker_lists@formal_type_accepts_actual(atype, ftype, world) of {
                            false -> false;
                            true -> call_site_record_fields_match_loop(rest, actual, world)
                        }
                    }
                }
            }
        }
    }
}

// Widen/narrow: integer literal infers as int64; impl may take int32 etc.
fn replace_int64_with_int32_in_type_text_loop(s: string, i: int64, out: string) -> string {
    case i >= len(s) of {
        true -> out;
        false -> case i + 5 <= len(s) and substring(s, i, i + 5) == "int64" of {
            true -> replace_int64_with_int32_in_type_text_loop(s, i + 5, concat(out, "int32"));
            false -> replace_int64_with_int32_in_type_text_loop(s, i + 1, concat(out, substring(s, i, i + 1)))
        }
    }
}

fn struct_literal_inferred_matches_impl_record(actual: string, impl_param: string) -> bool {
    a: string <- type_checker_traits@trim_type_text(actual);
    p: string <- type_checker_traits@trim_type_text(impl_param);
    case len(a) > 0 of {
        false -> false;
        true -> case substring(a, 0, 1) == "{" of {
            false -> false;
            true -> {
                relaxed: string <- replace_int64_with_int32_in_type_text_loop(a, 0, "");
                type_checker_tuple_decompose_helpers@types_equal_surface(relaxed, p)
            }
        }
    }
}

fn dispatch_actual_fits_impl_parameter(actual: string, impl_param: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> bool {
    case call_site_actual_matches_formal(actual, impl_param, world) of {
        true -> true;
        false -> {
            a: string <- type_checker_traits@trim_type_text(actual);
            p: string <- type_checker_traits@trim_type_text(impl_param);
            case type_checker_collections@is_collection_trait_type(a) of {
                true -> type_checker_collections@collection_type_fits_impl_parameter(a, p, world);
                false -> case a == "int64" of {
                    true -> p == "int8" or p == "int16" or p == "int32" or p == "uint8" or p == "uint16" or p == "uint32";
                    false -> case type_checker_lists@formal_type_accepts_actual(a, p, world) of {
                        true -> true;
                        false -> struct_literal_inferred_matches_impl_record(actual, impl_param)
                    }
                }
            }
        }
    }
}

// Tag-0 decl whose first parameter is not the trait Self type (skip e.g. provided fn double_area(s: Shape)).
fn impl_first_param_if_concrete_impl(trait_self: string, d: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) -> string {
    case d.tag == 0 of {
        false -> "";
        true -> case d.parameters.is_nil of {
            true -> "";
            false -> {
                source_params: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@list_param_reverse_with_acc(d.parameters, parser_ast@nil_list_param());
                p0: string <- type_checker_traits@trim_type_text(source_params.head.type_name);
                ts: string <- type_checker_traits@trim_type_text(trait_self);
                case p0 == ts of {
                    true -> "";
                    false -> p0
                }
            }
        }
    }
}

fn gather_trait_dispatch_candidates_loop(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], trait_self: string, inferred_actual: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], cand0: string, cand_count: int64) -> (int64, string) {
    case decls.is_nil of {
        true -> (cand_count, cand0);
        false -> {
            conc: string <- impl_first_param_if_concrete_impl(trait_self, decls.head);
            case len(conc) == 0 of {
                true -> gather_trait_dispatch_candidates_loop(decls.tail, trait_self, inferred_actual, world, cand0, cand_count);
                false -> case dispatch_actual_fits_impl_parameter(inferred_actual, conc, world) of {
                    false -> gather_trait_dispatch_candidates_loop(decls.tail, trait_self, inferred_actual, world, cand0, cand_count);
                    true -> case cand_count == 0 of {
                        true -> gather_trait_dispatch_candidates_loop(decls.tail, trait_self, inferred_actual, world, conc, 1);
                        false -> case cand0 == conc of {
                            true -> gather_trait_dispatch_candidates_loop(decls.tail, trait_self, inferred_actual, world, cand0, cand_count);
                            false -> (-1, "")
                        }
                    }
                }
            }
        }
    }
}

fn infer_trait_dispatch_concrete_from_impls_status(trait_prog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }, trait_self: string, inferred_actual: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (int64, string) {
    (cc: int64, ct: string) <- gather_trait_dispatch_candidates_loop(trait_prog.declarations, trait_self, inferred_actual, world, "", 0);
    case cc < 0 of {
        true -> (2, "");
        false -> case cc == 0 of {
            true -> (0, "");
            false -> (1, ct)
        }
    }
}

fn export_trait_name_from_decls_link(decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)]) -> string {
    case decls.is_nil of {
        true -> "";
        false -> case decls.head.tag == 5 of {
            true -> decls.head.name;
            false -> export_trait_name_from_decls_link(decls.tail)
        }
    }
}

// Phase 0.4 #5: resolve trait-dispatch concrete receiver type for link-name mangling (mirrors type-check dispatch).
fn recv_type_for_trait_link_mangle(recv_inferred: string, callee_decls: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> string {
    case len(recv_inferred) == 0 of {
        true -> "";
        false -> {
            trait_self: string <- export_trait_name_from_decls_link(callee_decls);
            case len(trait_self) == 0 of {
                true -> recv_inferred;
                false -> {
                    trait_prog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } <- { declarations: callee_decls, location: parser_ast@zero_location() };
                    (st: int64, conc: string) <- infer_trait_dispatch_concrete_from_impls_status(trait_prog, trait_self, recv_inferred, world);
                    case st == 1 of {
                        true -> conc;
                        false -> recv_inferred
                    }
                }
            }
        }
    }
}

fn resolve_site_bucket_type_for_param(inferred: string, formal_ty: string, callee_mod: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }, loc: { file: string, line: int64, column: int64, offset: int64 }, call_name: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (TypeCheckResult, string) {
    case call_site_actual_matches_formal(inferred, formal_ty, world) of {
        true -> (type_checker_core@type_check_ok(), inferred);
        false -> {
            ft: string <- type_checker_traits@trim_type_text(formal_ty);
            (is_trait: bool, _: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }) <- type_checker_traits@find_program_declaring_trait(world, ft);
            case is_trait of {
                false -> (type_checker_core@type_check_err("E2003", loc, concat(concat(concat(concat(concat("argument type ", inferred), " does not match parameter type "), formal_ty), " for "), call_name), "sect6"), "");
                true -> {
                    (st: int64, conc: string) <- infer_trait_dispatch_concrete_from_impls_status(callee_mod, ft, inferred, world);
                    case st == 1 of {
                        true -> (type_checker_core@type_check_ok(), conc);
                        false -> case st == 2 of {
                            true -> (type_checker_core@type_check_err("E2003", loc, concat("ambiguous trait dispatch: multiple implementation parameter types match this argument for ", call_name), "sect6"), "");
                            false -> (type_checker_core@type_check_err("E2003", loc, concat("no concrete trait implementation matches this argument for ", call_name), "sect6"), "")
                        }
                    }
                }
            }
        }
    }
}

fn find_qualified_call_resolved_declaration(pprog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }, fn_id: string, generic_value: string, ac: int64, recv_ty: string, expected_ty: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (bool, { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) {
    resolved_fn: string <- instantiated_fn_name@resolve_call_fn_name(fn_id, generic_value);
    (df0: bool, d0: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag0_decl_for_qualified_call(pprog.declarations, resolved_fn, ac, recv_ty, expected_ty, world);
    case df0 of {
        true -> (true, d0);
        false -> {
            (df7: bool, d7: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag7_decl_for_qualified_call(pprog.declarations, resolved_fn, ac, recv_ty, expected_ty, world);
            case df7 of {
                true -> (true, d7);
                false -> find_function_decl_by_name_arity_call(pprog.declarations, resolved_fn, ac)
            }
        }
    }
}

// Infer each actual's type; require it matches the corresponding formal (including trait formals). Returns types in source left-to-right order.
fn trait_arg_walk_state(collection_ctx: string, record_ctx: string, param_index: int64) -> { collection_ctx: string, record_ctx: string, param_index: int64 } {
    out: { collection_ctx: string, record_ctx: string, param_index: int64 } <- { collection_ctx: collection_ctx, record_ctx: record_ctx, param_index: param_index };
    out
}

fn infer_trait_site_call_args_walk(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, params_lr: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], call_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, callee_mod: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }, st: { collection_ctx: string, record_ctx: string, param_index: int64 }) -> (TypeCheckResult, List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) {
    case expr.inner.kind == -1 of {
        true -> case params_lr.is_nil of {
            true -> (type_checker_core@type_check_ok(), parser_ast@nil_list_param());
            false -> (type_checker_core@type_check_err("E2005", call_expr.location, concat(concat(call_expr.name, ": too few arguments for callee"), " (spec 6)"), "sect6"), parser_ast@nil_list_param())
        };
        false -> case params_lr.is_nil of {
            true -> (type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6"), parser_ast@nil_list_param());
            false -> {
                inferred: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                case len(inferred) == 0 of {
                    true -> (type_checker_core@type_check_err("E2005", expr.inner.location, "cannot infer concrete type for qualified call argument (needed to resolve trait-parameter body)", "sect6"), parser_ast@nil_list_param());
                    false -> {
                        formal_ty: string <- type_checker_collections@resolve_trait_formal_with_contexts(params_lr.head.type_name, st.collection_ctx, st.record_ctx, st.param_index);
                        case len(formal_ty) == 0 and type_checker_collections@is_assoc_type_placeholder(params_lr.head.type_name) of {
                            true -> (type_checker_core@type_check_err("E2017", expr.inner.location, concat(concat(concat("cannot resolve assoc type placeholder ", params_lr.head.type_name), " without collection declaration context for "), call_expr.name), "sect4"), parser_ast@nil_list_param());
                            false -> {
                                (res_ty: TypeCheckResult, bucket_ty: string) <- resolve_site_bucket_type_for_param(inferred, formal_ty, callee_mod, expr.inner.location, call_expr.name, world);
                                case res_ty.is_ok of {
                                    false -> (res_ty, parser_ast@nil_list_param());
                                    true -> {
                                        next_ctx: string <- type_checker_collections@infer_collection_decl_from_receiver(inferred, expr.inner, context.env);
                                        ctx_after: string <- case len(next_ctx) > 0 of {
                                            true -> next_ctx;
                                            false -> case len(st.collection_ctx) > 0 of { true -> st.collection_ctx; false -> "" }
                                        };
                                        rec_after: string <- case st.param_index == 0 of {
                                            true -> bucket_ty;
                                            false -> st.record_ctx
                                        };
                                        case params_lr.tail.is_nil of {
                                            true -> {
                                                extra: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- case expr.right_expr.kind == -1 of {
                                                    true -> type_checker_core@type_check_ok();
                                                    false -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6")
                                                };
                                                case extra.is_ok of {
                                                    false -> (extra, parser_ast@nil_list_param());
                                                    true -> (type_checker_core@type_check_ok(), parser_ast@build_infer_type_bucket(bucket_ty, parser_ast@nil_list_param()))
                                                }
                                            };
                                            false -> case expr.right_expr.kind == 4 of {
                                                true -> {
                                                    (rest_ok: TypeCheckResult, rest_inf: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- infer_trait_site_call_args_walk(expr.right_expr, params_lr.tail, context, world, call_expr, callee_mod, trait_arg_walk_state(ctx_after, rec_after, st.param_index + 1));
                                                    case rest_ok.is_ok of {
                                                        false -> (rest_ok, parser_ast@nil_list_param());
                                                        true -> (type_checker_core@type_check_ok(), parser_ast@build_infer_type_bucket(bucket_ty, rest_inf))
                                                    }
                                                };
                                                false -> {
                                                    (one_ok: TypeCheckResult, one_tail: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- infer_one_flat_second_arg(expr.right_expr, params_lr.tail, context, world, call_expr, callee_mod, trait_arg_walk_state(ctx_after, rec_after, st.param_index + 1));
                                                    case one_ok.is_ok of {
                                                        false -> (one_ok, parser_ast@nil_list_param());
                                                        true -> (type_checker_core@type_check_ok(), parser_ast@build_infer_type_bucket(bucket_ty, one_tail))
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn infer_one_flat_second_arg(second_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, params_tail: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], call_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, callee_mod: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }, st: { collection_ctx: string, record_ctx: string, param_index: int64 }) -> (TypeCheckResult, List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) {
    case params_tail.is_nil of {
        true -> (type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6"), parser_ast@nil_list_param());
        false -> case params_tail.tail.is_nil of {
            false -> (type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6"), parser_ast@nil_list_param());
            true -> {
                inf2: string <- type_checker_tuple_decompose_helpers@infer_expr_type(second_e, context);
                case len(inf2) == 0 of {
                    true -> (type_checker_core@type_check_err("E2005", second_e.location, "cannot infer concrete type for qualified call argument (needed to resolve trait-parameter body)", "sect6"), parser_ast@nil_list_param());
                    false -> {
                        formal_ty: string <- type_checker_collections@resolve_trait_formal_with_contexts(params_tail.head.type_name, st.collection_ctx, st.record_ctx, st.param_index);
                        case len(formal_ty) == 0 and type_checker_collections@is_assoc_type_placeholder(params_tail.head.type_name) of {
                            true -> (type_checker_core@type_check_err("E2017", second_e.location, concat(concat(concat("cannot resolve assoc type placeholder ", params_tail.head.type_name), " without collection declaration context for "), call_expr.name), "sect4"), parser_ast@nil_list_param());
                            false -> {
                                (res2: TypeCheckResult, bucket2: string) <- resolve_site_bucket_type_for_param(inf2, formal_ty, callee_mod, second_e.location, call_expr.name, world);
                                case res2.is_ok of {
                                    false -> (res2, parser_ast@nil_list_param());
                                    true -> (type_checker_core@type_check_ok(), parser_ast@build_infer_type_bucket(bucket2, parser_ast@nil_list_param()))
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn validate_callsite_return_record_type(rt: string, loc: { file: string, line: int64, column: int64, offset: int64 }) -> TypeCheckResult {
    case type_checker_record_types@record_type_starts(rt) of {
        false -> type_checker_core@type_check_ok();
        true -> case type_checker_record_types@record_type_braces_balanced(rt) of {
            true -> type_checker_core@type_check_ok();
            false -> type_checker_core@type_check_err("E2003", loc, "malformed inline record type (unbalanced braces)", "sect3.4.2")
        }
    }
}

// Re-check callee body with formal parameters bound to inferred concrete types (trait dispatch / overloads inside the body).
// Pass-1 env must be the callee module's (use/export/local fns). The caller's env omits callee private helpers → E2005 on mod@-qualified calls.
fn check_callsite_body_with_concrete_params(decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], conc_decl_order: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], callee_mod_id: string, callee_prog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }) -> TypeCheckResult proc[DeviceIO] {
    case list_param_length_call(decl.parameters) == list_param_length_call(conc_decl_order) of {
        false -> type_checker_core@type_check_err("E2005", decl.location, "internal: call-site parameter type list length mismatch", "sect6");
        true -> {
            (pass1: TypeCheckResult, ctx_callee: TypeContext) <- type_checker_callee_env@add_all_declarations_with_world(callee_prog.declarations, callee_prog.declarations, type_checker_core@create_type_context(), world, callee_mod_id);
            case pass1.is_ok of {
                false -> pass1;
                true -> {
                    dp: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@param_list_normalize(decl.parameters);
                    cp: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@param_list_normalize(conc_decl_order);
                    ctx_params: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_core@add_params_concrete_loop1({ decl_params: dp, concrete_types: cp, base: ctx_callee });
                    ret_rec: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- validate_callsite_return_record_type(decl.return_type_name, decl.return_type_location);
                    case ret_rec.is_ok of {
                        false -> ret_rec;
                        true -> case decl.return_type_name == "unit" of {
                            true -> type_checker_core@type_check_err("E2006", decl.return_type_location, "unit is not a valid return type", "sect4");
                            false -> case type_checker_memory_regions@is_memory_region_type(decl.return_type_name) of {
                                true -> {
                                    mem_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_memory_regions@validate_memory_region_type(decl.return_type_name, decl.return_type_location);
                                    case mem_result.is_ok of {
                                        false -> mem_result;
                                        true -> check_expr(decl.body, decl.return_type_name, ctx_params, world)
                                    }
                                };
                                false -> check_expr(decl.body, decl.return_type_name, ctx_params, world)
                            }
                        }
                    }
                }
            }
        }
    }
}

// §19.3.2: for `module@fn(args)`, resolve callee in `world` (includes the current module). Overloads share name/arity; pick by inferred first-argument type, then required trait signature (tag 7), then first tag-0 match.
fn try_resolve_qualified_call_func_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], context: TypeContext) -> (bool, string) {
    at: int64 <- first_at_in_expr_call_name(expr.name, 0);
    case at < 0 of {
        true -> (false, "");
        false -> {
            mod_id: string <- substring(expr.name, 0, at);
            fn_id: string <- substring(expr.name, at + 1, len(expr.name));
            resolved_fn: string <- instantiated_fn_name@resolve_call_fn_name(fn_id, expr.value);
            ac: int64 <- count_call_args_user_call(expr);
            (pf: bool, pprog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }) <- parser_ast@find_named_program(mod_id, world);
            case pf of {
                false -> (false, "");
                true -> {
                    recv_ty: string <- case ac > 0 of {
                        true -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                        false -> ""
                    };
                    (df0: bool, d0: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag0_decl_for_qualified_call(pprog.declarations, resolved_fn, ac, recv_ty, expected_type, world);
                    case df0 of {
                        true -> (true, build_function_type_for_decl_call(d0));
                        false -> {
                            (df7: bool, d7: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_tag7_decl_for_qualified_call(pprog.declarations, resolved_fn, ac, recv_ty, expected_type, world);
                            case df7 of {
                                true -> (true, build_function_type_for_decl_call(d7));
                                false -> {
                                    (df: bool, ddecl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_function_decl_by_name_arity_call(pprog.declarations, resolved_fn, ac);
                                    case df of {
                                        true -> (true, build_function_type_for_decl_call(ddecl));
                                        false -> case ac == 0 of {
                                            true -> {
                                                (df1: bool, d1: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_function_decl_by_name_arity_call(pprog.declarations, resolved_fn, 1);
                                                case df1 of {
                                                    true -> (true, build_function_type_for_decl_call(d1));
                                                    false -> (false, "")
                                                }
                                            };
                                            false -> (false, "")
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_user_call_result_user_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, return_type: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    receiver_resolved_return: string <- type_checker_collections@resolve_constructor_call_formal(expected_type, return_type);
    resolved_return: string <- type_checker_collections@resolve_accumulator_call_formal(expected_type, receiver_resolved_return);
    constructor_module: string <- type_checker_collections@module_name_from_qualified_call(expr.name);
    registered_collection_constructor: bool <- type_checker_collections@is_collection_constructor_base_name(expr.name) and type_checker_collections@is_registered_construction_module(constructor_module);
    case expected_type == "discard" of {
        true -> type_checker_core@type_check_ok();
        false -> case type_checker_collections@is_collection_trait_type(expected_type) of {
            true -> case type_checker_collections@collection_return_accepts_constructor(expected_type, resolved_return, world) of {
                false -> type_checker_core@type_check_err("E2017", expr.location, concat(concat(concat(concat(expr.name, " return type does not witness declared collection type "), expected_type), " (got "), concat(resolved_return, ")")), "sect4");
                true -> type_checker_core@type_check_ok()
            };
            false -> case registered_collection_constructor and type_checker_collections@runtime_record_accepts_constructor_return(expected_type, resolved_return, world) of {
                true -> type_checker_core@type_check_ok();
                false -> case type_checker_lists@formal_type_accepts_actual(resolved_return, expected_type, world) of {
                    true -> type_checker_core@type_check_ok();
                    false -> case type_checker_lists@formal_type_accepts_actual(expected_type, resolved_return, world) of {
                        true -> type_checker_core@type_check_ok();
                        false -> case type_checker_lists@types_equal_strip(expected_type, resolved_return) of {
                            true -> type_checker_core@type_check_ok();
                            false -> case trim_leading_user_call(expected_type) == "string" and return_type_has_ptr_len_out_fields(resolved_return) of {
                                true -> type_checker_core@type_check_ok();
                                false -> case type_checker_traits@actual_type_satisfies_trait_expectation(resolved_return, expected_type, world) of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat(expr.name, " returns "), resolved_return), ", expected "), expected_type), "sect6")
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn return_type_text_contains(s: string, needle: string) -> bool {
    case len(needle) == 0 of {
        true -> true;
        false -> case len(s) < len(needle) of {
            true -> false;
            false -> case substring(s, 0, len(needle)) == needle of {
                true -> true;
                false -> return_type_text_contains(substring(s, 1, len(s)), needle)
            }
        }
    }
}

fn return_type_has_ptr_len_out_fields(return_type: string) -> bool {
    ret: string <- trim_leading_user_call(return_type);
    case return_type_text_contains(ret, "text_ptr") of {
        true -> return_type_text_contains(ret, "text_len");
        false -> case return_type_text_contains(ret, "_ptr") of {
            true -> return_type_text_contains(ret, "_len");
            false -> false
        }
    }
}

fn params_inner_is_ptr_len_pair(params_inner: string) -> bool {
    (first_param: string, rest_params: string) <- get_first_param_type(params_inner);
    (second_param: string, rest_rest: string) <- get_first_param_type(rest_params);
    case len(rest_rest) > 0 of {
        true -> false;
        false -> trim_leading_user_call(first_param) == "uint8" and trim_leading_user_call(second_param) == "uint64"
    }
}

fn check_user_call_args_recursive(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, params_inner: string, expected_type: int64, return_type: string, context: TypeContext, call_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.inner.kind == -1 of {
        true -> case len(params_inner) == 0 of {
            true -> check_user_call_result_user_call(call_expr, expected_type, return_type, context, world);
            false -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6")
        };
        false -> {
            (first_param: string, rest_params: string) <- get_first_param_type(params_inner);
            case len(first_param) == 0 of {
                true -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6");
                false -> {
                    arg_inferred: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                    formal_bind: string <- case type_checker_collections@is_collection_trait_type(expected_type) of {
                        true -> expected_type;
                        false -> case type_checker_collections@bind_type_has_collection_runtime_fields(expected_type) of {
                            true -> expected_type;
                            false -> case len(arg_inferred) > 0 of {
                                true -> arg_inferred;
                                false -> expected_type
                            }
                        }
                    };
                    resolved_param: string <- type_checker_collections@resolve_constructor_call_formal(formal_bind, first_param);
                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, resolved_param, context, world);
                    case r1.is_ok of {
                        false -> r1;
                        true -> case len(rest_params) == 0 of {
                            true -> check_user_call_result_user_call(call_expr, expected_type, return_type, context, world);
                            false -> case expr.right_expr.kind == -1 of {
                                true -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6");
                                false -> case expr.right_expr.kind == 4 of {
                                    true -> case len(rest_params) == 0 of {
                                        true -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6");
                                        false -> check_user_call_args_recursive(expr.right_expr, rest_params, expected_type, return_type, context, call_expr, world)
                                    };
                                    false -> {
                                        (second_param: string, rest_rest: string) <- get_first_param_type(rest_params);
                                        case len(second_param) == 0 or len(rest_rest) > 0 of {
                                            true -> type_checker_core@type_check_err("E2005", call_expr.location, concat(call_expr.name, " argument count mismatch"), "sect6");
                                            false -> {
                                                r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, second_param, context, world);
                                                case r2.is_ok of {
                                                    false -> r2;
                                                    true -> check_user_call_result_user_call(call_expr, expected_type, return_type, context, world)
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_user_defined_world_call_with_inference(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, params_inner: string, return_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    arg_inferred: string <- case count_call_args_user_call(expr) > 0 of {
        true -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
        false -> ""
    };
    formal_bind: string <- case type_checker_collections@is_collection_trait_type(expected_type) of {
        true -> expected_type;
        false -> case type_checker_collections@bind_type_has_collection_runtime_fields(expected_type) of {
            true -> expected_type;
            false -> case len(arg_inferred) > 0 of {
                true -> arg_inferred;
                false -> expected_type
            }
        }
    };
    params_from_receiver: string <- type_checker_collections@resolve_constructor_call_formal(formal_bind, params_inner);
    return_from_receiver: string <- type_checker_collections@resolve_constructor_call_formal(formal_bind, return_type);
    // Collection witnesses resolve ItemType/KeyType/ValueType from argument
    // one.  AccType is independent and is witnessed by the expected result
    // type.  When the result is discarded, infer it from the initial
    // accumulator (argument two) instead; discarding a fold result must not
    // erase the callback's concrete signature.
    // AccType in callee formals is witnessed by the call's expected result, or —
    // when the result is discarded / still the AccType placeholder (trait impl
    // bodies) — by the concrete initial-accumulator argument.
    acc_witness: string <- case expected_type == "discard" or expected_type == "AccType" of {
        true -> case count_call_args_user_call(expr) >= 2 of {
            true -> {
                from_init: string <- type_checker_tuple_decompose_helpers@infer_expr_type(call_second_arg_expr(expr), context);
                case len(from_init) > 0 and from_init != "AccType" of {
                    true -> from_init;
                    false -> expected_type
                }
            };
            false -> expected_type
        };
        false -> expected_type
    };
    params_use: string <- type_checker_collections@resolve_accumulator_call_formal(acc_witness, params_from_receiver);
    resolved_return: string <- type_checker_collections@resolve_accumulator_call_formal(acc_witness, return_from_receiver);
    at: int64 <- first_at_in_expr_call_name(expr.name, 0);
    case at < 0 of {
        true -> check_user_call_args_recursive(expr, params_use, expected_type, return_type, context, expr, world);
        false -> {
            mod_id: string <- substring(expr.name, 0, at);
            fn_id: string <- substring(expr.name, at + 1, len(expr.name));
            ac: int64 <- count_call_args_user_call(expr);
            (pf: bool, pprog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }) <- parser_ast@find_named_program(mod_id, world);
            case pf of {
                false -> check_user_call_args_recursive(expr, params_use, expected_type, return_type, context, expr, world);
                true -> {
                    recv_ty: string <- case ac > 0 of {
                        true -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                        false -> ""
                    };
                    (pfd: bool, callee_decl: { tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }) <- find_qualified_call_resolved_declaration(pprog, fn_id, expr.value, ac, recv_ty, expected_type, world);
                    case pfd of {
                        false -> check_user_call_args_recursive(expr, params_use, expected_type, return_type, context, expr, world);
                        true -> case callee_decl.tag == 0 of {
                            false -> check_user_call_args_recursive(expr, params_use, expected_type, return_type, context, expr, world);
                                true -> case params_use != params_inner of {
                                true -> {
                                    chk_args: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_user_call_args_recursive(expr, params_use, expected_type, resolved_return, context, expr, world);
                                    case chk_args.is_ok of {
                                        false -> chk_args;
                                        true -> check_user_call_result_user_call(expr, expected_type, resolved_return, context, world)
                                    }
                                };
                                false -> {
                                    // Collection bind types must witness ItemType/KeyType/ValueType inside
                                    // constructor formals (e.g. OrderedMap[...] <- wbt_map@empty({...})).
                                    col_ctx: string <- case type_checker_collections@is_collection_trait_type(expected_type) of {
                                        true -> expected_type;
                                        false -> ""
                                    };
                                    params_lr: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@list_param_reverse_with_acc(callee_decl.parameters, parser_ast@nil_list_param());
                                    (inf_ok: TypeCheckResult, inf_left: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- infer_trait_site_call_args_walk(expr, params_lr, context, world, expr, pprog, trait_arg_walk_state(col_ctx, "", 0));
                                    case inf_ok.is_ok of {
                                        false -> inf_ok;
                                        true -> {
                                            params_inner_inf: string <- inferred_types_concat_comma(inf_left);
                                            chk_args: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_user_call_args_recursive(expr, params_inner_inf, expected_type, return_type, context, expr, world);
                                            case chk_args.is_ok of {
                                                false -> chk_args;
                                                true -> {
                                                    conc_decl: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- parser_ast@list_param_reverse_with_acc(inf_left, parser_ast@nil_list_param());
                                                    body_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_callsite_body_with_concrete_params(callee_decl, world, conc_decl, mod_id, pprog);
                                                    case body_ok.is_ok of {
                                                        false -> body_ok;
                                                        true -> check_user_call_result_user_call(expr, expected_type, return_type, context, world)
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_user_defined_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    (rw: bool, ft_world: string) <- try_resolve_qualified_call_func_type(expr, expected_type, world, context);
    local_name: string <- instantiated_fn_name@resolve_call_fn_name(expr.name, expr.value);
    resolved_type: string <- case rw of {
        true -> ft_world;
        false -> lookup_function_type_for_call_local(context.env, local_name)
    };
    case len(resolved_type) == 0 of {
        true -> type_checker_core@type_check_err("E2005", expr.location, concat("unknown or unsupported function: ", expr.name), "sect6");
        false -> case is_function_type_user_call(resolved_type) of {
            false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " is not a function"), "sect6");
            true -> check_user_defined_call_parsed(expr, expected_type, context, resolved_type, world, rw)
        }
    }
}

fn check_user_defined_call_parsed(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, func_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], callee_type_from_world: bool) -> TypeCheckResult proc[DeviceIO] {
    func_type_canon: string <- type_checker_expressions_identifiers@normalize_fn_type_idf(func_type);
    expected_arg_count: int64 <- parse_function_param_count(func_type_canon);
    params_inner: string <- get_params_inner_from_func_type(func_type_canon);
    return_type: string <- parse_function_return_type(func_type_canon);
    arg_count: int64 <- count_call_args_user_call(expr);
    case callee_type_from_world of {
        true -> check_user_defined_world_call_with_inference(expr, expected_type, context, params_inner, return_type, world);
                false -> case arg_count != expected_arg_count of {
        true -> case expected_arg_count == 2 and arg_count == 1 and params_inner_is_ptr_len_pair(params_inner) of {
            true -> {
                r_str: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "string", context, world);
                case r_str.is_ok of {
                    false -> r_str;
                    true -> check_user_call_result_user_call(expr, expected_type, return_type, context, world)
                }
            };
            false -> case expected_arg_count == 1 of {
            true -> case arg_count == 2 of {
                true -> case len(params_inner) >= 2 of {
                    true -> case substring(params_inner, 0, 1) == "(" of {
                        true -> case substring(params_inner, len(params_inner) - 1, len(params_inner)) == ")" of {
                            true -> check_user_call_two_args_as_tuple(expr, params_inner, expected_type, return_type, context, world);
                            false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
                        };
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
                    };
                    false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
                };
                false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
            };
            false -> case expected_arg_count == 2 of {
                true -> case arg_count == 1 of {
                    true -> case expr.inner.kind == 9 of {
                        true -> check_user_call_one_tuple_as_two_args(expr, params_inner, expected_type, return_type, context, world);
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
                    };
                    false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
                };
                false -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6")
            }
            }
        };
        false -> check_user_call_args_recursive(expr, params_inner, expected_type, return_type, context, expr, world)
        }
    }
}

fn check_user_call_one_tuple_as_two_args(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, params_inner: string, expected_type: int64, return_type: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    (first_param: string, rest_params: string) <- get_first_param_type(params_inner);
    (second_param: string, _: string) <- get_first_param_type(rest_params);
    case len(first_param) == 0 or len(second_param) == 0 of {
        true -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6");
        false -> {
            tuple_type: string <- concat(concat(concat(concat("(", first_param), ","), second_param), ")");
            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, tuple_type, context, world);
            case r1.is_ok of {
                false -> r1;
                true -> check_user_call_result_user_call(expr, expected_type, return_type, context, world)
            }
        }
    }
}

fn check_user_call_two_args_as_tuple(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, tuple_param: string, expected_type: int64, return_type: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    inner: string <- trim_leading_user_call(substring(tuple_param, 1, len(tuple_param) - 1));
    comma_pos: int64 <- find_top_level_comma_user_call(inner, 0, 0);
    case comma_pos >= len(inner) of {
        true -> type_checker_core@type_check_err("E2005", expr.location, concat(expr.name, " argument count mismatch"), "sect6");
        false -> {
            first_type: string <- trim_leading_user_call(substring(inner, 0, comma_pos));
            second_type: string <- trim_leading_user_call(substring(inner, comma_pos + 1, len(inner)));
            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, first_type, context, world);
            case r1.is_ok of {
                false -> r1;
                true -> {
                    r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, second_type, context, world);
                    case r2.is_ok of {
                        false -> r2;
                        true -> check_user_call_result_user_call(expr, expected_type, return_type, context, world)
                    }
                }
            }
        }
    }
}

fn is_collection_constructor_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    type_checker_collections@is_collection_constructor_base_name(expr.name)
}

fn constructor_functions_record_expr(rhs: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    case type_checker_core@user_call_base_function_name(rhs.name) of {
        "empty" -> rhs.inner;
        "with_root" -> rhs.inner;
        _: string -> call_second_arg_expr(rhs)
    }
}

fn verify_collection_witness_for_family(bind_type: string, record_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, location: { file: string, line: int64, column: int64, offset: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)], rhs: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> TypeCheckResult {
    case type_checker_collections@is_ordered_set_type(bind_type) of {
        true -> {
            wit_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_item", context);
            set_wit: string <- case len(wit_from_arg) > 0 of {
                true -> wit_from_arg;
                false -> empty_call_constructor_witness_fn_type(rhs, "compare_item", context, world)
            };
            case len(set_wit) == 0 of {
                true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_item witness", "sect4");
                false -> type_checker_collections@witness_matches_ordered_set_declaration(bind_type, set_wit, location)
            }
        };
        false -> case type_checker_collections@is_search_tree_type(bind_type) of {
            true -> {
                wit_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_item", context);
                set_wit: string <- case len(wit_from_arg) > 0 of { true -> wit_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_item", context, world) };
                case len(set_wit) == 0 of {
                    true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_item witness", "sect4");
                    false -> type_checker_collections@witness_matches_search_tree_declaration(bind_type, set_wit, location)
                }
            };
            false -> case type_checker_collections@is_tree_type(bind_type) of {
                true -> {
                    wit_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_item", context);
                    set_wit: string <- case len(wit_from_arg) > 0 of { true -> wit_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_item", context, world) };
                    case len(set_wit) == 0 of {
                        true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_item witness", "sect4");
                        false -> type_checker_collections@witness_matches_tree_declaration(bind_type, set_wit, location)
                    }
                };
                false -> case type_checker_collections@is_ordered_map_type(bind_type) of {
                    true -> {
                        key_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_key", context);
                        val_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_value", context);
                        key_wit: string <- case len(key_from_arg) > 0 of { true -> key_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_key", context, world) };
                        val_wit: string <- case len(val_from_arg) > 0 of { true -> val_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_value", context, world) };
                        case len(key_wit) == 0 or len(val_wit) == 0 of {
                            true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_key or compare_value witness", "sect4");
                            false -> type_checker_collections@witness_matches_ordered_map_declaration(bind_type, key_wit, val_wit, location)
                        }
                    };
                    false -> case type_checker_collections@is_heap_type(bind_type) of {
                        true -> {
                            item_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_item", context);
                            heap_wit: string <- case len(item_from_arg) > 0 of { true -> item_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_item", context, world) };
                            case len(heap_wit) == 0 of {
                                true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_item witness", "sect4");
                                false -> type_checker_collections@witness_matches_heap_declaration(bind_type, heap_wit, location)
                            }
                        };
                        false -> case type_checker_collections@is_priority_queue_type(bind_type) of {
                            true -> {
                                pri_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_priority", context);
                                item_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_item", context);
                                pri_wit: string <- case len(pri_from_arg) > 0 of { true -> pri_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_priority", context, world) };
                                item_wit: string <- case len(item_from_arg) > 0 of { true -> item_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_item", context, world) };
                                case len(pri_wit) == 0 or len(item_wit) == 0 of {
                                    true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_priority or compare_item witness", "sect4");
                                    false -> type_checker_collections@witness_matches_priority_queue_declaration(bind_type, pri_wit, item_wit, location)
                                }
                            };
                            false -> case type_checker_collections@is_directed_graph_type(bind_type) of {
                                true -> {
                                    node_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_node", context);
                                    edge_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_edge", context);
                                    target_from_arg: string <- constructor_record_field_fn_type(record_e, "edge_target", context);
                                    node_wit: string <- case len(node_from_arg) > 0 of { true -> node_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_node", context, world) };
                                    edge_wit: string <- case len(edge_from_arg) > 0 of { true -> edge_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_edge", context, world) };
                                    target_wit: string <- case len(target_from_arg) > 0 of { true -> target_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "edge_target", context, world) };
                                    case len(node_wit) == 0 or len(edge_wit) == 0 or len(target_wit) == 0 of {
                                        true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_node, compare_edge, or edge_target witness", "sect4");
                                        false -> type_checker_collections@witness_matches_directed_graph_declaration(bind_type, node_wit, edge_wit, target_wit, location)
                                    }
                                };
                                false -> case type_checker_collections@is_undirected_graph_type(bind_type) of {
                                    true -> {
                                        node_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_node", context);
                                        edge_data_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_edge_data", context);
                                        node_wit: string <- case len(node_from_arg) > 0 of { true -> node_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_node", context, world) };
                                        edge_data_wit: string <- case len(edge_data_from_arg) > 0 of { true -> edge_data_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_edge_data", context, world) };
                                        case len(node_wit) == 0 or len(edge_data_wit) == 0 of {
                                            true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_node or compare_edge_data witness", "sect4");
                                            false -> type_checker_collections@witness_matches_undirected_graph_declaration(bind_type, node_wit, edge_data_wit, location)
                                        }
                                    };
                                    false -> case type_checker_collections@is_weighted_graph_type(bind_type) of {
                                        true -> {
                                            node_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_node", context);
                                            edge_data_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_edge_data", context);
                                            weight_from_arg: string <- constructor_record_field_fn_type(record_e, "edge_weight", context);
                                            cmp_weight_from_arg: string <- constructor_record_field_fn_type(record_e, "compare_weight", context);
                                            node_wit: string <- case len(node_from_arg) > 0 of { true -> node_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_node", context, world) };
                                            edge_data_wit: string <- case len(edge_data_from_arg) > 0 of { true -> edge_data_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_edge_data", context, world) };
                                            weight_wit: string <- case len(weight_from_arg) > 0 of { true -> weight_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "edge_weight", context, world) };
                                            cmp_weight_wit: string <- case len(cmp_weight_from_arg) > 0 of { true -> cmp_weight_from_arg; false -> empty_call_constructor_witness_fn_type(rhs, "compare_weight", context, world) };
                                            case len(node_wit) == 0 or len(edge_data_wit) == 0 or len(weight_wit) == 0 or len(cmp_weight_wit) == 0 of {
                                                true -> type_checker_core@type_check_err("E2017", location, "constructor record missing compare_node, compare_edge_data, edge_weight, or compare_weight witness", "sect4");
                                                false -> type_checker_collections@witness_matches_weighted_graph_declaration(bind_type, node_wit, edge_data_wit, weight_wit, cmp_weight_wit, location)
                                            }
                                        };
                                        false -> type_checker_core@type_check_err("E2017", location, "unsupported collection constructor binding type", "sect4")
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn verify_collection_constructor_witness(bind_type: string, rhs: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, location: { file: string, line: int64, column: int64, offset: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    case rhs.kind == 4 of {
        false -> type_checker_core@type_check_err("E2017", location, "collection constructor binding requires a call to empty/1 or with_root/2", "sect4");
        true -> case is_collection_constructor_call(rhs) of {
            false -> type_checker_core@type_check_err("E2017", location, "collection constructor binding requires a call to empty/1 or with_root/2", "sect4");
            true -> {
                mod_name: string <- type_checker_collections@module_name_from_qualified_call(rhs.name);
                case len(mod_name) == 0 of {
                    true -> type_checker_core@type_check_err("E2017", location, "collection constructor requires qualified module@empty or module@with_root call", "sect4");
                    false -> case type_checker_collections@is_registered_construction_module(mod_name) of {
                        false -> type_checker_core@type_check_err("E2017", location, concat(concat(concat(concat("module ", mod_name), " is not a registered collection construction module for binding type "), bind_type), ""), "sect4");
                        true -> case type_checker_collections@collection_trait_accepts_module(bind_type, mod_name) of {
                            false -> type_checker_core@type_check_err("E2017", location, concat(concat(concat(concat("module ", mod_name), " does not match collection binding type "), bind_type), ""), "sect4");
                            true -> {
                                record_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- constructor_functions_record_expr(rhs);
                                fields_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_collections@validate_constructor_record_field_names(bind_type, record_e, location);
                                case fields_ok.is_ok of {
                                    false -> fields_ok;
                                    true -> verify_collection_witness_for_family(bind_type, record_e, context, location, world, rhs)
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn constructor_record_field_fn_type(record_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, field: string, context: TypeContext) -> string {
    case record_expr.kind == 21 of {
        true -> case record_expr.name == field of {
            true -> type_checker_tuple_decompose_helpers@infer_expr_type(record_expr.inner, context);
            false -> constructor_record_field_fn_type(record_expr.right_expr, field, context)
        };
        false -> {
            inferred: string <- type_checker_tuple_decompose_helpers@infer_expr_type(record_expr, context);
            case len(inferred) > 0 of {
                true -> type_checker_record_types@find_field_type_in_record(inferred, field);
                false -> case record_expr.kind == 1 of {
                    true -> {
                        (found: bool, var_type: string) <- type_checker_core@lookup_symbol_found(context.env, record_expr.name);
                        case found of {
                            false -> "";
                            true -> type_checker_record_types@find_field_type_in_record(var_type, field)
                        }
                    };
                    false -> ""
                }
            }
        }
    }
}

fn empty_call_constructor_witness_fn_type(rhs: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, field: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> string {
    (rw: bool, func_type: string) <- try_resolve_qualified_call_func_type(rhs, "discard", world, context);
    case rw of {
        false -> "";
        true -> {
            func_type_canon: string <- type_checker_expressions_identifiers@normalize_fn_type_idf(func_type);
            params_inner: string <- get_params_inner_from_func_type(func_type_canon);
            (first_param: string, _: string) <- get_first_param_type(params_inner);
            case len(first_param) == 0 of {
                true -> "";
                false -> type_checker_record_types@find_field_type_in_record(first_param, field)
            }
        }
    }
}

fn check_collection_trait_let(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, bind_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    // Pass the declared collection type (OrderedMap[...], OrderedSet[...], ...) so
    // KeyType/ValueType/ItemType inside constructor formals are witnessed. Checking
    // with "discard" leaves those placeholders unresolved for generic empty/1.
    rhs_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, bind_type, context, world);
    case rhs_result.is_ok of {
        false -> rhs_result;
        true -> {
            witness_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- verify_collection_constructor_witness(bind_type, expr.inner, context, expr.location, world);
            case witness_result.is_ok of {
                false -> witness_result;
                true -> {
                    bound_ctx: TypeContext <- type_checker_core@context_add_symbol_surface(context, expr.name, bind_type, parser_ast@empty_effect_name_list(), "");
                    new_env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)] <- enrich_env_for_actor_spawn(bound_ctx.env, expr.name, bind_type, expr.inner, context);
                    new_context: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_core@type_context_with_env(bound_ctx, new_env);
                    body_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, expected_type, new_context, world);
                    body_result
                }
            }
        }
    }
}

// Let binding: name: type <- rhs; body. Check rhs has declared type, add to env, check body.
// Reject unit for named variables; allow _ : unit <- expr (discard pattern for expression statements).
// Memory region types (ref, region, buf) are validated before type checking.
// Untyped `name <- rhs` (empty annotation) is a type error (E2018), not a parse defect.
fn check_let(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    bind_type: string <- expr.value;
    case len(bind_type) == 0 of {
        true -> type_checker_core@type_check_err("E2018", expr.location, concat(concat("variable binding '", expr.name), "' must have an explicit type annotation; write 'name: <type> <- ...'"), "sect8.3.1");
        false -> {
            list_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_lists@validate_list_type_declaration(bind_type, expr.location);
            case list_result.is_ok of {
                false -> list_result;
                true -> {
                    col_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_collections@validate_collection_type_declaration(bind_type, expr.location);
                    case col_result.is_ok of {
                        false -> col_result;
                        true -> {
                            result_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_collections@validate_collection_result_type_declaration(bind_type, expr.location);
                            case result_result.is_ok of {
                                false -> result_result;
                                true -> {
                                    rec_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_recursive_tuples@validate_recursive_type_declaration(bind_type, expr.location);
                                    case rec_result.is_ok of {
                                        false -> rec_result;
                                        true -> case type_checker_memory_regions@is_memory_region_type(bind_type) of {
                                            true -> check_let_memory_region(expr, expected_type, context, bind_type, world);
                                            false -> case type_checker_collections@is_collection_trait_type(bind_type) of {
                                                true -> check_collection_trait_let(expr, expected_type, context, bind_type, world);
                                                false -> check_let_after_validation(expr, expected_type, context, bind_type, world)
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_let_memory_region(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, bind_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    mem_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_memory_regions@validate_memory_region_type_in_context(bind_type, expr.location, context);
    case mem_result.is_ok of {
        false -> mem_result;
        true -> check_let_after_validation(expr, expected_type, context, bind_type, world)
    }
}

// When a let-binding creates an actor_ref from spawn (or dangerous_actor_ref from spawn_dangerous),
// record the behavior function type and enforce region move semantics (§12.1.5).
fn enrich_env_for_actor_spawn(env: ListSymbolEntry, var_name: string, bind_type: string, rhs: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext) -> ListSymbolEntry {
    rhs_base: string <- type_checker_core@user_call_base_function_name(rhs.name);
    spawn_pair_ok: bool <- case bind_type == "actor_ref" and rhs_base == "spawn" of {
        true -> true;
        false -> case bind_type == "dangerous_actor_ref" and rhs_base == "spawn_dangerous" of {
            true -> true;
            false -> bind_type == "dangerous_actor_ref" and rhs_base == "spawn_dangerous_registered"
        }
    };
    case rhs.kind == 4 and spawn_pair_ok of {
        true -> {
            beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(rhs);
            env1: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)] <- case beh_e.kind == 1 of {
                true -> {
                    (beh_found: bool, beh_type: string) <- type_checker_core@lookup_symbol_found(context.env, beh_e.name);
                    case beh_found of {
                        true -> {
                            env_beh: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)] <- type_checker_core@add_symbol(env, concat("__actor_beh__", var_name), beh_type, parser_ast@empty_effect_name_list(), "");
                            state_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_type);
                            sup_csv: string <- type_checker_core@lookup_supervisor_csv(context);
                            case type_checker_core@type_name_in_supervisor_csv(state_t, sup_csv) or len(sup_csv) > 0 of {
                                true -> type_checker_core@add_symbol(env_beh, actor_ref_supervisor_marker_name(var_name), "boolean", parser_ast@empty_effect_name_list(), "");
                                false -> env_beh
                            }
                        };
                        false -> env
                    }
                };
                false -> env
            };
            state_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- rhs.inner;
            case state_e.kind == 1 of {
                true -> {
                    (st_found: bool, st_type: string) <- type_checker_core@lookup_symbol_found(context.env, state_e.name);
                    case st_found and type_checker_memory_regions@is_memory_region_type(st_type) of {
                        true -> type_checker_core@add_symbol(env1, state_e.name, "moved_region", parser_ast@empty_effect_name_list(), "");
                        false -> env1
                    }
                };
                false -> env1
            }
        };
        false -> env
    }
}

// Lambda (kind 991): formals in pattern_bindings; value = synthesized `(T1,...) -> R`; right_expr kind 992 holds return type text.
// Body is checked in a context extended with formals on top of the current env so outer bindings remain visible (closure capture).
fn lambda_formal_bindings_valid(bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 }) -> TypeCheckResult {
    case bindings.is_nil of {
        true -> type_checker_core@type_check_ok();
        false -> case len(bindings.head.type_str) == 0 of {
            true -> type_checker_core@type_check_err("E2006", location, "lambda parameter must have an explicit type", "sect4");
            false -> lambda_formal_bindings_valid(bindings.tail, location)
        }
    }
}

fn check_lambda_expression(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    formal_ok: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- lambda_formal_bindings_valid(expr.pattern_bindings, expr.location);
    case formal_ok.is_ok of {
        false -> formal_ok;
        true -> {
            ret_ty: string <- expr.right_expr.value;
            inner_ctx: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_tuple_decompose_helpers@add_bindings_to_context(context, expr.pattern_bindings);
            case expected_type == "discard" of {
                true -> check_expr(expr.inner, ret_ty, inner_ctx, world);
                false -> {
                    synth: string <- expr.value;
                    expect_surf: string <- type_checker_core@surface_for_type_id(context, expected_type);
                    compat: bool <- type_checker_expressions_identifiers@function_types_compatible_first_class_closure(synth, expect_surf);
                    case compat of {
                        false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("lambda type ", synth), concat(" is incompatible with context ", expect_surf)), "sect6");
                        true -> check_expr(expr.inner, ret_ty, inner_ctx, world)
                    }
                }
            }
        }
    }
}

fn check_let_after_validation(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, bind_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case bind_type == "unit" of {
        true -> case expr.name == "_" of {
            true -> {
                rhs_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "discard", context, world);
                case rhs_result.is_ok of {
                    false -> rhs_result;
                    true -> {
                        body_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, expected_type, context, world);
                        body_result
                    }
                }
            };
            false -> type_checker_core@type_check_err("E2006", expr.location, "unit is not a valid type for variables or parameters", "sect4")
        };
        false -> {
            is_discard: bool <- len(expr.name) == 1 and substring(expr.name, 0, 1) == "_";
            rhs_expected: string <- case is_discard of { true -> "discard"; false -> bind_type };
            rhs_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, rhs_expected, context, world);
            case rhs_result.is_ok of {
                false -> rhs_result;
                true -> {
                    bound_ctx: TypeContext <- type_checker_core@context_add_symbol_surface(context, expr.name, bind_type, parser_ast@empty_effect_name_list(), "");
                    new_env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)] <- enrich_env_for_actor_spawn(bound_ctx.env, expr.name, bind_type, expr.inner, context);
                    new_context: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_core@type_context_with_env(bound_ctx, new_env);
                    body_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, expected_type, new_context, world);
                    body_result
                }
            }
        }
    }
}

// Build context with all bindings from sequence body (let chain and tuple decomposes).
// The produces pure expr sees variables bound in the body.
fn context_after_sequence_body(body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext) -> TypeContext {
    case body.kind == 6 of {
        true -> {
            is_discard: bool <- body.value == "unit" and body.name == "_";
            new_context: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- case is_discard of {
                true -> context;
                false -> {
                    bound_ctx: TypeContext <- type_checker_core@context_add_symbol_surface(context, body.name, body.value, parser_ast@empty_effect_name_list(), "");
                    enriched_env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)] <- enrich_env_for_actor_spawn(bound_ctx.env, body.name, body.value, body.inner, context);
                    type_checker_core@type_context_with_env(bound_ctx, enriched_env)
                }
            };
            context_after_sequence_body(body.right_expr, new_context)
        };
        false -> case body.kind == 10 of {
            true -> {
                rhs_type: string <- case body.inner.kind == 11 of {
                    true -> type_checker_tuple_decompose_helpers@infer_expr_type(body.inner.right_expr, context_after_sequence_body(body.inner.inner, context));
                    false -> type_checker_tuple_decompose_helpers@infer_expr_type(body.inner, context)
                };
                rhs_trimmed: string <- type_checker_tuple_decompose_helpers@tuple_trim(rhs_type);
                tuple_for_fill: string <- case len(body.value) > 0 of {
                    true -> type_checker_tuple_decompose_helpers@tuple_trim(body.value);
                    false -> rhs_trimmed
                };
                filled: List[{ name: string, path: string, type_str: string }, mem(normal)] <- type_checker_tuple_decompose_helpers@fill_empty_binding_types_from_rhs(body.pattern_bindings, tuple_for_fill);
                extended: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_tuple_decompose_helpers@add_bindings_to_context(context, filled);
                context_after_sequence_body(body.right_expr, extended)
            };
            false -> context
        }
    }
}

// Sequence block (kind 11): sequence [proc[ε]] {statements} produces pure expr end
// inner = body (statements before produces), right_expr = result (pure expr); sequence_effects validated by effect checker
// The produces expr is in scope of all bindings from the body.
// Phase A lifetime analysis: when return type contains ref/buf, region must be returned (spec §12.1.4).
fn check_sequence_block(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    body_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "discard", context, world);
    case body_result.is_ok of {
        false -> body_result;
        true -> {
            result_context: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- context_after_sequence_body(expr.inner, context);
            expr_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, expected_type, result_context, world);
            case expr_result.is_ok of {
                false -> expr_result;
                true -> type_checker_memory_regions@check_sequence_return_lifetime(expected_type, expr.right_expr.location)
            }
        }
    }
}

// Parsed tuple types may spell atom slots as lexemes (:reply, :no_reply, :ok). For checking expressions, that field has type atom.
fn tuple_elem_type_for_check(field_type: string) -> string {
    case len(field_type) > 0 and substring(field_type, 0, 1) == ":" of {
        true -> "atom";
        false -> field_type
    }
}

// Tuple literal (kind 9): check against expected tuple type "(T1, T2)"
fn check_tuple_literal_inline(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case len(expected_type) == 7 and substring(expected_type, 0, 1) == "d" and substring(expected_type, 1, 2) == "i" and substring(expected_type, 2, 3) == "s" and substring(expected_type, 3, 4) == "c" and substring(expected_type, 4, 5) == "a" and substring(expected_type, 5, 6) == "r" and substring(expected_type, 6, 7) == "d" of {
        true -> type_checker_core@type_check_ok();
        false -> case len(expected_type) < 2 of {
            true -> type_checker_core@type_check_err("E2005", expr.location, concat("tuple literal requires tuple type, got ", expected_type), "sect4");
            false -> case substring(expected_type, 0, 1) == "(" of {
                true -> case substring(expected_type, len(expected_type) - 1, len(expected_type)) == ")" of {
                    true -> {
                        inner: string <- substring(expected_type, 1, len(expected_type) - 1);
                        comma_pos: int64 <- case expr.inner.kind == 9 of {
                            true -> tuple_find_last_top_level_comma(inner);
                            false -> tuple_find_top_level_comma(inner, 0)
                        };
                        first_type_raw: string <- substring(inner, 0, comma_pos);
                        first_type: string <- tuple_trim_leading(first_type_raw);
                        first_type_wrapped: string <- case tuple_find_top_level_comma(first_type, 0) < len(first_type) of {
                            true -> concat(concat("(", first_type), ")");
                            false -> first_type
                        };
                        rest_start: int64 <- comma_pos + 1;
                        second_type_raw: string <- case rest_start < len(inner) of {
                            true -> substring(inner, rest_start, len(inner));
                            false -> ""
                        };
                        second_type: string <- tuple_trim_leading(second_type_raw);
                        second_type_wrapped: string <- case tuple_find_top_level_comma(second_type, 0) < len(second_type) of {
                            true -> concat(concat("(", second_type), ")");
                            false -> second_type
                        };
                        first_expect: string <- tuple_elem_type_for_check(first_type_wrapped);
                        second_expect: string <- tuple_elem_type_for_check(second_type_wrapped);
                        first_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, first_expect, context, world);
                        case first_result.is_ok of {
                            false -> first_result;
                            true -> {
                                second_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr, second_expect, context, world);
                                second_result
                            }
                        }
                    };
                    false -> type_checker_core@type_check_err("E2005", expr.location, concat("tuple literal requires tuple type, got ", expected_type), "sect4")
                };
                false -> type_checker_core@type_check_err("E2005", expr.location, concat("tuple literal requires tuple type, got ", expected_type), "sect4")
            }
        }
    }
}

fn tuple_trim_leading(s: string) -> string {
    tuple_trim_leading_from(s, 0)
}

fn tuple_trim_leading_from(s: string, i: int64) -> string {
    case i >= len(s) of {
        true -> "";
        false -> case substring(s, i, i + 1) == " " of {
            true -> tuple_trim_leading_from(s, i + 1);
            false -> substring(s, i, len(s))
        }
    }
}

// Find first comma at top level (not inside parentheses). Handles nested tuples recursively.
fn tuple_find_top_level_comma(s: string, i: int64) -> int64 {
    tc_tuple_find_top_level_comma_from(s, i, 0, 0, 0)
}

// Top-level over paren/brace/bracket depth (all three must be 0 for a comma to split tuple components).
// Spec: `(A, B)` tuple may carry record or list components (`{...}`, `List[...]`) whose internal commas
// are not tuple separators (§3.3 / §15.4.13.2 for supervisor init return shape).
// Prefixed `tc_` to avoid collision with parser_tuples' paren-only variant under the bootstrap compiler's
// flat function namespace.
fn tc_tuple_find_top_level_comma_from(s: string, i: int64, paren: int64, brace: int64, bracket: int64) -> int64 {
    case i >= len(s) of {
        true -> len(s);
        false -> {
            c: string <- substring(s, i, i + 1);
            case c == "(" of {
                true -> tc_tuple_find_top_level_comma_from(s, i + 1, paren + 1, brace, bracket);
                false -> case c == ")" of {
                    true -> case paren <= 0 and brace == 0 and bracket == 0 of {
                        true -> len(s);
                        false -> tc_tuple_find_top_level_comma_from(s, i + 1, paren - 1, brace, bracket)
                    };
                    false -> case c == "{" of {
                        true -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace + 1, bracket);
                        false -> case c == "}" of {
                            true -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace - 1, bracket);
                            false -> case c == "[" of {
                                true -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace, bracket + 1);
                                false -> case c == "]" of {
                                    true -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace, bracket - 1);
                                    false -> case c == "," of {
                                        true -> case paren == 0 and brace == 0 and bracket == 0 of {
                                            true -> i;
                                            false -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace, bracket)
                                        };
                                        false -> tc_tuple_find_top_level_comma_from(s, i + 1, paren, brace, bracket)
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Find last comma at top level (over paren/brace/bracket). For right-nested (a,(b,c)),
// inner has type (A,B), right_expr has type C; record/list components must not confuse the split.
fn tuple_find_last_top_level_comma(s: string) -> int64 {
    tc_tuple_find_last_top_level_comma_from(s, 0, 0, 0, 0, -1)
}

fn tc_tuple_find_last_top_level_comma_from(s: string, i: int64, paren: int64, brace: int64, bracket: int64, last: int64) -> int64 {
    case i >= len(s) of {
        true -> case last < 0 of { true -> len(s); false -> last };
        false -> {
            c: string <- substring(s, i, i + 1);
            case c == "(" of {
                true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren + 1, brace, bracket, last);
                false -> case c == ")" of {
                    true -> case paren <= 0 and brace == 0 and bracket == 0 of {
                        true -> case last < 0 of { true -> len(s); false -> last };
                        false -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren - 1, brace, bracket, last)
                    };
                    false -> case c == "{" of {
                        true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace + 1, bracket, last);
                        false -> case c == "}" of {
                            true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace - 1, bracket, last);
                            false -> case c == "[" of {
                                true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace, bracket + 1, last);
                                false -> case c == "]" of {
                                    true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace, bracket - 1, last);
                                    false -> case c == "," of {
                                        true -> case paren == 0 and brace == 0 and bracket == 0 of {
                                            true -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace, bracket, i);
                                            false -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace, bracket, last)
                                        };
                                        false -> tc_tuple_find_last_top_level_comma_from(s, i + 1, paren, brace, bracket, last)
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

// Whether expression mentions identifier `name` (for case guard rule §3.4: guard must depend on pattern bindings).
fn expr_references_name(e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, name: string) -> bool {
    case e.kind < 0 of {
        true -> false;
        false -> case e.kind of {
            0 -> false;
            1 -> e.name == name;
            2 -> expr_references_name(e.inner, name);
            3 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            4 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            5 -> false;
            6 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            7 -> false;
            8 -> false;
            9 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            10 -> expr_references_name(e.inner, name);
            11 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            12 -> expr_references_name(e.inner, name);
            13 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            19 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr.inner, name) or expr_references_name(e.right_expr.right_expr, name);
            20 -> expr_references_name(e.inner, name) or expr_references_name(e.right_expr, name);
            _: int64 -> false
        }
    }
}

fn case_record_guard_refs_any(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, pat_str: string, scrutinee_type: string) -> bool {
    inner: string <- type_checker_record_types@record_case_inner_body(pat_str);
    case_record_guard_refs_loop(branch, inner, scrutinee_type)
}

fn case_record_guard_refs_loop(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, rem: string, scrutinee_type: string) -> bool {
    (seg: string, rest2: string) <- type_checker_record_types@record_case_field_step(rem);
    case len(seg) == 0 of {
        true -> false;
        false -> {
            (fname: string, sub: string) <- type_checker_record_types@record_field_segment_name_sub(seg);
            case len(fname) == 0 of {
                true -> case_record_guard_refs_loop(branch, rest2, scrutinee_type);
                false -> {
                    fty: string <- type_checker_record_types@find_field_type_in_record(scrutinee_type, fname);
                    case len(fty) == 0 of {
                        true -> case_record_guard_refs_loop(branch, rest2, scrutinee_type);
                        false -> {
                            spi: { is_valid: bool, pattern_kind: int64, bool_lit: bool, str_lit: string, int_lit_str: string, bind_name: string, bind_type: string, list_tail_name: string, list_tail_type: string } <- type_checker_tuple_decompose_helpers@case_parse_record_field_subpat(sub, fty);
                            case spi.is_valid of {
                                false -> case_record_guard_refs_loop(branch, rest2, scrutinee_type);
                                true -> case spi.pattern_kind of {
                                    2 -> case expr_references_name(branch.inner, spi.bind_name) of {
                                        true -> true;
                                        false -> case_record_guard_refs_loop(branch, rest2, scrutinee_type)
                                    };
                                    8 -> case case_record_guard_refs_any(branch, spi.bind_type, fty) of {
                                        true -> true;
                                        false -> case_record_guard_refs_loop(branch, rest2, scrutinee_type)
                                    };
                                    _: int64 -> case_record_guard_refs_loop(branch, rest2, scrutinee_type)
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn case_list_cons_guard_ok_expr(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, pi: { is_valid: bool, pattern_kind: int64, bool_lit: bool, str_lit: string, int_lit_str: string, bind_name: string, bind_type: string, list_tail_name: string, list_tail_type: string }) -> bool {
    head_bound: bool <- len(pi.bind_name) > 0 and pi.bind_name != "_";
    tail_bound: bool <- len(pi.list_tail_name) > 0 and pi.list_tail_name != "_";
    case head_bound of {
        false -> case tail_bound of {
            false -> true;
            true -> expr_references_name(branch.inner, pi.list_tail_name)
        };
        true -> case tail_bound of {
            false -> expr_references_name(branch.inner, pi.bind_name);
            true -> expr_references_name(branch.inner, pi.bind_name) or expr_references_name(branch.inner, pi.list_tail_name)
        }
    }
}

fn check_case_guard_invariant(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, pi: { is_valid: bool, pattern_kind: int64, bool_lit: bool, str_lit: string, int_lit_str: string, bind_name: string, bind_type: string, list_tail_name: string, list_tail_type: string }, scrutinee_type: string) -> TypeCheckResult {
    case type_checker_tuple_decompose_helpers@case_pattern_has_named_binding(pi, scrutinee_type) of {
        true -> case pi.pattern_kind of {
            6 -> case case_list_cons_guard_ok_expr(branch, pi) of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2014", branch.inner.location, "Guard does not depend on pattern bindings — move it before the case or restructure.", "sect3")
            };
            8 -> case case_record_guard_refs_any(branch, pi.bind_type, scrutinee_type) of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2014", branch.inner.location, "Guard does not depend on pattern bindings — move it before the case or restructure.", "sect3")
            };
            _: int64 -> case expr_references_name(branch.inner, type_checker_tuple_decompose_helpers@case_pattern_binding_name(pi)) of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2014", branch.inner.location, "Guard does not depend on pattern bindings — move it before the case or restructure.", "sect3")
            }
        };
        false -> type_checker_core@type_check_err("E2014", branch.inner.location, "Guard does not depend on pattern bindings — move it before the case or restructure.", "sect3")
    }
}

fn check_case_branch_guard(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, pi: { is_valid: bool, pattern_kind: int64, bool_lit: bool, str_lit: string, int_lit_str: string, bind_name: string, bind_type: string, list_tail_name: string, list_tail_type: string }, ext_ctx: TypeContext, scrutinee_type: string, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case branch.inner.kind < 0 of {
        true -> type_checker_core@type_check_ok();
        false -> {
            g: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(branch.inner, "boolean", ext_ctx, world);
            case g.is_ok of {
                false -> g;
                true -> check_case_guard_invariant(branch, pi, scrutinee_type)
            }
        }
    }
}

fn check_one_case_branch(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, scrutinee_type: string, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case branch.kind == 18 of {
        false -> type_checker_core@type_check_err("E2005", branch.location, "malformed case branch", "sect3");
        true -> {
            pi: { is_valid: bool, pattern_kind: int64, bool_lit: bool, str_lit: string, int_lit_str: string, bind_name: string, bind_type: string, list_tail_name: string, list_tail_type: string } <- type_checker_tuple_decompose_helpers@case_parse_pattern(branch.value);
            case pi.is_valid of {
                false -> type_checker_core@type_check_err("E2005", branch.location, "invalid or unsupported case pattern", "sect3");
                true -> {
                    pat_list_r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_lists@validate_list_type_declaration(pi.bind_type, branch.location);
                    case pat_list_r.is_ok of {
                        false -> pat_list_r;
                        true -> {
                            tail_list_r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_lists@validate_list_type_declaration(pi.list_tail_type, branch.location);
                            case tail_list_r.is_ok of {
                                false -> tail_list_r;
                                true -> case type_checker_tuple_decompose_helpers@case_pattern_matches_scrutinee(pi, scrutinee_type) of {
                                    false -> type_checker_core@type_check_err("E2005", branch.location, "case pattern does not match scrutinee type", "sect3");
                                    true -> {
                                        ext_ctx: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_tuple_decompose_helpers@case_extend_context_for_pattern(context, scrutinee_type, branch.value);
                                        guard_r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_case_branch_guard(branch, pi, ext_ctx, scrutinee_type, world);
                                        case guard_r.is_ok of {
                                            false -> guard_r;
                                            true -> check_expr(branch.right_expr, expected_type, ext_ctx, world)
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn check_case_branch_list(branches: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, scrutinee_type: string, expected_type: int64, context: TypeContext, case_loc: { file: string, line: int64, column: int64, offset: int64 }, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case branches.kind == -1 of {
        true -> type_checker_core@type_check_ok();
        false -> case branches.kind == 17 of {
            true -> {
                b: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- branches.inner;
                rest: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- branches.right_expr;
                r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_one_case_branch(b, scrutinee_type, expected_type, context, world);
                case r.is_ok of {
                    false -> r;
                    true -> check_case_branch_list(rest, scrutinee_type, expected_type, context, case_loc, world)
                }
            };
            false -> type_checker_core@type_check_err("E2005", case_loc, "malformed case branch list", "sect3")
        }
    }
}

fn check_case_boolean_exhaustive(branches: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, scrutinee_type: string, case_loc: { file: string, line: int64, column: int64, offset: int64 }) -> TypeCheckResult {
    case scrutinee_type == "boolean" of {
        false -> type_checker_core@type_check_ok();
        true -> {
            cov: { has_true: int64, has_false: int64, has_wild: int64 } <- type_checker_tuple_decompose_helpers@case_scan_bool_coverage(branches, scrutinee_type);
            case type_checker_tuple_decompose_helpers@case_bool_coverage_ok(cov) of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2005", case_loc, "case on boolean must cover true and false or include a wildcard branch (_ : boolean)", "sect3")
            }
        }
    }
}

fn check_case_expression(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.right_expr.kind == -1 of {
        true -> type_checker_core@type_check_err("E2005", expr.location, "case expression must have at least one branch", "sect3");
        false -> {
            scrutinee_type: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
            case len(scrutinee_type) == 0 of {
                true -> type_checker_core@type_check_err("E2005", expr.location, "cannot infer type of case scrutinee", "sect3");
                false -> {
                    rscr: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, scrutinee_type, context, world);
                    case rscr.is_ok of {
                        false -> rscr;
                        true -> {
                            branches_r: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_case_branch_list(expr.right_expr, scrutinee_type, expected_type, context, expr.location, world);
                            case branches_r.is_ok of {
                                false -> branches_r;
                                true -> check_case_boolean_exhaustive(expr.right_expr, scrutinee_type, expr.location)
                            }
                        }
                    }
                }
            }
        }
    }
}

// true/false as identifiers: lexer may emit identifier; treat as boolean literal (spec: boolean values)
fn is_true_or_false_identifier(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> bool {
    case len(expr.name) of {
        4 -> case substring(expr.name, 0, 1) of {
            "t" -> case substring(expr.name, 1, 2) of {
                "r" -> case substring(expr.name, 2, 3) of {
                    "u" -> case substring(expr.name, 3, 4) of { "e" -> true; _: string -> false };
                    _: string -> false
                };
                _: string -> false
            };
            _: string -> false
        };
        5 -> case substring(expr.name, 0, 1) of {
            "f" -> case substring(expr.name, 1, 2) of {
                "a" -> case substring(expr.name, 2, 3) of {
                    "l" -> case substring(expr.name, 3, 4) of {
                        "s" -> case substring(expr.name, 4, 5) of { "e" -> true; _: string -> false };
                        _: string -> false
                    };
                    _: string -> false
                };
                _: string -> false
            };
            _: string -> false
        };
        _: int64 -> false
    }
}

// List literal (kind 20): value = full List[T, S] on root; inner/right_expr chain like tuple kind 9.
fn check_list_literal(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    lit_t: string <- case len(expr.value) > 0 of {
        true -> expr.value;
        false -> expected_type
    };
    case len(lit_t) == 0 of {
        true -> type_checker_core@type_check_err("E2005", expr.location, "list literal requires a typed binding (name: List[T, S] <- [...])", "list");
        false -> {
            case type_checker_lists@list_types_compatible(expected_type, lit_t) of {
                false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("list literal type ", lit_t), " does not match expected "), expected_type), ""), "list");
                true -> {
                    (_ok: bool, elem_t: string, _: string) <- type_checker_lists@parse_list_type_params(lit_t);
                    case expr.inner.kind == -1 of {
                        true -> type_checker_core@type_check_ok();
                        false -> check_list_elem_tree(expr, elem_t, context, world)
                    }
                }
            }
        }
    }
}

fn check_list_elem_tree(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, elem_type: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.kind == 20 of {
        true -> {
            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, elem_type, context, world);
            case r1.is_ok of {
                false -> r1;
                true -> case expr.right_expr.kind == -1 of {
                    true -> type_checker_core@type_check_ok();
                    false -> check_list_elem_tree(expr.right_expr, elem_type, context, world)
                }
            }
        };
        false -> check_expr(expr, elem_type, context, world)
    }
}

// Compiler-known list primitives (list_implementation_design.md §7): empty, length, prepend, remove_head, head.
// Phase 0.4: resolve List[Collectable, S] from expected_type once, then check like concrete generics.
fn check_list_primitive_resolved(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, elem_res: string, list_t: string, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.name == "empty" of {
        true -> {
            ac: int64 <- type_checker_lists@list_empty_effective_arg_count(expr);
            case ac == 0 of {
                false -> type_checker_core@type_check_err("E2005", expr.location, "empty expects 0 arguments", "list");
                true -> case expected_type == "discard" of {
                    true -> type_checker_core@type_check_err("E2016", expr.location, "empty[Collectable, S]() requires a typed List binding on the left-hand side", "list");
                    false -> case type_checker_lists@list_types_compatible(expected_type, list_t) of {
                        true -> type_checker_core@type_check_ok();
                        false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("empty returns ", list_t), ", expected "), expected_type), ""), "list")
                    }
                }
            }
        };
        false -> case expr.name == "length" of {
            true -> {
                ac: int64 <- type_checker_lists@count_call_args_list(expr);
                case ac == 1 of {
                    false -> type_checker_core@type_check_err("E2005", expr.location, "length expects 1 argument", "list");
                    true -> {
                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, list_t, context, world);
                        case r1.is_ok of {
                            false -> r1;
                            true -> case expected_type == "discard" of {
                                true -> type_checker_core@type_check_ok();
                                false -> case expected_type == "int64" of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("length returns int64, expected ", expected_type), ""), "list")
                                }
                            }
                        }
                    }
                }
            };
            false -> case expr.name == "prepend" of {
                true -> {
                    ac: int64 <- type_checker_lists@count_call_args_list(expr);
                    case ac == 2 of {
                        false -> type_checker_core@type_check_err("E2005", expr.location, "prepend expects 2 arguments (element, list)", "list");
                        true -> {
                            r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, elem_res, context, world);
                            case r1.is_ok of {
                                false -> r1;
                                true -> {
                                    r2: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(call_second_arg_expr(expr), list_t, context, world);
                                    case r2.is_ok of {
                                        false -> r2;
                                        true -> case expected_type == "discard" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> case type_checker_lists@list_types_compatible(expected_type, list_t) of {
                                                true -> type_checker_core@type_check_ok();
                                                false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("prepend returns ", list_t), ", expected "), expected_type), ""), "list")
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                };
                false -> case expr.name == "remove_head" of {
                    true -> {
                        ac: int64 <- type_checker_lists@count_call_args_list(expr);
                        case ac == 1 of {
                            false -> type_checker_core@type_check_err("E2005", expr.location, "remove_head expects 1 argument", "list");
                            true -> {
                                r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, list_t, context, world);
                                case r1.is_ok of {
                                    false -> r1;
                                    true -> case expected_type == "discard" of {
                                        true -> type_checker_core@type_check_ok();
                                        false -> case type_checker_lists@list_types_compatible(expected_type, list_t) of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("remove_head returns ", list_t), ", expected "), expected_type), ""), "list")
                                        }
                                    }
                                }
                            }
                        }
                    };
                    false -> case expr.name == "head" of {
                        true -> {
                            ac: int64 <- type_checker_lists@count_call_args_list(expr);
                            case ac == 1 of {
                                false -> type_checker_core@type_check_err("E2005", expr.location, "head expects 1 argument", "list");
                                true -> {
                                    r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, list_t, context, world);
                                    case r1.is_ok of {
                                        false -> r1;
                                        true -> case expected_type == "discard" of {
                                            true -> type_checker_core@type_check_ok();
                                            false -> case type_checker_lists@types_equal_strip(expected_type, elem_res) of {
                                                true -> type_checker_core@type_check_ok();
                                                false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("head returns ", elem_res), ", expected "), expected_type), ""), "list")
                                            }
                                        }
                                    }
                                }
                            }
                        };
                        false -> type_checker_core@type_check_err("E2005", expr.location, concat("unknown list primitive: ", expr.name), "list")
                    }
                }
            }
        }
    }
}

fn list_primitive_collectable_context_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext) -> string {
    case expr.name == "empty" of {
        true -> expected_type;
        false -> case expr.name == "prepend" of {
            true -> type_checker_tuple_decompose_helpers@infer_expr_type(call_second_arg_expr(expr), context);
            false -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context)
        }
    }
}

fn check_list_primitive_call(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    (elem: string, space: string) <- type_checker_lists@parse_generic_ts(expr.value);
    case len(elem) == 0 of {
        true -> type_checker_core@type_check_err("E2005", expr.location, concat(concat("list primitive ", expr.name), " requires [T, S], [T], or [Collectable, S] with a typed binding"), "list");
        false -> {
            list_context: string <- list_primitive_collectable_context_type(expr, expected_type, context);
            (ok_lt: bool, elem_res: string, space_res: string, list_t: string) <- type_checker_lists@resolve_list_type_from_collectable_generic(elem, space, list_context);
            case ok_lt of {
                false -> type_checker_core@type_check_err("E2016", expr.location, concat(concat("cannot resolve List[Collectable, Space] for ", expr.name), " without a concrete List type in context"), "list");
                true -> check_list_primitive_resolved(expr, expected_type, elem_res, list_t, context, world)
            }
        }
    }
}

fn count_struct_chain_nodes(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> int64 {
    case expr.kind == -1 or expr.kind == 999 of {
        true -> 0;
        false -> case expr.kind == 21 of {
            true -> 1 + count_struct_chain_nodes(expr.right_expr);
            false -> 0
        }
    }
}

fn check_struct_literal_discard_chain(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.kind == -1 or expr.kind == 999 of {
        true -> type_checker_core@type_check_ok();
        false -> case expr.kind == 21 of {
            false -> type_checker_core@type_check_err("E2005", expr.location, "ill-formed struct literal (expected kind 21)", "sect3");
            true -> {
                it: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
                case len(it) == 0 of {
                    true -> type_checker_core@type_check_err("E2005", expr.location, "cannot infer field expression type in struct literal", "sect3");
                    false -> {
                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, it, context, world);
                        case r1.is_ok of {
                            false -> r1;
                            true -> check_struct_literal_discard_chain(expr.right_expr, context, world)
                        }
                    }
                }
            }
        }
    }
}

fn check_struct_literal_chain(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expr.kind == -1 or expr.kind == 999 of {
        true -> type_checker_core@type_check_ok();
        false -> case expr.kind == 21 of {
            false -> type_checker_core@type_check_err("E2005", expr.location, "ill-formed struct literal (expected kind 21)", "sect3");
            true -> {
                ft: string <- type_checker_record_types@find_field_type_in_record(expected_type, expr.name);
                case len(ft) == 0 of {
                    true -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("field ", expr.name), " is not part of the record type"), "sect3");
                    false -> {
                        r1: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, ft, context, world);
                        case r1.is_ok of {
                            false -> r1;
                            true -> check_struct_literal_chain(expr.right_expr, expected_type, context, world)
                        }
                    }
                }
            }
        }
    }
}

fn check_struct_literal_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    case expected_type == "discard" of {
        true -> check_struct_literal_discard_chain(expr, context, world);
        false -> {
            exp_rec: string <- type_checker_record_types@expand_named_topology_type(expected_type);
            rec_expected: string <- case len(exp_rec) > 0 of { true -> exp_rec; false -> expected_type };
            case type_checker_record_types@record_type_starts(rec_expected) of {
                false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat("expected record type, got ", expected_type), ""), "sect3");
                true -> {
                    n_type: int64 <- type_checker_record_types@count_record_fields(rec_expected);
                    n_lit: int64 <- count_struct_chain_nodes(expr);
                    case n_type == n_lit of {
                        false -> type_checker_core@type_check_err("E2003", expr.location, "struct literal field count does not match record type", "sect3");
                        true -> check_struct_literal_chain(expr, rec_expected, context, world)
                    }
                }
            }
        }
    }
}

fn check_field_access_field_type(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, base_t: string, ft: string, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    sequence proc[DeviceIO]
        result: TypeCheckResult <- case len(ft) == 0 of {
            true -> type_checker_core@type_check_err("E2005", expr.location, concat(concat("no field ", expr.name), " on this type"), "sect3");
            false -> {
                rbase: TypeCheckResult <- check_expr(expr.inner, base_t, context, world);
                case rbase.is_ok of {
                    false -> rbase;
                    true -> case expected_type == "discard" of {
                        true -> type_checker_core@type_check_ok();
                        false -> case type_checker_tuple_decompose_helpers@types_equal_surface(ft, expected_type) of {
                            true -> type_checker_core@type_check_ok();
                            false -> case type_checker_expressions_identifiers@function_types_compatible(ft, expected_type) of {
                                true -> type_checker_core@type_check_ok();
                                false -> case type_checker_lists@formal_type_accepts_actual(ft, expected_type, world) of {
                                    true -> type_checker_core@type_check_ok();
                                    false -> type_checker_core@type_check_err("E2003", expr.location, concat(concat(concat(concat("field has type ", ft), ", expected "), expected_type), ""), "sect3")
                                }
                            }
                        }
                    }
                }
            }
        }
    produces
        pure result
    end
}

fn check_field_access_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult {
    sequence proc[DeviceIO]
        base_t: string <- type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context);
        result: TypeCheckResult <- case len(base_t) == 0 of {
            true -> type_checker_core@type_check_err("E2005", expr.location, "cannot infer type of field access base", "sect3");
            false -> {
                exp_base: string <- type_checker_record_types@expand_named_topology_type(base_t);
                rec_base: string <- case len(exp_base) > 0 of { true -> exp_base; false -> base_t };
                case type_checker_record_types@record_type_starts(rec_base) of {
                    true -> check_field_access_field_type(expr, base_t, type_checker_record_types@find_field_type_in_record(rec_base, expr.name), expected_type, context, world);
                    false -> {
                        list_ft: string <- type_checker_lists@list_field_access_type(rec_base, expr.name);
                        case len(list_ft) == 0 of {
                            true -> type_checker_core@type_check_err("E2005", expr.location, concat(concat("field access base must be record, List[T, S], or topology type (cpu_topology, core_info, ...), got ", base_t), ""), "sect3");
                            false -> check_field_access_field_type(expr, base_t, list_ft, expected_type, context, world)
                        }
                    }
                }
            }
        }
    produces
        pure result
    end
}

// Dispatch on { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } kind: 0=literal, 1=identifier, 2=parenthesized (recurse), 3=binary op, 4=call, 5=atom literal, 6=let binding, 7=boolean literal, 8=string literal, 12=unary minus (forbidden); 20=list literal; 21=struct literal; 22=field access; 991=lambda
fn check_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> (TypeCheckResult, TypeContext) proc[DeviceIO] {
    case expr.kind of {
        0 -> type_checker_expressions_literals@check_expr(expr, expected_type, context, world);
        1 -> case is_true_or_false_identifier(expr) of {
            true -> boolean_type_checker_expressions@check_expr(expr, expected_type, context, world);
            false -> type_checker_expressions_identifiers@check_expr(expr, expected_type, context, world)
        };
        2 -> check_expr(expr.inner, expected_type, context, world);
        3 -> check_binary_op(expr, expected_type, context, world);
        4 -> check_call(expr, expected_type, context, world);
        5 -> type_checker_expressions_atoms@check_expr(expr, expected_type, context, world);
        6 -> check_let(expr, expected_type, context, world);
        7 -> boolean_type_checker_expressions@check_expr(expr, expected_type, context, world);
        8 -> case len(expected_type) == 6 and substring(expected_type, 0, 1) == "s" and substring(expected_type, 1, 2) == "t" and substring(expected_type, 2, 3) == "r" and substring(expected_type, 3, 4) == "i" and substring(expected_type, 4, 5) == "n" and substring(expected_type, 5, 6) == "g" of {
            true -> type_checker_core@type_check_ok();
            false -> case len(expected_type) == 7 and substring(expected_type, 0, 1) == "d" and substring(expected_type, 1, 2) == "i" and substring(expected_type, 2, 3) == "s" and substring(expected_type, 3, 4) == "c" and substring(expected_type, 4, 5) == "a" and substring(expected_type, 5, 6) == "r" and substring(expected_type, 6, 7) == "d" of {
                true -> type_checker_core@type_check_ok();
                false -> type_checker_core@type_check_err("E2001", expr.location, concat("string literal requires string type, got ", case expected_type of { "" -> "[EMPTY]"; _: string -> expected_type }), "sect6")
            }
        };
        9 -> check_tuple_literal_inline(expr, expected_type, context, world);
        10 -> check_tuple_decompose(expr, expected_type, context, world);
        11 -> check_sequence_block(expr, expected_type, context, world);
        12 -> check_unary_minus_forbidden(expr, context);
        13 -> check_case_expression(expr, expected_type, context, world);
        19 -> check_if_expression(expr, expected_type, context, world);
        20 -> check_list_literal(expr, expected_type, context, world);
        21 -> check_struct_literal_expr(expr, expected_type, context, world);
        22 -> check_field_access_expr(expr, expected_type, context, world);
        991 -> check_lambda_expression(expr, expected_type, context, world);
        996 -> type_checker_core@type_check_err("E1040", expr.location, "expected ';' after this statement before the next statement", "sect3");
        995 -> type_checker_core@type_check_err("E1000", expr.location, "There is an uncategorized syntactic defect in the code you are working on.", "sect3");
        997 -> type_checker_core@type_check_err("E1041", expr.location, "the last expression in this block must not be followed by ';'", "sect3");
        _: int64 -> type_checker_core@type_check_ok()
    }
}

fn check_if_expression(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    rc: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, "boolean", context, world);
    case rc.is_ok of {
        false -> rc;
        true -> {
            rt: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.right_expr.inner, expected_type, context, world);
            case rt.is_ok of {
                false -> rt;
                true -> case expr.right_expr.right_expr.kind < 0 of {
                    true -> type_checker_core@type_check_ok();
                    false -> check_expr(expr.right_expr.right_expr, expected_type, context, world)
                }
            }
        }
    }
}

// Tuple decomposition (kind 10): (pattern) <- rhs; body
// Structured pattern_bindings from AST. Bare identifiers get types from the RHS tuple; explicit `ident : type` must agree with RHS.
fn check_tuple_decompose(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    rhs_type: string <- case expr.inner.kind == 11 of {
        true -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner.right_expr, context_after_sequence_body(expr.inner.inner, context));
        false -> type_checker_tuple_decompose_helpers@infer_expr_type(expr.inner, context)
    };
    case len(rhs_type) < 2 or substring(rhs_type, 0, 1) != "(" of {
        true -> type_checker_core@type_check_err("E2005", expr.location, concat("tuple decomposition RHS must have tuple type, got ", case len(rhs_type) == 0 of { true -> "[unknown]"; false -> rhs_type }), "sect4");
        false -> {
            bindings: List[{ name: string, path: string, type_str: string }, mem(normal)] <- expr.pattern_bindings;
            rhs_trimmed: string <- type_checker_tuple_decompose_helpers@tuple_trim(rhs_type);
            tuple_for_fill: string <- case len(expr.value) > 0 of {
                true -> type_checker_tuple_decompose_helpers@tuple_trim(expr.value);
                false -> rhs_trimmed
            };
            filled: List[{ name: string, path: string, type_str: string }, mem(normal)] <- type_checker_tuple_decompose_helpers@fill_empty_binding_types_from_rhs(bindings, tuple_for_fill);
            valid_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_tuple_decompose_helpers@pattern_bindings_valid(filled, expr.location);
            case valid_result.is_ok of {
                false -> valid_result;
                true -> check_tuple_decompose_after_valid(expr, expected_type, context, rhs_type, filled, world)
            }
        }
    }
}

fn check_tuple_decompose_after_valid(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: int64, context: TypeContext, rhs_type: string, bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)]) -> TypeCheckResult proc[DeviceIO] {
    match_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- type_checker_tuple_decompose_helpers@pattern_types_match_rhs(bindings, rhs_type, expr.location);
    case match_result.is_ok of {
        false -> match_result;
        true -> {
            rhs_result: { is_ok: bool, error_code: string, error_location: { file: string, line: int64, column: int64, offset: int64 }, error_message: string, spec_section: string } <- check_expr(expr.inner, rhs_type, context, world);
            case rhs_result.is_ok of {
                false -> rhs_result;
                true -> {
                    extended_context: { env: List[{ name: string, type_id: int64, surface_type: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: bool, source_module: string }, mem(normal)], interner: { nodes: List[{ kind: int64, arg_a: int64, arg_b: int64, arg_c: int64, buf_size_is_name: boolean }, mem(normal)], by_key: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, names: { root: ref?(L, normal, rec), compare_key: fn(string, string) -> :less | :equal | :greater, compare_value: fn(int64, int64) -> :less | :equal | :greater, region: region(L, normal), specialization_key: int64, compare_key_ordering_bundle: int64, compare_value_ordering_bundle: int64 }, name_lexemes: List[string, mem(normal)], next_id: int64, seq_nil_id: int64 } } <- type_checker_tuple_decompose_helpers@add_bindings_to_context(context, bindings);
                    check_expr(expr.right_expr, expected_type, extended_context, world)
                }
            }
        }
    }
}
