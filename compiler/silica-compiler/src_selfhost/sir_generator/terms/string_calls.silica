{-

   Copyright 2026 Lee Scott Barney

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not USE this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-}

// Function call expression to SIR term ({ nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } kind 4, extension)
// Built-in I/O: print_string(string) -> call(atom, main.print_string, [arg], [device_io])
// Built-in I/O: println(string) -> call(atom, main.println, [arg], [device_io]) - appends \n to string
// Stderr mirrors (silica §22-style device_io; fd 2): print_string_stderr, println_stderr
// concatenate/2: two string args (literals or bound vars) -> prim(string_concat, a, b) per sir_design_spec §7.10
// Effect set carried on SIR handle.value per sir_design_spec §5.2.5

use sir_ast;
use types;

export build_print_string_from_sir/2;
export build_println_from_sir/2;
export build_print_string_stderr_from_sir/2;
export build_println_stderr_from_sir/2;
export build_concatenate_from_sir/2;
export build_substring_from_sir/3;
export build_substring_until_char_from_sir/3;
export build_length_bytes_from_sir/1;
export build_length_chars_from_sir/1;
export build_string_predicate_from_sir/3;
export build_read_lines_from_sir/2;
export build_file_exists_from_sir/2;
export build_delete_file_from_sir/2;
export build_append_file_from_sir/2;

fn qual_name(name: string) -> string {
    concat("main.", name)
}

// Effect sets for built-in functions (spec §7.11 Device I/O)
fn builtin_call_effects(call_name: string) -> string {
    case call_name of {
        "print_string" -> "[device_io]";
        "println" -> "[device_io]";
        "print_string_stderr" -> "[device_io]";
        "println_stderr" -> "[device_io]";
        "read_lines" -> "[device_io]";
        "file_exists" -> "[device_io]";
        "delete_file" -> "[device_io]";
        "append_file" -> "[device_io]";
        _: string -> "[]"
    }
}

// Build SIR call term for print_string(string) -> atom. Effect: device_io.
fn build_print_string_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "print_string" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "atom", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

// Build SIR call term for println(string) -> atom. Effect: device_io. Semantics: print string then \n.
fn build_println_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "println" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "atom", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

fn build_print_string_stderr_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "print_string_stderr" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "atom", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

fn build_println_stderr_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "println_stderr" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "atom", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

// Build SIR prim term for concatenate(a: string, b: string) -> string. Per sir_design_spec §7.10: string_concat.
// Args can be string literals (const) or bound string variables (var).
fn build_concatenate_from_sir(arg1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    sir_type: string <- types@type_name_to_sir("string");
    sir_ast@make_sir_term(6, sir_type, "", "string_concat", arg1, arg2)
}

// substring(s: string, start: int64, end: int64) -> string. Spec §5.3.2 String Manipulation.
// 3-arg prim: encoded as prim(string_substring, s, prim(string_substring_rest, start, end)).
fn build_substring_from_sir(arg1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg3: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    sir_type: string <- types@type_name_to_sir("string");
    rest: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(6, "int64", "", "string_substring_rest", arg2, arg3);
    sir_ast@make_sir_term(6, sir_type, "", "string_substring", arg1, rest)
}

// substring_until_char(s: string, start: int64, char: char) -> string. Spec §5.3.2 String Manipulation.
// 3-arg prim: encoded as prim(string_substring_until_char, s, prim(string_substring_until_char_rest, start, char)).
fn build_substring_until_char_from_sir(arg1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg3: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    sir_type: string <- types@type_name_to_sir("string");
    rest: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(6, "char", "", "string_substring_until_char_rest", arg2, arg3);
    sir_ast@make_sir_term(6, sir_type, "", "string_substring_until_char", arg1, rest)
}

// length_bytes(s: string) -> int64. Spec §silica:StringLengthOperations.
fn build_length_bytes_from_sir(arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    sir_ast@make_sir_term(6, "int64", "", "string_length_bytes", arg, sir_ast@dummy_sir_term())
}

// length_chars(s: string) -> int64. Spec §silica:StringLengthOperations.
fn build_length_chars_from_sir(arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    sir_ast@make_sir_term(6, "int64", "", "string_length_chars", arg, sir_ast@dummy_sir_term())
}

// starts_with / ends_with / contains: (string, string) -> boolean. Spec §5.3.3.
fn build_string_predicate_from_sir(pred_name: string, arg1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    prim_name: string <- case pred_name of {
        "starts_with" -> "string_starts_with";
        "ends_with" -> "string_ends_with";
        "contains" -> "string_contains";
        _: string -> ""
    };
    sir_ast@make_sir_term(6, "bool", "", prim_name, arg1, arg2)
}

// read_lines(path: string) -> string. Effect: device_io. Runtime: silica_read_file_path.
fn build_read_lines_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "read_lines" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "string", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

// file_exists(path: string) -> bool. Effect: device_io.
fn build_file_exists_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "file_exists" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "bool", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

// delete_file(path: string) -> bool. Effect: device_io. Runtime: silica_delete_file_path.
fn build_delete_file_from_sir(call_name: string, arg: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case call_name == "delete_file" of {
        true -> {
            callee: string <- qual_name(call_name);
            eff: string <- builtin_call_effects(call_name);
            sir_ast@make_sir_term(4, "bool", eff, callee, arg, sir_ast@dummy_sir_term())
        };
        false -> sir_ast@dummy_sir_term()
    }
}

// append_file(path: string, content: string) -> bool. Effect: device_io. Runtime: silica_append_file_path.
fn build_append_file_from_sir(arg1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, arg2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    eff: string <- builtin_call_effects("append_file");
    sir_ast@make_sir_term(4, "bool", eff, "main.append_file", arg1, arg2)
}
