{-

   Copyright 2026 Lee Scott Barney

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not USE this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-}

// Spawn/supervisor/actor SIR lowering (open recursion via lower callbacks).

use actor_calls;
use list_terms;
use parser_ast;
use sir_ast;
use struct_terms;
use type_checker_core;
use type_checker_expressions_actors;
use type_checker_expressions_helpers;
use types;
use user_calls;
use terms_helpers;
use type_checker_record_types;

export lower_actor_builtin_expr/8;
export lower_call_supervisor_expr/7;
export lower_spawn_registered_supervisor_expr/7;

fn actor_message_literal(msg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    case type_checker_expressions_helpers@expr_kind(msg) == 21 of {
        true -> msg;
        false -> case type_checker_expressions_helpers@expr_kind(msg) == 4 of {
            true -> {
                inner: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_helpers@expr_child_inner(msg);
                case type_checker_expressions_helpers@expr_kind(inner) == 21 of {
                    true -> inner;
                    false -> msg
                }
            };
            false -> msg
        }
    }
}

fn actor_message_expected_type(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, msg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> string {
    lit: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(msg);
    case type_checker_expressions_helpers@expr_kind(lit) == 21 of {
        true -> {
            rt: string <- terms_helpers@sir_infer_struct_literal_type(context, lit);
            case type_checker_record_types@record_type_starts(rt) of {
                true -> rt;
                false -> "discard"
            }
        };
        false -> "discard"
    }
}

fn supervisor_child_info_type_name() -> string {
    "{ id: atom, agent_type: atom, child: actor_ref, restart: :permanent | :temporary | :transient }"
}

fn supervisor_children_list_type_name() -> string {
    "List[{ id: atom, agent_type: atom, child: actor_ref, restart: :permanent | :temporary | :transient },mem(normal)]"
}

fn build_supervisor_child_info_record(id_atom: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, agent_type_atom: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, child_ref: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, restart_atom: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    ci_t: string <- supervisor_child_info_type_name();
    ci_sir_t: string <- types@type_name_to_sir(ci_t);
    r4: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(ci_sir_t, "restart", restart_atom, sir_ast@dummy_sir_term());
    r3: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(ci_sir_t, "child", child_ref, r4);
    r2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(ci_sir_t, "agent_type", agent_type_atom, r3);
    struct_terms@build_record_make_layer(ci_sir_t, "id", id_atom, r2)
}

fn build_supervisor_empty_child_info_record() -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_supervisor_child_info_record(
        terms_helpers@sir_atom_const(":unknown"),
        terms_helpers@sir_atom_const(":unknown"),
        terms_helpers@sir_actor_ref_const("0"),
        terms_helpers@sir_atom_const(":temporary")
    )
}

fn build_supervisor_empty_children_list() -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    ci_t: string <- supervisor_child_info_type_name();
    list_t: string <- supervisor_children_list_type_name();
    list_terms@build_list_nil(ci_t, list_t)
}

fn build_call_supervisor_reply(tag: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, child: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, status: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, children: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, count: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, child_info: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, error: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    reply_t: string <- type_checker_expressions_actors@supervisor_reply_record_type();
    reply_sir_t: string <- types@type_name_to_sir(reply_t);
    r7: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "error", error, sir_ast@dummy_sir_term());
    r6: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "child_info", child_info, r7);
    r5: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "count", count, r6);
    r4: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "children", children, r5);
    r3: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "status", status, r4);
    r2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_make_layer(reply_sir_t, "child", child, r3);
    struct_terms@build_record_make_layer(reply_sir_t, "tag", tag, r2)
}

fn build_call_supervisor_child_started_reply(child_ref: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":child_started"),
        child_ref,
        terms_helpers@sir_atom_const(":ok"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_atom_const(":ok")
    )
}

fn build_call_supervisor_ok_status_reply(status_atom: string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":ok"),
        terms_helpers@sir_actor_ref_const("0"),
        terms_helpers@sir_atom_const(status_atom),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_atom_const(":ok")
    )
}

fn build_call_supervisor_children_reply(children: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":children"),
        terms_helpers@sir_actor_ref_const("0"),
        terms_helpers@sir_atom_const(":ok"),
        children,
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_atom_const(":ok")
    )
}

fn build_call_supervisor_count_reply(count: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":count"),
        terms_helpers@sir_actor_ref_const("0"),
        terms_helpers@sir_atom_const(":ok"),
        terms_helpers@sir_int64_const("0"),
        count,
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_atom_const(":ok")
    )
}

fn build_call_supervisor_child_info_reply(child_ref: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, child_info: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":child_info"),
        child_ref,
        terms_helpers@sir_atom_const(":ok"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        child_info,
        terms_helpers@sir_atom_const(":ok")
    )
}

fn build_call_supervisor_error_reply(reason: string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    build_call_supervisor_reply(
        terms_helpers@sir_atom_const(":error"),
        terms_helpers@sir_actor_ref_const("0"),
        terms_helpers@sir_atom_const(":error"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_int64_const("0"),
        terms_helpers@sir_atom_const(reason)
    )
}

fn atom_lexeme_from_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> string {
    node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
    case node.kind == 5 of {
        true -> node.value;
        false -> case node.kind == 2 of {
            true -> atom_lexeme_from_expr(parser_ast@expr_at(expr, node.inner));
            false -> ""
        }
    }
}

fn call_supervisor_op_atom(msg_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> string {
    (op_found: boolean, op_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) <- terms_helpers@find_struct_field_expr(msg_expr, "op");
    case op_found of {
        true -> atom_lexeme_from_expr(op_expr);
        false -> ""
    }
}

fn call_supervisor_id_atom_sir(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, msg_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> (boolean, { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) {
    (id_found: boolean, id_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) <- terms_helpers@find_struct_field_expr(msg_expr, "id");
    case id_found of {
        true -> (true, sir_subexpr_for_actor(context, id_expr, "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body));
        false -> (false, sir_ast@dummy_sir_term())
    }
}

// Lower actor / concurrency builtins (silica-spec §15–16) to SIR prims; used from kind-4 call dispatch.

fn sir_subexpr_for_actor(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, t: string, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case in_body of {
        true -> lower_in_body(context, e, bound_name, bound_type, t);
        false -> lower_expected(context, e, t)
    }
}

// Same chain encoding as type_checker_expressions_helpers@call_third_arg_expr / call_fourth_arg_expr.

fn spawn_call_third_arg_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    type_checker_expressions_helpers@call_third_arg_expr(expr)
}

fn spawn_call_fourth_arg_expr(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } {
    type_checker_expressions_helpers@call_fourth_arg_expr(expr)
}

// SIR-local symbol lookup (surface type_name); mirrors identifiers.silica.

fn sir_lookup_supervisor_csv(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }) -> string {
    (found: boolean, ty: string) <- terms_helpers@sir_lookup_symbol_found(context.env, "__silica_e3b_sup_csv");
    case found of { true -> ty; false -> "" }
}

// Arena SIR helpers: root index i lives at list position (len - 1 - i).

fn spawn_value_tag_e3b_if_supervisor_state(base: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, state_t: string, context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    csv: string <- sir_lookup_supervisor_csv(context);
    case type_checker_core@type_name_in_supervisor_csv(state_t, csv) of {
        true -> terms_helpers@sir_term_with_root_value(base, concat("[concurrency]", concat("|e3b:", state_t)));
        false -> case length_bytes(csv) > 0 of {
            true -> terms_helpers@sir_term_with_root_value(base, concat("[concurrency]", concat("|e3b:", csv)));
            false -> base
        }
    }
}

fn supervisor_type_name_from_spawn_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) -> string {
    case type_checker_expressions_helpers@expr_kind(type_checker_expressions_helpers@expr_child_inner(expr)) == 1 of {
        true -> type_checker_expressions_helpers@expr_name(type_checker_expressions_helpers@expr_child_inner(expr));
        false -> sir_lookup_supervisor_csv(context)
    }
}

fn lower_spawn_registered_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, in_body: boolean, bound_name: string, bound_type: string, prim_name: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    ac_sr: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    case ac_sr == 3 of {
        false -> sir_ast@dummy_sir_term();
        true -> {
            init_sr_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
            beh_sr_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
            name_sr_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- spawn_call_third_arg_expr(expr);
            case type_checker_expressions_helpers@expr_kind(beh_sr_e) == 1 of {
                false -> sir_ast@dummy_sir_term();
                true -> {
                    (beh_sr_found: boolean, beh_sr_type: string) <- terms_helpers@sir_lookup_symbol_found(context.env, type_checker_expressions_helpers@expr_name(beh_sr_e));
                    case beh_sr_found of {
                        false -> sir_ast@dummy_sir_term();
                        true -> {
                            state_sr: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_sr_type);
                            init_sr_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, init_sr_e, state_sr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            beh_sr_label: string <- user_calls@qual_name(type_checker_expressions_helpers@expr_name(beh_sr_e));
                            beh_sr_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(0, "fn_ptr", beh_sr_label, "", sir_ast@dummy_sir_term(), sir_ast@dummy_sir_term());
                            name_sr_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, name_sr_e, "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            case prim_name == "spawn_dangerous_registered" of {
                                true -> actor_calls@build_spawn_dangerous_registered_prim(init_sr_sir, beh_sr_sir, name_sr_sir);
                                false -> actor_calls@build_spawn_registered_prim(init_sr_sir, beh_sr_sir, name_sr_sir)
                            }
                        }
                    }
                }
            }
        }
    }
}

fn lower_spawn_registered_supervisor_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    init_srs_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_calls@actor_call_second_arg(expr);
    name_srs_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- spawn_call_third_arg_expr(expr);
    init_srs: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, init_srs_e, "discard", in_body, bound_name, bound_type, lower_expected, lower_in_body);
    name_srs: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, name_srs_e, "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
    sup_name: string <- supervisor_type_name_from_spawn_expr(context, expr);
    actor_calls@build_spawn_registered_supervisor_prim(sup_name, init_srs, name_srs)
}

fn lower_call_supervisor_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    case type_checker_expressions_actors@count_chain_args(expr) == 2 of {
        false -> sir_ast@dummy_sir_term();
        true -> {
            // Avoid binding `sup` to the ephemeral current X0 in deep sequence bodies.
            sup_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- case in_body of {
                true -> lower_in_body(context, type_checker_expressions_helpers@expr_child_inner(expr), "", "", "discard");
                false -> lower_expected(context, type_checker_expressions_helpers@expr_child_inner(expr), "discard")
            };
            msg_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_calls@actor_call_second_arg(expr);
            op_atom: string <- call_supervisor_op_atom(msg_expr);
            case op_atom == ":add_child" of {
                true -> {
                    (child_found: boolean, child_expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }) <- terms_helpers@find_struct_field_expr(msg_expr, "child");
                    case child_found of {
                        true -> {
                            child_spec_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, child_expr, "discard", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            child_ref_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_add_child_prim(sup_sir, child_spec_sir);
                            build_call_supervisor_child_started_reply(child_ref_sir)
                        };
                        false -> build_call_supervisor_error_reply(":badarg")
                    }
                };
                false -> case op_atom == ":remove_child" of {
                    true -> {
                        (id_found_rc: boolean, id_sir_rc: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) <- call_supervisor_id_atom_sir(context, msg_expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        case id_found_rc of {
                            true -> {
                                remove_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_remove_child_prim(sup_sir, id_sir_rc);
                                reply_removed: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_ok_status_reply(":removed");
                                sir_ast@make_sir_term(2, types@type_name_to_sir("atom"), "", "%_", remove_rt, reply_removed)
                            };
                            false -> build_call_supervisor_error_reply(":badarg")
                        }
                    };
                    false -> case op_atom == ":restart_child" of {
                        true -> {
                            (id_found_rs: boolean, id_sir_rs: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) <- call_supervisor_id_atom_sir(context, msg_expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            case id_found_rs of {
                                true -> {
                                    restarted_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_restart_child_prim(sup_sir, id_sir_rs);
                                    restarted_var: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- terms_helpers@sir_var("%_supervisor_restarted_child", "actor_ref");
                                    reply_restarted: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_child_started_reply(restarted_var);
                                    sir_ast@make_sir_term(2, types@type_name_to_sir("actor_ref"), "", "%_supervisor_restarted_child", restarted_rt, reply_restarted)
                                };
                                false -> build_call_supervisor_error_reply(":badarg")
                            }
                        };
                        false -> case op_atom == ":terminate_child" of {
                            true -> {
                                (id_found_tc: boolean, id_sir_tc: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) <- call_supervisor_id_atom_sir(context, msg_expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                case id_found_tc of {
                                    true -> {
                                        terminate_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_terminate_child_prim(sup_sir, id_sir_tc);
                                        reply_terminated: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_ok_status_reply(":terminated");
                                        sir_ast@make_sir_term(2, types@type_name_to_sir("atom"), "", "%_", terminate_rt, reply_terminated)
                                    };
                                    false -> build_call_supervisor_error_reply(":badarg")
                                }
                            };
                            false -> case op_atom == ":which_children" of {
                                true -> {
                                    children_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_which_children_prim(sup_sir);
                                    children_t: string <- supervisor_children_list_type_name();
                                    children_var: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- terms_helpers@sir_var("%_supervisor_children_ref", children_t);
                                    reply_children: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_children_reply(children_var);
                                    sir_ast@make_sir_term(2, types@type_name_to_sir(children_t), "", "%_supervisor_children_ref", children_rt, reply_children)
                                };
                                false -> case op_atom == ":count_children" of {
                                    true -> {
                                        count_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_count_children_prim(sup_sir);
                                        count_var: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- terms_helpers@sir_var("%_supervisor_live_count", "int64");
                                        reply_count: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_count_reply(count_var);
                                        sir_ast@make_sir_term(2, types@type_name_to_sir("int64"), "", "%_supervisor_live_count", count_rt, reply_count)
                                    };
                                    false -> case op_atom == ":get_child" of {
                                        true -> {
                                            (id_found_gc: boolean, id_sir_gc: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) <- call_supervisor_id_atom_sir(context, msg_expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                            case id_found_gc of {
                                                true -> {
                                                    get_info_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- actor_calls@build_supervisor_get_child_info_prim(sup_sir, id_sir_gc);
                                                    ci_t: string <- supervisor_child_info_type_name();
                                                    ci_var: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- terms_helpers@sir_var("%_supervisor_get_child_info", ci_t);
                                                    child_rt: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- struct_terms@build_record_proj_prim(types@type_name_to_sir("actor_ref"), "child", ci_var);
                                                    child_var: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- terms_helpers@sir_var("%_supervisor_get_child_ref", "actor_ref");
                                                    reply_ci: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- build_call_supervisor_child_info_reply(child_var, ci_var);
                                                    bind_child: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(2, types@type_name_to_sir("actor_ref"), "", "%_supervisor_get_child_ref", child_rt, reply_ci);
                                                    sir_ast@make_sir_term(2, types@type_name_to_sir(ci_t), "", "%_supervisor_get_child_info", get_info_rt, bind_child)
                                                };
                                                false -> build_call_supervisor_error_reply(":badarg")
                                            }
                                        };
                                        false -> build_call_supervisor_error_reply(":unknown_supervisor_op")
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
    }
}

fn lower_actor_builtin_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, expected_type: string, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    ab: string <- type_checker_core@user_call_base_function_name(type_checker_expressions_helpers@expr_name(expr));
    case ab == "spawn" of {
        true -> {
            ac: int64 <- type_checker_expressions_actors@count_chain_args(expr);
            case ac == 2 or ac == 3 of {
                false -> sir_ast@dummy_sir_term();
                true -> {
                    init_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
                    beh_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
                    case type_checker_expressions_helpers@expr_kind(beh_e) == 1 of {
                        false -> sir_ast@dummy_sir_term();
                        true -> {
                            (beh_found: boolean, beh_fn_type: string) <- terms_helpers@sir_lookup_symbol_found(context.env, type_checker_expressions_helpers@expr_name(beh_e));
                            case beh_found of {
                                false -> sir_ast@dummy_sir_term();
                                true -> {
                                    state_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_fn_type);
                                    init_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, init_e, state_t, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                    beh_label: string <- user_calls@qual_name(type_checker_expressions_helpers@expr_name(beh_e));
                                    beh_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(0, "fn_ptr", beh_label, "", sir_ast@dummy_sir_term(), sir_ast@dummy_sir_term());
                                    case ac == 3 of {
                                        true -> {
                                            pl_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_placement_expr(expr);
                                            pl_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, pl_e, "uint64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                            spawn_value_tag_e3b_if_supervisor_state(actor_calls@build_spawn_prim(init_sir, beh_sir, pl_sir, true), state_t, context)
                                        };
                                        false -> spawn_value_tag_e3b_if_supervisor_state(actor_calls@build_spawn_prim(init_sir, beh_sir, sir_ast@dummy_sir_term(), false), state_t, context)
                                    }
                                }
                            }
                        }
                    }
                }
            }
        };
        false -> case ab == "spawn_dangerous" of {
            true -> {
                ac_sd: int64 <- type_checker_expressions_actors@count_chain_args(expr);
                case ac_sd == 2 or ac_sd == 3 of {
                    false -> sir_ast@dummy_sir_term();
                    true -> {
                        init_sd: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
                        beh_sd: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
                        case type_checker_expressions_helpers@expr_kind(beh_sd) == 1 of {
                            false -> sir_ast@dummy_sir_term();
                            true -> {
                                (beh_sd_found: boolean, beh_sd_type: string) <- terms_helpers@sir_lookup_symbol_found(context.env, type_checker_expressions_helpers@expr_name(beh_sd));
                                case beh_sd_found of {
                                    false -> sir_ast@dummy_sir_term();
                                    true -> {
                                        state_sd: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_sd_type);
                                        init_sd_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, init_sd, state_sd, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        beh_sd_label: string <- user_calls@qual_name(type_checker_expressions_helpers@expr_name(beh_sd));
                                        beh_sd_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(0, "fn_ptr", beh_sd_label, "", sir_ast@dummy_sir_term(), sir_ast@dummy_sir_term());
                                        case ac_sd == 3 of {
                                            true -> {
                                                pl_sd: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_placement_expr(expr);
                                                pl_sd_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, pl_sd, "uint64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                actor_calls@build_spawn_dangerous_prim(init_sd_sir, beh_sd_sir, pl_sd_sir, true)
                                            };
                                            false -> actor_calls@build_spawn_dangerous_prim(init_sd_sir, beh_sd_sir, sir_ast@dummy_sir_term(), false)
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            false -> case ab == "spawn_registered" of {
            true -> lower_spawn_registered_expr(context, expr, in_body, bound_name, bound_type, "spawn_registered", lower_expected, lower_in_body);
            false -> case ab == "spawn_dangerous_registered" of {
            true -> lower_spawn_registered_expr(context, expr, in_body, bound_name, bound_type, "spawn_dangerous_registered", lower_expected, lower_in_body);
            false -> case ab == "spawn_registered_supervisor" of {
            true -> lower_spawn_registered_supervisor_expr(context, expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
        false -> case ab == "spawn_linked" of {
            true -> {
                ac_sl: int64 <- type_checker_expressions_actors@count_chain_args(expr);
                case ac_sl == 3 or ac_sl == 4 of {
                    false -> sir_ast@dummy_sir_term();
                    true -> {
                        init_sl: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_init_expr(expr);
                        beh_sl: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- type_checker_expressions_actors@spawn_behavior_expr(expr);
                        agent_sl: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- spawn_call_third_arg_expr(expr);
                        case type_checker_expressions_helpers@expr_kind(beh_sl) == 1 of {
                            false -> sir_ast@dummy_sir_term();
                            true -> {
                                (beh_sl_found: boolean, beh_sl_type: string) <- terms_helpers@sir_lookup_symbol_found(context.env, type_checker_expressions_helpers@expr_name(beh_sl));
                                case beh_sl_found of {
                                    false -> sir_ast@dummy_sir_term();
                                    true -> {
                                        state_sl_t: string <- type_checker_expressions_actors@parse_behavior_state_param_type(beh_sl_type);
                                        init_sl_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, init_sl, state_sl_t, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        beh_sl_label: string <- user_calls@qual_name(type_checker_expressions_helpers@expr_name(beh_sl));
                                        beh_sl_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@make_sir_term(0, "fn_ptr", beh_sl_label, "", sir_ast@dummy_sir_term(), sir_ast@dummy_sir_term());
                                        agent_sl_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, agent_sl, "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        case ac_sl == 4 of {
                                            true -> {
                                                pl_sl_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- spawn_call_fourth_arg_expr(expr);
                                                pl_sl_sir: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, pl_sl_e, "uint64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                spawn_value_tag_e3b_if_supervisor_state(actor_calls@build_spawn_linked_prim(init_sl_sir, beh_sl_sir, agent_sl_sir, pl_sl_sir, true), state_sl_t, context)
                                            };
                                            false -> spawn_value_tag_e3b_if_supervisor_state(actor_calls@build_spawn_linked_prim(init_sl_sir, beh_sl_sir, agent_sl_sir, sir_ast@dummy_sir_term(), false), state_sl_t, context)
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            };
            false -> case ab == "send" of {
            true -> {
                a0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                msg_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                a1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, msg_e, actor_message_expected_type(context, msg_e), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                actor_calls@build_send_prim(a0, a1)
            };
            false -> case ab == "cast" of {
                true -> {
                    c0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                    cmsg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                    c1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, cmsg, actor_message_expected_type(context, cmsg), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                    actor_calls@build_actor_cast_prim(c0, c1)
                };
                false -> case ab == "cast_registered" of {
                    true -> {
                        cr0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        crmsg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                        cr1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, crmsg, actor_message_expected_type(context, crmsg), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        actor_calls@build_actor_cast_registered_prim(cr0, cr1)
                    };
                false -> case ab == "cast_dangerous_registered" of {
                    true -> {
                        cdr0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        cdrmsg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                        cdr1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, cdrmsg, actor_message_expected_type(context, cdrmsg), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        actor_calls@build_actor_cast_dangerous_registered_prim(cdr0, cdr1)
                    };
                false -> case ab == "call" of {
                    true -> {
                        k0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        kmsg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                        k1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, kmsg, actor_message_expected_type(context, kmsg), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                        actor_calls@build_actor_call_prim(k0, k1, expected_type)
                    };
                    false -> case ab == "call_registered" of {
                        true -> {
                            kr0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "atom", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            krmsg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_message_literal(actor_calls@actor_call_second_arg(expr));
                            kr1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, krmsg, actor_message_expected_type(context, krmsg), in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            actor_calls@build_actor_call_registered_prim(kr0, kr1, expected_type)
                        };
                    false -> case ab == "self" of {
                        true -> case type_checker_expressions_actors@count_chain_args(expr) == 0 of {
                            true -> actor_calls@build_self_prim();
                            false -> sir_ast@dummy_sir_term()
                        };
                        false -> case ab == "link" of {
                            true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                true -> {
                                    lk: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                    actor_calls@build_link_prim(lk)
                                };
                                false -> sir_ast@dummy_sir_term()
                            };
                            false -> case ab == "add_child" of {
                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 2 of {
                                    true -> {
                                        ap: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        ac_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_calls@actor_call_second_arg(expr);
                                        ac: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, ac_e, "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        actor_calls@build_add_child_prim(ap, ac)
                                    };
                                    false -> sir_ast@dummy_sir_term()
                                };
                            false -> case ab == "monitor" of {
                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                    true -> {
                                        mon_t: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        actor_calls@build_monitor_prim(mon_t)
                                    };
                                    false -> sir_ast@dummy_sir_term()
                                };
                                false -> case ab == "demonitor" of {
                                    true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                        true -> {
                                            dm: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "monitor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                            actor_calls@build_demonitor_prim(dm)
                                        };
                                        false -> sir_ast@dummy_sir_term()
                                    };
                                    false -> case ab == "supervision_ingress_enqueue" of {
                                        true -> case type_checker_expressions_actors@count_chain_args(expr) == 2 of {
                                            true -> {
                                                sie_t: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                sie_p_e: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } <- actor_calls@actor_call_second_arg(expr);
                                                sie_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, sie_p_e, "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                actor_calls@build_supervision_ingress_enqueue_prim(sie_t, sie_p)
                                            };
                                            false -> sir_ast@dummy_sir_term()
                                        };
                                        false -> case ab == "supervision_wait_and_drain_one" of {
                                            true -> case type_checker_expressions_actors@count_chain_args(expr) == 0 of {
                                                true -> actor_calls@build_supervision_wait_and_drain_one_prim();
                                                false -> sir_ast@dummy_sir_term()
                                            };
                                            false -> case ab == "child_table_first_ref" of {
                                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 0 of {
                                                    true -> actor_calls@build_child_table_first_ref_prim();
                                                    false -> sir_ast@dummy_sir_term()
                                                };
                                            false -> case ab == "child_table_row_ref" of {
                                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                    true -> {
                                                        ctr_ix: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                        actor_calls@build_child_table_row_ref_prim(ctr_ix)
                                                    };
                                                    false -> sir_ast@dummy_sir_term()
                                                };
                                            false -> case ab == "register_failure_reporter" of {
                                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                    true -> {
                                                        rfr_t: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                        actor_calls@build_register_failure_reporter_prim(rfr_t)
                                                    };
                                                    false -> sir_ast@dummy_sir_term()
                                                };
                                            false -> case ab == "wait_for_exit" of {
                                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 0 of {
                                                    true -> actor_calls@build_wait_for_exit_prim();
                                                    false -> sir_ast@dummy_sir_term()
                                                };
                                                false -> case ab == "kill_abnormal" of {
                                                    true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                        true -> {
                                                            ka_t: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                            actor_calls@build_kill_abnormal_prim(ka_t)
                                                        };
                                                        false -> sir_ast@dummy_sir_term()
                                                    };
                                                false -> case ab == "failure_reason_tag" of {
                                                    true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                        true -> {
                                                            frt_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                            actor_calls@build_failure_reason_tag_prim(frt_p)
                                                        };
                                                        false -> sir_ast@dummy_sir_term()
                                                    };
                                                    false -> case ab == "failure_reason_atom" of {
                                                        true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                            true -> {
                                                                fra_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                actor_calls@build_failure_reason_atom_prim(fra_p)
                                                            };
                                                            false -> sir_ast@dummy_sir_term()
                                                        };
                                                        false -> case ab == "failure_agent_type" of {
                                                            true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                                true -> {
                                                                    fat_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                    actor_calls@build_failure_agent_type_prim(fat_p)
                                                                };
                                                                false -> sir_ast@dummy_sir_term()
                                                            };
                                                            false -> case ab == "failure_release" of {
                                                                true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                                    true -> {
                                                                        fr_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                        actor_calls@build_failure_release_prim(fr_p)
                                                                    };
                                                                    false -> sir_ast@dummy_sir_term()
                                                                };
                                                                false -> case ab == "start_child" of {
                                                                    true -> case type_checker_expressions_actors@count_chain_args(expr) == 1 of {
                                                                        true -> {
                                                                            sc_p: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "discard", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                            actor_calls@build_start_child_prim(sc_p)
                                                                        };
                                                                        false -> sir_ast@dummy_sir_term()
                                                                    };
                                    false -> case ab == "call_supervisor" of {
                                        true -> lower_call_supervisor_expr(context, expr, in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        false -> lower_actor_runtime_helper_expr(context, expr, in_body, bound_name, bound_type, lower_expected, lower_in_body)
                                    }
                            }
                        }
                    }
                    }
                }
            }
                                                }
                                                }
                                            }
                                            }
                                        }
                                    }
                                    }
                                }
                            }
                        }
                    }
                    }
                }
                }
            }
        }
        }
        }
        }
        }
        }
        }
    }
}

fn lower_actor_runtime_helper_expr(context: { env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, in_body: boolean, bound_name: string, bound_type: string, lower_expected: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }, lower_in_body: fn({ env: List[{ name: string, type_name: string, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], is_effect_alias: boolean, source_module: string }, mem(normal)] }, { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, string, string, string) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 }) -> { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } {
    n: int64 <- type_checker_expressions_actors@count_chain_args(expr);
    rb: string <- type_checker_core@user_call_base_function_name(type_checker_expressions_helpers@expr_name(expr));
    ret: string <- actor_calls@actor_runtime_sir_return_type(rb);
    d: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_ast@dummy_sir_term();
    case rb == "get_cpu_topology" of {
        true -> case n == 0 of { true -> actor_calls@build_get_cpu_topology_prim(); false -> sir_ast@dummy_sir_term() };
        false -> case rb == "get_registry_actor" or rb == "get_pid_registry" of {
            true -> case n == 0 of { true -> actor_calls@build_actor_runtime_prim(rb, ret, d, d, 0); false -> sir_ast@dummy_sir_term() };
            false -> case rb == "get_efficiency_cores" of {
            true -> case n == 0 of { true -> actor_calls@build_actor_runtime_prim(rb, ret, d, d, 0); false -> sir_ast@dummy_sir_term() };
            false -> case rb == "get_performance_cores" of {
                true -> case n == 0 of { true -> actor_calls@build_actor_runtime_prim(rb, ret, d, d, 0); false -> sir_ast@dummy_sir_term() };
                false -> case rb == "get_core_capabilities" of {
                    true -> case n == 1 of {
                        true -> {
                            a0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                            actor_calls@build_get_core_capabilities_prim(a0)
                        };
                        false -> sir_ast@dummy_sir_term()
                    };
                    false -> case rb == "pin_actor_to_core" of {
                        true -> case n == 2 of {
                            true -> {
                                p0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                p1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                actor_calls@build_actor_runtime_prim(rb, ret, p0, p1, 2)
                            };
                            false -> sir_ast@dummy_sir_term()
                        };
                        false -> case rb == "pin_actor_realtime" of {
                            true -> case n == 2 of {
                                true -> {
                                    r0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                    r1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                    actor_calls@build_actor_runtime_prim(rb, ret, r0, r1, 2)
                                };
                                false -> sir_ast@dummy_sir_term()
                            };
                            false -> case rb == "set_actor_priority" of {
                                true -> case n == 2 of {
                                    true -> {
                                        s0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        s1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "discard", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                        actor_calls@build_actor_runtime_prim(rb, ret, s0, s1, 2)
                                    };
                                    false -> sir_ast@dummy_sir_term()
                                };
                                false -> case rb == "pin_actor_to_numa_node" of {
                                    true -> case n == 2 of {
                                        true -> {
                                            u0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                            u1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "discard", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                            actor_calls@build_actor_runtime_prim(rb, ret, u0, u1, 2)
                                        };
                                        false -> sir_ast@dummy_sir_term()
                                    };
                                    false -> case rb == "pin_actor_to_efficiency_core" of {
                                        true -> case n == 1 of {
                                            true -> {
                                                e0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                actor_calls@build_actor_runtime_prim(rb, ret, e0, d, 1)
                                            };
                                            false -> sir_ast@dummy_sir_term()
                                        };
                                        false -> case rb == "pin_actor_to_performance_core" of {
                                            true -> case n == 1 of {
                                                true -> {
                                                    f0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                    actor_calls@build_actor_runtime_prim(rb, ret, f0, d, 1)
                                                };
                                                false -> sir_ast@dummy_sir_term()
                                            };
                                            false -> case rb == "unpin_actor" of {
                                                true -> case n == 1 of {
                                                    true -> {
                                                        v0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                        actor_calls@build_actor_runtime_prim(rb, ret, v0, d, 1)
                                                    };
                                                    false -> sir_ast@dummy_sir_term()
                                                };
                                                false -> case rb == "remove_actor" of {
                                                    true -> case n == 1 of {
                                                        true -> {
                                                            w0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                            actor_calls@build_actor_runtime_prim(rb, ret, w0, d, 1)
                                                        };
                                                        false -> sir_ast@dummy_sir_term()
                                                    };
                                                    false -> case rb == "register_actor" of {
                                                        true -> case n == 1 of {
                                                            true -> {
                                                                ra0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                actor_calls@build_actor_runtime_prim(rb, ret, ra0, d, 1)
                                                            };
                                                            false -> sir_ast@dummy_sir_term()
                                                        };
                                                        false -> case rb == "get_actor_memory_usage" of {
                                                        true -> case n == 1 of {
                                                            true -> {
                                                                mu0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                actor_calls@build_actor_runtime_prim(rb, ret, mu0, d, 1)
                                                            };
                                                            false -> sir_ast@dummy_sir_term()
                                                        };
                                                        false -> case rb == "migrate_actor" of {
                                                            true -> case n == 2 of {
                                                                true -> {
                                                                    ma0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                    ma1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                    actor_calls@build_actor_runtime_prim(rb, ret, ma0, ma1, 2)
                                                                };
                                                                false -> sir_ast@dummy_sir_term()
                                                            };
                                                            false -> case rb == "move" of {
                                                                true -> case n == 3 of {
                                                                    true -> {
                                                                        mv0: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_helpers@expr_child_inner(expr), "actor_ref", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                        mv1: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, actor_calls@actor_call_second_arg(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                        mv2: { nodes: List[{ kind: int64, type_name: string, value: string, name: string, inner: int64, right_expr: int64 }, mem(normal)], root: int64 } <- sir_subexpr_for_actor(context, type_checker_expressions_actors@spawn_placement_expr(expr), "int64", in_body, bound_name, bound_type, lower_expected, lower_in_body);
                                                                        actor_calls@build_actor_runtime_prim_3(rb, ret, mv0, mv1, mv2)
                                                                    };
                                                                    false -> sir_ast@dummy_sir_term()
                                                                };
                                                                false -> sir_ast@dummy_sir_term()
                                                            }
                                                        }
                                                    }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
        }
        }
    }
}

// Tuple decomposition RHS type: mirror check_tuple_decompose (infer_expr_type + sequence-body context).

