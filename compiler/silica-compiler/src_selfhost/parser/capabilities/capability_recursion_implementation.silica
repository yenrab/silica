{-

   Copyright 2026 Lee Scott Barney

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not USE this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-}

// Parser capability: design_documents/recursion_implementation.jsonld (all phases 1–6).
// Surface syntax for general recursion is already parsed by fn_decl, case, calls, return, rec, etc.
// This module wires the design doc phases into the parser build: same constraint contract as other
// capabilities, plus query helpers for tooling (phase ↔ token kind) via lexer_recursion_phases.

use lexer_token_kind;
use lexer_recursion_phases;
use constraint_core;

export constraints/0;
export initial_roles/1;
export recursion_jsonld_phase1_token_kind/1;
export recursion_jsonld_phase2_token_kind/1;
export recursion_jsonld_phase3_token_kind/1;
export recursion_jsonld_phase4_token_kind/1;
export recursion_jsonld_phase5_token_kind/1;
export recursion_jsonld_phase6_token_kind/1;
export recursion_jsonld_any_phase_token_kind/1;

fn constraints() -> ListConstraint {
    constraint_core@empty_list_constraint()
}

fn initial_roles(token_kind: int64) -> ListInt {
    constraint_core@empty_list_int()
}

fn recursion_jsonld_phase1_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase1_core_representation(kinds, k)
}

fn recursion_jsonld_phase2_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase2_lowering_execution(kinds, k)
}

fn recursion_jsonld_phase3_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase3_preemption_fairness(kinds, k)
}

fn recursion_jsonld_phase4_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase4_debugging_observability(kinds, k)
}

fn recursion_jsonld_phase5_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase5_optimizations(kinds, k)
}

fn recursion_jsonld_phase6_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_recursion_phase6_fallbacks(kinds, k)
}

fn recursion_jsonld_any_phase_token_kind(k: int64) -> bool {
    kinds: TokenKinds <- lexer_token_kind@token_kinds();
    lexer_recursion_phases@token_kind_any_recursion_implementation_phase(kinds, k)
}
