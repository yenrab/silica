# lowering_cleanup sources are compiled by the parent config-driven batch (silica.config → seed).
COMPONENT_NAME := lowering_cleanup
include $(dir $(abspath $(lastword $(MAKEFILE_LIST))))../subdir_stub.mk
