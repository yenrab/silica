# Module checker sources are compiled by the parent config-driven batch (silica.config → seed).
COMPONENT_NAME := module_checker
include $(dir $(abspath $(lastword $(MAKEFILE_LIST))))../subdir_stub.mk
