{-

   Copyright 2026 Lee Scott Barney

   Licensed under the Apache License, Version 2.0 (the "License");
   you may not USE this file except in compliance with the License.
   You may obtain a copy of the License at

       http://www.apache.org/licenses/LICENSE-2.0

   Unless required by applicable law or agreed to in writing, software
   distributed under the License is distributed on an "AS IS" BASIS,
   WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   See the License for the specific language governing permissions and
   limitations under the License.
-}

{- Effect propagation.
   Traverses { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } trees to collect effects from function calls,
   computes effect set unions, and expands effect aliases.
   Implements spec Section 9.3.1 (Effect Propagation, Effect Set Union Algorithm).

   Supervisors (design_documents/supervisors_implementation_development_plan.md):
   Phases B/C: ingress vs mailbox ordering and Phase C exit/failure payload delivery are
   runtime concerns; effect propagation only sees proc[concurrency] for actor builtins
   (spawn_linked, link, monitor, demonitor, call, cast, send, self, recv via builtin_fn_effects).
   Phase D: behavior-only rules for spawn_linked and link are type-checked (E2091), not
   allocated separate effects here.
   Phase E (?15.4.12??15.4.13.2, ?15.4.13.3): runtime restart from supervisor_flags/child_spec,
   static child table, escalation, and dynamic spawn_linked (no auto-restart) do not surface as
   new call effects; the effect propagator still classifies those intrinsics as proc[concurrency] only.
   Phase F (?15.4.11.4, ?15.4.13.4): runtime unwind delivery to FailureReporter is not a call the
   effect propagator models; user handle_report / region_dump_limit bodies USE normal rules.
   Phase G (?15.4.8.4??15.4.8.7): `link` / `monitor` / `demonitor`, `DOWN` to standard mailbox,
   and supervisor death signaling; ingress vs mailbox routing is not represented as extra effects;
   propagation still unifies to proc[concurrency] for these intrinsics and for messaging.
   Memory region ops (alloc_region, alloc_ref, alloc_buf, read_ref, write_ref,
   read_buf, write_buf, buf_load, buf_store, alloc_rec, build, alloc_region_on_numa_node)
   require mem(Space) per spec Section 4.4, Sections 22.1-22.3; alloc_ref/alloc_buf/alloc_rec into atomic space also require atomic (Section 17.1.2).
   List[T,S] literals and list primitives require mem(S) per list_implementation_design.md Section 9.3, Section 9.8.
   Tuple literals (parser kind 9) are right-nested like struct literals; effects are the union of element expressions (see constraint_extract extract_tuple_elements_tail).
   Struct literals (parser kind 21) and field access (kind 22) follow silica:StructLiterals / Section 3.3.7:
   effects are the union of subexpressions (field initializers; base expr for .field).
-}

use parser_ast;
use lexer_core;
use effect_checker_core;
use effect_checker_declarations;
use effect_checker_memory_regions;
use effect_checker_lists;
use type_checker_core;
use instantiated_fn_name;

export collect_expr_effects/2;
export collect_expr_effects_outside_sequence_blocks/2;
export collect_sequence_block_direct_effects/2;
export resolve_call_effects/2;
export union_effect_lists/2;
export add_effect_if_absent/2;
export expand_effect_list/2;
export expand_single_effect/2;

fn stamp_effect_locations(effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    case effects.is_nil of {
        true -> parser_ast@empty_effect_name_list();
        false -> {
            stamped_head: { name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } } <- {
                name: effects.head.name,
                parameter: effects.head.parameter,
                location: location
            };
            stamped_tail: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- stamp_effect_locations(effects.tail, location);
            parser_ast@cons_effect_name(stamped_head, stamped_tail)
        }
    }
}

// --- Effect collection from expressions ---

{- Traverse an { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } tree and collect the union of all effects from function calls.
   { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 } kinds: 0=integer literal, 1=identifier, 2=parenthesized, 3=binary_op, 4=call, 5=atom literal, 6=let binding, 9=tuple literal (right-nested inner/right_expr; also then/else payload under kind 19), 21=struct literal, 22=field access, -1=dummy.
   For calls (kind 4): resolve callee effects (region ops USE binding_type for Space), union with arg subtree.
   For let bindings (kind 6): pass expr.value (binding type) when recursing into RHS for region op effect extraction.
   Pure expressions contribute no effects (spec Section 9.3.1). -}
fn collect_expr_effects(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            collect_expr_effects_with_binding(expr, ctx, "")
    produces
        pure result
    end
}

{- Effects from calls that appear outside any SEQUENCE block (kind 11).
   Used for function-body E3001: nested SEQUENCE proc[...] blocks declare their own
   effects; those must not be attributed to the enclosing function body.
   Foreign c_wrapper calls are treated as pure here so adapter stubs may invoke them
   outside SEQUENCE blocks; SEQUENCE-block checking uses collect_sequence_block_direct_effects. -}
fn collect_expr_effects_outside_sequence_blocks(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            collect_expr_effects_outside_sequence_blocks_with_binding(expr, ctx, "", false)
    produces
        pure result
    end
}

{- Same as collect_expr_effects_outside_sequence_blocks, but foreign c_wrapper calls
   contribute external_danger. Used for SEQUENCE-block E3001/E3010 used-effect sets. -}
fn collect_sequence_block_direct_effects(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            collect_expr_effects_outside_sequence_blocks_with_binding(expr, ctx, "", true)
    produces
        pure result
    end
}

fn collect_expr_effects_outside_sequence_blocks_with_binding(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string, include_foreign: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == 11 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 4 of {
                    true -> {
                        callee_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- resolve_call_effects_with_binding(node.name, ctx, parser_ast@expr_at(expr, node.inner), binding_type, node.location, node.value, include_foreign);
                        // Walk first arg (inner) and remaining args (right_expr chain: kind-4 name=="" pairs).
                        arg_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "", include_foreign);
                        rest_arg_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "", include_foreign);
                        union_effect_lists(callee_effects, union_effect_lists(arg_effects, rest_arg_effects))
                    };
                    false -> case node.kind == 3 of {
                        true -> {
                            left_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "", include_foreign);
                            right_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "", include_foreign);
                            union_effect_lists(left_effects, right_effects)
                        };
                        false -> case node.kind == 6 of {
                            true -> {
                                rhs_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, node.value, include_foreign);
                                next_type_env: List[{ name: string, type_name: string }, mem(normal)] <- effect_checker_core@add_type_entry(ctx.type_env, node.name, node.value);
                                extended_ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] } <- effect_checker_core@effect_context_with_type_env(ctx, next_type_env);
                                body_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.right_expr), extended_ctx, "", include_foreign);
                                union_effect_lists(rhs_effects, body_effects)
                            };
                            false -> case node.kind == 2 of {
                                true -> collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type, include_foreign);
                                false -> case node.kind == 10 of {
                                    true -> {
                                        rhs_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "", include_foreign);
                                        body_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "", include_foreign);
                                        union_effect_lists(rhs_effects, body_effects)
                                    };
                                    false -> case node.kind == 20 of {
                                        true -> {
                                            lit_type_src: string <- case len(node.value) > 0 of {
                                                true -> node.value;
                                                false -> binding_type
                                            };
                                            lit_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- effect_checker_lists@mem_effects_for_list_literal(lit_type_src, node.location);
                                            rest_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "", include_foreign);
                                            union_effect_lists(lit_effects, rest_effects)
                                        };
                                        false -> case node.kind == 13 of {
                                            true -> {
                                                scrut_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "", include_foreign);
                                                branch_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_case_branch_list_effects_outside(parser_ast@expr_at(expr, node.right_expr), ctx, include_foreign);
                                                union_effect_lists(scrut_effects, branch_effects)
                                            };
                                            false -> case node.kind == 9 of {
                                                true -> collect_tuple_literal_chain_effects_outside(expr, ctx, binding_type, include_foreign);
                                                false -> case node.kind == 21 of {
                                                    true -> collect_struct_literal_chain_effects_outside(expr, ctx, binding_type, include_foreign);
                                                    false -> case node.kind == 22 of {
                                                        true -> collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type, include_foreign);
                                                        false -> parser_ast@empty_effect_name_list();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
    produces
        pure result
    end
}

fn collect_expr_effects_with_binding(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == 4 of {
                true -> {
                    callee_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- resolve_call_effects_with_binding(node.name, ctx, parser_ast@expr_at(expr, node.inner), binding_type, node.location, node.value, true);
                    binding_list_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- case type_checker_core@user_call_base_function_name(node.name) == "call_supervisor" of {
                        true -> parser_ast@empty_effect_name_list();
                        false -> effect_checker_lists@mem_effects_for_list_literal(binding_type, node.location)
                    };
                    // Walk first arg (inner) and remaining args (right_expr chain: kind-4 name=="" pairs).
                    arg_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "");
                    rest_arg_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "");
                    union_effect_lists(union_effect_lists(callee_effects, binding_list_effects), union_effect_lists(arg_effects, rest_arg_effects))
                };
                false -> case node.kind == 3 of {
                    true -> {
                        left_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "");
                        right_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "");
                        union_effect_lists(left_effects, right_effects)
                    };
                    false -> case node.kind == 6 of {
                        true -> {
                            rhs_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, node.value);
                            next_type_env: List[{ name: string, type_name: string }, mem(normal)] <- effect_checker_core@add_type_entry(ctx.type_env, node.name, node.value);
                                extended_ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] } <- effect_checker_core@effect_context_with_type_env(ctx, next_type_env);
                            body_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.right_expr), extended_ctx, "");
                            union_effect_lists(rhs_effects, body_effects)
                        };
                        false -> case node.kind == 2 of {
                            true -> collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type);
                            false -> case node.kind == 10 of {
                                true -> {
                                    rhs_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "");
                                    body_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.right_expr), ctx, "");
                                    union_effect_lists(rhs_effects, body_effects)
                                };
                                false -> case node.kind == 11 of {
                                    // Nested sequence blocks encapsulate their effects (checked separately).
                                    // Callers that need this sequence's own used-set must walk inner/right_expr
                                    // with collect_expr_effects_outside_sequence_blocks.
                                    true -> parser_ast@empty_effect_name_list();
                                    false -> case node.kind == 20 of {
                                        true -> {
                                            lit_type_src: string <- case len(node.value) > 0 of {
                                                true -> node.value;
                                                false -> binding_type
                                            };
                                            lit_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- effect_checker_lists@mem_effects_for_list_literal(lit_type_src, node.location);
                                            rest_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "");
                                            union_effect_lists(lit_effects, rest_effects)
                                        };
                                        false -> case node.kind == 13 of {
                                            true -> {
                                                scrut_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, "");
                                                branch_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_case_branch_list_effects(parser_ast@expr_at(expr, node.right_expr), ctx);
                                                union_effect_lists(scrut_effects, branch_effects)
                                            };
                                            false -> case node.kind == 9 of {
                                                true -> collect_tuple_literal_chain_effects(expr, ctx, binding_type);
                                                false -> case node.kind == 21 of {
                                                    true -> collect_struct_literal_chain_effects(expr, ctx, binding_type);
                                                    false -> case node.kind == 22 of {
                                                        true -> collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type);
                                                        false -> parser_ast@empty_effect_name_list();
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
    produces
        pure result
    end
}

// --- Tuple literals (kind 9), right-nested inner/right_expr ---

{- Right-nested chain of kind-9 nodes: inner = first element, right_expr = next element or tail { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }.
   Same spine shape as struct literals (kind 21); used for `(\.,\.[,])` and for then/else pair under kind 19 (union of branches). -}
fn collect_tuple_literal_chain_effects(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 9 of {
                    true -> {
                        head_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type);
                        tail_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_tuple_literal_chain_effects(parser_ast@expr_at(expr, node.right_expr), ctx, binding_type);
                        union_effect_lists(head_eff, tail_eff)
                    };
                    false -> collect_expr_effects_with_binding(expr, ctx, binding_type)
                }
            }
    produces
        pure result
    end
}

fn collect_tuple_literal_chain_effects_outside(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string, include_foreign: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 9 of {
                    true -> {
                        head_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type, include_foreign);
                        tail_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_tuple_literal_chain_effects_outside(parser_ast@expr_at(expr, node.right_expr), ctx, binding_type, include_foreign);
                        union_effect_lists(head_eff, tail_eff)
                    };
                    false -> collect_expr_effects_outside_sequence_blocks_with_binding(expr, ctx, binding_type, include_foreign)
                }
            }
    produces
        pure result
    end
}

// --- Struct literals (kind 21), ?3.3.7 / silica:StructLiterals ---

{- Right-nested chain of kind-21 nodes: inner = field initializer, right_expr = next field or dummy.
   Union of effects from every field expression. -}
fn collect_struct_literal_chain_effects(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 21 of {
                    true -> {
                        head_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type);
                        tail_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_struct_literal_chain_effects(parser_ast@expr_at(expr, node.right_expr), ctx, binding_type);
                        union_effect_lists(head_eff, tail_eff)
                    };
                    false -> collect_expr_effects_with_binding(expr, ctx, binding_type)
                }
            }
    produces
        pure result
    end
}

fn collect_struct_literal_chain_effects_outside(expr: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, binding_type: string, include_foreign: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(expr);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 21 of {
                    true -> {
                        head_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(expr, node.inner), ctx, binding_type, include_foreign);
                        tail_eff: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_struct_literal_chain_effects_outside(parser_ast@expr_at(expr, node.right_expr), ctx, binding_type, include_foreign);
                        union_effect_lists(head_eff, tail_eff)
                    };
                    false -> collect_expr_effects_outside_sequence_blocks_with_binding(expr, ctx, binding_type, include_foreign)
                }
            }
    produces
        pure result
    end
}

{- Resolve the effects of a called function by name.
   Lookup order: region builtins (alloc_region, alloc_ref, etc.), user-defined, then other builtins.
   Unknown functions are treated as pure (empty effects). -}
fn resolve_call_effects(fn_name: string, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            resolve_call_effects_with_binding(fn_name, ctx, parser_ast@dummy_expr(), "", parser_ast@zero_location(), "", true)
    produces
        pure result
    end
}

fn dangerous_module_prefix() -> string {
    "dangerous_"
}

fn is_dangerous_module_name(name: string) -> boolean {
    pref: string <- dangerous_module_prefix();
    case len(name) >= len(pref) of {
        false -> false;
        true -> substring(name, 0, len(pref)) == pref
    }
}

fn external_danger_effect_singleton(location: { file: string, line: int64, column: int64, offset: int64 }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    ed: { name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } } <- { name: "external_danger", parameter: "", location: location };
    parser_ast@cons_effect_name(ed, parser_ast@empty_effect_name_list())
}

fn first_arg_region_type(first_arg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> string {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(first_arg);
        result: string <-
            case node.kind == 1 of {
                true -> {
                    (found: boolean, typ: string) <- effect_checker_core@lookup_type_entry(ctx.type_env, node.name);
                    case found of { true -> typ; false -> "" }
                };
                false -> ""
            }
    produces
        pure result
    end
}

{- generic_value: kind-4 call value field (generic instantiation text), empty if none.
   include_foreign_c_wrapper_effects: when false, raw FOREIGN c_wrapper calls are treated as pure
   (adapter stubs may call them outside SEQUENCE blocks; dangerous_* module calls still carry external_danger). -}
fn resolve_call_effects_with_binding(fn_name: string, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, first_arg: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, binding_type: string, location: { file: string, line: int64, column: int64, offset: int64 }, generic_value: string, include_foreign_c_wrapper_effects: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case effect_checker_lists@is_list_primitive_fn(fn_name) of {
                true -> stamp_effect_locations(effect_checker_lists@mem_effects_for_list_call(fn_name, generic_value, location), location);
                false -> case fn_name == "checked_int64_add" or fn_name == "checked_int64_mul" or fn_name == "checked_int64_add1" or fn_name == "checked_int64_byte_size" of {
                    true -> parser_ast@empty_effect_name_list();
                    false -> case fn_name == "comparator_result_valid" or fn_name == "comparator_result_validate" of {
                    true -> parser_ast@empty_effect_name_list();
                    false -> case fn_name == "ordering_identity_fresh_env" or fn_name == "ordering_identity_materialize" or fn_name == "ordering_identity_same" or fn_name == "ordering_identity_bundle_make" of {
                    true -> parser_ast@empty_effect_name_list();
                    false -> case effect_checker_memory_regions@is_region_builtin_fn(fn_name) of {
                    true -> {
                        first_arg_type: string <- first_arg_region_type(first_arg, ctx);
                        (handled: boolean, region_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_memory_regions@get_region_call_effects(fn_name, first_arg, first_arg_type, binding_type, location);
                        case handled of {
                            true -> stamp_effect_locations(region_effects, location);
                            false -> parser_ast@empty_effect_name_list()
                        }
                    };
                    false -> {
                        case type_checker_core@call_name_is_module_qualified(fn_name) of {
                            true -> {
                                (qmod: string, _: string) <- type_checker_core@split_module_qualified_call_name(fn_name);
                                case is_dangerous_module_name(qmod) of {
                                    true -> stamp_effect_locations(external_danger_effect_singleton(location), location);
                                    false -> {
                                        base: string <- type_checker_core@user_call_base_function_name(fn_name);
                                        resolved: string <- instantiated_fn_name@resolve_call_fn_name(base, generic_value);
                                        (found_mod: boolean, mod_prog: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } }) <- parser_ast@find_named_program(qmod, ctx.world);
                                        (found_mq: boolean, mq_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- case found_mod of {
                                            true -> effect_checker_declarations@lookup_fn_declared_effects(mod_prog.declarations, resolved);
                                            false -> (false, parser_ast@empty_effect_name_list())
                                        };
                                        inst_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- effect_checker_lists@mem_effects_from_fn_instantiation(generic_value, resolved, location);
                                        resolved_mq_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- case found_mq of {
                                            true -> case effect_checker_core@effect_list_length(mq_effects) > 0 of {
                                                true -> mq_effects;
                                                false -> inst_effects
                                            };
                                            false -> inst_effects
                                        };
                                        case effect_checker_core@effect_list_length(resolved_mq_effects) > 0 of {
                                            true -> stamp_effect_locations(expand_effect_list(resolved_mq_effects, ctx.alias_env), location);
                                            false -> {
                                                (found_local: boolean, local_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@lookup_fn_effects(ctx.fn_env, resolved);
                                                case found_local of {
                                                    true -> stamp_effect_locations(expand_effect_list(local_effects, ctx.alias_env), location);
                                                    false -> {
                                                        (found_builtin_mq: boolean, builtin_mq_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@builtin_fn_effects(base);
                                                        case found_builtin_mq of {
                                                            true -> stamp_effect_locations(builtin_mq_effects, location);
                                                            false -> parser_ast@empty_effect_name_list()
                                                        }
                                                    }
                                                }
                                            }
                                        }
                                    }
                                }
                            };
                            false -> {
                                (found_user: boolean, user_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@lookup_fn_effects(ctx.fn_env, fn_name);
                                case found_user of {
                                    true -> stamp_effect_locations(expand_effect_list(user_effects, ctx.alias_env), location);
                                    false -> {
                                        case include_foreign_c_wrapper_effects of {
                                            true -> {
                                                (found_foreign: boolean, _: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@lookup_fn_effects(ctx.foreign_c_wrapper_env, fn_name);
                                                case found_foreign of {
                                                    true -> stamp_effect_locations(external_danger_effect_singleton(location), location);
                                                    false -> {
                                                        (found_builtin: boolean, builtin_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@builtin_fn_effects(fn_name);
                                                        case found_builtin of {
                                                            true -> stamp_effect_locations(builtin_effects, location);
                                                            false -> parser_ast@empty_effect_name_list()
                                                        }
                                                    }
                                                }
                                            };
                                            false -> {
                                                (found_builtin: boolean, builtin_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@builtin_fn_effects(fn_name);
                                                case found_builtin of {
                                                    true -> stamp_effect_locations(builtin_effects, location);
                                                    false -> parser_ast@empty_effect_name_list()
                                                }
                                            }
                                        }
                                    }
                                }
                            }
                        }
                    }
                }
            }
            }
            }
            }
    produces
        pure result
    end
}

{- kind 17 branch list: inner is kind 18 branch; right_expr is next 17 or dummy. -}
fn collect_single_case_branch_effects(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(branch);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == 18 of {
                true -> {
                    pat_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- effect_checker_lists@mem_effects_from_case_pattern(node.value, node.location);
                    rhs_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_expr_effects_with_binding(parser_ast@expr_at(branch, node.right_expr), ctx, "");
                    union_effect_lists(pat_effects, rhs_effects)
                };
                false -> collect_expr_effects_with_binding(branch, ctx, "")
            }
    produces
        pure result
    end
}

fn collect_case_branch_list_effects(branches: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(branches);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 17 of {
                    true -> {
                        one: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_single_case_branch_effects(parser_ast@expr_at(branches, node.inner), ctx);
                        rest: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_case_branch_list_effects(parser_ast@expr_at(branches, node.right_expr), ctx);
                        union_effect_lists(one, rest)
                    };
                    false -> parser_ast@empty_effect_name_list()
                }
            }
    produces
        pure result
    end
}

{- Case branch RHS only: List[..., mem(S)] in pattern text is a type annotation for
   the type checker and E3011 alignment inside SEQUENCE blocks, not list allocation. -}
fn collect_single_case_branch_effects_outside(branch: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, include_foreign: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(branch);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == 18 of {
                true -> collect_expr_effects_outside_sequence_blocks_with_binding(parser_ast@expr_at(branch, node.right_expr), ctx, "", include_foreign);
                false -> collect_expr_effects_outside_sequence_blocks_with_binding(branch, ctx, "", include_foreign)
            }
    produces
        pure result
    end
}

fn collect_case_branch_list_effects_outside(branches: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, ctx: { fn_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], foreign_c_wrapper_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], type_env: List[{ name: string, type_name: string }, mem(normal)], world: List[{ module_name: string, program: { declarations: List[{ tag: int64, name: string, parameters: List[{ name: string, type_name: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], return_type_name: string, return_type_location: { file: string, line: int64, column: int64, offset: int64 }, location: { file: string, line: int64, column: int64, offset: int64 }, body: { nodes: List[{ kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], root: int64 }, declared_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)], location: { file: string, line: int64, column: int64, offset: int64 } } }, mem(normal)] }, include_foreign: boolean) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    sequence
        node: { kind: int64, value: string, name: string, location: { file: string, line: int64, column: int64, offset: int64 }, inner: int64, right_expr: int64, pattern_bindings: List[{ name: string, path: string, type_str: string }, mem(normal)], sequence_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] } <- parser_ast@expr_root_node(branches);
        result: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <-
            case node.kind == -1 of {
                true -> parser_ast@empty_effect_name_list();
                false -> case node.kind == 17 of {
                    true -> {
                        one: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_single_case_branch_effects_outside(parser_ast@expr_at(branches, node.inner), ctx, include_foreign);
                        rest: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- collect_case_branch_list_effects_outside(parser_ast@expr_at(branches, node.right_expr), ctx, include_foreign);
                        union_effect_lists(one, rest)
                    };
                    false -> parser_ast@empty_effect_name_list()
                }
            }
    produces
        pure result
    end
}

// --- Effect set union ---

{- Compute the union of two effect lists (no duplicates).
   For each effect in list_a, add to list_b if not already present.
   Normalizes effects before comparison. -}
fn union_effect_lists(list_a: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], list_b: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    case list_a.is_nil of {
        true -> list_b;
        false -> {
            merged: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- add_effect_if_absent(list_b, effect_checker_core@normalize_effect(list_a.head));
            union_effect_lists(list_a.tail, merged)
        }
    }
}

{- Add an effect to the list if it is not already present (by equality after normalization). -}
fn add_effect_if_absent(list: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], eff: { name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    case effect_checker_core@effect_list_contains(list, eff) of {
        true -> list;
        false -> parser_ast@cons_effect_name(eff, list)
    }
}

// --- Alias expansion ---

{- Expand all aliases in an effect list.
   Each effect that is a built-in alias or user-defined alias is replaced
   by its expansion. Built-in effects pass through unchanged. -}
fn expand_effect_list(effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)], alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)]) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    case effects.is_nil of {
        true -> parser_ast@empty_effect_name_list();
        false -> {
            expanded_head: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- expand_single_effect(effects.head, alias_env);
            expanded_tail: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] <- expand_effect_list(effects.tail, alias_env);
            union_effect_lists(expanded_head, expanded_tail)
        }
    }
}

{- Expand a single effect. If it is a built-in effect, return as singleton list.
   If it is a built-in alias, return its expansion.
   If it is a user-defined alias, return its expansion.
   Otherwise, return as singleton list (will be caught by validation). -}
fn expand_single_effect(eff: { name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, alias_env: List[{ name: string, effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] }, mem(normal)]) -> List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)] {
    norm: { name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } } <- effect_checker_core@normalize_effect(eff);
    case effect_checker_core@is_builtin_effect(norm) of {
        true -> parser_ast@cons_effect_name(norm, parser_ast@empty_effect_name_list());
        false -> case effect_checker_core@is_builtin_alias_name(norm.name) of {
            true -> {
                (found_alias: boolean, alias_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@expand_builtin_alias(norm.name);
                case found_alias of {
                    true -> alias_effects;
                    false -> parser_ast@cons_effect_name(norm, parser_ast@empty_effect_name_list())
                }
            };
            false -> {
                (found_user: boolean, user_effects: List[{ name: string, parameter: string, location: { file: string, line: int64, column: int64, offset: int64 } }, mem(normal)]) <- effect_checker_core@lookup_fn_effects(alias_env, norm.name);
                case found_user of {
                    true -> user_effects;
                    false -> parser_ast@cons_effect_name(norm, parser_ast@empty_effect_name_list())
                }
            }
        }
    }
}
