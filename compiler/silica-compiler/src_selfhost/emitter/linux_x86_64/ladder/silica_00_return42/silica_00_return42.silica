// ESP32-S3 port early app 00: the smallest program. main's int64 result becomes the exit status.
// Needs: function prologue/epilogue, int64 constant, the runtime's exit marker.
fn main() -> int64 { 42 }
