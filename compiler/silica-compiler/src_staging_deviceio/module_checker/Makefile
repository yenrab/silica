# Makefile for Silica module checker (compiled by parent src/Makefile)
.DEFAULT_GOAL := all
.PHONY: all clean help

all:
	@echo "✅ Module checker (compiled by parent)"

clean:
	@echo "Module checker clean (handled by parent)"

help:
	@echo "Module checker modules are compiled by parent src/Makefile"
