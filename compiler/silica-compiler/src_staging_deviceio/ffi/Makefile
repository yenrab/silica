# Makefile for Silica FFI modules (compiled by parent src/Makefile)
.DEFAULT_GOAL := all
.PHONY: all clean help

all:
	@echo "✅ FFI modules (compiled by parent)"

clean:
	@echo "FFI clean (handled by parent)"

help:
	@echo "FFI modules are compiled by parent src/Makefile"
