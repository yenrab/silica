// Parser runner - entry point for main.silica

use lexer_runner;
use constraint_runner;

export parse_program/1;

fn parse_program(tokens: ListToken) -> ParseResult {
    constraint_runner@parse_program(tokens)
}
