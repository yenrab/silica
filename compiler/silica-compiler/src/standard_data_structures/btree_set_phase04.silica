export empty/1;
export insert/2;

fn empty(functions: { compare_item: fn(int64, int64) -> atom }) -> {
    compare_item: fn(int64, int64) -> atom
} {
    functions
}

fn insert(
    set: { compare_item: fn(int64, int64) -> atom },
    item: int64
) -> { compare_item: fn(int64, int64) -> atom } {
    set
}
