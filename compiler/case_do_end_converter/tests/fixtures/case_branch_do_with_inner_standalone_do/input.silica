fn branch_with_inner_do(value: Bool) -> int64 proc[DeviceIO] {
    case value of {
        true -> do
            do
                inner: int64 <- produce_inner();
                inner
            end
        end;
        false -> 0
    }
}
